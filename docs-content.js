window.MONYACODE_DOCS = [{"path":"all-actions.md","title":"All Actions","group":"Guides","content":"## All Actions\n\n{#ACTIONS_TABLE#}\n"},{"path":"command-line-interface.md","title":"Command-line Interface","group":"Guides","content":"# Command-line Interface\n\nMonyaCode has a CLI, on Linux this should come with the distribution's MonyaCode package\n(binary name can vary from distribution to distribution, `monyacode` will be used\nlater for brevity). For macOS, the CLI comes in the same package with the editor\nbinary, and could be installed into the system with the `cli: install` MonyaCode\ncommand which will create a symlink to the `/usr/local/bin/monyacode`. It can also be\nbuilt from source out of the `cli` crate in this repository.\n\nUse `monyacode --help` to see the full list of capabilities. General highlights:\n\n- Opening another empty MonyaCode window: `monyacode`\n\n- Opening a file or directory in MonyaCode: `monyacode /path/to/entry` (use `-n` to open\n  in the new window)\n\n- Reading from stdin: `ps axf | monyacode -`\n\n- Starting MonyaCode with logs in the terminal: `monyacode --foreground`\n\n- Uninstalling MonyaCode and all its related files: `monyacode --uninstall`\n"},{"path":"command-palette.md","title":"Command Palette","group":"Guides","content":"# Command Palette\n\nThe Command Palette is the main way to access available functionality in the\neditor. To open it, hit: {#kb command_palette::Toggle}.\n\nThe list of commands can be filtered by typing part of the command name to call.\nOnce the correct command is selected, hit enter to run it.\n\nAny time you see instructions that include commands of the form `monyacode: ...`,\n`editor: ...` or similar, you should be able to find and execute the command\nfrom the Command Palette. You can bind the command to a key combination in the\n[keymap editor](./key-bindings.md).\n"},{"path":"completions.md","title":"Completions","group":"Guides","content":"# Completions\n\n\"Code Completions\" provided by Language Servers (LSPs) automatically installed\nby MonyaCode or via [MonyaCode Language Extensions](./languages.md).\n\n## Language Server Code Completions {#code-completions}\n\nWhen there is an appropriate language server available, MonyaCode will provide\ncompletions of variable names, functions, and other symbols in the current file.\nYou can disable these by adding the following to your MonyaCode `settings.jsonc`\nfile:\n\n```json\n\"show_completions_on_input\": false\n```\n\nYou can manually trigger completions with `ctrl-space` or by triggering the\n`editor::ShowCompletions` action from the command palette.\n\nFor more information, see:\n\n- [Configuring Supported Languages](./configuring-languages.md)\n- [List of MonyaCode Supported Languages](./languages.md)\n"},{"path":"configuring-languages.md","title":"Configuring supported languages","group":"Guides","content":"# Configuring supported languages\n\nThe language support is built on two main technologies:\n\n1. Tree-sitter: This handles syntax highlighting and structure-based features\n   like the outline panel.\n2. Language Server Protocol (LSP): This provides semantic features such as code\n   completion and diagnostics.\n\nThese components work together to provide language capabilities.\n\n## Topics\n\n- Language-specific settings\n- File associations\n- Working with language servers\n- Formatting and linting configuration\n- Customizing syntax highlighting and themes\n- Advanced language features\n\nSee the list of [Supported Languages](./languages.md) for details on specific\nconfigurations. To add support for additional languages, see the guide on how to\ncreate new [Language Extensions](./extensions/languages.md).\n\n## Language-specific Settings\n\nIt is possible to override global settings for individual languages. These\ncustom configurations are defined in the `settings.jsonc` file under the\n`languages` key.\n\nHere's an example of language-specific settings:\n\n```json\n\"languages\": {\n  \"Python\": {\n    \"tab_size\": 4,\n    \"formatter\": \"language_server\",\n    \"format_on_save\": \"on\"\n  },\n  \"JavaScript\": {\n    \"tab_size\": 2,\n    \"formatter\": {\n      \"external\": {\n        \"command\": \"prettier\",\n        \"arguments\": [\"--stdin-filepath\", \"{buffer_path}\"]\n      }\n    }\n  }\n}\n```\n\nSome of the settings that can be customized include:\n\n- [`tab_size`](./configuring-monyacode.md#tab-size): The number of spaces for each\n  indentation level\n- [`formatter`](./configuring-monyacode.md#formatter): The tool used for code\n  formatting\n- [`format_on_save`](./configuring-monyacode.md#format-on-save): Whether to\n  automatically format code when saving\n- [`enable_language_server`](./configuring-monyacode.md#enable-language-server):\n  Toggle language server support\n- [`hard_tabs`](./configuring-monyacode.md#hard-tabs): Use tabs instead of spaces for\n  indentation\n- [`preferred_line_length`](./configuring-monyacode.md#preferred-line-length): The\n  recommended maximum line length\n- [`soft_wrap`](./configuring-monyacode.md#soft-wrap): How to wrap long lines of code\n- [`show_completions_on_input`](./configuring-monyacode.md#show-completions-on-input):\n  Whether or not to show completions as you type\n- [`show_completion_documentation`](./configuring-monyacode.md#show-completion-documentation):\n  Whether to display inline and alongside documentation for items in the\n  completions menu\n- [`colorize_brackets`](./configuring-monyacode.md#colorize-brackets): Whether to use\n  tree-sitter bracket queries to detect and colorize the brackets in the editor\n  (also known as \"rainbow brackets\")\n\n## File Associations\n\nMonyaCode automatically detects file types based on their extensions, but you can\ncustomize these associations.\n\nTo set up custom file associations, use the\n[`file_types`](./configuring-monyacode.md#file-types) setting in your\n`settings.jsonc`:\n\n```json\n\"file_types\": {\n  \"C++\": [\"c\"],\n  \"TOML\": [\"MyLockFile\"],\n  \"Dockerfile\": [\"Dockerfile*\"]\n}\n```\n\nThis configuration tells MonyaCode to:\n\n- Treat `.c` files as C++ instead of C\n- Recognize files named \"MyLockFile\" as TOML\n- Apply Dockerfile syntax to any file starting with \"Dockerfile\"\n\nYou can use glob patterns for more flexible matching, allowing you to handle\ncomplex naming conventions in your projects.\n\n## Working with Language Servers\n\nLanguage servers provide capabilities like auto-completion, go-to-definition,\nand real-time error checking.\n\n### What are Language Servers?\n\nLanguage servers implement the Language Server Protocol (LSP), which\nstandardizes communication between the editor and language-specific tools. This\nallows editors to support advanced features for multiple programming languages\nwithout implementing each feature separately.\n\nSome key features provided by language servers include:\n\n- Code completion\n- Error checking and diagnostics\n- Code navigation (go to definition, find references)\n- Code actions (Rename, extract method)\n- Hover information\n- Workspace symbol search\n\n### Managing Language Servers\n\nMonyaCode simplifies language server management for users:\n\n1. Suggested extensions: When you open a file with a known file type, you may be\n   prompted to install the associated extension. For the installation to work,\n   you will need to have [rustup](https://rustup.rs) configured correctly.\n\n2. Storage Location:\n\n   - macOS: `~/Library/Application Support/MonyaCode/languages`\n   - Linux: `$XDG_DATA_HOME/monyacode/languages`,\n     `$FLATPAK_XDG_DATA_HOME/monyacode/languages`, or\n     `$HOME/.local/share/monyacode/languages`\n\n### Choosing Language Servers\n\nSome languages offer multiple language server options. You might have multiple\nextensions installed that bundle language servers targeting the same language,\npotentially leading to overlapping capabilities. MonyaCode allows you to prioritize\nwhich language servers are used and in what order.\n\nSpecify preferences using the `language_servers` setting:\n\n```json\n  \"languages\": {\n    \"PHP\": {\n      \"language_servers\": [\"intelephense\", \"!phpactor\", \"!phptools\", \"...\"]\n    }\n  }\n```\n\nIn this example:\n\n- `intelephense` is set as the primary language server\n- `phpactor` is disabled (note the `!` prefix)\n- `...` expands to the rest of the language servers that are registered for PHP\n\n### Toolchains\n\nSome language servers need to be configured with a current \"toolchain\", which is\nan installation of a specific version of a programming language compiler or/and\ninterpreter, which can possibly include a full set of dependencies of a project.\n\nAn example of what MonyaCode considers a toolchain is a virtual environment in\nPython.\n\nNot all languages in MonyaCode support toolchain discovery and selection, but for\nthose that do, you can specify the toolchain from a toolchain picker (via\n{#action toolchain::Select}). To learn more about toolchains, see\n[`toolchains`](./toolchains.md).\n\n### Configuring Language Servers\n\nCustom configuration options for language servers are configured using the `lsp`\nsection of `settings.jsonc`:\n\n```json\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"initialization_options\": {\n        \"check\": {\n          \"command\": \"clippy\"\n        }\n      }\n    }\n  }\n```\n\nThis example configures the Rust Analyzer to use Clippy for additional linting\nwhen saving files.\n\n#### Nested objects\n\nWhen configuring language server options it's important to use nested objects\nrather than dot-delimited strings. This is particularly relevant when working\nwith more complex configurations. Let's look at a real-world example using the\nTypeScript language server:\n\nSuppose you want to configure the following settings for TypeScript:\n\n- Enable strict null checks\n- Set the target ECMAScript version to ES2020\n\nHere's how you would structure these settings in `settings.jsonc`:\n\n```json\n\"lsp\": {\n  \"typescript-language-server\": {\n    \"initialization_options\": {\n      // These are not supported (VSCode dotted style):\n      // \"preferences.strictNullChecks\": true,\n      // \"preferences.target\": \"ES2020\"\n      //\n      // These is correct (nested notation):\n      \"preferences\": {\n        \"strictNullChecks\": true,\n        \"target\": \"ES2020\"\n      },\n    }\n  }\n}\n```\n\n#### Possible configuration options\n\nDepending on how a particular language server is implemented, they may depend on\ninitialization options specified in the LSP.\n\n- [initializationOptions](https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/#version_3_17_0)\n\nThese options are sent during language server startup and require the server to\nbe restarted for changes to be applied.\n\nFor example, rust-analyzer and clangd are configured this way.\n\n```json\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"initialization_options\": {\n        \"checkOnSave\": false\n      }\n    }\n  }\n```\n\n- [Configuration Request](https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/#workspace_configuration)\n\nMost language servers use settings that can be updated without restarting.\n\n```json\n\"lsp\": {\n  \"tailwindcss-language-server\": {\n    \"settings\": {\n      \"tailwindCSS\": {\n        \"emmetCompletions\": true,\n      },\n    }\n  }\n}\n```\n\nSome language servers allow configuring how the language server binary is\nlaunched. Language servers are automatically downloaded or launched if found in\nyour path.\n\nTODO: This should be opt in.\n\nIf you wish to specify an explicit alternate binary you can specify that in\nsettings:\n\n```json\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"binary\": {\n        // Whether to fetch the binary from the internet, or attempt to find locally.\n        \"ignore_system_version\": false,\n        \"path\": \"/path/to/langserver/bin\",\n        \"arguments\": [\"--option\", \"value\"],\n        \"env\": {\n          \"FOO\": \"BAR\"\n        }\n      }\n    }\n  }\n```\n\n### Enabling or Disabling Language Servers\n\nYou can toggle language server support globally or per-language:\n\n```json\n  \"languages\": {\n    \"Markdown\": {\n      \"enable_language_server\": false\n    }\n  }\n```\n\nThis disables the language server for Markdown files, which can be useful for\nperformance in large documentation projects. You can configure this globally in\nyour `~/.config/monyacode/settings.jsonc` or inside a `.monyacode/settings.jsonc` in your\nproject directory.\n\n## Formatting and Linting\n\n### Configuring Formatters\n\nMonyaCode supports both built-in and external formatters. See\n[`formatter`](./configuring-monyacode.md#formatter) docs for more. Configure\nformatters globally or per-language in `settings.jsonc`:\n\n```json\n\"languages\": {\n  \"JavaScript\": {\n    \"formatter\": {\n      \"external\": {\n        \"command\": \"prettier\",\n        \"arguments\": [\"--stdin-filepath\", \"{buffer_path}\"]\n      }\n    },\n    \"format_on_save\": \"on\"\n  },\n  \"Rust\": {\n    \"formatter\": \"language_server\",\n    \"format_on_save\": \"on\"\n  }\n}\n```\n\nThis example uses Prettier for JavaScript and the language server's formatter\nfor Rust, both set to format on save.\n\nTo disable formatting for a specific language:\n\n```json\n\"languages\": {\n  \"Markdown\": {\n    \"format_on_save\": \"off\"\n  }\n}\n```\n\n### Setting Up Linters\n\nLinting is typically handled by language servers.\n\nMany language servers allow you to configure linting rules:\n\n```json\n\"lsp\": {\n  \"eslint\": {\n    \"settings\": {\n      \"codeActionOnSave\": {\n        \"rules\": [\"import/order\"]\n      }\n    }\n  }\n}\n```\n\nThis configuration sets up ESLint to organize imports on save for JavaScript\nfiles.\n\nTo run linter fixes automatically on save:\n\n```json\n\"languages\": {\n  \"JavaScript\": {\n    \"formatter\": {\n      \"code_action\": \"source.fixAll.eslint\"\n    }\n  }\n}\n```\n\n### Integrating Formatting and Linting\n\nHere's an example that uses Prettier for formatting and ESLint for linting\nJavaScript files automatically on save:\n\n```json\n\"languages\": {\n  \"JavaScript\": {\n    \"formatter\": [\n      {\n        \"code_action\": \"source.fixAll.eslint\"\n      },\n      {\n        \"external\": {\n          \"command\": \"prettier\",\n          \"arguments\": [\"--stdin-filepath\", \"{buffer_path}\"]\n        }\n      }\n    ],\n    \"format_on_save\": \"on\"\n  }\n}\n```\n\n### Troubleshooting\n\nIf you encounter issues with formatting or linting:\n\n1. Check the editor log file for error messages (Use the command palette:\n   `monyacode: open log`)\n2. Ensure external tools (formatters, linters) are correctly installed and in\n   your PATH.\n3. Verify configurations in both MonyaCode settings and language-specific config\n   files (e.g., `.eslintrc`, `.prettierrc`)\n\n## Syntax Highlighting and Themes\n\n### Customizing Syntax Highlighting\n\nMonyaCode uses Tree-sitter grammars for syntax highlighting. Override the default\nhighlighting using the `theme_overrides` setting.\n\nThis example makes comments italic and changes the color of strings:\n\n```json\n\"theme_overrides\": {\n  \"One Dark\": {\n    \"syntax\": {\n      \"comment\": {\n        \"font_style\": \"italic\"\n      },\n      \"string\": {\n        \"color\": \"#00AA00\"\n      }\n    }\n  }\n}\n```\n\n### Selecting and Customizing Themes\n\nTo change the editor theme:\n\n1. Use the theme selector ({#kb theme_selector::Toggle})\n2. Or set it in `settings.jsonc`:\n\n```json\n\"theme\": {\n  \"mode\": \"dark\",\n  \"dark\": \"One Dark\",\n  \"light\": \"GitHub Light\"\n}\n```\n\nCreate custom themes by creating a JSON file in `~/.config/monyacode/themes/`. MonyaCode\nwill automatically detect and make available any themes in this directory.\n\n### Using Theme Extensions\n\nMonyaCode supports theme extensions. Find the URL for the theme you want to install\nand install it from the Extensions panel ({#kb monyacode::Extensions}).\n\nTo create your own theme extension, refer to the\n[Developing Theme Extensions](./extensions/themes.md) guide.\n\n## Using Language Server Features\n\n### Inlay Hints\n\nInlay hints provide additional information inline in code, such as parameter\nnames or inferred types. Configure inlay hints in `settings.jsonc`:\n\n```json\n\"inlay_hints\": {\n  \"enabled\": true,\n  \"show_type_hints\": true,\n  \"show_parameter_hints\": true,\n  \"show_other_hints\": true\n}\n```\n\nFor language-specific inlay hint settings, refer to the documentation for each\nlanguage.\n\n### Code Actions\n\nCode actions provide quick fixes and refactoring options. Access code actions\nusing the `editor: Toggle Code Actions` command or by clicking the lightbulb\nicon that appears next to the cursor when actions are available.\n\n### Go To Definition and References\n\nUse these commands to navigate the codebase:\n\n- `editor: Go to Definition` (<kbd>f12|f12</kbd>)\n- `editor: Go to Type Definition` (<kbd>cmd-f12|ctrl-f12</kbd>)\n- `editor: Find All References` (<kbd>shift-f12|shift-f12</kbd>)\n\n### Rename Symbol\n\nTo rename a symbol across a project:\n\n1. Place the cursor on the symbol\n2. Use the `editor: Rename Symbol` command (<kbd>f2|f2</kbd>)\n3. Enter the new name and press Enter\n\nThese features depend on the capabilities of the language server for each\nlanguage.\n\nWhen renaming a symbol that spans multiple files, MonyaCode will open a preview in a\nmultibuffer. Here you can review all the changes before applying them. To\nconfirm the rename, simply save the multibuffer. If you decide not to proceed\nwith the rename, you can undo the changes or close the multibuffer without\nsaving.\n\n### Hover Information\n\nUse the `editor: Hover` command to display information about the symbol under\nthe cursor. This often includes type information, documentation, and links to\nrelevant resources.\n\n### Workspace Symbol Search\n\nThe `workspace: Open Symbol` command allows you to search for symbols\n(functions, classes, variables) across your entire project. This is useful for\nquickly navigating large codebases.\n\n### Code Completion\n\nMonyaCode provides code completion suggestions as you type. You can manually trigger\ncompletion with the `editor: Show Completions` command. Use <kbd>tab|tab</kbd>\nor <kbd>enter|enter</kbd> to accept suggestions.\n\n### Diagnostics\n\nLanguage servers provide real-time diagnostics (errors, warnings, hints) as you\ncode. View all diagnostics for your project using the `diagnostics: Toggle`\ncommand.\n"},{"path":"configuring-monyacode.md","title":"Configuration","group":"Guides","content":"# Configuration\n\nThere are separate pages describing how to configure these aspects of the\neditor:\n\n- [Theme](./themes.md)\n- [Key bindings](./key-bindings.md)\n- [Tasks](./tasks.md)\n- [Extensions](./extensions.md)\n\n## Settings Editor\n\nThe Settings Editor can be opened with the {#kb monyacode::OpenSettings} keybinding,\nor through the `monyacode: open settings` action in the command palette. Through it,\nyou can customize your local, user settings as well as project settings.\n\n> Note that not all settings that MonyaCode supports are available through the\n> Settings Editor yet. Some more intricate ones, such as language formatters,\n> can only be changed through the JSON settings file {#kb\n> monyacode::OpenSettingsFile}.\n\n## User Settings File\n\nYour settings.jsonc file can be opened with {#kb monyacode::OpenSettingsFile}. By\ndefault it is located at `~/.config/monyacode/settings.jsonc`, though if you have\n`XDG_CONFIG_HOME` in your environment on Linux it will be at\n`$XDG_CONFIG_HOME/monyacode/settings.json` instead.\n\nWhatever you have added to your user settings file gets merged with any local\nconfiguration inside your projects.\n\n### Default Settings\n\nIn the Settings Editor, the values you see set are the default ones. You can\nalso verify them in JSON by running {#action monyacode::OpenDefaultSettings} from the\ncommand palette.\n\nExtensions that provide language servers may also provide default settings for\nthose language servers.\n\n## Project Settings File\n\nSimilarly to user files, you can open your project settings file by running\n{#action monyacode::OpenProjectSettings} from the command palette. This will create a\n`.monyacode` directory containing`.monyacode/settings.jsonc`.\n\nAlthough most projects will only need one settings file at the root, you can add\nmore local settings files for subdirectories as needed. Not all settings can be\nset in local files, just those that impact the behavior of the editor and\nlanguage tooling. For example you can set `tab_size`, `formatter` etc. but not\n`theme`, `vim_mode` and similar.\n\nThe syntax for configuration files is a super-set of JSON that allows `//`\ncomments.\n\n## Per-release Channel Overrides\n\nYou can scope overrides to a specific channel by adding top-level `stable` or\n`dev` objects.\n\nThey are merged into the base configuration with settings from these keys taking\nprecedence upon launching the specified build. For example:\n\n```json\n{\n  \"theme\": \"sunset\",\n  \"vim_mode\": false,\n  \"stable\": {\n    \"theme\": \"monyacode-dark\",\n  },\n}\n```\n\nWith this configuration, Dev keeps all base preferences while Stable switches to\n`monyacode-dark`.\n\nChanging settings in the Settings Editor will always apply the change across all\nchannels.\n\n# Settings\n\nFind below an extensive run-through of many supported settings by MonyaCode.\n\n## Active Pane Modifiers\n\n- Description: Styling settings applied to the active pane.\n- Setting: `active_pane_modifiers`\n- Default:\n\n```json\n{\n  \"active_pane_modifiers\": {\n    \"border_size\": 0.0,\n    \"inactive_opacity\": 1.0,\n  },\n}\n```\n\n### Border size\n\n- Description: Size of the border surrounding the active pane. When set to 0,\n  the active pane doesn't have any border. The border is drawn inset.\n- Setting: `border_size`\n- Default: `0.0`\n\n**Options**\n\nNon-negative `float` values\n\n### Inactive Opacity\n\n- Description: Opacity of inactive panels. When set to 1.0, the inactive panes\n  have the same opacity as the active one. If set to 0, the inactive panes\n  content will not be visible at all. Values are clamped to the [0.0, 1.0]\n  range.\n- Setting: `inactive_opacity`\n- Default: `1.0`\n\n**Options**\n\n`float` values\n\n## Bottom Dock Layout\n\n- Description: Control the layout of the bottom dock, relative to the left and\n  right docks.\n- Setting: `bottom_dock_layout`\n- Default: `\"contained\"`\n\n**Options**\n\n1. Contain the bottom dock, giving the full height of the window to the left and\n   right docks.\n\n```json\n{\n  \"bottom_dock_layout\": \"contained\",\n}\n```\n\n2. Give the bottom dock the full width of the window, truncating the left and\n   right docks.\n\n```json\n{\n  \"bottom_dock_layout\": \"full\",\n}\n```\n\n3. Left align the bottom dock, truncating the left dock and giving the right\n   dock the full height of the window.\n\n```json\n{\n  \"bottom_dock_layout\": \"left_aligned\",\n}\n```\n\n4. Right align the bottom dock, giving the left dock the full height of the\n   window and truncating the right dock.\n\n```json\n{\n  \"bottom_dock_layout\": \"right_aligned\",\n}\n```\n\n## Allow Rewrap\n\n- Description: Controls where the {#action editor::Rewrap} action is allowed in\n  the current language scope\n- Setting: `allow_rewrap`\n- Default: `\"in_comments\"`\n\n**Options**\n\n1. Allow rewrap in comments only:\n\n```json\n{\n  \"allow_rewrap\": \"in_comments\",\n}\n```\n\n2. Allow rewrap in selections only:\n\n```json\n{\n  \"allow_rewrap\": \"in_selections\",\n}\n```\n\n3. Allow rewrap anywhere:\n\n```json\n{\n  \"allow_rewrap\": \"anywhere\",\n}\n```\n\nNote: This setting has no effect in Vim mode, as rewrap is already allowed\neverywhere.\n\n## Auto Indent\n\n- Description: Whether indentation should be adjusted based on the context\n  whilst typing. This can be specified on a per-language basis.\n- Setting: `auto_indent`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Auto Indent On Paste\n\n- Description: Whether indentation of pasted content should be adjusted based on\n  the context\n- Setting: `auto_indent_on_paste`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Auto Install extensions\n\nExtensions have to be manually installed in MonyaCode.\n\nYou can find the names of your currently installed extensions by listing the\nsubfolders under the\n[extension installation location](./extensions/installing-extensions.md#installation-location):\n\nOn macOS:\n\n```sh\nls ~/Library/Application\\ Support/MonyaCode/extensions/installed/\n```\n\nOn Linux:\n\n```sh\nls ~/.local/share/monyacode/extensions/installed\n```\n\n## Autosave\n\n- Description: When to automatically save edited buffers.\n- Setting: `autosave`\n- Default: `off`\n\n**Options**\n\n1. To disable autosave, set it to `off`:\n\n```json\n{\n  \"autosave\": \"off\",\n}\n```\n\n2. To autosave when focus changes, use `on_focus_change`:\n\n```json\n{\n  \"autosave\": \"on_focus_change\",\n}\n```\n\n3. To autosave when the active window changes, use `on_window_change`:\n\n```json\n{\n  \"autosave\": \"on_window_change\",\n}\n```\n\n4. To autosave after an inactivity period, use `after_delay`:\n\n```json\n{\n  \"autosave\": {\n    \"after_delay\": {\n      \"milliseconds\": 1000,\n    },\n  },\n}\n```\n\nNote that a save will be triggered when an unsaved tab is closed, even if this\nis earlier than the configured inactivity period.\n\n## Autoscroll on Clicks\n\n- Description: Whether to scroll when clicking near the edge of the visible text\n  area.\n- Setting: `autoscroll_on_clicks`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n## Auto Signature Help\n\n- Description: Show method signatures in the editor, when inside parentheses\n- Setting: `auto_signature_help`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n### Show Signature Help After Edits\n\n- Description: Whether to show the signature help after completion or a bracket\n  pair inserted. If `auto_signature_help` is enabled, this setting will be\n  treated as enabled also.\n- Setting: `show_signature_help_after_edits`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n## Base Keymap\n\n- Description: Base key bindings scheme. Base keymaps can be overridden with\n  user keymaps.\n- Setting: `base_keymap`\n- Default: `Emacs`\n\n**Options**\n\n1. VS Code\n\n```json\n{\n  \"base_keymap\": \"VSCode\",\n}\n```\n\n2. Atom\n\n```json\n{\n  \"base_keymap\": \"Atom\",\n}\n```\n\n3. JetBrains\n\n```json\n{\n  \"base_keymap\": \"JetBrains\",\n}\n```\n\n4. None\n\n```json\n{\n  \"base_keymap\": \"None\",\n}\n```\n\n5. Sublime Text\n\n```json\n{\n  \"base_keymap\": \"SublimeText\",\n}\n```\n\n6. TextMate\n\n```json\n{\n  \"base_keymap\": \"TextMate\",\n}\n```\n\n7. Emacs\n\n```json\n{\n  \"base_keymap\": \"Emacs\",\n}\n```\n\n8. Minimal\n\n```json\n{\n  \"base_keymap\": \"Minimal\",\n}\n```\n\n## Buffer Font Family\n\n- Description: The name of a font to use for rendering text in the editor.\n- Setting: `buffer_font_family`\n- Default: `.MonyaCodeMono`.\n\n**Options**\n\nThe name of any font family installed on the user's system, or `\".MonyaCodeMono\"`.\n\n## Buffer Font Features\n\n- Description: The OpenType features to enable for text in the editor.\n- Setting: `buffer_font_features`\n- Default: `null`\n- Platform: macOS and Windows.\n\n**Options**\n\nMonyaCode supports all OpenType features that can be enabled or disabled for a given\nbuffer or terminal font, as well as setting values for font features.\n\nFor example, to disable font ligatures, add the following to your settings:\n\n```json\n{\n  \"buffer_font_features\": {\n    \"calt\": false,\n  },\n}\n```\n\nYou can also set other OpenType features, like setting `cv01` to `7`:\n\n```json\n{\n  \"buffer_font_features\": {\n    \"cv01\": 7,\n  },\n}\n```\n\n## Buffer Font Fallbacks\n\n- Description: Set the buffer text's font fallbacks, this will be merged with\n  the platform's default fallbacks.\n- Setting: `buffer_font_fallbacks`\n- Default: `null`\n- Platform: macOS and Windows.\n\n**Options**\n\nFor example, to use `Nerd Font` as a fallback, add the following to your\nsettings:\n\n```json\n{\n  \"buffer_font_fallbacks\": [\"Nerd Font\"],\n}\n```\n\n## Buffer Font Size\n\n- Description: The default font size for text in the editor.\n- Setting: `buffer_font_size`\n- Default: `15`\n\n**Options**\n\nA font size from `6` to `100` pixels (inclusive)\n\n## Buffer Font Weight\n\n- Description: The default font weight for text in the editor.\n- Setting: `buffer_font_weight`\n- Default: `400`\n\n**Options**\n\n`integer` values between `100` and `900`\n\n## Buffer Line Height\n\n- Description: The default line height for text in the editor.\n- Setting: `buffer_line_height`\n- Default: `\"comfortable\"`\n\n**Options**\n\n`\"standard\"`, `\"comfortable\"` or `{ \"custom\": float }` (`1` is compact, `2` is\nloose)\n\n## Centered Layout\n\n- Description: Configuration for the centered layout mode.\n- Setting: `centered_layout`\n- Default:\n\n```json\n\"centered_layout\": {\n  \"left_padding\": 0.2,\n  \"right_padding\": 0.2,\n}\n```\n\n**Options**\n\nThe `left_padding` and `right_padding` options define the relative width of the\nleft and right padding of the central pane from the workspace when the centered\nlayout mode is activated. Valid values range is from `0` to `0.4`.\n\n## Close on File Delete\n\n- Description: Whether to automatically close editor tabs when their\n  corresponding files are deleted from disk.\n- Setting: `close_on_file_delete`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\nWhen enabled, this setting will automatically close tabs for files that have\nbeen deleted from the file system. This is particularly useful for workflows\ninvolving temporary or scratch files that are frequently created and deleted.\nWhen disabled (default), deleted files remain open with a strikethrough through\ntheir tab title.\n\nNote: Dirty files (files with unsaved changes) will not be automatically closed\neven when this setting is enabled, ensuring you don't lose unsaved work.\n\n## Confirm Quit\n\n- Description: Whether or not to prompt the user to confirm before closing the\n  application.\n- Setting: `confirm_quit`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n## Diagnostics Max Severity\n\n- Description: Which level to use to filter out diagnostics displayed in the\n  editor\n- Setting: `diagnostics_max_severity`\n- Default: `null`\n\n**Options**\n\n1. Allow all diagnostics (default):\n\n```json\n{\n  \"diagnostics_max_severity\": \"all\",\n}\n```\n\n2. Show only errors:\n\n```json\n{\n  \"diagnostics_max_severity\": \"error\",\n}\n```\n\n3. Show errors and warnings:\n\n```json\n{\n  \"diagnostics_max_severity\": \"warning\",\n}\n```\n\n4. Show errors, warnings, and information:\n\n```json\n{\n  \"diagnostics_max_severity\": \"info\",\n}\n```\n\n5. Show all including hints:\n\n```json\n{\n  \"diagnostics_max_severity\": \"hint\",\n}\n```\n\n## Direnv Integration\n\n- Description: Settings for [direnv](https://direnv.net/) integration. Requires\n  `direnv` to be installed. `direnv` integration make it possible to use the\n  environment variables set by a `direnv` configuration to detect some language\n  servers in `$PATH` instead of installing them. It also allows for those\n  environment variables to be used in tasks.\n- Setting: `load_direnv`\n- Default: `\"direct\"`\n\n**Options**\n\nThere are three options to choose from:\n\n1. `shell_hook`: Use the shell hook to load direnv. This relies on direnv to\n   activate upon entering the directory. Supports POSIX shells and fish.\n2. `direct`: Use `direnv export json` to load direnv. This will load direnv\n   directly without relying on the shell hook and might cause some\n   inconsistencies. This allows direnv to work with any shell.\n3. `disabled`: No shell environment will be loaded automatically; direnv must be\n   invoked manually (e.g. with `direnv exec`) to be used.\n\n## Double Click In Multibuffer\n\n- Description: What to do when multibuffer is double clicked in some of its\n  excerpts (parts of singleton buffers)\n- Setting: `double_click_in_multibuffer`\n- Default: `\"select\"`\n\n**Options**\n\n1. Behave as a regular buffer and select the whole word (default):\n\n```json\n{\n  \"double_click_in_multibuffer\": \"select\",\n}\n```\n\n2. Open the excerpt clicked as a new buffer in the new tab:\n\n```json\n{\n  \"double_click_in_multibuffer\": \"open\",\n}\n```\n\nFor the case of \"open\", regular selection behavior can be achieved by holding\n`alt` when double clicking.\n\n## Drop Target Size\n\n- Description: Relative size of the drop target in the editor that will open\n  dropped file as a split pane (0-0.5). For example, 0.25 means if you drop onto\n  the top/bottom quarter of the pane a new vertical split will be used, if you\n  drop onto the left/right quarter of the pane a new horizontal split will be\n  used.\n- Setting: `drop_target_size`\n- Default: `0.2`\n\n**Options**\n\n`float` values between `0` and `0.5`\n\n## Current Line Highlight\n\n- Description: How to highlight the current line in the editor.\n- Setting: `current_line_highlight`\n- Default: `all`\n\n**Options**\n\n1. Don't highlight the current line:\n\n```json\n\"current_line_highlight\": \"none\"\n```\n\n2. Highlight the gutter area:\n\n```json\n\"current_line_highlight\": \"gutter\"\n```\n\n3. Highlight the editor area:\n\n```json\n\"current_line_highlight\": \"line\"\n```\n\n4. Highlight the full line:\n\n```json\n\"current_line_highlight\": \"all\"\n```\n\n## Selection Highlight\n\n- Description: Whether to highlight all occurrences of the selected text in an\n  editor.\n- Setting: `selection_highlight`\n- Default: `true`\n\n## Rounded Selection\n\n- Description: Whether the text selection should have rounded corners.\n- Setting: `rounded_selection`\n- Default: `true`\n\n## Cursor Blink\n\n- Description: Whether or not the cursor blinks.\n- Setting: `cursor_blink`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Cursor Shape\n\n- Description: Cursor shape for the default editor.\n- Setting: `cursor_shape`\n- Default: `bar`\n\n**Options**\n\n1. A vertical bar:\n\n```json\n\"cursor_shape\": \"bar\"\n```\n\n2. A block that surrounds the following character:\n\n```json\n\"cursor_shape\": \"block\"\n```\n\n3. An underline / underscore that runs along the following character:\n\n```json\n\"cursor_shape\": \"underline\"\n```\n\n4. An box drawn around the following character:\n\n```json\n\"cursor_shape\": \"hollow\"\n```\n\n## Gutter\n\n- Description: Settings for the editor gutter\n- Setting: `gutter`\n- Default:\n\n```json\n{\n  \"gutter\": {\n    \"line_numbers\": true,\n    \"runnables\": true,\n    \"breakpoints\": true,\n    \"folds\": true,\n    \"min_line_number_digits\": 4,\n  },\n}\n```\n\n**Options**\n\n- `line_numbers`: Whether to show line numbers in the gutter\n- `runnables`: Whether to show runnable buttons in the gutter\n- `breakpoints`: Whether to show breakpoints in the gutter\n- `folds`: Whether to show fold buttons in the gutter\n- `min_line_number_digits`: Minimum number of characters to reserve space for in\n  the gutter\n\n## Hide Mouse\n\n- Description: Determines when the mouse cursor should be hidden in an editor or\n  input box.\n- Setting: `hide_mouse`\n- Default: `on_typing_and_movement`\n\n**Options**\n\n1. Never hide the mouse cursor:\n\n```json\n\"hide_mouse\": \"never\"\n```\n\n2. Hide only when typing:\n\n```json\n\"hide_mouse\": \"on_typing\"\n```\n\n3. Hide on both typing and cursor movement:\n\n```json\n\"hide_mouse\": \"on_typing_and_movement\"\n```\n\n## Snippet Sort Order\n\n- Description: Determines how snippets are sorted relative to other completion\n  items.\n- Setting: `snippet_sort_order`\n- Default: `inline`\n\n**Options**\n\n1. Place snippets at the top of the completion list:\n\n```json\n\"snippet_sort_order\": \"top\"\n```\n\n2. Place snippets normally without any preference:\n\n```json\n\"snippet_sort_order\": \"inline\"\n```\n\n3. Place snippets at the bottom of the completion list:\n\n```json\n\"snippet_sort_order\": \"bottom\"\n```\n\n4. Do not show snippets in the completion list at all:\n\n```json\n\"snippet_sort_order\": \"none\"\n```\n\n## Editor Scrollbar\n\n- Description: Whether or not to show the editor scrollbar and various elements\n  in it.\n- Setting: `scrollbar`\n- Default:\n\n```json\n\"scrollbar\": {\n  \"show\": \"auto\",\n  \"cursors\": true,\n  \"git_diff\": true,\n  \"search_results\": true,\n  \"selected_text\": true,\n  \"selected_symbol\": true,\n  \"diagnostics\": \"all\",\n  \"axes\": {\n    \"horizontal\": true,\n    \"vertical\": true,\n  },\n},\n```\n\n### Show Mode\n\n- Description: When to show the editor scrollbar.\n- Setting: `show`\n- Default: `auto`\n\n**Options**\n\n1. Show the scrollbar if there's important information or follow the system's\n   configured behavior:\n\n```json\n\"scrollbar\": {\n  \"show\": \"auto\"\n}\n```\n\n2. Match the system's configured behavior:\n\n```json\n\"scrollbar\": {\n  \"show\": \"system\"\n}\n```\n\n3. Always show the scrollbar:\n\n```json\n\"scrollbar\": {\n  \"show\": \"always\"\n}\n```\n\n4. Never show the scrollbar:\n\n```json\n\"scrollbar\": {\n  \"show\": \"never\"\n}\n```\n\n### Git Diff Indicators\n\n- Description: Whether to show git diff indicators in the scrollbar.\n- Setting: `git_diff`\n- Default: `true`\n\nGit diff indicators appear as colored marks showing lines that have been added,\nmodified, or deleted compared to the git HEAD.\n\n**Options**\n\n`boolean` values\n\n### Search Results Indicators\n\n- Description: Whether to show buffer search results in the scrollbar.\n- Setting: `search_results`\n- Default: `true`\n\nSearch result indicators appear as marks showing all locations in the file where\nyour current search query matches.\n\n**Options**\n\n`boolean` values\n\n### Selected Text Indicators\n\n- Description: Whether to show selected text occurrences in the scrollbar.\n- Setting: `selected_text`\n- Default: `true`\n\nSelected text indicators appear as marks showing all occurrences of the\ncurrently selected text throughout the file.\n\n**Options**\n\n`boolean` values\n\n### Selected Symbols Indicators\n\n- Description: Whether to show selected symbol occurrences in the scrollbar.\n- Setting: `selected_symbol`\n- Default: `true`\n\nSelected symbol indicators appear as marks showing all occurrences of the\ncurrently selected symbol (like a function or variable name) throughout the\nfile.\n\n**Options**\n\n`boolean` values\n\n### Diagnostics\n\n- Description: Which diagnostic indicators to show in the scrollbar.\n- Setting: `diagnostics`\n- Default: `all`\n\nDiagnostic indicators appear as colored marks showing errors, warnings, and\nother language server diagnostics at their corresponding line positions in the\nfile.\n\n**Options**\n\n1. Show all diagnostics:\n\n```json\n{\n  \"show_diagnostics\": \"all\",\n}\n```\n\n2. Do not show any diagnostics:\n\n```json\n{\n  \"show_diagnostics\": \"off\",\n}\n```\n\n3. Show only errors:\n\n```json\n{\n  \"show_diagnostics\": \"error\",\n}\n```\n\n4. Show only errors and warnings:\n\n```json\n{\n  \"show_diagnostics\": \"warning\",\n}\n```\n\n5. Show only errors, warnings, and information:\n\n```json\n{\n  \"show_diagnostics\": \"info\",\n}\n```\n\n### Axes\n\n- Description: Forcefully enable or disable the scrollbar for each axis\n- Setting: `axes`\n- Default:\n\n```json\n\"scrollbar\": {\n  \"axes\": {\n    \"horizontal\": true,\n    \"vertical\": true,\n  },\n}\n```\n\n#### Horizontal\n\n- Description: When false, forcefully disables the horizontal scrollbar.\n  Otherwise, obey other settings.\n- Setting: `horizontal`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n#### Vertical\n\n- Description: When false, forcefully disables the vertical scrollbar.\n  Otherwise, obey other settings.\n- Setting: `vertical`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Minimap\n\n- Description: Settings related to the editor's minimap, which provides an\n  overview of your document.\n- Setting: `minimap`\n- Default:\n\n```json\n{\n  \"minimap\": {\n    \"show\": \"never\",\n    \"thumb\": \"always\",\n    \"thumb_border\": \"left_open\",\n    \"current_line_highlight\": null,\n  },\n}\n```\n\n### Show Mode\n\n- Description: When to show the minimap in the editor.\n- Setting: `show`\n- Default: `never`\n\n**Options**\n\n1. Always show the minimap:\n\n```json\n{\n  \"show\": \"always\",\n}\n```\n\n2. Show the minimap if the editor's scrollbars are visible:\n\n```json\n{\n  \"show\": \"auto\",\n}\n```\n\n3. Never show the minimap:\n\n```json\n{\n  \"show\": \"never\",\n}\n```\n\n### Thumb Display\n\n- Description: When to show the minimap thumb (the visible editor area) in the\n  minimap.\n- Setting: `thumb`\n- Default: `always`\n\n**Options**\n\n1. Show the minimap thumb when hovering over the minimap:\n\n```json\n{\n  \"thumb\": \"hover\",\n}\n```\n\n2. Always show the minimap thumb:\n\n```json\n{\n  \"thumb\": \"always\",\n}\n```\n\n### Thumb Border\n\n- Description: How the minimap thumb border should look.\n- Setting: `thumb_border`\n- Default: `left_open`\n\n**Options**\n\n1. Display a border on all sides of the thumb:\n\n```json\n{\n  \"thumb_border\": \"full\",\n}\n```\n\n2. Display a border on all sides except the left side:\n\n```json\n{\n  \"thumb_border\": \"left_open\",\n}\n```\n\n3. Display a border on all sides except the right side:\n\n```json\n{\n  \"thumb_border\": \"right_open\",\n}\n```\n\n4. Display a border only on the left side:\n\n```json\n{\n  \"thumb_border\": \"left_only\",\n}\n```\n\n5. Display the thumb without any border:\n\n```json\n{\n  \"thumb_border\": \"none\",\n}\n```\n\n### Current Line Highlight\n\n- Description: How to highlight the current line in the minimap.\n- Setting: `current_line_highlight`\n- Default: `null`\n\n**Options**\n\n1. Inherit the editor's current line highlight setting:\n\n```json\n{\n  \"minimap\": {\n    \"current_line_highlight\": null,\n  },\n}\n```\n\n2. Highlight the current line in the minimap:\n\n```json\n{\n  \"minimap\": {\n    \"current_line_highlight\": \"line\",\n  },\n}\n```\n\nor\n\n```json\n{\n  \"minimap\": {\n    \"current_line_highlight\": \"all\",\n  },\n}\n```\n\n3. Do not highlight the current line in the minimap:\n\n```json\n{\n  \"minimap\": {\n    \"current_line_highlight\": \"gutter\",\n  },\n}\n```\n\nor\n\n```json\n{\n  \"minimap\": {\n    \"current_line_highlight\": \"none\",\n  },\n}\n```\n\n## Editor Tab Bar\n\n- Description: Settings related to the editor's tab bar.\n- Settings: `tab_bar`\n- Default:\n\n```json\n\"tab_bar\": {\n  \"show\": true,\n  \"show_nav_history_buttons\": true,\n  \"show_tab_bar_buttons\": true\n}\n```\n\n### Show\n\n- Description: Whether or not to show the tab bar in the editor.\n- Setting: `show`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n### Navigation History Buttons\n\n- Description: Whether or not to show the navigation history buttons.\n- Setting: `show_nav_history_buttons`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n### Tab Bar Buttons\n\n- Description: Whether or not to show the tab bar buttons.\n- Setting: `show_tab_bar_buttons`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Editor Tabs\n\n- Description: Configuration for the editor tabs.\n- Setting: `tabs`\n- Default:\n\n```json\n\"tabs\": {\n  \"close_position\": \"right\",\n  \"file_icons\": false,\n  \"git_status\": false,\n  \"activate_on_close\": \"history\",\n  \"show_close_button\": \"hover\",\n  \"show_diagnostics\": \"off\"\n},\n```\n\n### Close Position\n\n- Description: Where to display close button within a tab.\n- Setting: `close_position`\n- Default: `right`\n\n**Options**\n\n1. Display the close button on the right:\n\n```json\n{\n  \"close_position\": \"right\",\n}\n```\n\n2. Display the close button on the left:\n\n```json\n{\n  \"close_position\": \"left\",\n}\n```\n\n### File Icons\n\n- Description: Whether to show the file icon for a tab.\n- Setting: `file_icons`\n- Default: `false`\n\n### Git Status\n\n- Description: Whether or not to show Git file status in tab.\n- Setting: `git_status`\n- Default: `false`\n\n### Activate on close\n\n- Description: What to do after closing the current tab.\n- Setting: `activate_on_close`\n- Default: `history`\n\n**Options**\n\n1.  Activate the tab that was open previously:\n\n```json\n{\n  \"activate_on_close\": \"history\",\n}\n```\n\n2. Activate the right neighbour tab if present:\n\n```json\n{\n  \"activate_on_close\": \"neighbour\",\n}\n```\n\n3. Activate the left neighbour tab if present:\n\n```json\n{\n  \"activate_on_close\": \"left_neighbour\",\n}\n```\n\n### Show close button\n\n- Description: Controls the appearance behavior of the tab's close button.\n- Setting: `show_close_button`\n- Default: `hover`\n\n**Options**\n\n1.  Show it just upon hovering the tab:\n\n```json\n{\n  \"show_close_button\": \"hover\",\n}\n```\n\n2. Show it persistently:\n\n```json\n{\n  \"show_close_button\": \"always\",\n}\n```\n\n3. Never show it, even if hovering it:\n\n```json\n{\n  \"show_close_button\": \"hidden\",\n}\n```\n\n### Show Diagnostics\n\n- Description: Whether to show diagnostics indicators in tabs. This setting only\n  works when file icons are active and controls which files with diagnostic\n  issues to mark.\n- Setting: `show_diagnostics`\n- Default: `off`\n\n**Options**\n\n1. Do not mark any files:\n\n```json\n{\n  \"show_diagnostics\": \"off\",\n}\n```\n\n2. Only mark files with errors:\n\n```json\n{\n  \"show_diagnostics\": \"errors\",\n}\n```\n\n3. Mark files with errors and warnings:\n\n```json\n{\n  \"show_diagnostics\": \"all\",\n}\n```\n\n### Show Inline Code Actions\n\n- Description: Whether to show code action button at start of buffer line.\n- Setting: `inline_code_actions`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n### Drag And Drop Selection\n\n- Description: Whether to allow drag and drop text selection in buffer. `delay`\n  is the milliseconds that must elapse before drag and drop is allowed.\n  Otherwise, a new text selection is created.\n- Setting: `drag_and_drop_selection`\n- Default:\n\n```json\n\"drag_and_drop_selection\": {\n  \"enabled\": true,\n  \"delay\": 300\n}\n```\n\n## Editor Toolbar\n\n- Description: Whether or not to show various elements in the editor toolbar.\n- Setting: `toolbar`\n- Default:\n\n```json\n\"toolbar\": {\n  \"breadcrumbs\": true,\n  \"quick_actions\": true,\n  \"selections_menu\": true,\n  \"code_actions\": false\n},\n```\n\n**Options**\n\nEach option controls displaying of a particular toolbar element. If all elements\nare hidden, the editor toolbar is not displayed.\n\n## Use System Tabs\n\n- Description: Whether to allow windows to tab together based on the user’s\n  tabbing preference (macOS only).\n- Setting: `use_system_window_tabs`\n- Default: `false`\n\n**Options**\n\nThis setting enables integration with macOS’s native window tabbing feature.\nWhen set to `true`, MonyaCode windows can be grouped together as tabs in a single\nmacOS window, following the system-wide tabbing preferences set by the user\n(such as \"Always\", \"In Full Screen\", or \"Never\"). This setting is only available\non macOS.\n\n## Enable Language Server\n\n- Description: Whether or not to use language servers to provide code\n  intelligence.\n- Setting: `enable_language_server`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Ensure Final Newline On Save\n\n- Description: Removes any lines containing only whitespace at the end of the\n  file and ensures just one newline at the end.\n- Setting: `ensure_final_newline_on_save`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Expand Excerpt Lines\n\n- Description: The default number of lines to expand excerpts in the multibuffer\n  by\n- Setting: `expand_excerpt_lines`\n- Default: `5`\n\n**Options**\n\nPositive `integer` values\n\n## Excerpt Context Lines\n\n- Description: The number of lines of context to provide when showing excerpts\n  in the multibuffer.\n- Setting: `excerpt_context_lines`\n- Default: `2`\n\n**Options**\n\nPositive `integer` value between 1 and 32. Values outside of this range will be\nclamped to this range.\n\n## Extend Comment On Newline\n\n- Description: Whether to start a new line with a comment when a previous line\n  is a comment as well.\n- Setting: `extend_comment_on_newline`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Status Bar\n\n- Description: Control various elements in the status bar. Note that some items\n  in the status bar have their own settings set elsewhere.\n- Setting: `status_bar`\n- Default:\n\n```json\n\"status_bar\": {\n  \"show\": true,\n  \"position\": \"bottom\",\n  \"active_language_button\": true,\n  \"cursor_position_button\": true,\n  \"line_endings_button\": false,\n  \"active_encoding_button\": \"non_utf8\"\n},\n```\n\n## LSP\n\n- Description: Configuration for language servers.\n- Setting: `lsp`\n- Default: `null`\n\n**Options**\n\nThe following settings can be overridden for specific language servers:\n\n- `initialization_options`\n- `settings`\n\nTo override configuration for a language server, add an entry for that language\nserver's name to the `lsp` value.\n\nSome options are passed via `initialization_options` to the language server.\nThese are for options which must be specified at language server startup and\nwhen changed will require restarting the language server.\n\nFor example to pass the `check` option to `rust-analyzer`, use the following\nconfiguration:\n\n```json\n\"lsp\": {\n  \"rust-analyzer\": {\n    \"initialization_options\": {\n      \"check\": {\n        \"command\": \"clippy\" // rust-analyzer.check.command (default: \"check\")\n      }\n    }\n  }\n}\n```\n\nWhile other options may be changed at a runtime and should be placed under\n`settings`:\n\n```json\n\"lsp\": {\n  \"yaml-language-server\": {\n    \"settings\": {\n      \"yaml\": {\n        \"keyOrdering\": true // Enforces alphabetical ordering of keys in maps\n      }\n    }\n  }\n}\n```\n\n## Global LSP Settings\n\n- Description: Configuration for global LSP settings that apply to all language\n  servers\n- Setting: `global_lsp_settings`\n- Default:\n\n```json\n{\n  \"global_lsp_settings\": {\n    \"button\": true,\n  },\n}\n```\n\n**Options**\n\n- `button`: Whether to show the LSP status button in the status bar\n\n## LSP Highlight Debounce\n\n- Description: The debounce delay in milliseconds before querying highlights\n  from the language server based on the current cursor location.\n- Setting: `lsp_highlight_debounce`\n- Default: `75`\n\n**Options**\n\n`integer` values representing milliseconds\n\n## Features\n\n- Description: Features that can be globally enabled or disabled\n- Setting: `features`\n- Default:\n\n```json\n{\n  \"features\": {},\n}\n```\n\n## Format On Save\n\n- Description: Whether or not to perform a buffer format before saving.\n- Setting: `format_on_save`\n- Default: `on`\n\n**Options**\n\n1. `on`, enables format on save obeying `formatter` setting:\n\n```json\n{\n  \"format_on_save\": \"on\",\n}\n```\n\n2. `off`, disables format on save:\n\n```json\n{\n  \"format_on_save\": \"off\",\n}\n```\n\n## Formatter\n\n- Description: How to perform a buffer format.\n- Setting: `formatter`\n- Default: `auto`\n\n**Options**\n\n1. To use the current language server, use `\"language_server\"`:\n\n```json\n{\n  \"formatter\": \"language_server\",\n}\n```\n\n2. Or to use an external command, use `\"external\"`. Specify the name of the\n   formatting program to run, and an array of arguments to pass to the program.\n   The buffer's text will be passed to the program on stdin, and the formatted\n   output should be written to stdout. For example, the following command would\n   strip trailing spaces using [`sed(1)`](https://linux.die.net/man/1/sed):\n\n```json\n{\n  \"formatter\": {\n    \"external\": {\n      \"command\": \"sed\",\n      \"arguments\": [\"-e\", \"s/ *$//\"],\n    },\n  },\n}\n```\n\n3. External formatters may optionally include a `{buffer_path}` placeholder\n   which at runtime will include the path of the buffer being formatted.\n   Formatters operate by receiving file content via standard input, reformatting\n   it and then outputting it to standard output and so normally don't know the\n   filename of what they are formatting. Tools like Prettier support receiving\n   the file path via a command line argument which can then used to impact\n   formatting decisions.\n\nWARNING: `{buffer_path}` should not be used to direct your formatter to read\nfrom a filename. Your formatter should only read from standard input and should\nnot read or write files directly.\n\n```json\n  \"formatter\": {\n    \"external\": {\n      \"command\": \"prettier\",\n      \"arguments\": [\"--stdin-filepath\", \"{buffer_path}\"]\n    }\n  }\n```\n\n4. Or to use code actions provided by the connected language servers, use\n   `\"code_actions\"`:\n\n```json\n{\n  \"formatter\": [\n    // Use ESLint's --fix:\n    { \"code_action\": \"source.fixAll.eslint\" },\n    // Organize imports on save:\n    { \"code_action\": \"source.organizeImports\" },\n  ],\n}\n```\n\n5. Or to use multiple formatters consecutively, use an array of formatters:\n\n```json\n{\n  \"formatter\": [\n    { \"language_server\": { \"name\": \"rust-analyzer\" } },\n    {\n      \"external\": {\n        \"command\": \"sed\",\n        \"arguments\": [\"-e\", \"s/ *$//\"],\n      },\n    },\n  ],\n}\n```\n\nHere `rust-analyzer` will be used first to format the code, followed by a call\nof sed. If any of the formatters fails, the subsequent ones will still be\nexecuted.\n\n## Auto close\n\n- Description: Whether to automatically add matching closing characters when\n  typing opening parenthesis, bracket, brace, single or double quote characters.\n- Setting: `use_autoclose`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Always Treat Brackets As Autoclosed\n\n- Description: Controls how the editor handles the autoclosed characters.\n- Setting: `always_treat_brackets_as_autoclosed`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n**Example**\n\nIf the setting is set to `true`:\n\n1. Enter in the editor: `)))`\n2. Move the cursor to the start: `^)))`\n3. Enter again: `)))`\n\nThe result is still `)))` and not `))))))`, which is what it would be by\ndefault.\n\n## File Scan Exclusions\n\n- Setting: `file_scan_exclusions`\n- Description: Files or globs of files that will be excluded by MonyaCode entirely.\n  They will be skipped during file scans, file searches, and not be displayed in\n  the project file tree. Overrides `file_scan_inclusions`.\n- Default:\n\n```json\n\"file_scan_exclusions\": [\n  \"**/.git\",\n  \"**/.svn\",\n  \"**/.hg\",\n  \"**/.jj\",\n  \"**/CVS\",\n  \"**/.DS_Store\",\n  \"**/Thumbs.db\",\n  \"**/.classpath\",\n  \"**/.settings\"\n],\n```\n\nNote, specifying `file_scan_exclusions` in settings.jsonc will override the\ndefaults (shown above). If you are looking to exclude additional items you will\nneed to include all the default values in your settings.\n\n## File Scan Inclusions\n\n- Setting: `file_scan_inclusions`\n- Description: Files or globs of files that will be included by MonyaCode, even when\n  ignored by git. This is useful for files that are not tracked by git, but are\n  still important to your project. Note that globs that are overly broad can\n  slow down MonyaCode's file scanning. `file_scan_exclusions` takes precedence over\n  these inclusions.\n- Default:\n\n```json\n\"file_scan_inclusions\": [\".env*\"],\n```\n\n## File Types\n\n- Setting: `file_types`\n- Description: Configure how MonyaCode selects a language for a file based on its\n  filename or extension. Supports glob entries.\n- Default:\n\n```json\n\"file_types\": {\n  \"Shell Script\": [\".env.*\"]\n}\n```\n\n**Examples**\n\nTo interpret all `.c` files as C++, files called `MyLockFile` as TOML and files\nstarting with `Dockerfile` as Dockerfile:\n\n```json\n{\n  \"file_types\": {\n    \"C++\": [\"c\"],\n    \"TOML\": [\"MyLockFile\"],\n    \"Dockerfile\": [\"Dockerfile*\"],\n  },\n}\n```\n\n## Diagnostics\n\n- Description: Configuration for diagnostics-related features.\n- Setting: `diagnostics`\n- Default:\n\n```json\n{\n  \"diagnostics\": {\n    \"include_warnings\": true,\n    \"inline\": {\n      \"enabled\": false,\n    },\n    \"update_with_cursor\": false,\n    \"primary_only\": false,\n    \"use_rendered\": false,\n  },\n}\n```\n\n### Inline Diagnostics\n\n- Description: Whether or not to show diagnostics information inline.\n- Setting: `inline`\n- Default:\n\n```json\n{\n  \"diagnostics\": {\n    \"inline\": {\n      \"enabled\": false,\n      \"update_debounce_ms\": 150,\n      \"padding\": 4,\n      \"min_column\": 0,\n      \"max_severity\": null,\n    },\n  },\n}\n```\n\n**Options**\n\n1. Enable inline diagnostics.\n\n```json\n{\n  \"diagnostics\": {\n    \"inline\": {\n      \"enabled\": true,\n    },\n  },\n}\n```\n\n2. Delay diagnostic updates until some time after the last diagnostic update.\n\n```json\n{\n  \"diagnostics\": {\n    \"inline\": {\n      \"enabled\": true,\n      \"update_debounce_ms\": 150,\n    },\n  },\n}\n```\n\n3. Set padding between the end of the source line and the start of the\n   diagnostic.\n\n```json\n{\n  \"diagnostics\": {\n    \"inline\": {\n      \"enabled\": true,\n      \"padding\": 4,\n    },\n  },\n}\n```\n\n4. Horizontally align inline diagnostics at the given column.\n\n```json\n{\n  \"diagnostics\": {\n    \"inline\": {\n      \"enabled\": true,\n      \"min_column\": 80,\n    },\n  },\n}\n```\n\n5. Show only warning and error diagnostics.\n\n```json\n{\n  \"diagnostics\": {\n    \"inline\": {\n      \"enabled\": true,\n      \"max_severity\": \"warning\",\n    },\n  },\n}\n```\n\n## Git\n\n- Description: Configuration for git-related features.\n- Setting: `git`\n- Default:\n\n```json\n{\n  \"git\": {\n    \"git_gutter\": \"tracked_files\",\n    \"inline_blame\": {\n      \"enabled\": true,\n    },\n    \"branch_picker\": {\n      \"show_author_name\": true,\n    },\n    \"hunk_style\": \"staged_hollow\",\n  },\n}\n```\n\n### Git Gutter\n\n- Description: Whether or not to show the git gutter.\n- Setting: `git_gutter`\n- Default: `tracked_files`\n\n**Options**\n\n1. Show git gutter in tracked files\n\n```json\n{\n  \"git\": {\n    \"git_gutter\": \"tracked_files\",\n  },\n}\n```\n\n2. Hide git gutter\n\n```json\n{\n  \"git\": {\n    \"git_gutter\": \"hide\",\n  },\n}\n```\n\n### Gutter Debounce\n\n- Description: Sets the debounce threshold (in milliseconds) after which changes\n  are reflected in the git gutter.\n- Setting: `gutter_debounce`\n- Default: `null`\n\n**Options**\n\n`integer` values representing milliseconds\n\nExample:\n\n```json\n{\n  \"git\": {\n    \"gutter_debounce\": 100,\n  },\n}\n```\n\n### Inline Git Blame\n\n- Description: Whether or not to show git blame information inline, on the\n  currently focused line.\n- Setting: `inline_blame`\n- Default:\n\n```json\n{\n  \"git\": {\n    \"inline_blame\": {\n      \"enabled\": true,\n    },\n  },\n}\n```\n\n**Options**\n\n1. Disable inline git blame:\n\n```json\n{\n  \"git\": {\n    \"inline_blame\": {\n      \"enabled\": false,\n    },\n  },\n}\n```\n\n2. Only show inline git blame after a delay (that starts after cursor stops\n   moving):\n\n```json\n{\n  \"git\": {\n    \"inline_blame\": {\n      \"delay_ms\": 500,\n    },\n  },\n}\n```\n\n3. Show a commit summary next to the commit date and author:\n\n```json\n{\n  \"git\": {\n    \"inline_blame\": {\n      \"show_commit_summary\": true,\n    },\n  },\n}\n```\n\n4. Use this as the minimum column at which to display inline blame information:\n\n```json\n{\n  \"git\": {\n    \"inline_blame\": {\n      \"min_column\": 80,\n    },\n  },\n}\n```\n\n5. Set the padding between the end of the line and the inline blame hint, in\n   ems:\n\n```json\n{\n  \"git\": {\n    \"inline_blame\": {\n      \"padding\": 10,\n    },\n  },\n}\n```\n\n### Branch Picker\n\n- Description: Configuration related to the branch picker.\n- Setting: `branch_picker`\n- Default:\n\n```json\n{\n  \"git\": {\n    \"branch_picker\": {\n      \"show_author_name\": false,\n    },\n  },\n}\n```\n\n**Options**\n\n1. Show the author name in the branch picker:\n\n```json\n{\n  \"git\": {\n    \"branch_picker\": {\n      \"show_author_name\": true,\n    },\n  },\n}\n```\n\n### Hunk Style\n\n- Description: What styling we should use for the diff hunks.\n- Setting: `hunk_style`\n- Default:\n\n```json\n{\n  \"git\": {\n    \"hunk_style\": \"staged_hollow\",\n  },\n}\n```\n\n**Options**\n\n1. Show the staged hunks faded out and with a border:\n\n```json\n{\n  \"git\": {\n    \"hunk_style\": \"staged_hollow\",\n  },\n}\n```\n\n2. Show unstaged hunks faded out and with a border:\n\n```json\n{\n  \"git\": {\n    \"hunk_style\": \"unstaged_hollow\",\n  },\n}\n```\n\n## Go to Definition Fallback\n\n- Description: What to do when the {#action editor::GoToDefinition} action fails\n  to find a definition\n- Setting: `go_to_definition_fallback`\n- Default: `\"find_all_references\"`\n\n**Options**\n\n1. Do nothing:\n\n```json\n{\n  \"go_to_definition_fallback\": \"none\",\n}\n```\n\n2. Find references for the same symbol (default):\n\n```json\n{\n  \"go_to_definition_fallback\": \"find_all_references\",\n}\n```\n\n## Hard Tabs\n\n- Description: Whether to indent lines using tab characters or multiple spaces.\n- Setting: `hard_tabs`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n## Helix Mode\n\n- Description: Whether or not to enable Helix mode. Enabling `helix_mode` also\n  enables `vim_mode`. See the [Helix documentation](./helix.md) for more\n  details.\n- Setting: `helix_mode`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n## Indent Guides\n\n- Description: Configuration related to indent guides. Indent guides can be\n  configured separately for each language.\n- Setting: `indent_guides`\n- Default:\n\n```json\n{\n  \"indent_guides\": {\n    \"enabled\": true,\n    \"line_width\": 1,\n    \"active_line_width\": 1,\n    \"coloring\": \"fixed\",\n    \"background_coloring\": \"disabled\",\n  },\n}\n```\n\n**Options**\n\n1. Disable indent guides\n\n```json\n{\n  \"indent_guides\": {\n    \"enabled\": false,\n  },\n}\n```\n\n2. Enable indent guides for a specific language.\n\n```json\n{\n  \"languages\": {\n    \"Python\": {\n      \"indent_guides\": {\n        \"enabled\": true,\n      },\n    },\n  },\n}\n```\n\n3. Enable indent aware coloring (\"rainbow indentation\"). The colors that are\n   used for different indentation levels are defined in the theme (theme key:\n   `accents`). They can be customized by using theme overrides.\n\n```json\n{\n  \"indent_guides\": {\n    \"enabled\": true,\n    \"coloring\": \"indent_aware\",\n  },\n}\n```\n\n4. Enable indent aware background coloring (\"rainbow indentation\"). The colors\n   that are used for different indentation levels are defined in the theme\n   (theme key: `accents`). They can be customized by using theme overrides.\n\n```json\n{\n  \"indent_guides\": {\n    \"enabled\": true,\n    \"coloring\": \"indent_aware\",\n    \"background_coloring\": \"indent_aware\",\n  },\n}\n```\n\n## Hover Popover Enabled\n\n- Description: Whether or not to show the informational hover box when moving\n  the mouse over symbols in the editor.\n- Setting: `hover_popover_enabled`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Hover Popover Delay\n\n- Description: Time to wait in milliseconds before showing the informational\n  hover box.\n- Setting: `hover_popover_delay`\n- Default: `300`\n\n**Options**\n\n`integer` values representing milliseconds\n\n## Icon Theme\n\n- Description: The icon theme setting can be specified in two forms - either as\n  the name of an icon theme or as an object containing the `mode`, `dark`, and\n  `light` icon themes for files/folders inside MonyaCode.\n- Setting: `icon_theme`\n- Default: `MonyaCode (Default)`\n\n### Icon Theme Object\n\n- Description: Specify the icon theme using an object that includes the `mode`,\n  `dark`, and `light`.\n- Setting: `icon_theme`\n- Default:\n\n```json\n\"icon_theme\": {\n  \"mode\": \"system\",\n  \"dark\": \"MonyaCode (Default)\",\n  \"light\": \"MonyaCode (Default)\"\n},\n```\n\n### Mode\n\n- Description: Specify the icon theme mode.\n- Setting: `mode`\n- Default: `system`\n\n**Options**\n\n1. Set the icon theme to dark mode\n\n```json\n{\n  \"mode\": \"dark\",\n}\n```\n\n2. Set the icon theme to light mode\n\n```json\n{\n  \"mode\": \"light\",\n}\n```\n\n3. Set the icon theme to system mode\n\n```json\n{\n  \"mode\": \"system\",\n}\n```\n\n### Dark\n\n- Description: The name of the dark icon theme.\n- Setting: `dark`\n- Default: `MonyaCode (Default)`\n\n**Options**\n\nRun the {#action icon_theme_selector::Toggle} action in the command palette to\nsee a current list of valid icon themes names.\n\n### Light\n\n- Description: The name of the light icon theme.\n- Setting: `light`\n- Default: `MonyaCode (Default)`\n\n**Options**\n\nRun the {#action icon_theme_selector::Toggle} action in the command palette to\nsee a current list of valid icon themes names.\n\n## Image Viewer\n\n- Description: Settings for image viewer functionality\n- Setting: `image_viewer`\n- Default:\n\n```json\n{\n  \"image_viewer\": {\n    \"unit\": \"binary\",\n  },\n}\n```\n\n**Options**\n\n### Unit\n\n- Description: The unit for image file sizes\n- Setting: `unit`\n- Default: `\"binary\"`\n\n**Options**\n\n1. Use binary units (KiB, MiB):\n\n```json\n{\n  \"image_viewer\": {\n    \"unit\": \"binary\",\n  },\n}\n```\n\n2. Use decimal units (KB, MB):\n\n```json\n{\n  \"image_viewer\": {\n    \"unit\": \"decimal\",\n  },\n}\n```\n\n## Inlay hints\n\n- Description: Configuration for displaying extra text with hints in the editor.\n- Setting: `inlay_hints`\n- Default:\n\n```json\n\"inlay_hints\": {\n  \"enabled\": false,\n  \"show_type_hints\": true,\n  \"show_parameter_hints\": true,\n  \"show_other_hints\": true,\n  \"show_background\": false,\n  \"edit_debounce_ms\": 700,\n  \"scroll_debounce_ms\": 50,\n  \"toggle_on_modifiers_press\": null\n}\n```\n\n**Options**\n\nInlay hints querying consists of two parts: editor (client) and LSP server. With\nthe inlay settings above are changed to enable the hints, editor will start to\nquery certain types of hints and react on LSP hint refresh request from the\nserver. At this point, the server may or may not return hints depending on its\nimplementation, further configuration might be needed, refer to the\ncorresponding LSP server documentation.\n\nThe following languages have inlay hints preconfigured by MonyaCode:\n\n- Go\n- Rust\n- Svelte\n- TypeScript\n\nUse the `lsp` section for the server configuration. Examples are provided in the\ncorresponding language documentation.\n\nHints are not instantly queried in MonyaCode, two kinds of debounces are used, either\nmay be set to 0 to be disabled. Settings-related hint updates are not debounced.\n\nAll possible config values for `toggle_on_modifiers_press` are:\n\n```json\n\"inlay_hints\": {\n  \"toggle_on_modifiers_press\": {\n    \"control\": true,\n    \"shift\": true,\n    \"alt\": true,\n    \"platform\": true,\n    \"function\": true\n  }\n}\n```\n\nUnspecified values have a `false` value, hints won't be toggled if all the\nmodifiers are `false` or not all the modifiers are pressed.\n\n## Journal\n\n- Description: Configuration for the journal.\n- Setting: `journal`\n- Default:\n\n```json\n\"journal\": {\n  \"path\": \"~\",\n  \"hour_format\": \"hour12\"\n}\n\n```\n\n### Path\n\n- Description: The path of the directory where journal entries are stored. If an\n  invalid path is specified, the journal will fall back to using `~` (the home\n  directory).\n- Setting: `path`\n- Default: `~`\n\n**Options**\n\n`string` values\n\n### Hour Format\n\n- Description: The format to use for displaying hours in the journal.\n- Setting: `hour_format`\n- Default: `hour12`\n\n**Options**\n\n1. 12-hour format:\n\n```json\n{\n  \"hour_format\": \"hour12\",\n}\n```\n\n2. 24-hour format:\n\n```json\n{\n  \"hour_format\": \"hour24\",\n}\n```\n\n## JSX Tag Auto Close\n\n- Description: Whether to automatically close JSX tags\n- Setting: `jsx_tag_auto_close`\n- Default:\n\n```json\n{\n  \"jsx_tag_auto_close\": {\n    \"enabled\": true,\n  },\n}\n```\n\n**Options**\n\n- `enabled`: Whether to enable automatic JSX tag closing\n\n## Languages\n\n- Description: Configuration for specific languages.\n- Setting: `languages`\n- Default: `null`\n\n**Options**\n\nTo override settings for a language, add an entry for that languages name to the\n`languages` value. Example:\n\n```json\n\"languages\": {\n  \"C\": {\n    \"format_on_save\": \"off\",\n    \"preferred_line_length\": 64,\n    \"soft_wrap\": \"preferred_line_length\"\n  },\n  \"JSON\": {\n    \"tab_size\": 4\n  }\n}\n```\n\nThe following settings can be overridden for each specific language:\n\n- [`enable_language_server`](#enable-language-server)\n- [`ensure_final_newline_on_save`](#ensure-final-newline-on-save)\n- [`format_on_save`](#format-on-save)\n- [`formatter`](#formatter)\n- [`hard_tabs`](#hard-tabs)\n- [`preferred_line_length`](#preferred-line-length)\n- [`remove_trailing_whitespace_on_save`](#remove-trailing-whitespace-on-save)\n- [`show_whitespaces`](#show-whitespaces)\n- [`whitespace_map`](#whitespace-map)\n- [`soft_wrap`](#soft-wrap)\n- [`tab_size`](#tab-size)\n- [`use_autoclose`](#use-autoclose)\n- [`always_treat_brackets_as_autoclosed`](#always-treat-brackets-as-autoclosed)\n\nThese values take in the same options as the root-level settings with the same\nname.\n\n## Line Indicator Format\n\n- Description: Format for line indicator in the status bar\n- Setting: `line_indicator_format`\n- Default: `\"short\"`\n\n**Options**\n\n1. Short format:\n\n```json\n{\n  \"line_indicator_format\": \"short\",\n}\n```\n\n2. Long format:\n\n```json\n{\n  \"line_indicator_format\": \"long\",\n}\n```\n\n## Linked Edits\n\n- Description: Whether to perform linked edits of associated ranges, if the\n  language server supports it. For example, when editing opening `<html>` tag,\n  the contents of the closing `</html>` tag will be edited as well.\n- Setting: `linked_edits`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## LSP Document Colors\n\n- Description: Whether to show document color information from the language\n  server\n- Setting: `lsp_document_colors`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Max Tabs\n\n- Description: Maximum number of tabs to show in the tab bar\n- Setting: `max_tabs`\n- Default: `null`\n\n**Options**\n\nPositive `integer` values or `null` for unlimited tabs\n\n## Middle Click Paste (Linux only)\n\n- Description: Enable middle-click paste on Linux\n- Setting: `middle_click_paste`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Multi Cursor Modifier\n\n- Description: Determines the modifier to be used to add multiple cursors with\n  the mouse. The open hover link mouse gestures will adapt such that it do not\n  conflict with the multicursor modifier.\n- Setting: `multi_cursor_modifier`\n- Default: `alt`\n\n**Options**\n\n1. Maps to `Alt` on Linux and Windows and to `Option` on macOS:\n\n```json\n{\n  \"multi_cursor_modifier\": \"alt\",\n}\n```\n\n2. Maps `Control` on Linux and Windows and to `Command` on macOS:\n\n```json\n{\n  \"multi_cursor_modifier\": \"cmd_or_ctrl\", // alias: \"cmd\", \"ctrl\"\n}\n```\n\n## Node\n\n- Description: Configuration for Node.js integration\n- Setting: `node`\n- Default:\n\n```json\n{\n  \"node\": {\n    \"ignore_system_version\": false,\n    \"path\": null,\n    \"npm_path\": null,\n  },\n}\n```\n\n**Options**\n\n- `ignore_system_version`: Whether to ignore the system Node.js version\n- `path`: Custom path to Node.js binary\n- `npm_path`: Custom path to npm binary\n\n## Network Proxy\n\n- Description: Configure a network proxy for MonyaCode.\n- Setting: `proxy`\n- Default: `null`\n\n**Options**\n\nThe proxy setting must contain a URL to the proxy.\n\nThe following URI schemes are supported:\n\n- `http`\n- `https`\n- `socks4` - SOCKS4 proxy with local DNS\n- `socks4a` - SOCKS4 proxy with remote DNS\n- `socks5` - SOCKS5 proxy with local DNS\n- `socks5h` - SOCKS5 proxy with remote DNS\n\n`http` will be used when no scheme is specified.\n\nBy default no proxy will be used, or MonyaCode will attempt to retrieve proxy\nsettings from environment variables, such as `http_proxy`, `HTTP_PROXY`,\n`https_proxy`, `HTTPS_PROXY`, `all_proxy`, `ALL_PROXY`, `no_proxy` and\n`NO_PROXY`.\n\nFor example, to set an `http` proxy, add the following to your settings:\n\n```json\n{\n  \"proxy\": \"http://127.0.0.1:10809\",\n}\n```\n\nOr to set a `socks5` proxy:\n\n```json\n{\n  \"proxy\": \"socks5h://localhost:10808\",\n}\n```\n\nIf you wish to exclude certain hosts from using the proxy, set the `NO_PROXY`\nenvironment variable. This accepts a comma-separated list of hostnames, host\nsuffixes, IPv4/IPv6 addresses or blocks that should not use the proxy. For\nexample if your environment included `NO_PROXY=\"google.com, 192.168.1.0/24\"` all\nhosts in `192.168.1.*`, `google.com` and `*.google.com` would bypass the proxy.\nSee\n[reqwest NoProxy docs](https://docs.rs/reqwest/latest/reqwest/struct.NoProxy.html#method.from_string)\nfor more.\n\n## On Last Window Closed\n\n- Description: What to do when the last window is closed\n- Setting: `on_last_window_closed`\n- Default: `\"platform_default\"`\n\n**Options**\n\n1. Use platform default behavior:\n\n```json\n{\n  \"on_last_window_closed\": \"platform_default\",\n}\n```\n\n2. Always quit the application:\n\n```json\n{\n  \"on_last_window_closed\": \"quit_app\",\n}\n```\n\n## Profiles\n\n- Description: Configuration profiles that can be applied on top of existing\n  settings\n- Setting: `profiles`\n- Default: `{}`\n\n**Options**\n\nConfiguration object for defining settings profiles. Example:\n\n```json\n{\n  \"profiles\": {\n    \"presentation\": {\n      \"buffer_font_size\": 20,\n      \"ui_font_size\": 18,\n      \"theme\": \"One Light\",\n    },\n  },\n}\n```\n\n## Preview tabs\n\n- Description: Preview tabs allow you to open files in preview mode, where they\n  close automatically when you switch to another file unless you explicitly pin\n  them. This is useful for quickly viewing files without cluttering your\n  workspace. Preview tabs display their file names in italics. \\\n  There are several ways to convert a preview tab into a regular tab:\n\n  - Double-clicking on the file\n  - Double-clicking on the tab header\n  - Using the {#action project_panel::OpenPermanent} action\n  - Editing the file\n  - Dragging the file to a different pane\n\n- Setting: `preview_tabs`\n- Default:\n\n```json\n\"preview_tabs\": {\n  \"enabled\": true,\n  \"enable_preview_from_project_panel\": true,\n  \"enable_preview_from_file_finder\": false,\n  \"enable_preview_from_multibuffer\": true,\n  \"enable_preview_multibuffer_from_code_navigation\": false,\n  \"enable_preview_file_from_code_navigation\": true,\n  \"enable_keep_preview_on_code_navigation\": false,\n}\n```\n\n### Enable preview from project panel\n\n- Description: Determines whether to open files in preview mode when opened from\n  the project panel with a single click.\n- Setting: `enable_preview_from_project_panel`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n### Enable preview from file finder\n\n- Description: Determines whether to open files in preview mode when selected\n  from the file finder.\n- Setting: `enable_preview_from_file_finder`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n### Enable preview from multibuffer\n\n- Description: Determines whether to open files in preview mode when opened from\n  a multibuffer.\n- Setting: `enable_preview_from_multibuffer`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n### Enable preview multibuffer from code navigation\n\n- Description: Determines whether to open tabs in preview mode when code\n  navigation is used to open a multibuffer.\n- Setting: `enable_preview_multibuffer_from_code_navigation`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n### Enable preview file from code navigation\n\n- Description: Determines whether to open tabs in preview mode when code\n  navigation is used to open a single file.\n- Setting: `enable_preview_file_from_code_navigation`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n### Enable keep preview on code navigation\n\n- Description: Determines whether to keep tabs in preview mode when code\n  navigation is used to navigate away from them. If\n  `enable_preview_file_from_code_navigation` or\n  `enable_preview_multibuffer_from_code_navigation` is also true, the new tab\n  may replace the existing one.\n- Setting: `enable_keep_preview_on_code_navigation`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n## File Finder\n\n### File Icons\n\n- Description: Whether to show file icons in the file finder.\n- Setting: `file_icons`\n- Default: `true`\n\n### Modal Max Width\n\n- Description: Max-width of the file finder modal. It can take one of these\n  values: `small`, `medium`, `large`, `xlarge`, and `full`.\n- Setting: `modal_max_width`\n- Default: `small`\n\n### Skip Focus For Active In Search\n\n- Description: Determines whether the file finder should skip focus for the\n  active file in search results.\n- Setting: `skip_focus_for_active_in_search`\n- Default: `true`\n\n## Pane Split Direction Horizontal\n\n- Description: The direction that you want to split panes horizontally\n- Setting: `pane_split_direction_horizontal`\n- Default: `\"up\"`\n\n**Options**\n\n1. Split upward:\n\n```json\n{\n  \"pane_split_direction_horizontal\": \"up\",\n}\n```\n\n2. Split downward:\n\n```json\n{\n  \"pane_split_direction_horizontal\": \"down\",\n}\n```\n\n## Pane Split Direction Vertical\n\n- Description: The direction that you want to split panes vertically\n- Setting: `pane_split_direction_vertical`\n- Default: `\"left\"`\n\n**Options**\n\n1. Split to the left:\n\n```json\n{\n  \"pane_split_direction_vertical\": \"left\",\n}\n```\n\n2. Split to the right:\n\n```json\n{\n  \"pane_split_direction_vertical\": \"right\",\n}\n```\n\n## Preferred Line Length\n\n- Description: The column at which to soft-wrap lines, for buffers where\n  soft-wrap is enabled.\n- Setting: `preferred_line_length`\n- Default: `80`\n\n**Options**\n\n`integer` values\n\n## Private Files\n\n- Description: Globs to match against file paths to determine if a file is\n  private\n- Setting: `private_files`\n- Default:\n  `[\"**/.env*\", \"**/*.pem\", \"**/*.key\", \"**/*.cert\", \"**/*.crt\", \"**/secrets.yml\"]`\n\n**Options**\n\nList of `string` glob patterns\n\n## Projects Online By Default\n\n- Description: Whether or not to show the online projects view by default.\n- Setting: `projects_online_by_default`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Read SSH Config\n\n- Description: Whether to read SSH configuration files\n- Setting: `read_ssh_config`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Redact Private Values\n\n- Description: Hide the values of variables from visual display in private files\n- Setting: `redact_private_values`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n## Relative Line Numbers\n\n- Description: Whether to show relative line numbers in the gutter\n- Setting: `relative_line_numbers`\n- Default: `\"disabled\"`\n\n**Options**\n\n1. Show relative line numbers in the gutter whilst counting wrapped lines as one\n   line:\n\n```json\n{\n  \"relative_line_numbers\": \"enabled\",\n}\n```\n\n2. Show relative line numbers in the gutter, including wrapped lines in the\n   counting:\n\n```json\n{\n  \"relative_line_numbers\": \"wrapped\",\n}\n```\n\n2. Do not use relative line numbers:\n\n```json\n{\n  \"relative_line_numbers\": \"disabled\",\n}\n```\n\n## Remove Trailing Whitespace On Save\n\n- Description: Whether or not to remove any trailing whitespace from lines of a\n  buffer before saving it.\n- Setting: `remove_trailing_whitespace_on_save`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Resize All Panels In Dock\n\n- Description: Whether to resize all the panels in a dock when resizing the\n  dock. Can be a combination of \"left\", \"right\" and \"bottom\".\n- Setting: `resize_all_panels_in_dock`\n- Default: `[\"left\"]`\n\n**Options**\n\nList of strings containing any combination of:\n\n- `\"left\"`: Resize left dock panels together\n- `\"right\"`: Resize right dock panels together\n- `\"bottom\"`: Resize bottom dock panels together\n\n## Restore on File Reopen\n\n- Description: Whether to attempt to restore previous file's state when opening\n  it again. The state is stored per pane.\n- Setting: `restore_on_file_reopen`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Restore on Startup\n\n- Description: Controls session restoration on startup.\n- Setting: `restore_on_startup`\n- Default: `last_session`\n\n**Options**\n\n1. Restore all workspaces that were open when quitting MonyaCode:\n\n```json\n{\n  \"restore_on_startup\": \"last_session\",\n}\n```\n\n2. Restore the workspace that was closed last:\n\n```json\n{\n  \"restore_on_startup\": \"last_workspace\",\n}\n```\n\n3. Always start with an empty editor:\n\n```json\n{\n  \"restore_on_startup\": \"empty_tab\",\n}\n```\n\n4. Always start with the welcome launchpad:\n\n```json\n{\n  \"restore_on_startup\": \"launchpad\",\n}\n```\n\n## Scroll Beyond Last Line\n\n- Description: Whether the editor will scroll beyond the last line\n- Setting: `scroll_beyond_last_line`\n- Default: `\"one_page\"`\n\n**Options**\n\n1. Scroll one page beyond the last line by one page:\n\n```json\n{\n  \"scroll_beyond_last_line\": \"one_page\",\n}\n```\n\n2. The editor will scroll beyond the last line by the same amount of lines as\n   `vertical_scroll_margin`:\n\n```json\n{\n  \"scroll_beyond_last_line\": \"vertical_scroll_margin\",\n}\n```\n\n3. The editor will not scroll beyond the last line:\n\n```json\n{\n  \"scroll_beyond_last_line\": \"off\",\n}\n```\n\n**Options**\n\n`boolean` values\n\n## Scroll Sensitivity\n\n- Description: Scroll sensitivity multiplier. This multiplier is applied to both\n  the horizontal and vertical delta values while scrolling.\n- Setting: `scroll_sensitivity`\n- Default: `1.0`\n\n**Options**\n\nPositive `float` values\n\n### Fast Scroll Sensitivity\n\n- Description: Scroll sensitivity multiplier for fast scrolling. This multiplier\n  is applied to both the horizontal and vertical delta values while scrolling.\n  Fast scrolling happens when a user holds the alt or option key while\n  scrolling.\n- Setting: `fast_scroll_sensitivity`\n- Default: `4.0`\n\n**Options**\n\nPositive `float` values\n\n### Horizontal Scroll Margin\n\n- Description: The number of characters to keep on either side when scrolling\n  with the mouse\n- Setting: `horizontal_scroll_margin`\n- Default: `5`\n\n**Options**\n\nNon-negative `integer` values\n\n### Vertical Scroll Margin\n\n- Description: The number of lines to keep above/below the cursor when scrolling\n  with the keyboard\n- Setting: `vertical_scroll_margin`\n- Default: `3`\n\n**Options**\n\nNon-negative `integer` values\n\n## Search\n\n- Description: Search options to enable by default when opening new project and\n  buffer searches.\n- Setting: `search`\n- Default:\n\n```json\n\"search\": {\n  \"button\": true,\n  \"whole_word\": false,\n  \"case_sensitive\": false,\n  \"include_ignored\": false,\n  \"regex\": false,\n  \"center_on_match\": false\n},\n```\n\n### Button\n\n- Description: Whether to show the project search button in the status bar.\n- Setting: `button`\n- Default: `true`\n\n### Whole Word\n\n- Description: Whether to only match on whole words.\n- Setting: `whole_word`\n- Default: `false`\n\n### Case Sensitive\n\n- Description: Whether to match case sensitively. This setting affects both\n  searches and editor actions like \"Select Next Occurrence\", \"Select Previous\n  Occurrence\", and \"Select All Occurrences\".\n- Setting: `case_sensitive`\n- Default: `false`\n\n### Include Ignore\n\n- Description: Whether to include gitignored files in search results.\n- Setting: `include_ignored`\n- Default: `false`\n\n### Regex\n\n- Description: Whether to interpret the search query as a regular expression.\n- Setting: `regex`\n- Default: `false`\n\n### Center On Match\n\n- Description: Whether to center the cursor on each search match when\n  navigating.\n- Setting: `center_on_match`\n- Default: `false`\n\n## Search Wrap\n\n- Description: If `search_wrap` is disabled, search result do not wrap around\n  the end of the file\n- Setting: `search_wrap`\n- Default: `true`\n\n## Center on Match\n\n- Description: If `center_on_match` is enabled, the editor will center the\n  cursor on the current match when searching.\n- Setting: `center_on_match`\n- Default: `false`\n\n## Seed Search Query From Cursor\n\n- Description: When to populate a new search's query based on the text under the\n  cursor.\n- Setting: `seed_search_query_from_cursor`\n- Default: `always`\n\n**Options**\n\n1. `always` always populate the search query with the word under the cursor\n2. `selection` only populate the search query when there is text selected\n3. `never` never populate the search query\n\n## Use Smartcase Search\n\n- Description: When enabled, automatically adjusts search case sensitivity based\n  on your query. If your search query contains any uppercase letters, the search\n  becomes case-sensitive; if it contains only lowercase letters, the search\n  becomes case-insensitive. \\\n  This applies to both in-file searches and project-wide searches.\n- Setting: `use_smartcase_search`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\nExamples:\n\n- Searching for \"function\" would match \"function\", \"Function\", \"FUNCTION\", etc.\n- Searching for \"Function\" would only match \"Function\", not \"function\" or\n  \"FUNCTION\"\n\n## Completions\n\n- Description: Controls how completions are processed for this language.\n- Setting: `completions`\n- Default:\n\n```json\n{\n  \"completions\": {\n    \"words\": \"fallback\",\n    \"words_min_length\": 3,\n    \"lsp\": true,\n    \"lsp_fetch_timeout_ms\": 0,\n    \"lsp_insert_mode\": \"replace_suffix\",\n  },\n}\n```\n\n### Words\n\n- Description: Controls how words are completed. For large documents, not all\n  words may be fetched for completion.\n- Setting: `words`\n- Default: `fallback`\n\n**Options**\n\n1. `enabled` - Always fetch document's words for completions along with LSP\n   completions\n2. `fallback` - Only if LSP response errors or times out, use document's words\n   to show completions\n3. `disabled` - Never fetch or complete document's words for completions\n   (word-based completions can still be queried via a separate action)\n\n### Min Words Query Length\n\n- Description: Minimum number of characters required to automatically trigger\n  word-based completions. Before that value, it's still possible to trigger the\n  words-based completion manually with the corresponding editor command.\n- Setting: `words_min_length`\n- Default: `3`\n\n**Options**\n\nPositive integer values\n\n### LSP\n\n- Description: Whether to fetch LSP completions or not.\n- Setting: `lsp`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n### LSP Fetch Timeout (ms)\n\n- Description: When fetching LSP completions, determines how long to wait for a\n  response of a particular server. When set to 0, waits indefinitely.\n- Setting: `lsp_fetch_timeout_ms`\n- Default: `0`\n\n**Options**\n\n`integer` values representing milliseconds\n\n### LSP Insert Mode\n\n- Description: Controls what range to replace when accepting LSP completions.\n- Setting: `lsp_insert_mode`\n- Default: `replace_suffix`\n\n**Options**\n\n1. `insert` - Replaces text before the cursor, using the `insert` range\n   described in the LSP specification\n2. `replace` - Replaces text before and after the cursor, using the `replace`\n   range described in the LSP specification\n3. `replace_subsequence` - Behaves like `\"replace\"` if the text that would be\n   replaced is a subsequence of the completion text, and like `\"insert\"`\n   otherwise\n4. `replace_suffix` - Behaves like `\"replace\"` if the text after the cursor is a\n   suffix of the completion, and like `\"insert\"` otherwise\n\n## Show Completions On Input\n\n- Description: Whether or not to show completions as you type.\n- Setting: `show_completions_on_input`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Show Completion Documentation\n\n- Description: Whether to display inline and alongside documentation for items\n  in the completions menu.\n- Setting: `show_completion_documentation`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Show Whitespaces\n\n- Description: Whether or not to render whitespace characters in the editor.\n- Setting: `show_whitespaces`\n- Default: `selection`\n\n**Options**\n\n1. `all`\n2. `selection`\n3. `none`\n4. `boundary`\n\n## Whitespace Map\n\n- Description: Specify the characters used to render whitespace when\n  show_whitespaces is enabled.\n- Setting: `whitespace_map`\n- Default:\n\n```json\n{\n  \"whitespace_map\": {\n    \"space\": \"•\",\n    \"tab\": \"→\",\n  },\n}\n```\n\n## Soft Wrap\n\n- Description: Whether or not to automatically wrap lines of text to fit editor\n  / preferred width.\n- Setting: `soft_wrap`\n- Default: `none`\n\n**Options**\n\n1. `none` to avoid wrapping generally, unless the line is too long\n2. `prefer_line` (deprecated, same as `none`)\n3. `editor_width` to wrap lines that overflow the editor width\n4. `preferred_line_length` to wrap lines that overflow `preferred_line_length`\n   config value\n5. `bounded` to wrap lines at the minimum of `editor_width` and\n   `preferred_line_length`\n\n## Show Wrap Guides\n\n- Description: Whether to show wrap guides (vertical rulers) in the editor.\n  Setting this to true will show a guide at the 'preferred_line_length' value if\n  'soft_wrap' is set to 'preferred_line_length', and will show any additional\n  guides as specified by the 'wrap_guides' setting.\n- Setting: `show_wrap_guides`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Use On Type Format\n\n- Description: Whether to use additional LSP queries to format (and amend) the\n  code after every \"trigger\" symbol input, defined by LSP server capabilities\n- Setting: `use_on_type_format`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Use Auto Surround\n\n- Description: Whether to automatically surround selected text when typing\n  opening parenthesis, bracket, brace, single or double quote characters. For\n  example, when you select text and type '(', MonyaCode will surround the text with\n  ().\n- Setting: `use_auto_surround`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Use System Path Prompts\n\n- Description: Whether to use the system provided dialogs for Open and Save As.\n  When set to false, MonyaCode will use the built-in keyboard-first pickers.\n- Setting: `use_system_path_prompts`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Use System Prompts\n\n- Description: Whether to use the system provided dialogs for prompts, such as\n  confirmation prompts. When set to false, MonyaCode will use its built-in prompts.\n  Note that on Linux, this option is ignored and MonyaCode will always use the\n  built-in prompts.\n- Setting: `use_system_prompts`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Wrap Guides (Vertical Rulers)\n\n- Description: Where to display vertical rulers as wrap-guides. Disable by\n  setting `show_wrap_guides` to `false`.\n- Setting: `wrap_guides`\n- Default: []\n\n**Options**\n\nList of `integer` column numbers\n\n## Tab Size\n\n- Description: The number of spaces to use for each tab character.\n- Setting: `tab_size`\n- Default: `4`\n\n**Options**\n\n`integer` values\n\n## Tasks\n\n- Description: Configuration for tasks that can be run within MonyaCode\n- Setting: `tasks`\n- Default:\n\n```json\n{\n  \"tasks\": {\n    \"variables\": {},\n    \"enabled\": true,\n    \"prefer_lsp\": false,\n  },\n}\n```\n\n**Options**\n\n- `variables`: Custom variables for task configuration\n- `enabled`: Whether tasks are enabled\n- `prefer_lsp`: Whether to prefer LSP-provided tasks over MonyaCode language\n  extension ones\n\n### Diagnostics\n\n- Description: Setting for sending debug-related data, such as crash reports.\n- Setting: `diagnostics`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n## Terminal\n\n- Description: Configuration for the terminal.\n- Setting: `terminal`\n- Default:\n\n```json\n{\n  \"terminal\": {\n    \"alternate_scroll\": \"off\",\n    \"blinking\": \"terminal_controlled\",\n    \"copy_on_select\": false,\n    \"keep_selection_on_copy\": true,\n    \"dock\": \"bottom\",\n    \"default_width\": 640,\n    \"default_height\": 320,\n    \"detect_venv\": {\n      \"on\": {\n        \"directories\": [\".env\", \"env\", \".venv\", \"venv\"],\n        \"activate_script\": \"default\",\n      },\n    },\n    \"env\": {},\n    \"font_family\": null,\n    \"font_features\": null,\n    \"font_size\": null,\n    \"line_height\": \"comfortable\",\n    \"minimum_contrast\": 45,\n    \"option_as_meta\": false,\n    \"button\": true,\n    \"shell\": \"system\",\n    \"scroll_multiplier\": 3.0,\n    \"toolbar\": {\n      \"breadcrumbs\": false,\n    },\n    \"working_directory\": \"current_project_directory\",\n    \"scrollbar\": {\n      \"show\": null,\n    },\n  },\n}\n```\n\n### Terminal: Dock\n\n- Description: Control the position of the dock\n- Setting: `dock`\n- Default: `bottom`\n\n**Options**\n\n`\"bottom\"`, `\"left\"` or `\"right\"`\n\n### Terminal: Alternate Scroll\n\n- Description: Set whether Alternate Scroll mode (DECSET code: `?1007`) is\n  active by default. Alternate Scroll mode converts mouse scroll events into up\n  / down key presses when in the alternate screen (e.g. when running\n  applications like vim or less). The terminal can still set and unset this mode\n  with ANSI escape codes.\n- Setting: `alternate_scroll`\n- Default: `off`\n\n**Options**\n\n1. Default alternate scroll mode to off\n\n```json\n{\n  \"terminal\": {\n    \"alternate_scroll\": \"off\",\n  },\n}\n```\n\n2. Default alternate scroll mode to on\n\n```json\n{\n  \"terminal\": {\n    \"alternate_scroll\": \"on\",\n  },\n}\n```\n\n### Terminal: Blinking\n\n- Description: Set the cursor blinking behavior in the terminal\n- Setting: `blinking`\n- Default: `terminal_controlled`\n\n**Options**\n\n1. Never blink the cursor, ignore the terminal mode\n\n```json\n{\n  \"terminal\": {\n    \"blinking\": \"off\",\n  },\n}\n```\n\n2. Default the cursor blink to off, but allow the terminal to turn blinking on\n\n```json\n{\n  \"terminal\": {\n    \"blinking\": \"terminal_controlled\",\n  },\n}\n```\n\n3. Always blink the cursor, ignore the terminal mode\n\n```json\n{\n  \"terminal\": {\n    \"blinking\": \"on\",\n  },\n}\n```\n\n### Terminal: Copy On Select\n\n- Description: Whether or not selecting text in the terminal will automatically\n  copy to the system clipboard.\n- Setting: `copy_on_select`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n**Example**\n\n```json\n{\n  \"terminal\": {\n    \"copy_on_select\": true,\n  },\n}\n```\n\n### Terminal: Cursor Shape\n\n- Description: Controls the visual shape of the cursor in the terminal. When not\n  explicitly set, it defaults to a block shape.\n- Setting: `cursor_shape`\n- Default: `null` (defaults to block)\n\n**Options**\n\n1. A block that surrounds the following character\n\n```json\n{\n  \"terminal\": {\n    \"cursor_shape\": \"block\",\n  },\n}\n```\n\n2. A vertical bar\n\n```json\n{\n  \"terminal\": {\n    \"cursor_shape\": \"bar\",\n  },\n}\n```\n\n3. An underline / underscore that runs along the following character\n\n```json\n{\n  \"terminal\": {\n    \"cursor_shape\": \"underline\",\n  },\n}\n```\n\n4. A box drawn around the following character\n\n```json\n{\n  \"terminal\": {\n    \"cursor_shape\": \"hollow\",\n  },\n}\n```\n\n### Terminal: Keep Selection On Copy\n\n- Description: Whether or not to keep the selection in the terminal after\n  copying text.\n- Setting: `keep_selection_on_copy`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n**Example**\n\n```json\n{\n  \"terminal\": {\n    \"keep_selection_on_copy\": false,\n  },\n}\n```\n\n### Terminal: Env\n\n- Description: Any key-value pairs added to this object will be added to the\n  terminal's environment. Keys must be unique, use `:` to separate multiple\n  values in a single variable\n- Setting: `env`\n- Default: `{}`\n\n**Example**\n\n```json\n{\n  \"terminal\": {\n    \"env\": {\n      \"MONYACODE\": \"1\",\n      \"KEY\": \"value1:value2\",\n    },\n  },\n}\n```\n\n### Terminal: Font Size\n\n- Description: What font size to use for the terminal. When not set defaults to\n  matching the editor's font size\n- Setting: `font_size`\n- Default: `null`\n\n**Options**\n\n`integer` values\n\n```json\n{\n  \"terminal\": {\n    \"font_size\": 15,\n  },\n}\n```\n\n### Terminal: Font Family\n\n- Description: What font to use for the terminal. When not set, defaults to\n  matching the editor's font.\n- Setting: `font_family`\n- Default: `null`\n\n**Options**\n\nThe name of any font family installed on the user's system\n\n```json\n{\n  \"terminal\": {\n    \"font_family\": \"Berkeley Mono\",\n  },\n}\n```\n\n### Terminal: Font Features\n\n- Description: What font features to use for the terminal. When not set,\n  defaults to matching the editor's font features.\n- Setting: `font_features`\n- Default: `null`\n- Platform: macOS and Windows.\n\n**Options**\n\nSee Buffer Font Features\n\n```json\n{\n  \"terminal\": {\n    \"font_features\": {\n      \"calt\": false,\n      // See Buffer Font Features for more features\n    },\n  },\n}\n```\n\n### Terminal: Line Height\n\n- Description: Set the terminal's line height.\n- Setting: `line_height`\n- Default: `standard`\n\n**Options**\n\n1. Use a line height that's `comfortable` for reading, 1.618.\n\n```json\n{\n  \"terminal\": {\n    \"line_height\": \"comfortable\",\n  },\n}\n```\n\n2. Use a `standard` line height, 1.3. This option is useful for TUIs,\n   particularly if they use box characters. (default)\n\n```json\n{\n  \"terminal\": {\n    \"line_height\": \"standard\",\n  },\n}\n```\n\n3.  Use a custom line height.\n\n```json\n{\n  \"terminal\": {\n    \"line_height\": {\n      \"custom\": 2,\n    },\n  },\n}\n```\n\n### Terminal: Minimum Contrast\n\n- Description: Controls the minimum contrast between foreground and background\n  colors in the terminal. Uses the APCA (Accessible Perceptual Contrast\n  Algorithm) for color adjustments. Set this to 0 to disable this feature.\n- Setting: `minimum_contrast`\n- Default: `45`\n\n**Options**\n\n`integer` values from 0 to 106. Common recommended values:\n\n- `0`: No contrast adjustment\n- `45`: Minimum for large fluent text (default)\n- `60`: Minimum for other content text\n- `75`: Minimum for body text\n- `90`: Preferred for body text\n\n```json\n{\n  \"terminal\": {\n    \"minimum_contrast\": 45,\n  },\n}\n```\n\n### Terminal: Option As Meta\n\n- Description: Re-interprets the option keys to act like a 'meta' key, like in\n  Emacs.\n- Setting: `option_as_meta`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\n```json\n{\n  \"terminal\": {\n    \"option_as_meta\": true,\n  },\n}\n```\n\n### Terminal: Shell\n\n- Description: What shell to use when launching the terminal.\n- Setting: `shell`\n- Default: `system`\n\n**Options**\n\n1. Use the system's default terminal configuration (usually the `/etc/passwd`\n   file).\n\n```json\n{\n  \"terminal\": {\n    \"shell\": \"system\",\n  },\n}\n```\n\n2. A program to launch:\n\n```json\n{\n  \"terminal\": {\n    \"shell\": {\n      \"program\": \"sh\",\n    },\n  },\n}\n```\n\n3. A program with arguments:\n\n```json\n{\n  \"terminal\": {\n    \"shell\": {\n      \"with_arguments\": {\n        \"program\": \"/bin/bash\",\n        \"args\": [\"--login\"],\n      },\n    },\n  },\n}\n```\n\n## Terminal: Detect Virtual Environments {#terminal-detect_venv}\n\n- Description: Activate the\n  [Python Virtual Environment](https://docs.python.org/3/library/venv.html), if\n  one is found, in the terminal's working directory (as resolved by the\n  working_directory and automatically activating the virtual environment.\n- Setting: `detect_venv`\n- Default:\n\n```json\n{\n  \"terminal\": {\n    \"detect_venv\": {\n      \"on\": {\n        // Default directories to search for virtual environments, relative\n        // to the current working directory. We recommend overriding this\n        // in your project's settings, rather than globally.\n        \"directories\": [\".env\", \"env\", \".venv\", \"venv\"],\n        // Can also be `csh`, `fish`, and `nushell`\n        \"activate_script\": \"default\",\n      },\n    },\n  },\n}\n```\n\nDisable with:\n\n```json\n{\n  \"terminal\": {\n    \"detect_venv\": \"off\",\n  },\n}\n```\n\n### Terminal: Scroll Multiplier\n\n- Description: The multiplier for scrolling speed in the terminal when using\n  mouse wheel or trackpad.\n- Setting: `scroll_multiplier`\n- Default: `1.0`\n\n**Options**\n\nPositive floating point values. Values less than or equal to 0 will be clamped\nto a minimum of 0.01.\n\n**Example**\n\n```json\n{\n  \"terminal\": {\n    \"scroll_multiplier\": 5.0\n  }\n}\n```\n\n## Terminal: Toolbar\n\n- Description: Whether or not to show various elements in the terminal toolbar.\n- Setting: `toolbar`\n- Default:\n\n```json\n{\n  \"terminal\": {\n    \"toolbar\": {\n      \"breadcrumbs\": false,\n    },\n  },\n}\n```\n\n**Options**\n\nAt the moment, only the `breadcrumbs` option is available, it controls\ndisplaying of the terminal title that can be changed via `PROMPT_COMMAND`.\n\nIf the terminal title is empty, the breadcrumbs won't be shown.\n\nThe shell running in the terminal needs to be configured to emit the title.\n\nExample command to set the title: `echo -e \"\\e]2;New Title\\007\";`\n\n### Terminal: Button\n\n- Description: Control to show or hide the terminal button in the status bar\n- Setting: `button`\n- Default: `true`\n\n**Options**\n\n`boolean` values\n\n```json\n{\n  \"terminal\": {\n    \"button\": false,\n  },\n}\n```\n\n### Terminal: Working Directory\n\n- Description: What working directory to use when launching the terminal.\n- Setting: `working_directory`\n- Default: `\"current_project_directory\"`\n\n**Options**\n\n1. Use the current file's project directory. Fallback to the first project\n   directory strategy if unsuccessful.\n\n```json\n{\n  \"terminal\": {\n    \"working_directory\": \"current_project_directory\",\n  },\n}\n```\n\n2. Use the first project in this workspace's directory. Fallback to using this\n   platform's home directory.\n\n```json\n{\n  \"terminal\": {\n    \"working_directory\": \"first_project_directory\",\n  },\n}\n```\n\n3. Always use this platform's home directory if it can be found.\n\n```json\n{\n  \"terminal\": {\n    \"working_directory\": \"always_home\",\n  },\n}\n```\n\n4. Always use a specific directory. This value will be shell expanded. If this\n   path is not a valid directory the terminal will default to this platform's\n   home directory.\n\n```json\n{\n  \"terminal\": {\n    \"working_directory\": {\n      \"always\": {\n        \"directory\": \"~/monyacode/projects/\",\n      },\n    },\n  },\n}\n```\n\n### Terminal: Path Hyperlink Regexes\n\n- Description: Regexes used to identify path hyperlinks. The regexes can be\n  specified in two forms - a single regex string, or an array of strings (which\n  will be collected into a single multi-line regex string).\n- Setting: `path_hyperlink_regexes`\n- Default:\n\n```json\n{\n  \"terminal\": {\n    \"path_hyperlink_regexes\": [\n      // Python-style diagnostics\n      \"File \\\"(?<path>[^\\\"]+)\\\", line (?<line>[0-9]+)\",\n      // Common path syntax with optional line, column, description, trailing punctuation, or\n      // surrounding symbols or quotes\n      [\n        \"(?x)\",\n        \"# optionally starts with 0-2 opening prefix symbols\",\n        \"[({\\\\[<]{0,2}\",\n        \"# which may be followed by an opening quote\",\n        \"(?<quote>[\\\"'`])?\",\n        \"# `path` is the shortest sequence of any non-space character\",\n        \"(?<link>(?<path>[^ ]+?\",\n        \"    # which may end with a line and optionally a column,\",\n        \"    (?<line_column>:+[0-9]+(:[0-9]+)?|:?\\\\([0-9]+([,:][0-9]+)?\\\\))?\",\n        \"))\",\n        \"# which must be followed by a matching quote\",\n        \"(?(<quote>)\\\\k<quote>)\",\n        \"# and optionally a single closing symbol\",\n        \"[)}\\\\]>]?\",\n        \"# if line/column matched, may be followed by a description\",\n        \"(?(<line_column>):[^ 0-9][^ ]*)?\",\n        \"# which may be followed by trailing punctuation\",\n        \"[.,:)}\\\\]>]*\",\n        \"# and always includes trailing whitespace or end of line\",\n        \"([ ]+|$)\",\n      ],\n    ],\n  },\n}\n```\n\n### Terminal: Path Hyperlink Timeout (ms)\n\n- Description: Maximum time to search for a path hyperlink. When set to 0, path\n  hyperlinks are disabled.\n- Setting: `path_hyperlink_timeout_ms`\n- Default: `1`\n\n## REPL\n\n- Description: Repl settings.\n- Setting: `repl`\n- Default:\n\n```json\n\"repl\": {\n  // Maximum number of columns to keep in REPL's scrollback buffer.\n  // Clamped with [20, 512] range.\n  \"max_columns\": 128,\n  // Maximum number of lines to keep in REPL's scrollback buffer.\n  // Clamped with [4, 256] range.\n  \"max_lines\": 32\n},\n```\n\n## Theme\n\n- Description: The theme setting can be specified in two forms - either as the\n  name of a theme or as an object containing the `mode`, `dark`, and `light`\n  themes for the MonyaCode UI.\n- Setting: `theme`\n- Default: `One Dark`\n\n### Theme Object\n\n- Description: Specify the theme using an object that includes the `mode`,\n  `dark`, and `light` themes.\n- Setting: `theme`\n- Default:\n\n```json\n\"theme\": {\n  \"mode\": \"system\",\n  \"dark\": \"One Dark\",\n  \"light\": \"One Light\"\n},\n```\n\n### Mode\n\n- Description: Specify theme mode.\n- Setting: `mode`\n- Default: `system`\n\n**Options**\n\n1. Set the theme to dark mode\n\n```json\n{\n  \"mode\": \"dark\",\n}\n```\n\n2. Set the theme to light mode\n\n```json\n{\n  \"mode\": \"light\",\n}\n```\n\n3. Set the theme to system mode\n\n```json\n{\n  \"mode\": \"system\",\n}\n```\n\n### Dark\n\n- Description: The name of the dark MonyaCode theme to use for the UI.\n- Setting: `dark`\n- Default: `One Dark`\n\n**Options**\n\nRun the {#action theme_selector::Toggle} action in the command palette to see a\ncurrent list of valid themes names.\n\n### Light\n\n- Description: The name of the light MonyaCode theme to use for the UI.\n- Setting: `light`\n- Default: `One Light`\n\n**Options**\n\nRun the {#action theme_selector::Toggle} action in the command palette to see a\ncurrent list of valid themes names.\n\n## Title Bar\n\n- Description: Whether or not to show various elements in the title bar\n- Setting: `title_bar`\n- Default:\n\n```json\n\"title_bar\": {\n  \"show\": true,\n  \"show_branch_icon\": false,\n  \"show_branch_name\": true,\n  \"show_project_items\": true,\n  \"show_onboarding_banner\": true,\n  \"show_user_picture\": true,\n  \"show_menus\": false\n}\n```\n\n**Options**\n\n- `show_branch_icon`: Whether to show the branch icon beside branch switcher in\n  the titlebar\n- `show_branch_name`: Whether to show the branch name button in the titlebar\n- `show_project_items`: Whether to show the project host and name in the\n  titlebar\n- `show_onboarding_banner`: Whether to show onboarding banners in the titlebar\n- `show_user_picture`: Whether to show user picture in the titlebar\n- `show_menus`: Whether to show the menus in the titlebar\n\n## Vim\n\n- Description: Whether or not to enable vim mode.\n- Setting: `vim_mode`\n- Default: `false`\n\n## When Closing With No Tabs\n\n- Description: Whether the window should be closed when using 'close active\n  item' on a window with no tabs\n- Setting: `when_closing_with_no_tabs`\n- Default: `\"platform_default\"`\n\n**Options**\n\n1. Use platform default behavior:\n\n```json\n{\n  \"when_closing_with_no_tabs\": \"platform_default\",\n}\n```\n\n2. Always close the window:\n\n```json\n{\n  \"when_closing_with_no_tabs\": \"close_window\",\n}\n```\n\n3. Never close the window:\n\n```json\n{\n  \"when_closing_with_no_tabs\": \"keep_window_open\",\n}\n```\n\n## Project Panel\n\n- Description: Customize project panel\n- Setting: `project_panel`\n- Default:\n\n```json\n{\n  \"project_panel\": {\n    \"button\": true,\n    \"default_width\": 240,\n    \"dock\": \"left\",\n    \"entry_spacing\": \"comfortable\",\n    \"file_icons\": true,\n    \"folder_icons\": true,\n    \"git_status\": true,\n    \"indent_size\": 20,\n    \"auto_reveal_entries\": true,\n    \"auto_fold_dirs\": true,\n    \"drag_and_drop\": true,\n    \"scrollbar\": {\n      \"show\": null,\n    },\n    \"sticky_scroll\": true,\n    \"show_diagnostics\": \"all\",\n    \"indent_guides\": {\n      \"show\": \"always\",\n    },\n    \"sort_mode\": \"directories_first\",\n    \"hide_root\": false,\n    \"hide_hidden\": false,\n    \"starts_open\": true,\n    \"auto_open\": {\n      \"on_create\": true,\n      \"on_paste\": true,\n      \"on_drop\": true,\n    },\n  },\n}\n```\n\n### Dock\n\n- Description: Control the position of the dock\n- Setting: `dock`\n- Default: `left`\n\n**Options**\n\n1. Default dock position to left\n\n```json\n{\n  \"dock\": \"left\",\n}\n```\n\n2. Default dock position to right\n\n```json\n{\n  \"dock\": \"right\",\n}\n```\n\n### Entry Spacing\n\n- Description: Spacing between worktree entries\n- Setting: `entry_spacing`\n- Default: `comfortable`\n\n**Options**\n\n1. Comfortable entry spacing\n\n```json\n{\n  \"entry_spacing\": \"comfortable\",\n}\n```\n\n2. Standard entry spacing\n\n```json\n{\n  \"entry_spacing\": \"standard\",\n}\n```\n\n### Git Status\n\n- Description: Indicates newly created and updated files\n- Setting: `git_status`\n- Default: `true`\n\n**Options**\n\n1. Default enable git status\n\n```json\n{\n  \"git_status\": true,\n}\n```\n\n2. Default disable git status\n\n```json\n{\n  \"git_status\": false,\n}\n```\n\n### Default Width\n\n- Description: Customize default width taken by project panel\n- Setting: `default_width`\n- Default: `240`\n\n**Options**\n\n`float` values\n\n### Auto Reveal Entries\n\n- Description: Whether to reveal it in the project panel automatically, when a\n  corresponding project entry becomes active. Gitignored entries are never auto\n  revealed.\n- Setting: `auto_reveal_entries`\n- Default: `true`\n\n**Options**\n\n1. Enable auto reveal entries\n\n```json\n{\n  \"auto_reveal_entries\": true,\n}\n```\n\n2. Disable auto reveal entries\n\n```json\n{\n  \"auto_reveal_entries\": false,\n}\n```\n\n### Auto Fold Dirs\n\n- Description: Whether to fold directories automatically when directory has only\n  one directory inside.\n- Setting: `auto_fold_dirs`\n- Default: `true`\n\n**Options**\n\n1. Enable auto fold dirs\n\n```json\n{\n  \"auto_fold_dirs\": true,\n}\n```\n\n2. Disable auto fold dirs\n\n```json\n{\n  \"auto_fold_dirs\": false,\n}\n```\n\n### Indent Size\n\n- Description: Amount of indentation (in pixels) for nested items.\n- Setting: `indent_size`\n- Default: `20`\n\n### Indent Guides: Show\n\n- Description: Whether to show indent guides in the project panel.\n- Setting: `indent_guides`\n- Default:\n\n```json\n\"indent_guides\": {\n  \"show\": \"always\"\n}\n```\n\n**Options**\n\n1. Show indent guides in the project panel\n\n```json\n{\n  \"indent_guides\": {\n    \"show\": \"always\",\n  },\n}\n```\n\n2. Hide indent guides in the project panel\n\n```json\n{\n  \"indent_guides\": {\n    \"show\": \"never\",\n  },\n}\n```\n\n### Scrollbar: Show\n\n- Description: Whether to show a scrollbar in the project panel. Possible\n  values: null, \"auto\", \"system\", \"always\", \"never\". Inherits editor settings\n  when absent, see its description for more details.\n- Setting: `scrollbar`\n- Default:\n\n```json\n\"scrollbar\": {\n  \"show\": null\n}\n```\n\n**Options**\n\n1. Show scrollbar in the project panel\n\n```json\n{\n  \"scrollbar\": {\n    \"show\": \"always\",\n  },\n}\n```\n\n2. Hide scrollbar in the project panel\n\n```json\n{\n  \"scrollbar\": {\n    \"show\": \"never\",\n  },\n}\n```\n\n### Sort Mode\n\n- Description: Sort order for entries in the project panel\n- Setting: `sort_mode`\n- Default: `directories_first`\n\n**Options**\n\n1. Show directories first, then files\n\n```json\n{\n  \"sort_mode\": \"directories_first\",\n}\n```\n\n2. Mix directories and files together\n\n```json\n{\n  \"sort_mode\": \"mixed\",\n}\n```\n\n3. Show files first, then directories\n\n```json\n{\n  \"sort_mode\": \"files_first\",\n}\n```\n\n### Auto Open\n\n- Description: Control whether files are opened automatically after different\n  creation flows in the project panel.\n- Setting: `auto_open`\n- Default:\n\n```json\n\"auto_open\": {\n  \"on_create\": true,\n  \"on_paste\": true,\n  \"on_drop\": true\n}\n```\n\n**Options**\n\n- `on_create`: Whether to automatically open newly created files in the editor.\n- `on_paste`: Whether to automatically open files after pasting or duplicating\n  them.\n- `on_drop`: Whether to automatically open files dropped from external sources.\n\n## Debugger\n\n- Description: Configuration for debugger panel and settings\n- Setting: `debugger`\n- Default:\n\n```json\n{\n  \"debugger\": {\n    \"stepping_granularity\": \"line\",\n    \"save_breakpoints\": true,\n    \"dock\": \"bottom\",\n    \"button\": true,\n  },\n}\n```\n\nSee the [debugger page](./debugger.md) for more information about debugging\nsupport within MonyaCode.\n\n## Git Panel\n\n- Description: Setting to customize the behavior of the git panel.\n- Setting: `git_panel`\n- Default:\n\n```json\n{\n  \"git_panel\": {\n    \"button\": true,\n    \"dock\": \"left\",\n    \"default_width\": 360,\n    \"status_style\": \"icon\",\n    \"fallback_branch_name\": \"main\",\n    \"sort_by_path\": false,\n    \"collapse_untracked_diff\": false,\n    \"tree_view\": false,\n    \"diff_stats\": true,\n    \"scrollbar\": {\n      \"show\": null,\n    },\n  },\n}\n```\n\n**Options**\n\n- `button`: Whether to show the git panel button in the status bar\n- `dock`: Where to dock the git panel. Can be `left` or `right`\n- `default_width`: Default width of the git panel\n- `status_style`: How to display git status. Can be `label_color` or `icon`\n- `fallback_branch_name`: What branch name to use if `init.defaultBranch` is not\n  set\n- `sort_by_path`: Whether to sort entries in the panel by path or by status (the\n  default)\n- `collapse_untracked_diff`: Whether to collapse untracked files in the diff\n  panel\n- `tree_view`: Whether to show entries as a tree rather than a flat list\n- `diff_stats`: Whether to show the number of added and deleted lines for each\n  entry, and the total for the repository\n- `scrollbar`: When to show the scrollbar in the git panel\n\n## Git Hosting Providers\n\n- Description: Register self-hosted GitHub, GitLab, or Bitbucket instances so\n  commit hashes, issue references, and permalinks resolve to the right host.\n- Setting: `git_hosting_providers`\n- Default: `[]`\n\n**Options**\n\nEach entry accepts:\n\n- `provider`: One of `github`, `gitlab`, or `bitbucket`\n- `name`: Display name for the instance\n- `base_url`: Base URL, e.g. `https://git.example.corp`\n\nYou can define these in user or project settings; project settings are merged on\ntop of user settings.\n\n```json\n{\n  \"git_hosting_providers\": [\n    {\n      \"provider\": \"github\",\n      \"name\": \"BigCorp GitHub\",\n      \"base_url\": \"https://git.example.corp\",\n    },\n  ],\n}\n```\n\n## Outline Panel\n\n- Description: Customize outline Panel\n- Setting: `outline_panel`\n- Default:\n\n```json\n\"outline_panel\": {\n  \"button\": true,\n  \"default_width\": 300,\n  \"dock\": \"left\",\n  \"file_icons\": true,\n  \"folder_icons\": true,\n  \"git_status\": true,\n  \"indent_size\": 20,\n  \"auto_reveal_entries\": true,\n  \"auto_fold_dirs\": true,\n  \"indent_guides\": {\n    \"show\": \"always\"\n  },\n  \"scrollbar\": {\n    \"show\": null\n  }\n}\n```\n\n## Calls\n\n- Description: Customize behavior when participating in a call\n- Setting: `calls`\n- Default:\n\n```json\n\"calls\": {\n  // Join calls with the microphone live by default\n  \"mute_on_join\": false,\n  // Share your project when you are the first to join a channel\n  \"share_on_join\": false\n},\n```\n\n## Colorize Brackets\n\n- Description: Whether to use tree-sitter bracket queries to detect and colorize\n  the brackets in the editor (also known as \"rainbow brackets\").\n- Setting: `colorize_brackets`\n- Default: `false`\n\n**Options**\n\n`boolean` values\n\nThe colors that are used for different indentation levels are defined in the\ntheme (theme key: `accents`). They can be customized by using theme overrides.\n\n## Unnecessary Code Fade\n\n- Description: How much to fade out unused code.\n- Setting: `unnecessary_code_fade`\n- Default: `0.3`\n\n**Options**\n\nFloat values between `0.0` and `0.9`, where:\n\n- `0.0` means no fading (unused code looks the same as used code)\n- `0.9` means maximum fading (unused code is very faint but still visible)\n\n**Example**\n\n```json\n{\n  \"unnecessary_code_fade\": 0.5,\n}\n```\n\n## UI Font Family\n\n- Description: The name of the font to use for text in the UI.\n- Setting: `ui_font_family`\n- Default: `.MonyaCodeSans`. This currently aliases to\n  [IBM Plex](https://www.ibm.com/plex/).\n\n**Options**\n\nThe name of any font family installed on the system, `\".MonyaCodeSans\"` to use the\nMonyaCode-provided default, or `\".SystemUIFont\"` to use the system's default UI font\n(on macOS and Windows).\n\n## UI Font Features\n\n- Description: The OpenType features to enable for text in the UI.\n- Setting: `ui_font_features`\n- Default:\n\n```json\n\"ui_font_features\": {\n  \"calt\": false\n}\n```\n\n- Platform: macOS and Windows.\n\n**Options**\n\nMonyaCode supports all OpenType features that can be enabled or disabled for a given\nUI font, as well as setting values for font features.\n\nFor example, to disable font ligatures, add the following to your settings:\n\n```json\n{\n  \"ui_font_features\": {\n    \"calt\": false,\n  },\n}\n```\n\nYou can also set other OpenType features, like setting `cv01` to `7`:\n\n```json\n{\n  \"ui_font_features\": {\n    \"cv01\": 7,\n  },\n}\n```\n\n## UI Font Fallbacks\n\n- Description: The font fallbacks to use for text in the UI.\n- Setting: `ui_font_fallbacks`\n- Default: `null`\n- Platform: macOS and Windows.\n\n**Options**\n\nFor example, to use `Nerd Font` as a fallback, add the following to your\nsettings:\n\n```json\n{\n  \"ui_font_fallbacks\": [\"Nerd Font\"],\n}\n```\n\n## UI Font Size\n\n- Description: The default font size for text in the UI.\n- Setting: `ui_font_size`\n- Default: `16`\n\n**Options**\n\n`integer` values from `6` to `100` pixels (inclusive)\n\n## UI Font Weight\n\n- Description: The default font weight for text in the UI.\n- Setting: `ui_font_weight`\n- Default: `400`\n\n**Options**\n\n`integer` values between `100` and `900`\n\n## Client Side Decoration Rounding\n\n- Description: Defines window border radius for platforms that use client side\n  decorations.\n- Setting: `client_side_decoration_rounding`\n- Default: `10.0`\n\n## Client Side Decoration Shadow\n\n- Description: Defines window shadow size for platforms that use client side\n  decorations.\n- Setting: `client_side_decoration_shadow`\n- Default: `10.0`\n\n## Settings Profiles\n\n- Description: Configure any number of settings profiles that are temporarily\n  applied on top of your existing user settings when selected from\n  `settings profile selector: toggle`.\n- Setting: `profiles`\n- Default: `{}`\n\nIn your `settings.jsonc` file, add the `profiles` object. Each key within this\nobject is the name of a settings profile, and each value is an object that can\ninclude any of MonyaCode's settings.\n\nExample:\n\n```json\n\"profiles\": {\n  \"Presenting (Dark)\": {\n    \"buffer_font_size\": 18.0,\n    \"theme\": \"One Dark\",\n    \"ui_font_size\": 18.0\n  },\n  \"Presenting (Light)\": {\n    \"buffer_font_size\": 18.0,\n    \"theme\": \"One Light\",\n    \"ui_font_size\": 18.0\n  },\n  \"Writing\": {\n    \"buffer_font_size\": 15.0,\n    \"theme\": \"Catppuccin Frappé - No Italics\",\n    \"ui_font_size\": 15.0,\n    \"tab_bar\": { \"show\": false },\n    \"toolbar\": { \"breadcrumbs\": false }\n  }\n}\n```\n\nTo preview and enable a settings profile, open the command palette via {#kb\ncommand_palette::Toggle} and search for `settings profile selector: toggle`.\n\n## An example configuration:\n\n```json\n// ~/.config/monyacode/settings.jsonc\n{\n  \"theme\": \"cave-light\",\n  \"tab_size\": 2,\n  \"preferred_line_length\": 80,\n  \"soft_wrap\": \"none\",\n\n  \"buffer_font_size\": 18,\n  \"buffer_font_family\": \".MonyaCodeMono\",\n\n  \"autosave\": \"on_focus_change\",\n  \"format_on_save\": \"off\",\n  \"vim_mode\": false,\n  \"projects_online_by_default\": true,\n  \"terminal\": {\n    \"font_family\": \"FiraCode Nerd Font Mono\",\n    \"blinking\": \"off\",\n  },\n  \"languages\": {\n    \"C\": {\n      \"format_on_save\": \"on\",\n      \"formatter\": \"language_server\",\n      \"preferred_line_length\": 64,\n      \"soft_wrap\": \"preferred_line_length\",\n    },\n  },\n}\n```\n"},{"path":"debugger.md","title":"Debugger","group":"Guides","content":"# Debugger\n\nMonyaCode uses the\n[Debug Adapter Protocol (DAP)](https://microsoft.github.io/debug-adapter-protocol/)\nto provide debugging functionality across multiple programming languages. DAP is\na standardized protocol that defines how debuggers, editors, and IDEs\ncommunicate with each other. It allows MonyaCode to support various debuggers without\nneeding to implement language-specific debugging logic. MonyaCode implements the\nclient side of the protocol, and various _debug adapters_ implement the server\nside.\n\nThis protocol enables features like setting breakpoints, stepping through code,\ninspecting variables, and more, in a consistent manner across different\nprogramming languages and runtime environments.\n\n## Supported Languages\n\nTo debug code written in a specific language, MonyaCode needs to find a debug adapter\nfor that language. Some debug adapters are provided by MonyaCode without additional\nsetup, and some are provided by\n[language extensions](./extensions/debugger-extensions.md). The following\nlanguages currently have debug adapters available:\n\n- [C](./languages/c.md#debugging) (built-in)\n- [C++](./languages/cpp.md#debugging) (built-in)\n- [Go](./languages/go.md#debugging) (built-in)\n- [Java](./languages/java.md#debugging) (provided by extension)\n- [JavaScript](./languages/javascript.md#debugging) (built-in)\n- [PHP](./languages/php.md#debugging) (built-in)\n- [Python](./languages/python.md#debugging) (built-in)\n- [Ruby](./languages/ruby.md#debugging) (provided by extension)\n- [Rust](./languages/rust.md#debugging) (built-in)\n- [Swift](./languages/swift.md#debugging) (provided by extension)\n- [TypeScript](./languages/typescript.md#debugging) (built-in)\n\n> If your language isn't listed, you can contribute by\n> [adding a debug adapter for it](./extensions/debugger-extensions.md).\n\nFollow those links for language- and adapter-specific information and examples,\nor read on for more about MonyaCode's general debugging features that apply to all\nadapters.\n\n## Getting Started\n\nFor most languages, the fastest way to get started is to run {#action\ndebugger::Start} ({#kb debugger::Start}). This opens the _new process modal_,\nwhich shows you a contextual list of preconfigured debug tasks for the current\nproject. Debug tasks are created from tests, entry points (like a `main`\nfunction), and from other sources — consult the documentation for your language\nfor full information about what's supported.\n\nYou can open the same modal by clicking the \"plus\" button at the top right of\nthe debug panel.\n\nFor languages that don't provide preconfigured debug tasks (this includes C,\nC++, and some extension-supported languages), you can define debug\nconfigurations in the `.monyacode/debug.jsonc` file in your project root. This file\nshould be an array of configuration objects:\n\n```json [debug]\n[\n  {\n    \"adapter\": \"CodeLLDB\",\n    \"label\": \"First configuration\"\n    // ...\n  },\n  {\n    \"adapter\": \"Debugpy\",\n    \"label\": \"Second configuration\"\n    // ...\n  }\n]\n```\n\nCheck the documentation for your language for example configurations covering\ntypical use-cases. Once you've added configurations to `.monyacode/debug.jsonc`,\nthey'll appear in the list in the new process modal.\n\nMonyaCode will also load debug configurations from `.vscode/launch.json`, and show\nthem in the new process modal if no configurations are found in\n`.monyacode/debug.jsonc`.\n\n#### Global debug configurations\n\nIf you run the same launch profiles across multiple projects, you can store them\nonce in your user configuration. Invoke {#action monyacode::OpenDebugTasks} from the\ncommand palette to open the global `debug.jsonc` file; MonyaCode creates it next to\nyour user `settings.jsonc` and keeps it in sync with the debugger UI. The file\nlives at:\n\n- **macOS:** `~/Library/Application Support/MonyaCode/debug.jsonc`\n- **Linux/BSD:** `$XDG_CONFIG_HOME/monyacode/debug.jsonc` (falls back to\n  `~/.config/monyacode/debug.jsonc`)\n- **Windows:** `%APPDATA%\\MonyaCode\\debug.jsonc`\n\nPopulate this file with the same array of objects you would place in\n`.monyacode/debug.jsonc`. Any scenarios defined there are merged into every\nworkspace, so your favorite launch presets appear automatically in the \"New\nDebug Session\" dialog.\n\n### Launching & Attaching\n\nMonyaCode debugger offers two ways to debug your program; you can either _launch_ a\nnew instance of your program or _attach_ to an existing process. Which one you\nchoose depends on what you are trying to achieve.\n\nWhen launching a new instance, MonyaCode (and the underlying debug adapter) can often\ndo a better job at picking up the debug information compared to attaching to an\nexisting process, since it controls the lifetime of a whole program. Running\nunit tests or a debug build of your application is a good use case for\nlaunching.\n\nCompared to launching, attaching to an existing process might seem inferior, but\nthat's far from truth; there are cases where you cannot afford to restart your\nprogram, because for example, the bug is not reproducible outside of a\nproduction environment or some other circumstances.\n\n## Configuration\n\nMonyaCode requires the `adapter` and `label` fields for all debug tasks. In addition,\nMonyaCode will use the `build` field to run any necessary setup steps before the\ndebugger starts [(see below)](#build-tasks), and can accept a `tcp_connection`\nfield to connect to an existing process.\n\nAll other fields are provided by the debug adapter and can contain\n[task variables](./tasks.md#variables). Most adapters support `request`,\n`program`, and `cwd`:\n\n```json [debug]\n[\n  {\n    // The label for the debug configuration and used to identify the debug session inside the debug panel & new process modal\n    \"label\": \"Example Start debugger config\",\n    // The debug adapter that MonyaCode should use to debug the program\n    \"adapter\": \"Example adapter name\",\n    // Request:\n    //  - launch: MonyaCode will launch the program if specified, or show a debug terminal with the right configuration\n    //  - attach: MonyaCode will attach to a running program to debug it, or when the process_id is not specified, will show a process picker (only supported for node currently)\n    \"request\": \"launch\",\n    // The program to debug. This field supports path resolution with ~ or . symbols.\n    \"program\": \"path_to_program\",\n    // cwd: defaults to the current working directory of your project ($MONYACODE_WORKTREE_ROOT)\n    \"cwd\": \"$MONYACODE_WORKTREE_ROOT\"\n  }\n]\n```\n\nCheck your debug adapter's documentation for more information on the fields it\nsupports.\n\n### Build tasks\n\nMonyaCode allows embedding a MonyaCode task in the `build` field that is run before the\ndebugger starts. This is useful for setting up the environment or running any\nnecessary setup steps before the debugger starts.\n\n```json [debug]\n[\n  {\n    \"label\": \"Build Binary\",\n    \"adapter\": \"CodeLLDB\",\n    \"program\": \"path_to_program\",\n    \"request\": \"launch\",\n    \"build\": {\n      \"command\": \"make\",\n      \"args\": [\"build\", \"-j8\"]\n    }\n  }\n]\n```\n\nBuild tasks can also refer to the existing tasks by unsubstituted label:\n\n```json [debug]\n[\n  {\n    \"label\": \"Build Binary\",\n    \"adapter\": \"CodeLLDB\",\n    \"program\": \"path_to_program\",\n    \"request\": \"launch\",\n    \"build\": \"my build task\" // Or \"my build task for $MONYACODE_FILE\"\n  }\n]\n```\n\n### Automatic scenario creation\n\nGiven a MonyaCode task, MonyaCode can automatically create a scenario for you. Automatic\nscenario creation also powers our scenario creation from gutter. Automatic\nscenario creation is currently supported for Rust, Go, Python, JavaScript, and\nTypeScript.\n\n## Breakpoints\n\nTo set a breakpoint, simply click next to the line number in the editor gutter.\nBreakpoints can be tweaked depending on your needs; to access additional options\nof a given breakpoint, right-click on the breakpoint icon in the gutter and\nselect the desired option. At present, you can:\n\n- Add a log to a breakpoint, which will output a log message whenever that\n  breakpoint is hit.\n- Make the breakpoint conditional, which will only stop at the breakpoint when\n  the condition is met. The syntax for conditions is adapter-specific.\n- Add a hit count to a breakpoint, which will only stop at the breakpoint after\n  it's hit a certain number of times.\n- Disable a breakpoint, which will prevent it from being hit while leaving it\n  visible in the gutter.\n\nSome debug adapters (e.g. CodeLLDB and JavaScript) will also _verify_ whether\nyour breakpoints can be hit; breakpoints that cannot be hit are surfaced more\nprominently in the UI.\n\nAll breakpoints enabled for a given project are also listed in \"Breakpoints\"\nitem in your debugging session UI. From \"Breakpoints\" item in your UI you can\nalso manage exception breakpoints. The debug adapter will then stop whenever an\nexception of a given kind occurs. Which exception types are supported depends on\nthe debug adapter.\n\n## Settings\n\nThe settings for the debugger are grouped under the `debugger` key in\n`settings.jsonc`:\n\n- `dock`: Determines the position of the debug panel in the UI.\n- `stepping_granularity`: Determines the stepping granularity.\n- `save_breakpoints`: Whether the breakpoints should be reused across MonyaCode\n  sessions.\n- `button`: Whether to show the debug button in the status bar.\n- `timeout`: Time in milliseconds until timeout error when connecting to a TCP\n  debug adapter.\n- `log_dap_communications`: Whether to log messages between active debug\n  adapters and MonyaCode.\n- `format_dap_log_messages`: Whether to format DAP messages when adding them to\n  the debug adapter logger.\n\n### Dock\n\n- Description: The position of the debug panel in the UI.\n- Default: `bottom`\n- Setting: debugger.dock\n\n**Options**\n\n1. `left` - The debug panel will be docked to the left side of the UI.\n2. `right` - The debug panel will be docked to the right side of the UI.\n3. `bottom` - The debug panel will be docked to the bottom of the UI.\n\n```json\n\"debugger\": {\n  \"dock\": \"bottom\"\n},\n```\n\n### Stepping granularity\n\n- Description: The Step granularity that the debugger will use\n- Default: `line`\n- Setting: `debugger.stepping_granularity`\n\n**Options**\n\n1. Statement - The step should allow the program to run until the current\n   statement has finished executing. The meaning of a statement is determined by\n   the adapter and it may be considered equivalent to a line. For example\n   'for(int i = 0; i < 10; i++)' could be considered to have 3 statements 'int i\n   = 0', 'i < 10', and 'i++'.\n\n```json\n{\n  \"debugger\": {\n    \"stepping_granularity\": \"statement\",\n  },\n}\n```\n\n2. Line - The step should allow the program to run until the current source line\n   has executed.\n\n```json\n{\n  \"debugger\": {\n    \"stepping_granularity\": \"line\",\n  },\n}\n```\n\n3. Instruction - The step should allow one instruction to execute (e.g. one x86\n   instruction).\n\n```json\n{\n  \"debugger\": {\n    \"stepping_granularity\": \"instruction\",\n  },\n}\n```\n\n### Save Breakpoints\n\n- Description: Whether the breakpoints should be saved across MonyaCode sessions.\n- Default: `true`\n- Setting: `debugger.save_breakpoints`\n\n**Options**\n\n`boolean` values\n\n```json\n{\n  \"debugger\": {\n    \"save_breakpoints\": true,\n  },\n}\n```\n\n### Button\n\n- Description: Whether the button should be displayed in the debugger toolbar.\n- Default: `true`\n- Setting: `debugger.show_button`\n\n**Options**\n\n`boolean` values\n\n```json\n{\n  \"debugger\": {\n    \"show_button\": true,\n  },\n}\n```\n\n### Timeout\n\n- Description: Time in milliseconds until timeout error when connecting to a TCP\n  debug adapter.\n- Default: `2000`\n- Setting: `debugger.timeout`\n\n**Options**\n\n`integer` values\n\n```json\n{\n  \"debugger\": {\n    \"timeout\": 3000,\n  },\n}\n```\n\n### Inline Values\n\n- Description: Whether to enable editor inlay hints showing the values of\n  variables in your code during debugging sessions.\n- Default: `true`\n- Setting: `inlay_hints.show_value_hints`\n\n**Options**\n\n```json\n{\n  \"inlay_hints\": {\n    \"show_value_hints\": false,\n  },\n}\n```\n\nInline value hints can also be toggled from the Editor Controls menu in the\neditor toolbar.\n\n### Log Dap Communications\n\n- Description: Whether to log messages between active debug adapters and MonyaCode.\n  (Used for DAP development)\n- Default: false\n- Setting: debugger.log_dap_communications\n\n**Options**\n\n`boolean` values\n\n```json\n{\n  \"debugger\": {\n    \"log_dap_communications\": true,\n  },\n}\n```\n\n### Format Dap Log Messages\n\n- Description: Whether to format DAP messages when adding them to the debug\n  adapter logger. (Used for DAP development)\n- Default: false\n- Setting: debugger.format_dap_log_messages\n\n**Options**\n\n`boolean` values\n\n```json\n{\n  \"debugger\": {\n    \"format_dap_log_messages\": true,\n  },\n}\n```\n\n### Customizing Debug Adapters\n\n- Description: Custom program path and arguments to override how MonyaCode launches a\n  specific debug adapter.\n- Default: Adapter-specific\n- Setting: `dap.$ADAPTER.binary` and `dap.$ADAPTER.args`\n\nYou can pass `binary`, `args`, or both. `binary` should be a path to a _debug\nadapter_ (like `lldb-dap`) not a _debugger_ (like `lldb` itself). The `args`\nsetting overrides any arguments that MonyaCode would otherwise pass to the adapter.\n\n```json\n{\n  \"dap\": {\n    \"CodeLLDB\": {\n      \"binary\": \"/Users/name/bin/lldb-dap\",\n      \"args\": [\"--wait-for-debugger\"],\n    },\n  },\n}\n```\n\n## Theme\n\nThe Debugger supports the following theme options:\n\n- `debugger.accent`: Color used to accent breakpoint & breakpoint-related\n  symbols\n- `editor.debugger_active_line.background`: Background color of active debug\n  line\n\n## Troubleshooting\n\nIf you're running into problems with the debugger, there are some features you\ncan use to gather more information about the problem:\n\n- When you have a session running in the debug panel, you can run the {#action\n  dev::CopyDebugAdapterArguments} action to copy a JSON blob to the clipboard\n  that describes how MonyaCode initialized the session. This is especially useful\n  when the session failed to start, and is great context to add if you open a\n  GitHub issue.\n- You can also use the {#action dev::OpenDebugAdapterLogs} action to see a trace\n  of all of MonyaCode's communications with debug adapters during the most recent\n  debug sessions.\n"},{"path":"development/debuggers.md","title":"Using a debugger","group":"Development","content":"# Using a debugger\n\n> **DISCLAIMER**: This is not documentation for\n> [configuring MonyaCode's debugger](../debugger.md). Rather, it is intended to\n> provide information on how to use a debugger while developing MonyaCode itself.\n\n## Using MonyaCode's built-in debugger\n\nWhile the MonyaCode project is open you can open the `New Process Modal` and select\nthe `Debug` tab. There you can see to debug configurations to debug MonyaCode with,\none for GDB and one for LLDB. Select the configuration you want and MonyaCode will\nbuild and launch the binary.\n\nNote: GDB isn't supported on ARM Macbooks.\n\n## Release build profile considerations\n\nBy default, builds using the release profile (release is the profile used for\nproduction builds, i.e. stable) include limited debug info.\n\nThis is done by setting the `profile.(release).debug` field in the root\n`Cargo.toml` field to `\"limited\"`.\n\nThe official documentation for the `debug` field can be found\n[here](https://doc.rust-lang.org/cargo/reference/profiles.html#debug). But the\nTLDR is that `\"limited\"` strips type and variable level debug info.\n\nIn release builds, this is done to reduce the binary size, as type and variable\nlevel debug info is not required, and does not impact the usability of generated\nstack traces.\n\nHowever, while the type and variable level debug info is not required for good\nstack traces, it is very important for a good experience using debuggers, as\nwithout the type and variable level debug info, the debugger has no way to\nresolve local variables, inspect them, format them using pretty-printers, etc.\n\nTherefore, in order to use a debugger to it's fullest extent when debugging a\nrelease build, you must compile a new MonyaCode binary, with full debug info.\n\nThe simplest way to do this, is to use the `--config` flag to override the\n`debug` field in the root `Cargo.toml` file when running `cargo run` or\n`cargo build` like so:\n\n```sh\ncargo run --config 'profile.release.debug=\"full\"'\ncargo build --config 'profile.release.debug=\"full\"'\n```\n\n> If you wish to avoid passing the `--config` flag on every invocation of\n> `cargo`. You may also change the section in the\n> [root `Cargo.toml`](https://github.com/zed-industries/zed/blob/main/Cargo.toml)\n>\n> from\n>\n> ```toml\n> [profile.release]\n> debug = \"limited\"\n> ```\n>\n> to\n>\n> ```toml\n> [profile.release]\n> debug = \"full\"\n> ```\n>\n> This will ensure all invocations of `cargo run --release` or\n> `cargo build --release` will compile with full debug info.\n>\n> **WARNING:** Make sure to avoid committing these changes!\n\n## Running MonyaCode with a shell debugger GDB/LLDB\n\n### Background\n\nThe recommended way to get rust for MonyaCode development is via\n[rustup](https://rustup.rs). For more details, see the documentation for getting\n[started on your platform](../development.md).\n\nWhen rust is installed via rustup, a few additional scripts are installed and\nput on your path to assist with debugging binaries compiled with rust:\n`rust-gdb` and `rust-lldb`. Learn more about these scripts and why they are\nuseful on\n[a bit of respite](https://michaelwoerister.github.io/2015/03/27/rust-xxdb.html).\n\nIn summary, they are simple shell scripts that wrap the standard `gdb` and\n`lldb` commands, injecting the relevant commands and flags to enable additional\nrust-specific features such as pretty-printers and type information.\n\nIn order to use `rust-gdb` or `rust-lldb`, you must have `gdb` or `lldb`\ninstalled on your system. If you don't have them installed, you will need to\ninstall them in a manner appropriate for your platform.\n\nQuoting the article above,\n\n> \"The minimum supported debugger versions are GDB 7.7 and LLDB 310. However,\n> the general rule is: the newer the better.\"\n\nIt is recommended to install the latest version of `gdb` or `lldb` if possible.\n\n> **Note**: On Windows, `rust-gdb` is not installed by default as `gdb` support\n> for Windows is not very stable. Use `lldb` with `rust-lldb` instead.\n\n- [gdb](https://www.gnu.org/software/gdb/)\n- [lldb](https://lldb.llvm.org/)\n\n### Usage\n\nFollow the steps above to include full debug info when compiling MonyaCode. Then, run\n`rust-gdb` or `rust-lldb` on the compiled MonyaCode binary after building it with\n`cargo build`:\n\n```\n# for gdb:\nrust-gdb target/debug/monyacode\n\n# for lldb:\nrust-lldb target/debug/monyacode\n```\n\nAlternatively, attach to a running instance of MonyaCode (such as an instance of MonyaCode\nstarted using `cargo run`):\n\n```\nrust-gdb -p <pid>\nrust-lldb -p <pid>\n```\n\nWhere `<pid>` is the process ID of the MonyaCode instance you want to attach to.\n\nTo get the process ID of a running MonyaCode instance, you can use your systems\nprocess management tools such as `Task Manager` on windows or `Activity Monitor`\non macOS, or from a terminal:\n\n```\n# Mac or Linux\nps aux | grep monyacode\n\n# Windows (PowerShell)\nGet-Process | Select-Object Id, ProcessName\n```\n\n#### Debugging Panics and Crashes\n\nDebuggers can be an excellent tool for debugging the cause of panics and crashes\nin all programs, including MonyaCode.\n\nBy default, when a process that `gdb` or `lldb` is attached to hits an exception\nsuch as a panic, the debugger will automatically stop at the point of the panic\nand allow you to inspect the state of the program.\n\nMost likely, the point at which the debugger stops will be deep in the rust\nstandard library panic or exception handling code, so you will need to navigate\nup the stack trace to find the actual cause of the panic.\n\nThis can be accomplished using the `backtrace` command in combination with the\n`frame select` command in `lldb`, with similar commands available in `gdb`.\n\nOnce the program is stopped, you will not be able to continue execution as you\ncan before an exception is hit. However, you can jump around to different stack\nframes, and inspect the values of variables and expressions within each frame,\nwhich can be very useful in identifying the root cause of the crash.\n\nRead more on [Debugging Crashes](./debugging-crashes.md).\n"},{"path":"development/debugging-crashes.md","title":"Debugging Crashes","group":"Development","content":"# Debugging Crashes\n\nTo generate a minidump when MonyaCode panics or crashes, set the environment variable\n`MONYACODE_GENERATE_MINIDUMPS=1`:\n\n```sh\n$ MONYACODE_GENERATE_MINIDUMPS=1 monyacode\n```\n\nIf set, MonyaCode sends a message to a sidecar process which inspects the memory of\nthe crashing editor to create a\n[minidump](https://chromium.googlesource.com/breakpad/breakpad/+/master/docs/getting_started_with_breakpad.md#the-minidump-file-format)\nfile in `~/Library/Logs/MonyaCode` or `$XDG_DATA_HOME/monyacode/logs`. This minidump can\nbe used to generate backtraces for the stacks of all threads.\n\nThese crash reports contain rich information; but they are hard to read because\nthey don't contain spans or symbol information. You can still work with them\nlocally by downloading sources and an unstripped binary (or separate symbols\nfile) for your MonyaCode release and running:\n\n```sh\nzstd -d ~/.local/share/monyacode/<uuid>.dmp -o minidump.dmp\nminidump-stackwalk minidump.dmp\n```\n\nAlongside the minidump file in your logs dir, there should be a `<uuid>.json`\nwhich contains additional metadata like the panic message, span, and system\nspecs.\n\n## Using a Debugger\n\nIf you can reproduce the crash consistently, a debugger can be used to inspect\nthe state of the program at the time of the crash, often providing useful\ninsights into the cause of the crash.\n\nYou can read more about setting up and using a debugger with MonyaCode, and\nspecifically for debugging crashes\n[here](./debuggers.md#debugging-panics-and-crashes).\n"},{"path":"development/flatpak.md","title":"Building MonyaCode for Linux (Flatpak)","group":"Development","content":"# Building MonyaCode for Linux (Flatpak)\n\nTo build a `Flatpak`, you will need `Flatpak` installed.\n\n```bash\n./script/linux\n./script/flatpak/deps\n./script/flatpak/bundle-flatpak\n```\n"},{"path":"development/freebsd.md","title":"Building MonyaCode for FreeBSD","group":"Development","content":"# Building MonyaCode for FreeBSD\n\nNote, FreeBSD is not currently a supported platform, and so this is a\nwork-in-progress.\n\n## Repository\n\nClone the [MonyaCode repository](https://github.com/monyacode/monyacode).\n\n## Dependencies\n\n- Install the necessary system packages and rustup:\n\n  ```sh\n  script/freebsd\n  ```\n\n  If preferred, you can inspect\n  [`script/freebsd`](https://github.com/zed-industries/zed/blob/main/script/freebsd)\n  and perform the steps manually.\n\n## Building from source\n\nOnce the dependencies are installed, you can build MonyaCode using\n[Cargo](https://doc.rust-lang.org/cargo/).\n\nFor a debug build of the editor:\n\n```sh\ncargo run\n```\n\nAnd to run the tests:\n\n```sh\ncargo test --workspace\n```\n\nIn release mode, the primary user interface is the `cli` crate. You can run it\nin development with:\n\n```sh\ncargo run -p cli\n```\n\n## Troubleshooting\n\n### Cargo errors claiming that a dependency is using unstable features\n\nTry `cargo clean` and `cargo build`.\n"},{"path":"development/glossary.md","title":"Development: Glossary","group":"Development","content":"# Development: Glossary\n\n## Naming conventions\n\nThese are generally true for the whole codebase. Note that Name can be anything\nhere. An example would be `AnyElement` and `LspStore`.\n\n- `AnyName`: A type erased version of _name_. Think `Box<dyn NameTrait>`.\n- `NameStore`: A wrapper type which abstracts over whether operations are\n  running locally or on a remote.\n\n## GPUI\n\n### State management\n\n- `App`: A singleton which holds the full application state including all the\n  entities. Crucially: `App` is not `Send`, which means that `App` only exists\n  on the thread that created it (which is the main/UI thread, usually). Thus, if\n  you see a `&mut App`, know that you're on UI thread.\n- `Context`: A wrapper around the `App` struct with specialized behavior for a\n  specific `Entity`. Think of it as `(&mut App, Entity<V>)`. The specialized\n  behavior is surfaced in the API surface of `Context`. E.g., `App::spawn` takes\n  an `AsyncFnOnce(AsyncApp) -> Ret`, whereas `Context::spawn` takes an\n  `AsyncFnOnce(WeakEntity<V>, AsyncApp) -> Ret`.\n- `AsyncApp`: An owned version of `App` for use in async contexts. This type is\n  _still_ not `Send` (so `AsyncApp` = you're on the main thread) and any use of\n  it may be fallible (to account for the fact that the `App` might've been\n  terminated by the time this closure runs). The convenience of `AsyncApp` lies\n  in the fact that you usually interface with `App` via `&mut App`, which would\n  be inconvenient to use with async closures; `AsyncApp` is owned, so you can\n  use it in async closures with no sweat.\n- `AppContext` A trait which abstracts over `App`, `AsyncApp` & `Context` and\n  their Test versions.\n- `Task`: A future running or scheduled to run on the background or foreground\n  executor. In contradiction to regular Futures Tasks do not need `.await` to\n  start running. You do need to await them to get the result of the task.\n- `Executor`: Used to spawn tasks that run either on the foreground or\n  background thread. Try to run the tasks on the background thread.\n  - `BackgroundExecutor`: A threadpool running `Task`s.\n  - `ForegroundExecutor`: The main thread running `Task`s.\n- `Entity`: A strong, well-typed reference to a struct which is managed by gpui.\n  Effectively a pointer/map key into the `App::EntityMap`.\n- `WeakEntity`: A runtime checked reference to an `Entity` which may no longer\n  exist. Similar to\n  [`std::rc::Weak`](https://doc.rust-lang.org/std/rc/struct.Weak.html).\n- `Global`: A singleton type which has only one value, that is stored in the\n  `App`.\n- `Event`: A datatype which can be send by an `Entity` to subscribers\n- `Action`: An event that represents a user's keyboard input that can be handled\n  by listeners Example: `file finder: toggle`\n- `Observing`: reacting entities notifying they've changed\n- `Subscription`: An event handler that is used to react to the changes of state\n  in the application.\n  1. Emitted event handling\n  2. Observing `{new,release,on notify}` of an entity\n\n### UI\n\n- `View`: An `Entity` which can produce an `Element` through its implementation\n  of `Render`.\n- `Element`: A type that can be laid out and painted to the screen.\n- `element expression`: An expression that builds an element tree, example:\n\n```rust\nh_flex()\n    .id(text[i])\n    .relative()\n    .when(selected, |this| {\n        this.child(\n            div()\n                .h_4()\n                .absolute()\n                etc etc\n```\n\n- `Component`: A builder which can be rendered turning it into an `Element`.\n- `Dispatch tree`: TODO\n- `Focus`: The place where keystrokes are handled first\n- `Focus tree`: Path from the place that has the current focus to the UI Root.\n  Example <img> TODO\n\n## MonyaCode UI\n\n- `Window`: A struct in monyacode representing a monyacode window in your desktop\n  environment (see image below). There can be multiple if you have multiple monyacode\n  instances open. Mostly passed around for rendering.\n- `Modal`: A UI element that floats on top of the rest of the UI\n- `Picker`: A struct representing a list of items in floating on top of the UI\n  (Modal). You can select an item and confirm. What happens on select or confirm\n  is determined by the picker's delegate. (The 'Model' in the image below is a\n  picker.)\n- `PickerDelegate`: A trait used to specialize behavior for a `Picker`. The\n  `Picker` stores the `PickerDelegate` in the field delegate.\n- `Center`: The middle of the monyacode window, the center is split into multiple\n  `Pane`s. In the codebase this is a field on the `Workspace` struct. (see image\n  below).\n- `Pane`: An area in the `Center` where we can place items, such as an editor,\n  multi-buffer or terminal (see image below).\n- `Panel`: An `Entity` implementing the `Panel` trait. These can be placed in a\n  `Dock`. In the image below we see the: `ProjectPanel` in the left dock and the\n  `DebugPanel` in the bottom dock. Note `Editor` does not implement `Panel` and\n  hence is not a `Panel`.\n- `Dock`: A UI element similar to a `Pane` which can be opened and hidden. There\n  can be up to 3 docks open at a time, left right and below the center. A dock\n  contains one or more `Panel`s not `Pane`s. (see image).\n\n<img width=\"1921\" height=\"1080\" alt=\"Screenshot for the Pane and Dock features\" src=\"https://github.com/user-attachments/assets/2cb1170e-2850-450d-89bb-73622b5d07b2\" />\n\n- `Project`: One or more `Worktree`s\n- `Worktree`: Represents either local or remote files.\n\n<img width=\"552\" height=\"1118\" alt=\"Screenshot for the Worktree feature\" src=\"https://github.com/user-attachments/assets/da5c58e4-b02e-4038-9736-27e3509fdbfa\" />\n\n- [Multibuffer](../multibuffers.md): A list of Editors, a multi-buffer allows\n  editing multiple files simultaneously. A multi-buffer opens when an operation\n  in MonyaCode returns multiple locations, examples: _search_ or _go to definition_.\n  See project search in the image below.\n\n<img width=\"800\" height=\"886\" alt=\"Screenshot for the MultiBuffer feature\" src=\"https://github.com/user-attachments/assets/d59dcecd-8ab6-4172-8fb6-b1fc3c3eaf9d\" />\n\n## Editor\n\n- `Editor`: _The_ text editor, nearly everything in monyacode is an `Editor`, even\n  single line inputs. Each pane in the image above contains one or more `Editor`\n  instances.\n- `Workspace`: The root of the window\n- `Entry`: A file, dir, pending dir or unloaded dir.\n- `Buffer`: The in-memory representation of a 'file' together with relevant data\n  such as syntax trees, git status and diagnostics.\n- `pending selection`: You have mouse down and you're dragging but you have not\n  yet released.\n\n## Debugger\n\n- `DapStore`: Is an entity that manages debugger sessions\n- `debugger::Session`: Is an entity that manages the lifecycle of a debug\n  session and communication with DAPS\n- `BreakpointStore`: Is an entity that manages breakpoints states in local and\n  remote instances of MonyaCode\n- `DebugSession`: Manages a debug session's UI and running state\n- `RunningState`: Directly manages all the views of a debug session\n- `VariableList`: The variable and watch list view of a debug session\n- `Console`: TODO\n- `Terminal`: TODO\n- `BreakpointList`: TODO\n"},{"path":"development/linux.md","title":"Building MonyaCode for Linux","group":"Development","content":"# Building MonyaCode for Linux\n\n## Repository\n\nClone the [MonyaCode repository](https://github.com/monyacode/monyacode).\n\n## Dependencies\n\n- Install [rustup](https://www.rust-lang.org/tools/install)\n\n- Install the necessary system libraries:\n\n  ```sh\n  script/linux\n  ```\n\n  If you prefer to install the system libraries manually, you can find the list\n  of required packages in the `script/linux` file.\n\n### Linkers {#linker}\n\nOn Linux, Rust's default linker is\n[LLVM's `lld`](https://blog.rust-lang.org/2025/09/18/Rust-1.90.0/). Alternative\nlinkers, especially [Wild](https://github.com/davidlattimore/wild) and\n[Mold](https://github.com/rui314/mold) can significantly improve clean and\nincremental build time.\n\nFor local development Wild is recommended because it's 5-20% faster than Mold.\n\nThese linkers can be installed with `script/install-mold` and\n`script/install-wild`.\n\nTo use Wild as your default, add these lines to your `~/.cargo/config.toml`:\n\n```toml\n[target.x86_64-unknown-linux-gnu]\nlinker = \"clang\"\nrustflags = [\"-C\", \"link-arg=--ld-path=wild\"]\n\n[target.aarch64-unknown-linux-gnu]\nlinker = \"clang\"\nrustflags = [\"-C\", \"link-arg=--ld-path=wild\"]\n```\n\nTo use Mold as your default:\n\n```toml\n[target.'cfg(target_os = \"linux\")']\nrustflags = [\"-C\", \"link-arg=-fuse-ld=mold\"]\n```\n\n## Building from source\n\nOnce the dependencies are installed, you can build using\n[Cargo](https://doc.rust-lang.org/cargo/).\n\nFor a debug build of the editor:\n\n```sh\ncargo run\n```\n\nTo run the tests:\n\n```sh\ncargo test --workspace\n```\n\nIn release mode, the primary user interface is the `cli` crate. You can run it\nin development with:\n\n```sh\ncargo run -p cli\n```\n\n## Installing a development build\n\nYou can install a local build on your machine with:\n\n```sh\n./script/install.sh --build\n```\n\nThis will build monyacode and the cli in release mode and make them available at\n`~/.local/bin/monyacode`, installing .desktop files to `~/.local/share`.\n\n> **_Note_**: If you encounter linker errors similar to the following:\n>\n> ```bash\n> error: linking with `cc` failed: exit status: 1 ...\n> = note: /usr/bin/ld: /tmp/rustcISMaod/libaws_lc_sys-79f08eb6d32e546e.rlib(f8e4fd781484bd36-bcm.o): in function `aws_lc_0_25_0_handle_cpu_env':\n>           /aws-lc/crypto/fipsmodule/cpucap/cpu_intel.c:(.text.aws_lc_0_25_0_handle_cpu_env+0x63): undefined reference to `__isoc23_sscanf'\n>           /usr/bin/ld: /tmp/rustcISMaod/libaws_lc_sys-79f08eb6d32e546e.rlib(f8e4fd781484bd36-bcm.o): in function `pkey_rsa_ctrl_str':\n>           /aws-lc/crypto/fipsmodule/evp/p_rsa.c:741:(.text.pkey_rsa_ctrl_str+0x20d): undefined reference to `__isoc23_strtol'\n>           /usr/bin/ld: /aws-lc/crypto/fipsmodule/evp/p_rsa.c:752:(.text.pkey_rsa_ctrl_str+0x258): undefined reference to `__isoc23_strtol'\n>           collect2: error: ld returned 1 exit status\n>   = note: some `extern` functions couldn't be found; some native libraries may need to be installed or have their path specified\n>   = note: use the `-l` flag to specify native libraries to link\n>   = note: use the `cargo:rustc-link-lib` directive to specify the native libraries to link with Cargo (see https://doc.rust-lang.org/cargo/reference/build-scripts.html#rustc-link-lib)\n> error: could not compile `remote_server` (bin \"remote_server\") due to 1 previous error\n> ```\n>\n> **Cause**: this is caused by known bugs in aws-lc-rs(doesn't support GCC >=\n> 14):\n> [FIPS fails to build with GCC >= 14](https://github.com/aws/aws-lc-rs/issues/569)\n> &\n> [GCC-14 - build failure for FIPS module](https://github.com/aws/aws-lc/issues/2010)\n>\n> You can refer to\n> [linux: Linker error for remote_server when using script/install-linux](https://github.com/zed-industries/zed/issues/24880)\n> for more information.\n>\n> **Workarounds**: Set the remote server target to `x86_64-unknown-linux-gnu`\n> like so\n> `export REMOTE_SERVER_TARGET=x86_64-unknown-linux-gnu; script/install-linux`\n\n## Wayland & X11\n\nMonyaCode supports both X11 and Wayland. By default, we pick whichever we can find at\nruntime. If you're on Wayland and want to run in X11 mode, use the environment\nvariable `WAYLAND_DISPLAY=''`.\n\n## Packaging MonyaCode\n\nMonyaCode has two main binaries:\n\n- You will need to build `crates/cli` and make its binary available in `$PATH`\n  with the name `monyacode`.\n- You will need to build `crates/monyacode` and put it at\n  `$PATH/to/cli/../../libexec/monyacode-editor`. For example, if you are going to put\n  the cli at `~/.local/bin/monyacode` put monyacode at `~/.local/libexec/monyacode-editor`. As\n  some linux distributions (notably Arch) discourage the use of `libexec`, you\n  can also put this binary at `$PATH/to/cli/../../lib/monyacode/monyacode-editor` (e.g.\n  `~/.local/lib/monyacode/monyacode-editor`) instead.\n- If you are going to provide a `.desktop` file you can find a template in\n  `crates/monyacode/resources/monyacode.desktop.in`, and use `envsubst` to populate it\n  with the values required. This file should also be renamed to\n  `$APP_ID.desktop` so that the file\n  [follows the FreeDesktop standards](https://github.com/zed-industries/zed/issues/12707#issuecomment-2168742761).\n  You should also make this desktop file executable (`chmod 755`).\n- You will need to ensure that the necessary libraries are installed. You can\n  get the current list by\n  [inspecting the built binary](https://github.com/zed-industries/zed/blob/935cf542aebf55122ce6ed1c91d0fe8711970c82/script/bundle-linux#L65-L67)\n  on your system.\n- For an example of a complete build script, see\n  [script/bundle-linux](https://github.com/zed-industries/zed/blob/935cf542aebf55122ce6ed1c91d0fe8711970c82/script/bundle-linux).\n- Set the environment variable `MONYACODE_NO_BUNDLED_UNINSTALL` to something like\n  \"Use flatpak to uninstall\" so that the CLI uninstall command doesn't try to\n  run the uninstallation script.\n\n### Other things to note\n\nZed comes with some (in our opinion) pretty severe caveats:\n\n- It automatically downloads and installs binaries without explicit user\n  prompting in the same way as rustup/rbenv/pyenv, etc. They say that they\n  \"understand this is contentious\", and yet they insist on doing it.\n  [See here](https://github.com/zed-industries/zed/issues/12589).\n- They download and install extension binaries from\n  [zed-industries/extensions](https://github.com/zed-industries/extensions)\n  without explicit permission or any way to inspect what is being installed.\n  These extensions may in turn install further software without explicit\n  permission or control. For more details,\n  [see here](https://github.com/zed-industries/zed/issues/12358).\n- It does not play nice with sandboxes,\n  [see here](https://github.com/zed-industries/zed/pull/12006#issuecomment-2130421220)\n\nOur (MonyaCode's) intention is to resolve all of these problems.\n\n## Distro Packages\n\nThe MonyaCode project includes scripts to build binary packages for various targets,\nincluding `.deb`, `.rpm` and `AppImage`. The driver for all these is the\n`scripts/bundle-linux` script which takes the following flags (among others):\n\n- `--deb` (requires [`cargo-deb`](https://github.com/kornelski/cargo-deb))\n- `--rpm` (requires\n  [`cargo-generate-rpm`](https://github.com/cat-in-136/cargo-generate-rpm))\n- `--appimage` (requires\n  [`pkg2appimage`](https://github.com/AppImageCommunity/pkg2appimage))\n\nAdding any of these will build the corresponding package. Note that the\n`AppImage` is created from the `.deb.` package so while you can only provide the\n`--appimage` flag it will automatically build a `.deb` as well.\n\nNote that each of these package builds requires an additional dependency as\nnoted above.\n\n## Flatpak\n\n> MonyaCode's current Flatpak integration exits the sandbox on startup. Workflows\n> that rely on Flatpak's sandboxing may not work as expected.\n\nTo build & install the Flatpak package locally follow the steps below:\n\n1. Install Flatpak for your distribution as outlined\n   [here](https://flathub.org/setup).\n2. Run `script/linux` to install Linux dependencies.\n3. Run `script/flatpak/deps` script to install the required dependencies.\n4. Run `script/flatpak/bundle-flatpak`.\n5. Now the package has been installed and has a bundle available at\n   `target/release/{app-id}.flatpak`.\n\n## Memory profiling\n\n[`heaptrack`](https://github.com/KDE/heaptrack) is quite useful for diagnosing\nmemory leaks. To install it:\n\n```sh\n$ sudo apt install heaptrack heaptrack-gui\n$ cargo install cargo-heaptrack\n```\n\nThen, to build and run MonyaCode with the profiler attached:\n\n```sh\n$ cargo heaptrack -b monyacode\n```\n\nWhen this editor instance is exited, terminal output will include a command to\nrun `heaptrack_interpret` to convert the `*.raw.zst` profile to a `*.zst` file\nwhich can be passed to `heaptrack_gui` for viewing.\n\n## Perf recording\n\nThis section describes how to generate a flamegraph with resolved symbols from a\nrunning editor instance. This can be used to debug when the editor is using a\nlot of CPU. It's not useful for debugging hangs or memory usage.\n\n### During the incident\n\n- Find the PID (process ID) using:\n  `ps -eo size,pid,comm | grep monyacode | sort | head -n 1 | cut -d ' ' -f 2` Or\n  find the pid of the command monyacode-editor with the most ram usage in something\n  like htop/btop/top.\n\n- Install perf: On Ubuntu (derivatives) run `sudo apt install linux-tools`.\n\n- Perf Record: run `sudo perf record -p <pid you just found>`, wait a few\n  seconds to gather data then press Ctrl+C. You should now have a perf.data file\n\n- Make the output file user owned: run `sudo chown $USER:$USER perf.data`\n\n- Get build info: Run monyacode again and type `monyacode: about` in the command pallet to\n  get the exact commit.\n\n### Later\n\n- Build MonyaCode with symbols:\n  - Check out the commit found previously and modify `Cargo.toml`.\n  - Apply the following diff, then make a release build.\n\n    ```diff\n    [profile.release]\n    -debug = \"limited\"\n    +debug = \"full\"\n    ```\n\n- Add the symbols to perf database:\n  `pref buildid-cache -v -a <path to release monyacode binary>`\n\n- Resolve the symbols from the db:\n  `perf inject -i perf.data -o perf_with_symbols.data`\n\n- Install flamegraph: `cargo install cargo-flamegraph`\n\n- Render the flamegraph: `flamegraph --perfdata perf_with_symbols.data`\n\n## Troubleshooting\n\n### Cargo errors claiming that a dependency is using unstable features\n\nTry `cargo clean` and `cargo build`.\n"},{"path":"development/macos.md","title":"Building MonyaCode for macOS","group":"Development","content":"# Building MonyaCode for macOS\n\n## Repository\n\nClone down the [MonyaCode repository](https://github.com/monyacode/monyacode).\n\n```zsh\n# Your apple ID (email)\nexport APPLE_ID=\"\"\n# App-specific password (create in account.apple.com)\nexport APPLE_PASSWORD_MONYACODE=\"\"\n# Apple Team ID (find it in XCode)\nexport APPLE_TEAM_ID=\"\"\n# Apple signing key: security find-identity -p codesigning\nexport APPLE_SIGNING_KEY=\"\"\n# Build, sign and notarise the app bundle\n./script/bundle-mac\n```\n\n## Dependencies\n\n- Install [rustup](https://www.rust-lang.org/tools/install)\n\n- Install [Xcode](https://apps.apple.com/us/app/xcode/id497799835?mt=12) from\n  the macOS App Store, or from the\n  [Apple Developer](https://developer.apple.com/download/all/) website. Note\n  this requires a developer account.\n\n> Ensure you launch Xcode after installing, and install the macOS components,\n> which is the default option.\n\n- Install\n  [Xcode command line tools](https://developer.apple.com/xcode/resources/)\n\n  ```zsh\n  xcode-select --install\n  ```\n\n- Ensure that the Xcode command line tools are using your newly installed copy\n  of Xcode:\n\n  ```zsh\n  sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer\n  sudo xcodebuild -license accept\n  ```\n\n- Install `cmake` (required by\n  [a dependency](https://docs.rs/wasmtime-c-api-impl/latest/wasmtime_c_api/))\n\n  ```zsh\n  brew install cmake\n  ```\n\n## Building MonyaCode from Source\n\nOnce you have the dependencies installed, you can build MonyaCode using\n[Cargo](https://doc.rust-lang.org/cargo/).\n\nFor a debug build:\n\n```zsh\ncargo run\n```\n\nFor a release build:\n\n```zsh\ncargo run --release\n```\n\nFor an explicit Apple Silicon build, use the `aarch64-apple-darwin` target:\n\n```zsh\nrustup target add aarch64-apple-darwin\ncargo build --release --package monyacode --package cli --target aarch64-apple-darwin\n```\n\nThe quick helper builds the same target:\n\n```zsh\n./script/build-macos-arm\n```\n\nTo create a local Apple Silicon DMG:\n\n```zsh\n./script/build-macos-arm --dmg\n```\n\nThe DMG is written to `target/aarch64-apple-darwin/release/MonyaCode-aarch64.dmg`.\n\nQuick helpers are available for every release target:\n\n| Target | Rust target | Helper |\n| --- | --- | --- |\n| macOS Apple Silicon | `aarch64-apple-darwin` | `./script/build-macos-arm` |\n| macOS Intel | `x86_64-apple-darwin` | `./script/build-macos-intel` |\n| Linux amd64 | `x86_64-unknown-linux-gnu` | `./script/build-linux-amd64` |\n| Linux arm64 | `aarch64-unknown-linux-gnu` | `./script/build-linux-arm64` |\n| Windows x86_64 | `x86_64-pc-windows-msvc` | `./script/build-windows-x86_64` |\n\nEach helper builds the MonyaCode and CLI release binaries. The two macOS helpers\nalso accept `--dmg` to create a local DMG. Linux and Windows cross-builds need\ntheir platform linker and SDK installed, so GitHub Actions remains the reliable\nway to produce distributable packages for those targets.\n\nAnd to run the tests:\n\n```zsh\ncargo test --workspace\n```\n\n## Troubleshooting\n\n### Error compiling metal shaders\n\n```zsh\nerror: failed to run custom build command for gpui v0.1.0 (/Users/path/to/monyacode)`**\nxcrun: error: unable to find utility \"metal\", not a developer tool or in PATH\n```\n\nTry `sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer`\n\nIf you're on macOS 26, try `xcodebuild -downloadComponent MetalToolchain`\n\n### Cargo errors claiming that a dependency is using unstable features\n\nTry `cargo clean` and `cargo build`.\n\n### Error: 'dispatch/dispatch.h' file not found\n\nIf you encounter an error similar to:\n\n```zsh\nsrc/platform/mac/dispatch.h:1:10: fatal error: 'dispatch/dispatch.h' file not found\n\nCaused by:\n  process didn't exit successfully\n\n  --- stdout\n  cargo:rustc-link-lib=framework=System\n  cargo:rerun-if-changed=src/platform/mac/dispatch.h\n  cargo:rerun-if-env-changed=TARGET\n  cargo:rerun-if-env-changed=BINDGEN_EXTRA_CLANG_ARGS_aarch64-apple-darwin\n  cargo:rerun-if-env-changed=BINDGEN_EXTRA_CLANG_ARGS_aarch64_apple_darwin\n  cargo:rerun-if-env-changed=BINDGEN_EXTRA_CLANG_ARGS\n```\n\nThis file is part of Xcode. Ensure you have installed the Xcode command line\ntools and set the correct path:\n\n```zsh\nxcode-select --install\nsudo xcode-select --switch /Applications/Xcode.app/Contents/Developer\n```\n\nAdditionally, set the `BINDGEN_EXTRA_CLANG_ARGS` environment variable:\n\n```zsh\nexport BINDGEN_EXTRA_CLANG_ARGS=\"--sysroot=$(xcrun --show-sdk-path)\"\n```\n\nThen clean and rebuild the project:\n\n```zsh\ncargo clean\ncargo run\n```\n\n### Tests failing due to `Too many open files (os error 24)`\n\nThis error seems to be caused by OS resource constraints. Installing and running\ntests with `cargo-nextest` should resolve the issue.\n\n- `cargo install cargo-nextest --locked`\n- `cargo nextest run --workspace --no-fail-fast`\n\n## Tips & Tricks\n\n### Avoiding continual rebuilds\n\nIf you are finding that MonyaCode is continually rebuilding root crates, it may be\nbecause you are pointing your development MonyaCode at the codebase itself.\n\nThis causes problems because `cargo run` exports a bunch of environment\nvariables which are picked up by the `rust-analyzer` that runs in the\ndevelopment build of MonyaCode. These environment variables are in turn passed to\n`cargo check`, which invalidates the build cache of some of the crates we depend\non.\n\nYou can easily avoid running the built binary on the checked-out MonyaCode codebase\nusing `cargo run ~/path/to/other/project` to ensure that you don't hit this.\n\n### Speeding up verification\n\nIf you are building MonyaCode a lot, you may find that macOS continually verifies new\nbuilds which can add a few seconds to your iteration cycles.\n\nTo fix this, you can:\n\n- Run `sudo spctl developer-mode enable-terminal` to enable the Developer Tools\n  panel in System Settings.\n- In System Settings, search for \"Developer Tools\" and add your terminal (e.g.\n  iTerm or Ghostty) to the list under \"Allow applications to use developer\n  tools\"\n- Restart your terminal.\n\nThanks to the nextest developers for publishing\n[this](https://nexte.st/docs/installation/macos/#gatekeeper).\n"},{"path":"development/ownership-and-data-flow.md","title":"GPUI: Ownership and data flow","group":"Development","content":"# GPUI: Ownership and data flow\n\nIn GPUI, every model or view in the application is actually owned by a single top-level object called the `App`. When a new entity or view is created (referred to collectively as _entities_), the application is given ownership of their state to enable their participation in a variety of app services and interaction with other entities.\n\nTo illustrate, consider the trivial app below. We start the app by calling `run` with a callback, which is passed a reference to the `App` that owns all the state for the application. This `App` is our gateway to all application-level services, such as opening windows, presenting dialogs, etc. It also has an `insert_entity` method, which is called below to create an entity and give ownership of it to the application.\n\n```no_run\n# use gpui::{App, AppContext, Application, Entity};\n# struct Counter {\n#     count: usize,\n# }\nApplication::new().run(|cx: &mut App| {\n    let _counter: Entity<Counter> = cx.new(|_cx| Counter { count: 0 });\n    // ...\n});\n```\n\nThe call to `new_entity` returns an _entity handle_, which carries a type parameter based on the type of object it references. By itself, this `Entity<Counter>` handle doesn't provide access to the entity's state. It's merely an inert identifier plus a compile-time type tag, and it maintains a reference counted pointer to the underlying `Counter` object that is owned by the app.\n\nMuch like an `Rc` from the Rust standard library, this reference count is incremented when the handle is cloned and decremented when it is dropped to enable shared ownership over the underlying model, but unlike an `Rc` it only provides access to the model's state when a reference to an `App` is available. The handle doesn't truly _own_ the state, but it can be used to access the state from its true owner, the `App`. Stripping away some of the setup code for brevity:\n\n```no_run\n# use gpui::{App, AppContext, Application, Context, Entity};\n# struct Counter {\n#     count: usize,\n# }\nApplication::new().run(|cx: &mut App| {\n    let counter: Entity<Counter> = cx.new(|_cx| Counter { count: 0 });\n    // Call `update` to access the model's state.\n    counter.update(cx, |counter: &mut Counter, _cx: &mut Context<Counter>| {\n        counter.count += 1;\n    });\n});\n```\n\nTo update the counter, we call `update` on the handle, passing the context reference and a callback. The callback is yielded a mutable reference to the counter, which can be used to manipulate state.\n\nThe callback is also provided a second `Context<Counter>` reference. This reference is similar to the `App` reference provided to the `run` callback. A `Context` is actually a wrapper around the `App`, including some additional data to indicate which particular entity it is tied to; in this case the counter.\n\nIn addition to the application-level services provided by `App`, a `Context` provides access to entity-level services. For example, it can be used it to inform observers of this entity that its state has changed. Let's add that to our example, by calling `cx.notify()`.\n\n```no_run\n# use gpui::{App, AppContext, Application, Entity};\n# struct Counter {\n#     count: usize,\n# }\nApplication::new().run(|cx: &mut App| {\n    let counter: Entity<Counter> = cx.new(|_cx| Counter { count: 0 });\n    counter.update(cx, |counter, cx| {\n        counter.count += 1;\n        cx.notify(); // Notify observers\n    });\n});\n```\n\nNext, these notifications need to be observed and reacted to. Before updating the counter, we'll construct a second counter that observes it. Whenever the first counter changes, twice its count is assigned to the second counter. Note how `observe` is called on the `Context` belonging to our second counter to arrange for it to be notified whenever the first counter notifies. The call to `observe` returns a `Subscription`, which is `detach`ed to preserve this behavior for as long as both counters exist. We could also store this subscription and drop it at a time of our choosing to cancel this behavior.\n\nThe `observe` callback is passed a mutable reference to the observer and a _handle_ to the observed counter, whose state we access with the `read` method.\n\n```no_run\n # use gpui::{App, AppContext, Application, Entity, prelude::*};\n # struct Counter {\n #     count: usize,\n # }\n Application::new().run(|cx: &mut App| {\n     let first_counter: Entity<Counter> = cx.new(|_cx| Counter { count: 0 });\n\n     let second_counter = cx.new(|cx: &mut Context<Counter>| {\n         // Note we can set up the callback before the Counter is even created!\n         cx.observe(\n             &first_counter,\n             |second: &mut Counter, first: Entity<Counter>, cx| {\n                 second.count = first.read(cx).count * 2;\n             },\n         )\n         .detach();\n\n         Counter { count: 0 }\n     });\n\n     first_counter.update(cx, |counter, cx| {\n         counter.count += 1;\n         cx.notify();\n     });\n\n     assert_eq!(second_counter.read(cx).count, 2);\n });\n```\n\nAfter updating the first counter, it can be noted that the observing counter's state is maintained according to our subscription.\n\nIn addition to `observe` and `notify`, which indicate that an entity's state has changed, GPUI also offers `subscribe` and `emit`, which enables entities to emit typed events. To opt into this system, the emitting object must implement the `EventEmitter` trait.\n\nLet's introduce a new event type called `CounterChangeEvent`, then indicate that `Counter` can emit this type of event:\n\n```no_run\nuse gpui::EventEmitter;\n# struct Counter {\n#     count: usize,\n# }\nstruct CounterChangeEvent {\n    increment: usize,\n}\n\nimpl EventEmitter<CounterChangeEvent> for Counter {}\n```\n\nNext, the example should be updated, replacing the observation with a subscription. Whenever the counter is incremented, a `Change` event is emitted to indicate the magnitude of the increase.\n\n```no_run\n# use gpui::{App, AppContext, Application, Context, Entity, EventEmitter};\n# struct Counter {\n#     count: usize,\n# }\n# struct CounterChangeEvent {\n#     increment: usize,\n# }\n# impl EventEmitter<CounterChangeEvent> for Counter {}\nApplication::new().run(|cx: &mut App| {\n    let first_counter: Entity<Counter> = cx.new(|_cx| Counter { count: 0 });\n\n    let second_counter = cx.new(|cx: &mut Context<Counter>| {\n        // Note we can set up the callback before the Counter is even created!\n        cx.subscribe(&first_counter, |second: &mut Counter, _first: Entity<Counter>, event, _cx| {\n            second.count += event.increment * 2;\n        })\n        .detach();\n\n        Counter {\n            count: first_counter.read(cx).count * 2,\n        }\n    });\n\n    first_counter.update(cx, |first, cx| {\n        first.count += 2;\n        cx.emit(CounterChangeEvent { increment: 2 });\n        cx.notify();\n    });\n\n    assert_eq!(second_counter.read(cx).count, 4);\n});\n```\n"},{"path":"development/windows.md","title":"Building MonyaCode for Windows","group":"Development","content":"# Building MonyaCode for Windows\n\n> The following commands may be executed in any shell.\n\n## Repository\n\nClone down the MonyaCode [repository](https://github.com/monyacode/monyacode).\n\n> [!NOTE]\n>\n> No idea if the Windows build still works, or what is required to get it\n> working. Windows builds are also signed, so you will need a certificate.\n\nMaybe something like this?\n\n```powershell\n.\\script\\bundle-windows.ps1\n```\n\n## Dependencies\n\n- Install [rustup](https://www.rust-lang.org/tools/install)\n\n- Install either [Visual Studio](https://visualstudio.microsoft.com/downloads)\n  with the optional components `MSVC v*** - VS YYYY C++ x64/x86 build tools` and\n  `MSVC v*** - VS YYYY C++ x64/x86 Spectre-mitigated libs (latest)` (`v***` is\n  your VS version and `YYYY` is year when your VS was released. Pay attention to\n  the architecture and change it to yours if needed.)\n- Or, if you prefer to have a slimmer installer of only the MSVC compiler tools,\n  you can install the\n  [build tools](https://visualstudio.microsoft.com/visual-cpp-build-tools)\n  (+libs as above) and the \"Desktop development with C++\" workload. But beware\n  this installation is not automatically picked up by rustup. You must\n  initialize your environment variables by first launching the \"developer\" shell\n  (cmd/powershell) this installation places in the start menu or in Windows\n  Terminal and then compile.\n- Install Windows 11 or 10 SDK depending on your system, but ensure that at\n  least `Windows 10 SDK version 2104 (10.0.20348.0)` is installed on your\n  machine. You can download it from the\n  [Windows SDK Archive](https://developer.microsoft.com/windows/downloads/windows-sdk)\n- Install [CMake](https://cmake.org/download) (required by\n  [a dependency](https://docs.rs/wasmtime-c-api-impl/latest/wasmtime_c_api)). Or\n  you can install it through Visual Studio Installer, then manually add the\n  `bin` directory to your `PATH`, for example:\n  `C:\\Program Files\\Microsoft Visual Studio\\2022\\Community\\Common7\\IDE\\CommonExtensions\\Microsoft\\CMake\\CMake\\bin`.\n\nIf you can't compile MonyaCode, make sure that you have at least the following\ncomponents installed in case of a Visual Studio installation:\n\n```json\n{\n  \"version\": \"1.0\",\n  \"components\": [\n    \"Microsoft.VisualStudio.Component.CoreEditor\",\n    \"Microsoft.VisualStudio.Workload.CoreEditor\",\n    \"Microsoft.VisualStudio.Component.VC.Tools.x86.x64\",\n    \"Microsoft.VisualStudio.ComponentGroup.WebToolsExtensions.CMake\",\n    \"Microsoft.VisualStudio.Component.VC.CMake.Project\",\n    \"Microsoft.VisualStudio.Component.Windows11SDK.26100\",\n    \"Microsoft.VisualStudio.Component.VC.Runtimes.x86.x64.Spectre\",\n  ],\n  \"extensions\": [],\n}\n```\n\nOr if in case of just Build Tools, the following components:\n\n```json\n{\n  \"version\": \"1.0\",\n  \"components\": [\n    \"Microsoft.VisualStudio.Component.Roslyn.Compiler\",\n    \"Microsoft.Component.MSBuild\",\n    \"Microsoft.VisualStudio.Component.CoreBuildTools\",\n    \"Microsoft.VisualStudio.Workload.MSBuildTools\",\n    \"Microsoft.VisualStudio.Component.Windows10SDK\",\n    \"Microsoft.VisualStudio.Component.VC.CoreBuildTools\",\n    \"Microsoft.VisualStudio.Component.VC.Tools.x86.x64\",\n    \"Microsoft.VisualStudio.Component.VC.Redist.14.Latest\",\n    \"Microsoft.VisualStudio.Component.Windows11SDK.26100\",\n    \"Microsoft.VisualStudio.Component.VC.CMake.Project\",\n    \"Microsoft.VisualStudio.Component.TextTemplating\",\n    \"Microsoft.VisualStudio.Component.VC.CoreIde\",\n    \"Microsoft.VisualStudio.ComponentGroup.NativeDesktop.Core\",\n    \"Microsoft.VisualStudio.Workload.VCTools\",\n    \"Microsoft.VisualStudio.Component.VC.Runtimes.x86.x64.Spectre\",\n  ],\n  \"extensions\": [],\n}\n```\n\nThe list can be obtained as follows:\n\n- Open the Visual Studio Installer\n- Click on `More` in the `Installed` tab\n- Click on `Export configuration`\n\n### Notes\n\nYou should modify the `pg_hba.conf` file in the `data` directory to use `trust`\ninstead of `scram-sha-256` for the `host` method. Otherwise, the connection will\nfail with the error `password authentication failed`. The `pg_hba.conf` file\ntypically locates at `C:\\Program Files\\PostgreSQL\\17\\data\\pg_hba.conf`. After\nthe modification, the file should look like this:\n\n```ini\n# IPv4 local connections:\nhost    all             all             127.0.0.1/32            trust\n# IPv6 local connections:\nhost    all             all             ::1/128                 trust\n```\n\nAlso, if you are using a non-latin Windows version, you must modify\nthe`lc_messages` parameter in the `postgresql.conf` file in the `data` directory\nto `English_United States.1252` (or whatever UTF8-compatible encoding you have).\nOtherwise, the database will panic. The `postgresql.conf` file should look like\nthis:\n\n```ini\n# lc_messages = 'Chinese (Simplified)_China.936' # locale for system error message strings\nlc_messages = 'English_United States.1252'\n```\n\nAfter this, you should restart the `postgresql` service. Press the `win` key +\n`R` to launch the `Run` window. Type the `services.msc` and hit the `OK` button\nto open the Services Manager. Then, find the `postgresql-x64-XX` service,\nright-click on it, and select `Restart`.\n\n## Building from source\n\nOnce you have the dependencies installed, you can build MonyaCode using\n[Cargo](https://doc.rust-lang.org/cargo).\n\nFor a debug build:\n\n```powershell\ncargo run\n```\n\nFor a release build:\n\n```powershell\ncargo run --release\n```\n\nAnd to run the tests:\n\n```powershell\ncargo test --workspace\n```\n\n## Installing from msys2\n\nMonyaCode does not support unofficial MSYS2 MonyaCode packages built for Mingw-w64, and\ninstead uses [mingw-w64-zed](https://packages.msys2.org/base/mingw-w64-zed).\n\nPlease refer to\n[MSYS2 documentation](https://www.msys2.org/docs/ides-editors/#zed) first.\n\n## Troubleshooting\n\n### Setting `RUSTFLAGS` env var breaks builds\n\nIf you set the `RUSTFLAGS` env var, it will override the `rustflags` settings in\n`.cargo/config.toml` which is required to properly build MonyaCode.\n\nSince these settings can vary from time to time, the build errors you receive\nmay vary from linker errors, to other stranger errors.\n\nIf you'd like to add extra rust flags, you may do 1 of the following in\n`.cargo/config.toml`:\n\nAdd your flags in the build section\n\n```toml\n[build]\nrustflags = [\"-C\", \"symbol-mangling-version=v0\", \"--cfg\", \"tokio_unstable\"]\n```\n\nAdd your flags in the windows target section\n\n```toml\n[target.'cfg(target_os = \"windows\")']\nrustflags = [\n    \"--cfg\",\n    \"windows_slim_errors\",\n    \"-C\",\n    \"target-feature=+crt-static\",\n]\n```\n\nOr, you can create a new `.cargo/config.toml` in the same folder as the MonyaCode\nrepo (see below). This is particularly useful if you are doing CI builds since\nyou don't have to edit the original `.cargo/config.toml`.\n\n```tree\nupper_dir\n├── .cargo          // <-- Make this folder\n│   └── config.toml // <-- Make this file\n└── monyacode\n    ├── .cargo\n    │   └── config.toml\n    └── crates\n        └── ...\n```\n\nIn the new (above) `.cargo/config.toml`, if we wanted to add `--cfg gles` to our\nrustflags, it would look like this\n\n```toml\n[target.'cfg(all())']\nrustflags = [\"--cfg\", \"gles\"]\n```\n\n### Cargo errors claiming that a dependency is using unstable features\n\nTry `cargo clean` and `cargo build`.\n\n### `STATUS_ACCESS_VIOLATION`\n\nThis error can happen if you are using the \"rust-lld.exe\" linker. Consider\ntrying a different linker.\n\nIf you are using a global config, consider moving the MonyaCode repository to a\nnested directory and add a `.cargo/config.toml` with a custom linker config in\nthe parent directory.\n\nSee this issue for more information\n[#12041](https://github.com/zed-industries/zed/issues/12041)\n\n### Invalid RC path selected\n\nSometimes, depending on the security rules applied to your laptop, you may get\nthe following error while compiling MonyaCode:\n\n```powershell\nerror: failed to run custom build command for `monyacode(C:\\Users\\USER\\src\\monyacode\\crates\\monyacode)`\n\nCaused by:\n  process didn't exit successfully: `C:\\Users\\USER\\src\\monyacode\\target\\debug\\build\\monyacode-b24f1e9300107efc\\build-script-build` (exit code: 1)\n  --- stdout\n  cargo:rerun-if-changed=../../.git/logs/HEAD\n  cargo:rustc-env=MONYACODE_COMMIT_SHA=25e2e9c6727ba9b77415588cfa11fd969612adb7\n  cargo:rustc-link-arg=/stack:8388608\n  cargo:rerun-if-changed=resources/windows/app-icon.ico\n  package.metadata.winresource does not exist\n  Selected RC path: 'bin\\x64\\rc.exe'\n\n  --- stderr\n  The system cannot find the path specified. (os error 3)\nwarning: build failed, waiting for other jobs to finish...\n```\n\nIn order to fix this issue, you can manually set the `MONYACODE_RC_TOOLKIT_PATH`\nenvironment variable to the RC toolkit path. Usually, you can set it to:\n`C:\\Program Files (x86)\\Windows Kits\\10\\bin\\<SDK_version>\\x64`.\n\nSee this [issue](https://github.com/zed-industries/zed/issues/18393) for more\ninformation.\n\n### Graphics issues\n\n#### MonyaCode fails to launch\n\nCurrently, MonyaCode uses Vulkan as its graphics API on Windows. However, Vulkan\nisn't always the most reliable on Windows, so if MonyaCode fails to launch, it's\nlikely a Vulkan-related issue.\n\nYou can check the MonyaCode log at: `C:\\Users\\YOU\\AppData\\Local\\MonyaCode\\logs\\MonyaCode.log`\n\nIf you see messages like:\n\n- `MonyaCode failed to open a window: NoSupportedDeviceFound`\n- `ERROR_INITIALIZATION_FAILED`\n- `GPU Crashed`\n- `ERROR_SURFACE_LOST_KHR`\n\nThen Vulkan might not be working properly on your system. In most cases,\nupdating your GPU drivers may help resolve this.\n\nIf there's nothing Vulkan-related in the logs and you happen to have Bandicam\ninstalled, try uninstalling it. MonyaCode is currently not compatible with Bandicam.\n"},{"path":"development.md","title":"Developing MonyaCode","group":"Guides","content":"# Developing MonyaCode\n\nSee the platform-specific instructions for building MonyaCode from source:\n\n- [macOS](./development/macos.md)\n- [Linux](./development/linux.md)\n- [Windows](./development/windows.md)\n\n## System Requirements\n\nRust is notoriously memory hungry, and MonyaCode is a huge Rust project. The minimum\nrequirement for compiling the project seems to be somewhere around 16GB of RAM,\nand at least 8GB available for compilation.\n\nLinking is the part of the process which takes the most RAM, and alternative\nlinkers may offer better performance and lower RAM usage. Check the platform\nspecific instructions for more details on compiling with an alternative linker\nlike `wild` or `mold`.\n\nIf you have a lot of CPU cores, Rust will try to use all of them and may require\neven more RAM as a consequence. You can limit the level of concurrency by\nsetting the `CARGO_BUILD_JOBS` environment variable. For example, if you have 16\nCPU cores available, Cargo will run up to 16 jobs in parallel. By setting\n`CARGO_BUILD_JOBS=8` you can reduce RAM usage by only running up to 8 jobs in\nparallel.\n\nTo build with less than 12GB of RAM, try setting these environment variables:\n\n```sh\nMAKEOPTS=\"-j2 -l2\"\nCARGO_BUILD_JOBS=1\n```\n\nIt's also possible that using an alternative linker like `wild` or `mold` may\nuse less memory.\n\n## Performance Measurements\n\nMonyaCode includes a frame time measurement system that can be used to profile how\nlong it takes to render each frame. This is particularly useful when comparing\nrendering performance between different versions or when optimizing frame\nrendering code.\n\n### Using MONYACODE_MEASUREMENTS\n\nTo enable performance measurements, set the `MONYACODE_MEASUREMENTS` environment\nvariable:\n\n```sh\nexport MONYACODE_MEASUREMENTS=1\n```\n\nWhen enabled, MonyaCode will print frame rendering timing information to stderr,\nshowing how long each frame takes to render.\n\n### Performance Comparison Workflow\n\nHere's a typical workflow for comparing frame rendering performance between\ndifferent versions:\n\n1. **Enable measurements:**\n\n   ```sh\n   export MONYACODE_MEASUREMENTS=1\n   ```\n\n2. **Test the first version:**\n\n   - Checkout the commit you want to measure\n   - Run MonyaCode in release mode and use it for 5-10 seconds:\n     `cargo run --release &> version-a`\n\n3. **Test the second version:**\n\n   - Checkout another commit you want to compare\n   - Run MonyaCode in release mode and use it for 5-10 seconds:\n     `cargo run --release &> version-b`\n\n4. **Generate comparison:**\n\n   ```sh\n   script/histogram version-a version-b\n   ```\n\nThe `script/histogram` tool can accept as many measurement files as you like and\nwill generate a histogram visualization comparing the frame rendering\nperformance data between the provided versions.\n\n### Using `util_macros::perf`\n\nFor benchmarking unit tests, annotate them with the `#[perf]` attribute from the\n`util_macros` crate. Then run `cargo perf-test -p $CRATE` to benchmark them. See\nthe rustdoc documentation on `crates/util_macros` and `tooling/perf` for\nin-depth examples and explanations.\n"},{"path":"diagnostics.md","title":"Diagnostics","group":"Guides","content":"# Diagnostics\n\nMonyaCode gets its diagnostics from the language servers and supports both push and\npull variants of the LSP which should make it compatible with all existing\nlanguage servers.\n\n# Regular diagnostics\n\nBy default, all diagnostics are displayed as underlined text in the editor and\nthe scrollbar.\n\nEditor diagnostics can be filtered using the editor setting:\n\n```json\n\"diagnostics_max_severity\": null\n```\n\nPossible values: `\"off\"`, `\"error\"`, `\"warning\"`, `\"info\"`, `\"hint\"`, `null`\n(default, all diagnostics)\n\nThe scrollbar diagnostics are configured using\n\n```json\n\"scrollbar\": {\n  \"diagnostics\": \"all\",\n}\n```\n\nPossible values: `\"none\"`, `\"error\"`, `\"warning\"`, `\"information\"`, `\"all\"`\n(default)\n\nThe diagnostics can be hovered to display a tooltip with full, rendered\ndiagnostic message. Or, `editor::GoToDiagnostic` and\n`editor::GoToPreviousDiagnostic` could be used to navigate between diagnostics\nin the editor, showing a popover for the currently active diagnostic.\n\n# Inline diagnostics (Error lens)\n\nIt's possible to display diagnostics as a lens to the right of the code. This is\ndisabled by default, but can either be temporarily turned on (or off) using the\neditor menu, or permanently, using the\n\n```json\n\"diagnostics\": {\n  \"inline\": {\n    \"enabled\": true,\n    \"max_severity\": null, // same values as the `diagnostics_max_severity` from the editor settings\n  }\n}\n```\n\n# Other UI places\n\n## Project Panel\n\nThe project panel can have its entries coloured based on the severity of the\ndiagnostics in the file:\n\n```json\n\"project_panel\": {\n  \"show_diagnostics\": \"all\",\n}\n```\n\nPossible values: `\"off\"`, `\"errors\"`, `\"all\"` (default)\n\n## Editor tabs\n\nEditor tabs can be coloured in the same way as project panel entries:\n\n```json\n\"tabs\": {\n  \"show_diagnostics\": \"off\",\n}\n```\n\nPossible values: `\"off\"` (default), `\"errors\"`, `\"all\"`\n"},{"path":"environment.md","title":"Environment Variables","group":"Guides","content":"# Environment Variables\n\nMultiple features in MonyaCode are affected by environment variables:\n\n- Tasks\n- Built-in terminal\n- Look-up of language servers\n- Language servers\n\nIn order to make the best use of these features, it's helpful to understand\nwhere MonyaCode gets its environment variables from and how they're used.\n\n## Where does MonyaCode get its environment variables from?\n\nHow MonyaCode was started — whether it's icon was clicked in the macOS Dock or in a\nLinux window manager, or whether it was started via the CLI `monyacode` that comes\nwith MonyaCode — influences which environment variables MonyaCode can use.\n\n### Launched from the CLI\n\nIf MonyaCode is opened via the CLI (`monyacode`), it will inherit the environment\nvariables from the surrounding shell session.\n\nThat means if you do\n\n```\n$ export MY_ENV_VAR=hello\n$ zed .\n```\n\nthe environment variable `MY_ENV_VAR` is now available inside MonyaCode. For example,\nin the built-in terminal.\n\nThe CLI `monyacode` will _always_ pass along its environment to MonyaCode, regardless of\nwhether a MonyaCode instance was previously running or not.\n\n### Launched via window manager, Dock, or launcher\n\nWhen MonyaCode has been launched via the macOS Dock, or a GNOME or KDE icon on Linux,\nor an application launcher like Alfred or Raycast, it has no surrounding shell\nenvironment from which to inherit its environment variables.\n\nIn order to still have a useful environment, MonyaCode spawns a login shell in the\nuser's home directory and gets its environment. This environment is then set on\nthe MonyaCode _process_. That means all MonyaCode windows and projects will inherit that\nhome directory environment.\n\nSince that can lead to problems for users that require different environment\nvariables for a project (because they use `direnv`, or `asdf`, or `mise`, ... in\nthat project), when opening project, MonyaCode spawns another login shell. This time\nin the project's directory. The environment from that login shell is _not_ set\non the process (because that would mean opening a new project changes the\nenvironment for all MonyaCode windows). Instead, the environment is stored and passed\nalong when running tasks, opening terminals, or spawning language servers.\n\n## Where and how are environment variables used?\n\nThere are two sets of environment variables:\n\n1. Environment variables of the MonyaCode process\n2. Environment variables stored per project\n\nThe variables from (1) are always used, since they are stored on the process\nitself and every spawned process (tasks, terminals, language servers, ...) will\ninherit them by default.\n\nThe variables from (2) are used explicitly, depending on the feature.\n\n### Tasks\n\nTasks are spawned with a combined environment. In order of precedence (low to\nhigh, with the last overwriting the first):\n\n- the MonyaCode process environment\n- if the project was opened from the CLI: the CLI environment\n- if the project was not opened from the CLI: the project environment variables\n  obtained by running a login shell in the project's root folder\n- optional, explicitly configured environment in settings\n\n### Built-in terminal\n\nBuilt-in terminals, like tasks, are spawned with an combined environment. In\norder of precedence (low to high):\n\n- the MonyaCode process environment\n- if the project was opened from the CLI: the CLI environment\n- if the project was not opened from the CLI: the project environment variables\n  obtained by running a login shell in the project's root folder\n- optional, explicitly configured environment in settings\n\n### Look-up of language servers\n\nFor some languages the language server adapters lookup the binary in the user's\n`$PATH`. Examples:\n\n- Go\n- Zig\n- Rust (if [configured to do so](./languages/rust.md#binary))\n- C\n- TypeScript\n\nFor this look-up, MonyaCode uses the following the environment:\n\n- if the project was opened from the CLI: the CLI environment\n- if the project was not opened from the CLI: the project environment variables\n  obtained by running a login shell in the project's root folder\n\n### Language servers\n\nAfter looking up a language server, MonyaCode starts them.\n\nThese language server processes always inherit MonyaCode's process environment. But,\ndepending on the language server look-up, additional environment variables might\nbe set or overwrite the process environment.\n\n- If the language server was found in the project environment's `$PATH`, then\n  the project environment's is passed along to the language server process.\n  Where the project environment comes from depends on how the project was\n  opened, via CLI or not. See previous point on look-up of language servers.\n- If the language servers was not found in the project environment, MonyaCode tries\n  to install it globally and start it globally. In that case, the process will\n  inherit MonyaCode's process environment, and — if the project was opened via ClI —\n  from the CLI.\n"},{"path":"extensions/capabilities.md","title":"Extension Capabilities","group":"Extensions","content":"# Extension Capabilities\n\nThe operations that MonyaCode extensions are able to perform are governed by a\ncapability system.\n\n## Restricting capabilities\n\nAs a user, you have the option of restricting the capabilities that are granted\nto extensions.\n\nThis is controlled via the `granted_extension_capabilities` setting.\n\nRestricting or removing a capability will cause an error to be returned when an\nextension attempts to call the corresponding extension API without sufficient\ncapabilities.\n\nFor instance, if you wanted to restrict downloads to just files from GitHub, you\ncould modify `host` for the `download_file` capability:\n\n```diff\n{\n  \"granted_extension_capabilities\": [\n    { \"kind\": \"process:exec\", \"command\": \"*\", \"args\": [\"**\"] },\n-   { \"kind\": \"download_file\", \"host\": \"*\", \"path\": [\"**\"] },\n+   { \"kind\": \"download_file\", \"host\": \"github.com\", \"path\": [\"**\"] },\n    { \"kind\": \"npm:install\", \"package\": \"*\" }\n  ]\n}\n```\n\nIf you don't want extensions to be able to perform _any_ capabilities, you can\nremove all granted capabilities:\n\n```json\n{\n  \"granted_extension_capabilities\": []\n}\n```\n\n> Note that this will likely make many extensions non-functional, at least in\n> their default configuration.\n\n## Capabilities\n\n### `process:exec`\n\nThe `process:exec` capability grants extensions the ability to invoke commands\nusing\n[`zed_extension_api::process::Command`](https://docs.rs/zed_extension_api/latest/zed_extension_api/process/struct.Command.html).\n\n#### Examples\n\nTo allow any command to be executed with any arguments:\n\n```toml\n{ kind = \"process:exec\", command = \"*\", args = [\"**\"] }\n```\n\nTo allow a specific command (e.g., `gem`) to be executed with any arguments:\n\n```toml\n{ kind = \"process:exec\", command = \"gem\", args = [\"**\"] }\n```\n\n### `download_file`\n\nThe `download_file` capability grants extensions the ability to download files\nusing\n[`zed_extension_api::download_file`](https://docs.rs/zed_extension_api/latest/zed_extension_api/fn.download_file.html).\n\n#### Examples\n\nTo allow any file to be downloaded:\n\n```toml\n{ kind = \"download_file\", host = \"github.com\", path = [\"**\"] }\n```\n\nTo allow any file to be downloaded from `github.com`:\n\n```toml\n{ kind = \"download_file\", host = \"github.com\", path = [\"**\"] }\n```\n\nTo allow any file to be downloaded from a specific GitHub repository:\n\n```toml\n{ kind = \"download_file\", host = \"github.com\", path = [\"zed-industries\", \"zed\", \"**\"] }\n```\n\n### `npm:install`\n\nThe `npm:install` capability grants extensions the ability to install npm\npackages using\n[`zed_extension_api::npm_install_package`](https://docs.rs/zed_extension_api/latest/zed_extension_api/fn.npm_install_package.html).\n\n#### Examples\n\nTo allow any npm package to be installed:\n\n```toml\n{ kind = \"npm:install\", package = \"*\" }\n```\n\nTo allow a specific npm package (e.g., `typescript`) to be installed:\n\n```toml\n{ kind = \"npm:install\", package = \"typescript\" }\n```\n"},{"path":"extensions/debugger-extensions.md","title":"Debugger Extensions","group":"Extensions","content":"# Debugger Extensions\n\n[Debug Adapter Protocol](https://microsoft.github.io/debug-adapter-protocol)\nServers can be exposed as extensions for use in the [debugger](../debugger.md).\n\n## Defining Debugger Extensions\n\nA given extension may provide one or more DAP servers. Each DAP server must be\nregistered in the `extension.toml`:\n\n```toml\n[debug_adapters.my-debug-adapter]\n# Optional relative path to the JSON schema for the debug adapter configuration schema. Defaults to `debug_adapter_schemas/$DEBUG_ADAPTER_NAME_ID.json`.\n# Note that while this field is optional, a schema is mandatory.\nschema_path = \"relative/path/to/schema.json\"\n```\n\nThen, in the Rust code for your extension, implement the `get_dap_binary` method\non your extension:\n\n```rust\nimpl monyacode::Extension for MyExtension {\n    fn get_dap_binary(\n        &mut self,\n        adapter_name: String,\n        config: DebugTaskDefinition,\n        user_provided_debug_adapter_path: Option<String>,\n        worktree: &Worktree,\n    ) -> Result<DebugAdapterBinary, String>;\n}\n```\n\nThis method should return the command to start up a debug adapter protocol\nserver, along with any arguments or environment variables necessary for it to\nfunction.\n\nIf you need to download the DAP server from an external source—like GitHub\nReleases or npm—you can also do that in this function. Make sure to check for\nupdates only periodically, as this function is called whenever a user spawns a\nnew debug session with your debug adapter.\n\nYou must also implement `dap_request_kind`. This function is used to determine\nwhether a given debug scenario will _launch_ a new debuggee or _attach_ to an\nexisting one. We also use it to determine that a given debug scenario requires\nrunning a _locator_.\n\n```rust\nimpl monyacode::Extension for MyExtension {\n    fn dap_request_kind(\n        &mut self,\n        _adapter_name: String,\n        _config: Value,\n    ) -> Result<StartDebuggingRequestArgumentsRequest, String>;\n}\n```\n\nThese two functions are sufficient to expose your debug adapter in\n`debug.jsonc`-based user workflows, but you should strongly consider\nimplementing `dap_config_to_scenario` as well.\n\n```rust\nimpl monyacode::Extension for MyExtension {\n    fn dap_config_to_scenario(\n        &mut self,\n        _adapter_name: DebugConfig,\n    ) -> Result<DebugScenario, String>;\n}\n```\n\n`dap_config_to_scenario` is used when the user spawns a session via new process\nmodal UI. At a high level, it takes a generic debug configuration (that isn't\nspecific to any debug adapter) and tries to turn it into a concrete debug\nscenario for your adapter. Put another way, it is supposed to answer the\nquestion: \"Given a program, a list of arguments, current working directory and\nenvironment variables, what would the configuration for spawning this debug\nadapter look like?\".\n\n## Defining Debug Locators\n\nMonyaCode offers an automatic way to create debug scenarios with _debug locators_. A\nlocator locates the debug target and figures out how to spawn a debug session\nfor it. Thanks to locators, we can automatically convert existing user tasks\n(e.g. `cargo run`) and convert them into debug scenarios (e.g. `cargo build`\nfollowed by spawning a debugger with `target/debug/my_program` as the program to\ndebug).\n\n> Your extension can define its own debug locators even if it does not expose a\n> debug adapter. We strongly recommend doing so when your extension already\n> exposes language tasks, as it allows users to spawn a debug session without\n> having to manually configure the debug adapter.\n\nLocators can (but don't have to) be agnostic to the debug adapter they are used\nwith. They are simply responsible for locating the debug target and figuring out\nhow to spawn a debug session for it. This allows for a more flexible and\nextensible debugging experience.\n\nYour extension can define one or more debug locators. Each debug locator must be\nregistered in the `extension.toml`:\n\n```toml\n[debug_locators.my-debug-locator]\n```\n\nLocators have two components. First, each locator is ran on each available task\nto figure out if any of the available locators can provide a debug scenario for\na given task. This is done by calling `dap_locator_create_scenario`.\n\n```rust\nimpl monyacode::Extension for MyExtension {\n    fn dap_locator_create_scenario(\n        &mut self,\n        _locator_name: String,\n        _build_task: TaskTemplate,\n        _resolved_label: String,\n        _debug_adapter_name: String,\n    ) -> Option<DebugScenario>;\n}\n```\n\nThis function should return `Some` debug scenario when that scenario defines a\ndebugging counterpart to a given user task. Note that a `DebugScenario` can\ninclude a [build task](../debugger.md#build-tasks). If there is one, we will\nexecute `run_dap_locator` after a build task is finished successfully.\n\n```rust\nimpl monyacode::Extension for MyExtension {\n    fn run_dap_locator(\n        &mut self,\n        _locator_name: String,\n        _build_task: TaskTemplate,\n    ) -> Result<DebugRequest, String>;\n}\n```\n\n`run_dap_locator` is useful in case you cannot determine a build target\ndeterministically. Some build systems may produce artifacts whose names are not\nknown up-front. Note however that you do _not_ need to go through a 2-phase\nresolution; if you can determine the full debug configuration with just\n`dap_locator_create_scenario`, you can omit `build` property on a returned\n`DebugScenario`. Please also note that your locator **will be** called with\ntasks it's unlikely to accept; thus you should take some effort to return `None`\nearly before performing any expensive operations.\n\n## Testing\n\nTo test your new Debug Adapter Protocol server extension, you can install it.\nRead more in [Developing Extensions](../extensions/developing-extensions.md).\n"},{"path":"extensions/developing-extensions.md","title":"Developing Extensions","group":"Extensions","content":"# Developing Extensions\n\n## Extension Features\n\nExtensions are able to provide the following features to MonyaCode:\n\n- [Languages](../extensions/languages.md)\n- [Debuggers](../extensions/debugger-extensions.md)\n- [Themes](../extensions/themes.md)\n- [Icon Themes](../extensions/icon-themes.md)\n\n## Developing an Extension\n\nBefore starting to develop an extension for MonyaCode, be sure to\n[install Rust via rustup](https://www.rust-lang.org/tools/install).\n\nNote: If you have installed rustup via [Homebrew](https://brew.sh), you may need\nto run `rustup-init` for the command to work properly.\n\nWhen developing an extension, you can use it in MonyaCode by installing it:\n\nFrom the extensions page, click the `Install Local` button (or the {#action\nmonyacode::InstallExtensionFromFolder} action) and select the directory containing\nyour extension.\n\nIf you need to troubleshoot, you can check the MonyaCode.log ({#action\nmonyacode::OpenLog}) for additional output. For debug output, close and relaunch the\neditor with the flag `monyacode --foreground` from the command line which show more\nverbose INFO level logging.\n\n## Directory Structure of a MonyaCode Extension\n\nA MonyaCode extension is a Git repository that contains an `extension.toml`. This\nfile must contain some basic information about the extension:\n\n```toml\nid = \"my-extension\"\nname = \"My extension\"\nversion = \"0.0.1\"\nschema_version = 1\nauthors = [\"Your Name <you@example.com>\"]\ndescription = \"My cool extension\"\nrepository = \"https://github.com/your-name/my-monyacode-extension\"\n```\n\nIn addition to this, there are several other optional files and directories that\ncan be used to add functionality to a MonyaCode extension. An example directory\nstructure of an extension that provides all capabilities is as follows:\n\n```\nmy-extension/\n  extension.toml\n  Cargo.toml\n  src/\n    lib.rs\n  languages/\n    my-language/\n      config.toml\n      highlights.scm\n  themes/\n    my-theme.json\n```\n\n## WebAssembly\n\nProcedural parts of extensions are written in Rust and compiled to WebAssembly.\nTo develop an extension that includes custom code, include a `Cargo.toml` like\nthis:\n\n```toml\n[package]\nname = \"my-extension\"\nversion = \"0.0.1\"\nedition = \"2021\"\n\n[lib]\ncrate-type = [\"cdylib\"]\n\n[dependencies]\nzed_extension_api = \"0.1.0\"\n```\n\nUse the latest version of the\n[`zed_extension_api`](https://crates.io/crates/zed_extension_api) available on\ncrates.io. Make sure it's still compatible with MonyaCode versions you want to\nsupport.\n\nIn the `src/lib.rs` file in your Rust crate you will need to define a struct for\nyour extension and implement the `Extension` trait, as well as use the\n`register_extension!` macro to register your extension:\n\n```rs\nuse zed_extension_api as zed;\n\nstruct MyExtension {\n    // ... state\n}\n\nimpl monyacode::Extension for MyExtension {\n    // ...\n}\n\nmonyacode::register_extension!(MyExtension);\n```\n\n> `stdout`/`stderr` is forwarded directly to the MonyaCode process. In order to see\n> `println!`/`dbg!` output from your extension, you can start MonyaCode in your\n> terminal with a `--foreground` flag.\n\n## Extension License\n\nExtension repositories should include a license. The following licenses are\nrecommended:\n\n- [GNU GPLv3](https://www.gnu.org/licenses/gpl-3.0.en.html)\n- [Apache 2.0](https://www.apache.org/licenses/LICENSE-2.0)\n- [BSD 3-Clause](https://opensource.org/license/bsd-3-clause)\n- [MIT](https://opensource.org/license/mit)\n\nYour license file should be at the root of your extension repository. Any\nfilename that has `LICENCE` or `LICENSE` as a prefix (case insensitive).\n\n> Note: This license applies only to your extension code itself. It does not\n> apply to any tools your extension may download or interact with, such as\n> language servers or other external dependencies. If your repository contains\n> both extension code and other projects (like a language server), you don't\n> need to relicense those other projects.\n"},{"path":"extensions/icon-themes.md","title":"Icon Themes","group":"Extensions","content":"# Icon Themes\n\nExtensions may provide icon themes in order to change the icons MonyaCode uses for\nfolders and files.\n\n## Example extension\n\nThe [Material Icon Theme](https://github.com/zed-extensions/material-icon-theme)\nserves as an example for the structure of an extension containing an icon theme.\n\n## Directory structure\n\nThere are two important directories for an icon theme extension:\n\n- `icon_themes`: This directory will contain one or more JSON files containing\n  the icon theme definitions.\n- `icons`: This directory contains the icon assets that will be distributed with\n  the extension. You can created subdirectories in this directory, if so\n  desired.\n\nEach icon theme file should adhere to the JSON schema specified at\n[`https://zed.dev/schema/icon_themes/v0.3.0.json`](https://zed.dev/schema/icon_themes/v0.3.0.json).\n\nHere is an example of the structure of an icon theme:\n\n```json [icon-theme]\n{\n  \"$schema\": \"https://zed.dev/schema/icon_themes/v0.3.0.json\",\n  \"name\": \"My Icon Theme\",\n  \"author\": \"Your Name\",\n  \"themes\": [\n    {\n      \"name\": \"My Icon Theme\",\n      \"appearance\": \"dark\",\n      \"directory_icons\": {\n        \"collapsed\": \"./icons/folder.svg\",\n        \"expanded\": \"./icons/folder-open.svg\"\n      },\n      \"named_directory_icons\": {\n        \"stylesheets\": {\n          \"collapsed\": \"./icons/folder-stylesheets.svg\",\n          \"expanded\": \"./icons/folder-stylesheets-open.svg\"\n        }\n      },\n      \"chevron_icons\": {\n        \"collapsed\": \"./icons/chevron-right.svg\",\n        \"expanded\": \"./icons/chevron-down.svg\"\n      },\n      \"file_stems\": {\n        \"Makefile\": \"make\"\n      },\n      \"file_suffixes\": {\n        \"mp3\": \"audio\",\n        \"rs\": \"rust\"\n      },\n      \"file_icons\": {\n        \"audio\": { \"path\": \"./icons/audio.svg\" },\n        \"default\": { \"path\": \"./icons/file.svg\" },\n        \"make\": { \"path\": \"./icons/make.svg\" },\n        \"rust\": { \"path\": \"./icons/rust.svg\" }\n        // ...\n      }\n    }\n  ]\n}\n```\n\nEach icon path is resolved relative to the root of the extension directory.\n\nIn this example, the extension would have a structure like so:\n\n```\nextension.toml\nicon_themes/\n  my-icon-theme.json\nicons/\n  audio.svg\n  chevron-down.svg\n  chevron-right.svg\n  file.svg\n  folder-open.svg\n  folder.svg\n  rust.svg\n```\n"},{"path":"extensions/installing-extensions.md","title":"Installing Extensions","group":"Extensions","content":"# Installing Extensions\n\nSome extensions, particularly extensions that add language support, need to\ncompile the extension into a Wasm bundle. To do this, MonyaCode uses Rust together\nwith the `wasm32-wasip2` target to build the Wasm extension on your local\nmachine.\n\nYou can either install these dependencies yourself, or alternatively if you have\n[installed Rust via rustup](https://www.rust-lang.org/tools/install), MonyaCode will\ntry to add the `wasm32-wasip2` target for you.\n\nIf you have installed Rust via a package manager but don't have the\n`wasm32-wasip2` target installed, installing the extension will not work.\n\nThe editor will also need the WASI SDK in order to compile the extension and\ntree-sitter grammar for language extensions. If the system-installed version of\n`cargo` is able to compile to Wasm, the editor will only download the WASI\nsysroot instead.\n\n> **Note:** This is a core difference between Zed and MonyaCode. Zed will download a\n> precompiled Wasm bundle from the Zed website when installing an extension, to\n> avoid having to build the extension locally.\n\nYou can manage your extensions by launching the MonyaCode Extension Gallery.\n\nPress {#kb monyacode::Extensions} , open the command palette and select {#action\nmonyacode::Extensions} or select \"MonyaCode > Extensions\" from the menu bar.\n\nHere you can view the extensions that you currently have installed, and you can\nalso install extensions for your local file system, or via a git URL. To get the\ngit URL for a Zed extension, go to the Zed extensions website, find the\nextension you want to use and copy the \"Visit Repository\" link.\n\n## Requirements\n\nThe exact requirements for compiling Wasm extensions varies by OS and\narchitecture, so giving exact instructions is very difficult.\n\nYou will probably need to install `clang`.\n\nOn Chimera Linux, you will need to install `wasm-component-ld` and `wasi-clang`.\n\nOn Manjaro, you will need `rust-wasm` and `wasi-compiler-rt`.\n\n## Installing Local Extensions\n\nFrom the extensions page, click the `Install Local` button (or the {#action\nmonyacode::InstallExtensionFromFolder} action) and select the directory containing\nyour extension. You should see the extension begin to install in the status bar.\n\n## Installing Extensions From A Git URL\n\nFrom the extensions page, click the `Install From URL` button (or the {#action\nmonyacode::InstallExtensionFromUrl} action) and enter the git url for the extension.\nYou should see the extension begin to install in the status bar.\n\n> **Note:** The editor must be able to authenticate the git URL, but does not\n> support SSH authentication. Using a public URL such as an `http(s)` git URL is\n> recommended. Check the logs to debug ({#action monyacode::OpenLog}).\n\n## Installation Location\n\n- On macOS, extensions are installed in\n  `~/Library/Application Support/MonyaCode/extensions`.\n- On Linux, they are installed in either `$XDG_DATA_HOME/monyacode/extensions` or\n  `~/.local/share/monyacode/extensions`.\n\nThis directory contains two subdirectories:\n\n- `installed`, which contains the source code for each extension.\n- `work` which contains files created by the extension itself, such as\n  downloaded language servers.\n\n## Removing extensions\n\nRemove extensions from the Extensions interface.\n"},{"path":"extensions/languages.md","title":"Language Extensions","group":"Extensions","content":"# Language Extensions\n\nLanguage support in MonyaCode has several components:\n\n- Language metadata and configuration\n- Grammar\n- Queries\n- Language servers\n\n## Language Metadata\n\nEach language supported by MonyaCode must be defined in a subdirectory inside the\n`languages` directory of your extension.\n\nThis subdirectory must contain a file called `config.toml` file with the\nfollowing structure:\n\n```toml\nname = \"My Language\"\ngrammar = \"my-language\"\npath_suffixes = [\"myl\"]\nline_comments = [\"# \"]\n```\n\n- `name` (required) is the human readable name that will show up in the Select\n  Language dropdown.\n- `grammar` (required) is the name of a grammar. Grammars are registered\n  separately, described below.\n- `path_suffixes` is an array of file suffixes that should be associated with\n  this language. Unlike `file_types` in settings, this does not support glob\n  patterns.\n- `line_comments` is an array of strings that are used to identify line comments\n  in the language. This is used for the `editor::ToggleComments` keybind: {#kb\n  editor::ToggleComments} for toggling lines of code.\n- `tab_size` defines the indentation/tab size used for this language (default is\n  `4`).\n- `hard_tabs` whether to indent with tabs (`true`) or spaces (`false`, the\n  default).\n- `first_line_pattern` is a regular expression, that in addition to\n  `path_suffixes` (above) or `file_types` in settings can be used to match files\n  which should use this language. For example MonyaCode uses this to identify Shell\n  Scripts by matching the\n  [shebangs lines](https://github.com/zed-industries/zed/blob/main/crates/languages/src/bash/config.toml)\n  in the first line of a script.\n- `debuggers` is an array of strings that are used to identify debuggers in the\n  language. When launching a debugger's `New Process Modal`, MonyaCode will order\n  available debuggers by the order of entries in this array.\n\n<!--\nTBD: Document `language_name/config.toml` keys\n\n- autoclose_before\n- brackets (start, end, close, newline, not_in: [\"comment\", \"string\"])\n- word_characters\n- prettier_parser_name\n- opt_into_language_servers\n- code_fence_block_name\n- scope_opt_in_language_servers\n- increase_indent_pattern, decrease_indent_pattern\n- collapsed_placeholder\n- auto_indent_on_paste, auto_indent_using_last_non_empty_line\n- overrides: `[overrides.element]`, `[overrides.string]`\n-->\n\n## Grammar\n\nMonyaCode uses the [Tree-sitter](https://tree-sitter.github.io) parsing library to\nprovide built-in language-specific features. There are grammars available for\nmany languages, and you can also\n[develop your own grammar](https://tree-sitter.github.io/tree-sitter/creating-parsers#writing-the-grammar).\nA growing list of MonyaCode features are built using pattern matching over syntax\ntrees with Tree-sitter queries. As mentioned above, every language that is\ndefined in an extension must specify the name of a Tree-sitter grammar that is\nused for parsing. These grammars are then registered separately in extensions'\n`extension.toml` file, like this:\n\n```toml\n[grammars.gleam]\nrepository = \"https://github.com/gleam-lang/tree-sitter-gleam\"\nrev = \"58b7cac8fc14c92b0677c542610d8738c373fa81\"\n```\n\nThe `repository` field must specify a repository where the Tree-sitter grammar\nshould be loaded from, and the `rev` field must contain a Git revision to use,\nsuch as the SHA of a Git commit. If you're developing an extension locally and\nwant to load a grammar from the local filesystem, you can use a `file://` URL\nfor `repository`. An extension can provide multiple grammars by referencing\nmultiple tree-sitter repositories.\n\n## Tree-sitter Queries\n\nMonyaCode uses the syntax tree produced by the\n[Tree-sitter](https://tree-sitter.github.io) query language to implement several\nfeatures:\n\n- Syntax highlighting\n- Bracket matching\n- Code outline/structure\n- Auto-indentation\n- Code injections\n- Syntax overrides\n- Text redactions\n- Runnable code detection\n- Selecting classes, functions, etc.\n\nThe following sections elaborate on how\n[Tree-sitter queries](https://tree-sitter.github.io/tree-sitter/using-parsers#query-syntax)\nenable these features in MonyaCode, using\n[JSON syntax](https://www.json.org/json-en.html) as a guiding example.\n\n### Syntax highlighting\n\nIn Tree-sitter, the `highlights.scm` file defines syntax highlighting rules for\na particular syntax.\n\nHere's an example from a `highlights.scm` for JSON:\n\n```scheme\n(string) @string\n\n(pair\n  key: (string) @property.json_key)\n\n(number) @number\n```\n\nThis query marks strings, object keys, and numbers for highlighting. The\nfollowing is a comprehensive list of captures supported by themes:\n\n| Capture                  | Description                            |\n| ------------------------ | -------------------------------------- |\n| @attribute               | Captures attributes                    |\n| @boolean                 | Captures boolean values                |\n| @comment                 | Captures comments                      |\n| @comment.doc             | Captures documentation comments        |\n| @constant                | Captures constants                     |\n| @constructor             | Captures constructors                  |\n| @embedded                | Captures embedded content              |\n| @emphasis                | Captures emphasized text               |\n| @emphasis.strong         | Captures strongly emphasized text      |\n| @enum                    | Captures enumerations                  |\n| @function                | Captures functions                     |\n| @hint                    | Captures hints                         |\n| @keyword                 | Captures keywords                      |\n| @label                   | Captures labels                        |\n| @link_text               | Captures link text                     |\n| @link_uri                | Captures link URIs                     |\n| @number                  | Captures numeric values                |\n| @operator                | Captures operators                     |\n| @predictive              | Captures predictive text               |\n| @preproc                 | Captures preprocessor directives       |\n| @primary                 | Captures primary elements              |\n| @property                | Captures properties                    |\n| @punctuation             | Captures punctuation                   |\n| @punctuation.bracket     | Captures brackets                      |\n| @punctuation.delimiter   | Captures delimiters                    |\n| @punctuation.list_marker | Captures list markers                  |\n| @punctuation.special     | Captures special punctuation           |\n| @string                  | Captures string literals               |\n| @string.escape           | Captures escaped characters in strings |\n| @string.regex            | Captures regular expressions           |\n| @string.special          | Captures special strings               |\n| @string.special.symbol   | Captures special symbols               |\n| @tag                     | Captures tags                          |\n| @tag.doctype             | Captures doctypes (e.g., in HTML)      |\n| @text.literal            | Captures literal text                  |\n| @title                   | Captures titles                        |\n| @type                    | Captures types                         |\n| @variable                | Captures variables                     |\n| @variable.special        | Captures special variables             |\n| @variant                 | Captures variants                      |\n\n### Bracket matching\n\nThe `brackets.scm` file defines matching brackets.\n\nHere's an example from a `brackets.scm` file for JSON:\n\n```scheme\n(\"[\" @open \"]\" @close)\n(\"{\" @open \"}\" @close)\n(\"\\\"\" @open \"\\\"\" @close)\n```\n\nThis query identifies opening and closing brackets, braces, and quotation marks.\n\n| Capture | Description                                   |\n| ------- | --------------------------------------------- |\n| @open   | Captures opening brackets, braces, and quotes |\n| @close  | Captures closing brackets, braces, and quotes |\n\nMonyaCode uses these to highlight matching brackets: painting each bracket pair with\na different color (\"rainbow brackets\") and highlighting the brackets if the\ncursor is inside the bracket pair.\n\nTo opt out of rainbow brackets colorization, add the following to the\ncorresponding `brackets.scm` entry:\n\n```scheme\n((\"\\\"\" @open \"\\\"\" @close) (#set! rainbow.exclude))\n```\n\n### Code outline/structure\n\nThe `outline.scm` file defines the structure for the code outline.\n\nHere's an example from an `outline.scm` file for JSON:\n\n```scheme\n(pair\n  key: (string (string_content) @name)) @item\n```\n\nThis query captures object keys for the outline structure.\n\n| Capture        | Description                                                                          |\n| -------------- | ------------------------------------------------------------------------------------ |\n| @name          | Captures the content of object keys                                                  |\n| @item          | Captures the entire key-value pair                                                   |\n| @context       | Captures elements that provide context for the outline item                          |\n| @context.extra | Captures additional contextual information for the outline item                      |\n| @annotation    | Captures nodes that annotate outline item (doc comments, attributes, decorators)[^1] |\n\n[^1]:\n    These annotations are used by Assistant when generating code modification\n    steps.\n\n### Auto-indentation\n\nThe `indents.scm` file defines indentation rules.\n\nHere's an example from an `indents.scm` file for JSON:\n\n```scheme\n(array \"]\" @end) @indent\n(object \"}\" @end) @indent\n```\n\nThis query marks the end of arrays and objects for indentation purposes.\n\n| Capture | Description                                        |\n| ------- | -------------------------------------------------- |\n| @end    | Captures closing brackets and braces               |\n| @indent | Captures entire arrays and objects for indentation |\n\n### Code injections\n\nThe `injections.scm` file defines rules for embedding one language within\nanother, such as code blocks in Markdown or SQL queries in Python strings.\n\nHere's an example from an `injections.scm` file for Markdown:\n\n```scheme\n(fenced_code_block\n  (info_string\n    (language) @injection.language)\n  (code_fence_content) @injection.content)\n\n((inline) @content\n (#set! injection.language \"markdown-inline\"))\n```\n\nThis query identifies fenced code blocks, capturing the language specified in\nthe info string and the content within the block. It also captures inline\ncontent and sets its language to \"markdown-inline\".\n\n| Capture             | Description                                                |\n| ------------------- | ---------------------------------------------------------- |\n| @injection.language | Captures the language identifier for a code block          |\n| @injection.content  | Captures the content to be treated as a different language |\n\nNote that we couldn't use JSON as an example here because it doesn't support\nlanguage injections.\n\n### Syntax overrides\n\nThe `overrides.scm` file defines syntactic _scopes_ that can be used to override\ncertain editor settings within specific language constructs.\n\nFor example, there is a language-specific setting called `word_characters` that\ncontrols which non-alphabetic characters are considered part of a word, for\nexample when you double click to select a variable. In JavaScript, \"$\" and \"#\"\nare considered word characters.\n\nThere is also a language-specific setting called `completion_query_characters`\nthat controls which characters trigger autocomplete suggestions. In JavaScript,\nwhen your cursor is within a _string_, \"-\" is should be considered a completion\nquery character. To achieve this, the JavaScript `overrides.scm` file contains\nthe following pattern:\n\n```scheme\n[\n  (string)\n  (template_string)\n] @string\n```\n\nAnd the JavaScript `config.toml` contains this setting:\n\n```toml\nword_characters = [\"#\", \"$\"]\n\n[overrides.string]\ncompletion_query_characters = [\"-\"]\n```\n\nYou can also disable certain auto-closing brackets in a specific scope. For\nexample, to prevent auto-closing `'` within strings, you could put the following\nin the JavaScript `config.toml`:\n\n```toml\nbrackets = [\n  { start = \"'\", end = \"'\", close = true, newline = false, not_in = [\"string\"] },\n  # other pairs...\n]\n```\n\n#### Range inclusivity\n\nBy default, the ranges defined in `overrides.scm` are _exclusive_. So in the\ncase above, if you cursor was _outside_ the quotation marks delimiting the\nstring, the `string` scope would not take effect. Sometimes, you may want to\nmake the range _inclusive_. You can do this by adding the `.inclusive` suffix to\nthe capture name in the query.\n\nFor example, in JavaScript, we also disable auto-closing of single quotes within\ncomments. And the comment scope must extend all the way to the newline after a\nline comment. To achieve this, the JavaScript `overrides.scm` contains the\nfollowing pattern:\n\n```scheme\n(comment) @comment.inclusive\n```\n\n### Text objects\n\nThe `textobjects.scm` file defines rules for navigating by text objects. This\nwas added in MonyaCode v0.165 and is currently used only in Vim mode.\n\nVim provides two levels of granularity for navigating around files.\nSection-by-section with `[]` etc., and method-by-method with `]m` etc. Even\nlanguages that don't support functions and classes can work well by defining\nsimilar concepts. For example CSS defines a rule-set as a method, and a\nmedia-query as a class.\n\nFor languages with closures, these typically should not count as functions in\nMonyaCode. This is best-effort however, as languages like JavaScript do not\nsyntactically differentiate syntactically between closures and top-level\nfunction declarations.\n\nFor languages with declarations like C, provide queries that match\n`@class.around` or `@function.around`. The `if` and `ic` text objects will\ndefault to these if there is no inside.\n\nIf you are not sure what to put in textobjects.scm, both\n[nvim-treesitter-textobjects](https://github.com/nvim-treesitter/nvim-treesitter-textobjects),\nand the [Helix editor](https://github.com/helix-editor/helix) have queries for\nmany languages. You can refer to the MonyaCode\n[built-in languages](https://github.com/zed-industries/zed/tree/main/crates/languages/src)\nto see how to adapt these.\n\n| Capture          | Description                                                             | Vim mode                                         |\n| ---------------- | ----------------------------------------------------------------------- | ------------------------------------------------ |\n| @function.around | An entire function definition or equivalent small section of a file.    | `[m`, `]m`, `[M`,`]M` motions. `af` text object  |\n| @function.inside | The function body (the stuff within the braces).                        | `if` text object                                 |\n| @class.around    | An entire class definition or equivalent large section of a file.       | `[[`, `]]`, `[]`, `][` motions. `ac` text object |\n| @class.inside    | The contents of a class definition.                                     | `ic` text object                                 |\n| @comment.around  | An entire comment (e.g. all adjacent line comments, or a block comment) | `gc` text object                                 |\n| @comment.inside  | The contents of a comment                                               | `igc` text object (rarely supported)             |\n\nFor example:\n\n```scheme\n; include only the content of the method in the function\n(method_definition\n    body: (_\n        \"{\"\n        (_)* @function.inside\n        \"}\")) @function.around\n\n; match function.around for declarations with no body\n(function_signature_item) @function.around\n\n; join all adjacent comments into one\n(comment)+ @comment.around\n```\n\n### Runnable code detection\n\nThe `runnables.scm` file defines rules for detecting runnable code.\n\nHere's an example from a `runnables.scm` file for JSON:\n\n```scheme\n(\n    (document\n        (object\n            (pair\n                key: (string\n                    (string_content) @_name\n                    (#eq? @_name \"scripts\")\n                )\n                value: (object\n                    (pair\n                        key: (string (string_content) @run @script)\n                    )\n                )\n            )\n        )\n    )\n    (#set! tag package-script)\n    (#set! tag composer-script)\n)\n```\n\nThis query detects runnable scripts in package.json and composer.json files.\n\nThe `@run` capture specifies where the run button should appear in the editor.\nOther captures, except those prefixed with an underscore, are exposed as\nenvironment variables with a prefix of `MONYACODE_CUSTOM_$(capture_name)` when\nrunning the code.\n\n| Capture | Description                                            |\n| ------- | ------------------------------------------------------ |\n| @\\_name | Captures the \"scripts\" key                             |\n| @run    | Captures the script name                               |\n| @script | Also captures the script name (for different purposes) |\n\n<!--\nTBD: `#set! tag`\n-->\n\n## Language Servers\n\nMonyaCode uses the\n[Language Server Protocol](https://microsoft.github.io/language-server-protocol/)\nto provide advanced language support.\n\nAn extension may provide any number of language servers. To provide a language\nserver from your extension, add an entry to your `extension.toml` with the name\nof your language server and the language(s) it applies to. The entry in the list\nof `languages` has to match the `name` field from the `config.toml` file for\nthat language:\n\n```toml\n[language_servers.my-language-server]\nname = \"My Language LSP\"\nlanguages = [\"My Language\"]\n```\n\nThen, in the Rust code for your extension, implement the\n`language_server_command` method on your extension:\n\n```rust\nimpl monyacode::Extension for MyExtension {\n    fn language_server_command(\n        &mut self,\n        language_server_id: &LanguageServerId,\n        worktree: &monyacode::Worktree,\n    ) -> Result<monyacode::Command> {\n        Ok(monyacode::Command {\n            command: get_path_to_language_server_executable()?,\n            args: get_args_for_language_server()?,\n            env: get_env_for_language_server()?,\n        })\n    }\n}\n```\n\nYou can customize the handling of the language server using several optional\nmethods in the `Extension` trait. For example, you can control how completions\nare styled using the `label_for_completion` method. For a complete list of\nmethods, see the\n[API docs for the MonyaCode extension API](https://docs.rs/zed_extension_api).\n\n### Multi-Language Support\n\nIf your language server supports additional languages, you can use\n`language_ids` to map MonyaCode `languages` to the desired\n[LSP-specific `languageId`](https://microsoft.github.io/language-server-protocol/specifications/lsp/3.17/specification/#textDocumentItem)\nidentifiers:\n\n```toml\n\n[language-servers.my-language-server]\nname = \"Whatever LSP\"\nlanguages = [\"JavaScript\", \"HTML\", \"CSS\"]\n\n[language-servers.my-language-server.language_ids]\n\"JavaScript\" = \"javascript\"\n\"TSX\" = \"typescriptreact\"\n\"HTML\" = \"html\"\n\"CSS\" = \"css\"\n```\n"},{"path":"extensions/themes.md","title":"Themes","group":"Extensions","content":"# Themes\n\nThe `themes` directory in an extension should contain one or more theme files.\n\nEach theme file should adhere to the JSON schema specified at\n[`https://zed.dev/schema/themes/v0.2.0.json`](https://zed.dev/schema/themes/v0.2.0.json).\n\nSee [this Zed blog post](https://zed.dev/blog/user-themes-now-in-preview) for\nmore details about creating themes.\n\n## Theme JSON Structure\n\nThe structure of a MonyaCode theme is defined in the\n[Zed Theme JSON Schema](https://zed.dev/schema/themes/v0.2.0.json).\n\nA MonyaCode theme consists of a Theme Family object including:\n\n- `name`: The name for the theme family\n- `author`: The name of the author of the theme family\n- `themes`: An array of Themes belonging to the theme family\n\nThe core components a Theme object include:\n\n1. Theme Metadata:\n\n   - `name`: The name of the theme\n   - `appearance`: Either \"light\" or \"dark\"\n\n2. Style Properties under the `style`, such as:\n\n   - `background`: The main background color\n   - `foreground`: The main text color\n   - `accent`: The accent color used for highlighting and emphasis\n\n3. Syntax Highlighting:\n\n   - `syntax`: An object containing color definitions for various syntax\n     elements (e.g., keywords, strings, comments)\n\n4. UI Elements:\n\n   - Colors for various UI components such as:\n     - `element.background`: Background color for UI elements\n     - `border`: Border colors for different states (normal, focused, selected)\n     - `text`: Text colors for different states (normal, muted, accent)\n\n5. Editor-specific Colors:\n\n   - Colors for editor-related elements such as:\n     - `editor.background`: Editor background color\n     - `editor.gutter`: Gutter colors\n     - `editor.line_number`: Line number colors\n\n6. Terminal Colors:\n   - ANSI color definitions for the integrated terminal\n"},{"path":"extensions.md","title":"Extensions","group":"Guides","content":"# Extensions\n\nMonyaCode lets you add new functionality using user-defined extensions.\n\n- [Installing Extensions](./extensions/installing-extensions.md)\n- [Extension Capabilities](./extensions/capabilities.md)\n- [Developing Extensions](./extensions/developing-extensions.md)\n  - [Developing Language Extensions](./extensions/languages.md)\n  - [Developing Debugger Extensions](./extensions/debugger-extensions.md)\n  - [Developing Themes](./extensions/themes.md)\n  - [Developing Icon Themes](./extensions/icon-themes.md)\n"},{"path":"git.md","title":"Git","group":"Guides","content":"# Git\n\nThere is integrated support for working with git and git forges in the code\neditor.\n\nHere's an overview of all currently supported features:\n\n- Committing\n- Staging, pushing, pulling, and fetching\n- Project Diff: A multibuffer view of all changes\n- Diff indicators in buffers and editor scrollbars\n- Inline diff toggle and reverts in the editor for unstaged changes\n- Git status in the Project Panel\n- Branch creating and switching\n- Git blame viewing\n- Git stash pop, apply, drop and view\n\n## Git Panel\n\nThe Git Panel provides a birds-eye view of the state of the working tree and\nstaging area.\n\nOpen the Git Panel using {#action git_panel::ToggleFocus}, or by clicking the\nGit icon in the status bar.\n\nThe Git Panel shows the state of the project: Which repository and branch are\nactive, what files have changed and the current staging state of each file.\n\nMonyaCode monitors the project repository, so any changes made within or outside the\neditor are instantly visible in the git panel.\n\n### Configuration\n\nConfigure how the editor hard wraps commit messages with the\n`preferred-line-length` setting of the \"Git Commit\" language. The default is\n`72`, but it can be set to any number of characters, `0` or more.\n\nThe Git Panel also allows configuring the `soft_wrap` setting to adjust how\ncommit messages display while typing them in the Git Panel. The default setting\nis `editor_width`, however, `none`, `preferred_line_length`, and `bounded` are\nalso options.\n\n#### Example\n\n```json\n\"languages\": {\n  \"Git Commit\": {\n    \"soft_wrap\": \"editor_width\",\n    \"preferred_line_length\": 72\n  },\n}\n```\n\n## Project Diff\n\nSee all of the current changes by opening the Project Diff ({#kb git::Diff}),\naccessible via the {#action git::Diff} action in the Command Palette or the Git\nPanel.\n\nAll of the changes displayed in the Project Diff behave the same as any other\nmultibuffer: they are all editable excerpts of files.\n\nStage or unstage each hunk as well as a whole file by hitting the buttons on the\ntab bar or their corresponding keybindings.\n\n## Fetch, push, and pull\n\nFetch, push, or pull from the Git repository in MonyaCode via the buttons available\non the Git Panel or via the Command Palette actions: {#action git::Fetch},\n{#action git::Push}, and {#action git::Pull}.\n\n## Staging Workflow\n\nMonyaCode has two primary staging workflows, using either the Project Diff or the\npanel directly.\n\n### Using the Project Diff\n\nIn the Project Diff view it's possible to focus on each hunk and stage them\nindividually by clicking on the tab bar buttons or via the keybindings {#action\ngit::StageAndNext} ({#kb git::StageAndNext}).\n\nSimilarly, stage all hunks at the same time with the {#action git::StageAll}\n({#kb git::StageAll}) keybinding and then immediately commit with {#action\ngit::Commit} ({#kb git::Commit}).\n\n### Using the Git Panel\n\nFrom the panel, simply type a commit message and hit the commit button, or\n{#action git::Commit}. This will automatically stage all tracked files\n(indicated by a `[·]` in the entry's checkbox) and commit them.\n\nEntries can be staged using each individual entry's checkbox. All changes can be\nstaged using the button at the top of the panel, or {#action git::StageAll}.\n\n## Committing\n\nThere are two ways to commit changes from within the editor:\n\n1. Using the button at the bottom of the Git Panel. Hitting {#kb git::Commit}\n   immediately commits all of your staged changes.\n2. Invoke the action {#action git::ExpandCommitEditor} or use the keyboard\n   shortcut {#kb git::ExpandCommitEditor} while focused on the Git Panel commit\n   text area.\n\n### Undo a commit\n\nAs soon as you commit in MonyaCode, in the Git Panel, you'll see a bar right under\nthe commit textarea which will show the most recently submitted commit.\n\nIn there, you can use the \"Uncommit\" button, which will run a command similar to\n`git reset HEADˆ --soft`.\n\n### Commit Line Length\n\nBy default, the commit line length is set to `72`. It can be configured in\n`settings.json`.\n\nSee `preferred-line-length` in the [Configuration](#configuration) section.\n\n## Stashing\n\nGit stash allows you to temporarily save your uncommitted changes and return to\na clean working directory. This is useful to quickly switch branches or pull\nupdates without committing incomplete work.\n\n### Creating Stashes\n\nTo stash all current changes, use the {#action git::StashAll} action. This will\nsave both staged and unstaged changes to a new stash entry and set up a clean\nworking directory.\n\n### Managing Stashes\n\nMonyaCode provides a comprehensive stash picker accessible via {#action\ngit::ViewStash}. From the stash picker, you can:\n\n- **View stash list**: Browse all your saved stashes with their descriptions and\n  timestamps\n- **Open diffs**: See exactly what changes are stored in each stash\n- **Apply stashes**: Apply stash changes to your working directory while keeping\n  the stash entry\n- **Pop stashes**: Apply stash changes and remove the stash entry from the list\n- **Drop stashes**: Delete unwanted stash entries without applying them\n\n### Quick Stash Operations\n\nFor faster workflows, MonyaCode provides direct actions to work with the most recent\nstash:\n\n- **Apply latest stash**: Use {#action git::StashApply} to apply the most recent\n  stash without removing it\n- **Pop latest stash**: Use {#action git::StashPop} to apply and remove the most\n  recent stash\n\n### Stash Diff View\n\nWhen viewing a specific stash in the diff view, you have additional options\navailable through the interface:\n\n- Apply the current stash to your working directory\n- Pop the current stash (apply and remove)\n- Remove the stash without applying changes\n\nTo open the stash diff view, select a stash from the stash picker and use the\n{#action stash_picker::ShowStashItem} ({#kb stash_picker::ShowStashItem})\nkeybinding.\n\n## Git Integrations\n\nMonyaCode integrates with popular Git hosting services to ensure that Git commit\nhashes and references to Issues, Pull Requests, and Merge Requests become\nclickable links.\n\nMonyaCode currently supports links to the hosted versions of\n[GitHub](https://github.com), [GitLab](https://gitlab.com),\n[Bitbucket](https://bitbucket.org), [Sourcehut](https://sr.ht) and\n[Codeberg](https://codeberg.org).\n\nFor self-hosted GitHub, GitLab, or Bitbucket instances, add them to the\n`git_hosting_providers` setting so commit hashes and permalinks resolve to your\ndomain:\n\n```json\n{\n  \"git_hosting_providers\": [\n    {\n      \"provider\": \"gitlab\",\n      \"name\": \"Corp GitLab\",\n      \"base_url\": \"https://git.example.corp\",\n    },\n  ],\n}\n```\n\n## Copy Permalink\n\nCreate a permanent link to a code snippet on your Git hosting service.\n\nThese links are useful for sharing a specific line or range of lines in a file\nat a specific commit. Trigger this action via the Command Palette (search for\n`permalink`), by creating a\n[custom key bindings](./key-bindings.md#custom-key-bindings) to the\n`editor::CopyPermalinkToLine` or `editor::OpenPermalinkToLine` actions or by\nsimply right clicking and selecting `Copy Permalink` with line(s) selected in\nyour editor.\n\n## Diff Hunk Keyboard Shortcuts\n\nWhen viewing files with changes, MonyaCode displays diff hunks that can be expanded\nor collapsed for detailed review:\n\n- **Expand all diff hunks**: {#action editor::ExpandAllDiffHunks} ({#kb\n  editor::ExpandAllDiffHunks})\n- **Collapse all diff hunks**: Press `Escape` (bound to {#action\n  editor::Cancel})\n- **Toggle selected diff hunks**: {#action editor::ToggleSelectedDiffHunks}\n  ({#kb editor::ToggleSelectedDiffHunks})\n- **Navigate between hunks**: {#action editor::GoToHunk} and {#action\n  editor::GoToPreviousHunk}\n\n> **Tip:** The `Escape` key is the quickest way to collapse all expanded diff\n> hunks and return to an overview of your changes.\n\n## Action Reference\n\n| Action                                    | Keybinding                            |\n| ----------------------------------------- | ------------------------------------- |\n| {#action git::Add}                        | {#kb git::Add}                        |\n| {#action git::StageAll}                   | {#kb git::StageAll}                   |\n| {#action git::UnstageAll}                 | {#kb git::UnstageAll}                 |\n| {#action git::ToggleStaged}               | {#kb git::ToggleStaged}               |\n| {#action git::StageAndNext}               | {#kb git::StageAndNext}               |\n| {#action git::UnstageAndNext}             | {#kb git::UnstageAndNext}             |\n| {#action git::Commit}                     | {#kb git::Commit}                     |\n| {#action git::ExpandCommitEditor}         | {#kb git::ExpandCommitEditor}         |\n| {#action git::Push}                       | {#kb git::Push}                       |\n| {#action git::ForcePush}                  | {#kb git::ForcePush}                  |\n| {#action git::Pull}                       | {#kb git::Pull}                       |\n| {#action git::PullRebase}                 | {#kb git::PullRebase}                 |\n| {#action git::Fetch}                      | {#kb git::Fetch}                      |\n| {#action git::Diff}                       | {#kb git::Diff}                       |\n| {#action git::Restore}                    | {#kb git::Restore}                    |\n| {#action git::RestoreFile}                | {#kb git::RestoreFile}                |\n| {#action git::Branch}                     | {#kb git::Branch}                     |\n| {#action git::Switch}                     | {#kb git::Switch}                     |\n| {#action git::CheckoutBranch}             | {#kb git::CheckoutBranch}             |\n| {#action git::Blame}                      | {#kb git::Blame}                      |\n| {#action git::StashAll}                   | {#kb git::StashAll}                   |\n| {#action git::StashPop}                   | {#kb git::StashPop}                   |\n| {#action git::StashApply}                 | {#kb git::StashApply}                 |\n| {#action git::ViewStash}                  | {#kb git::ViewStash}                  |\n| {#action editor::ToggleGitBlameInline}    | {#kb editor::ToggleGitBlameInline}    |\n| {#action editor::ExpandAllDiffHunks}      | {#kb editor::ExpandAllDiffHunks}      |\n| {#action editor::ToggleSelectedDiffHunks} | {#kb editor::ToggleSelectedDiffHunks} |\n\n> Not all actions have default keybindings, but can be bound by\n> [customizing your keymap](./key-bindings.md#user-keymaps).\n\n## Git CLI Configuration\n\nIf you would like to also use MonyaCode for your\n[git commit message editor](https://git-scm.com/book/en/v2/Customizing-Git-Git-Configuration#_core_editor)\nwhen committing from the command line you can use `monyacode --wait`:\n\n```sh\ngit config --global core.editor \"monyacode --wait\"\n```\n\nOr add the following to your shell environment (in `~/.zshrc`, `~/.bashrc`,\netc):\n\n```sh\nexport GIT_EDITOR=\"monyacode --wait\"\n```\n"},{"path":"globs.md","title":"Globs","group":"Guides","content":"# Globs\n\n[Glob patterns](<https://en.wikipedia.org/wiki/Glob_(programming)>) is the\nformal name for Unix shell-style path matching wildcards like `*.md` or\n`docs/**/*.md` supported by sh, bash, zsh, etc. A glob is similar but distinct\nfrom a\n[regex (regular expression)](https://en.wikipedia.org/wiki/Regular_expression).\nIn MonyaCode these are commonly used when matching filenames.\n\n## Glob Flavor\n\nMonyaCode uses two different rust crates for matching glob patterns:\n\n- [ignore crate](https://docs.rs/ignore/latest/ignore/) for matching glob\n  patterns stored in `.gitignore` files\n- [glob crate](https://docs.rs/glob/latest/glob/) for matching file paths in\n  MonyaCode\n\nWhile simple expressions are portable across environments (e.g. running\n`ls *.py` or `*.tmp` in a gitignore) there is significant divergence in the\nsupport for and syntax of more advanced features varies (character classes,\nexclusions, `**`, etc) across implementations. The rest of this document will be\ndescribing globs as supported in MonyaCode via the `glob` crate implementation. See\n[References](#references) below for documentation links for glob pattern syntax\nfor `.gitignore`, shells and other programming languages.\n\nThe `glob` crate is implemented entirely in rust and does not rely on the `glob`\n/ `fnmatch` interfaces provided by libc. This means that globs in MonyaCode should\nbehave similarly across platforms.\n\n## Introduction\n\nA glob \"pattern\" is used to match a file name or complete file path. For\nexample, when using \"Search all files\" {#kb project_search::ToggleFocus} you can\nclick the funnel shaped Toggle Filters\" button or {#kb\nproject_search::ToggleFilters} and it will show additional search fields for\n\"Include\" and \"Exclude\" which support specifying glob patterns for matching file\npaths and file names.\n\nWhen creating a glob pattern you can use one or multiple special characters:\n\n| Special Character | Meaning                                                           |\n| ----------------- | ----------------------------------------------------------------- |\n| `?`               | Matches any single character                                      |\n| `*`               | Matches any (possibly empty) sequence of characters               |\n| `**`              | Matches the current directory and arbitrary subdirectories        |\n| `[abc]`           | Matches any one character in the brackets                         |\n| `[a-z]`           | Matches any of a range of characters (ordered by Unicode)         |\n| `[!...]`          | The negation of `[...]` (matches a character not in the brackets) |\n\nNotes:\n\n1. Shell-style brace-expansions like `{a,b,c}` are not supported.\n2. To match a literal `-` character inside brackets it must come first `[-abc]`\n   or last `[abc-]`.\n3. To match the literal `[` character use `[[]` or put it as the first character\n   in the group `[[abc]`.\n4. To match the literal `]` character use `[]]` or put it as the last character\n   in the group `[abc]]`.\n\n## Examples\n\n### Matching file extensions\n\nIf you wanted to only search Markdown files add `*.md` to the \"Include\" search\nfield.\n\n### Case insensitive matching\n\nGlobs in MonyaCode are case-sensitive, so `*.c` will not match `main.C` (even on\ncase-insensitive filesystems like HFS+/APFS on macOS). Instead use brackets to\nmatch characters. So instead of `*.c` use `*.[cC]`.\n\n### Matching directories\n\nIf you wanted to search the\n[monyacode repository](https://github.com/monyacode/monyacode) for examples of\n[Configuring Language Servers](./configuring-languages#configuring-language-servers.md)\n(under `\"lsp\"` in MonyaCode `settings.jsonc`) you could search for `\"lsp\"` and in the\n\"Include\" filter specify `docs/**/*.md`. This would only match files whose path\nwas under the `docs` directory or any nested subdirectories `**/` of that folder\nwith a filename that ends in `.md`.\n\nIf instead you wanted to restrict yourself only to MonyaCode Language-Specific\nDocumentation pages you could define a narrower pattern of:\n`docs/languages/*.md` this would match `docs/languages/rust.md` and\n`docs/languages/cpp.md` but not `docs/configuring-languages.md`.\n\n### Implicit Wildcards\n\nWhen using the \"Include\" / \"Exclude\" filters on a Project Search each glob is\nwrapped in implicit wildcards. For example to exclude any files with license in\nthe path or filename from your search just type `license` in the exclude box.\nBehind the scenes MonyaCode transforms `license` to `**license**`. This means that\nfiles named `license.*`, `*.license` or inside a `license` subdirectory will all\nbe filtered out. This enables users to easily filter for `*.ts` without having\nto remember to type `**/*.ts` every time.\n\nAlternatively, if in your MonyaCode settings you wanted a\n[`file_types`](./configuring-monyacode.md#file-types) override which only applied to\na certain directory you must explicitly include the wildcard globs. For example,\nif you had a directory of template files with the `html` extension that you\nwanted to recognize as Jinja2 template you could use the following:\n\n```json\n{\n  \"file_types\": {\n    \"C++\": [\"[cC]\"],\n    \"Jinja2\": [\"**/templates/*.html\"],\n  },\n}\n```\n\n## References\n\nWhile globs in MonyaCode are implemented as described above, when writing code using\nglobs in other languages, please reference your platform's glob documentation:\n\n- [macOS fnmatch](https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man3/fnmatch.3.html)\n  (BSD C Standard Library)\n- [Linux fnmatch](https://www.gnu.org/software/libc/manual/html_node/Wildcard-Matching.html)\n  (GNU C Standard Library)\n- [POSIX fnmatch](https://pubs.opengroup.org/onlinepubs/9699919799/functions/fnmatch.html)\n  (POSIX Specification)\n- [node-glob](https://github.com/isaacs/node-glob) (Node.js `glob` package)\n- [Python glob](https://docs.python.org/3/library/glob.html) (Python Standard\n  Library)\n- [Golang glob](https://pkg.go.dev/path/filepath#Match) (Go Standard Library)\n- [gitignore patterns](https://git-scm.com/docs/gitignore) (Gitignore Pattern\n  Format)\n- [PowerShell: About Wildcards](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_wildcards)\n  (Wildcards in PowerShell)\n"},{"path":"helix.md","title":"Helix Mode","group":"Guides","content":"# Helix Mode\n\n_Work in progress! Not all Helix keybindings are implemented yet._\n\nThe Helix mode is an emulation layer that brings Helix-style keybindings and\nmodal editing to MonyaCode. It builds upon [Vim mode](./vim.md), so much of the core\nfunctionality is shared. Enabling `helix_mode` will also enable `vim_mode`.\n\nFor a guide on Vim-related features that are also available in Helix mode,\nplease refer to the [Vim mode documentation](./vim.md).\n\nFor a detailed list of Helix's default keybindings, please visit the\n[official Helix documentation](https://docs.helix-editor.com/keymap.html).\n\n## Core differences\n\nAny text object that works with `m i` or `m a` also works with `]` and `[`, so\nfor example `] (` selects the next pair of parentheses after the cursor.\n"},{"path":"i-cant-open-any-files.md","title":"Help: I can't open any files","group":"Guides","content":"# Help: I can't open any files\n\n## Linux: Clicking links isn't working\n\nThese features are provided by XDG desktop portals, specifically:\n\n- `org.freedesktop.portal.FileChooser`\n- `org.freedesktop.portal.OpenURI`\n\nSome window managers, such as `Hyprland`, don't provide a file picker by\ndefault. See\n[this list](https://wiki.archlinux.org/title/XDG_Desktop_Portal#List_of_backends_and_interfaces)\nas a starting point for alternatives.\n\n## Linux: Could not start inotify\n\nMonyaCode relies on inotify to watch your filesystem for changes. If you cannot start\ninotify then MonyaCode will not work reliably.\n\nIf you are seeing \"too many open files\" then first try `sysctl fs.inotify`.\n\n- You should see that max_user_instances is 128 or higher (you can change the\n  limit with `sudo sysctl fs.inotify.max_user_instances=1024`). MonyaCode needs only\n  1 inotify instance.\n- You should see that `max_user_watches` is 8000 or higher (you can change the\n  limit with `sudo sysctl fs.inotify.max_user_watches=64000`). MonyaCode needs one\n  watch per directory in all your open projects + one per git repository + a\n  handful more for settings, themes, keymaps, extensions.\n\nIt is also possible that you are running out of file descriptors. You can check\nthe limits with `ulimit` and update them by editing `/etc/security/limits.conf`.\n\n## Mac: Clicking documentation links isn't working\n\nIf you build and run from source and haven't already installed MonyaCode as an\napplication, the `monyacode://` links will not work properly. To get them working,\nyou will need to either build an application bundle yourself or install one of\nthe pre-built bundles from the [MonyaCode website](https://monyacode-editor.com).\n"},{"path":"icon-themes.md","title":"Icon Themes","group":"Guides","content":"# Icon Themes\n\nMonyaCode comes with a built-in icon theme, with more icon themes available as\nextensions.\n\n## Enabling Icon Themes\n\nFile and folder icons are disabled by default. You can enable them in\n`Settings > Panels > File Icons` and `Settings > Panels > File Icons`.\n\n## Selecting an Icon Theme\n\nSee what icon themes are installed and preview them via the Icon Theme Selector,\nwhich you can open from the command palette with `icon theme selector: toggle`.\n\nNavigating through the icon theme list by moving up and down will change the\nicon theme in real time and hitting enter will save it to your settings file.\n\n## Installing more Icon Themes\n\nMonyaCode can use icon themes created for the Zed editor. If you find an icon theme\nyou like, you can install it using the repository URL from the Extensions page.\n\n## Configuring Icon Themes\n\nYour selected icon theme is stored in your settings file. You can open your\nsettings file from the command palette with {#action monyacode::OpenSettingsFile}\n(bound to {#kb monyacode::OpenSettingsFile}).\n\nJust like with themes, MonyaCode allows for configuring different icon themes for\nlight and dark mode. You can set the mode to `\"light\"` or `\"dark\"` to ignore the\ncurrent system mode.\n\n```json\n{\n  \"icon_theme\": {\n    \"mode\": \"system\",\n    \"light\": \"Light Icon Theme\",\n    \"dark\": \"Dark Icon Theme\",\n  },\n}\n```\n\n## Icon Theme Development\n\nSee: [Developing MonyaCode Icon Themes](./extensions/icon-themes.md)\n"},{"path":"index.md","title":"Documentation","group":"Guides","content":"# Documentation\n\nMonyaCode is a source code editor and an IDE. It has a lot of features but is also\nvery much a work in progress. It is a community project, although the community\nright now consists of one grumpy toad in its spare time and a small but\ndedicated group of contributors (much love to them). The project hopes to move\nslowly but steadily in the direction of stability, reliability and respect.\n\nIt has a lot of things going for it:\n\n- It mostly works today. I use it every day to write code.\n- Decent performance.\n- Cross-platform support for Mac, Linux, FreeBSD and Windows.\n- Remote development over SSH.\n- Integrated debugger support via the `DAP` protocol.\n- Jupyter Notebook support (REPL mode).\n- Built in git support.\n- Pretty decent Vim and Helix modal editing modes.\n- Partial/limited support for Zed Wasm extensions.\n- No AI integration or monetization scheme.\n- No telemetry or proprietary server component.\n- No video or audio chat.\n- No involuntary auto updates.\n- Tries not to install anything without explicit permission.\n- Integrated documentation.\n- Built in support for a lot of languages and language servers.\n- Lots of builtin themes.\n\nThis project did not start with a vision of the perfect editor, but as a\nreaction against what was and still is happening to the world of software\ndevelopment, the world at large and to editors like VS Code, Zed and Vim. Its\ncore philosophy is that writing code by hand with care is good and righteous,\nand aims to be a good tool for doing just that.\n\nMonyaCode started as a **hard fork** of the Zed editor. For more details on the\nbackground and motivation behind this fork, read the\n[Mission Statement](./mission.md).\n\n### Migrating\n\n- From [Zed](./migrate/zed.md)\n- From [VS Code](./migrate/vs-code.md)\n\n## Features\n\n- [Debugger](./debugger.md): Integrated support for DAP, the debugger adapter\n  protocol.\n- [Remote Development](./remote-development.md): Connect to remote servers via\n  SSH and edit as if working on a local project.\n- [Extensions](./extensions.md): Add support for additional languages, themes\n  and icons using the extension system.\n- [Supported Languages](./languages.md)\n- [Language Servers](./language-servers.md): MonyaCode relies on language servers for\n  providing advanced semantic functionality for various programming languages.\n\n## Development\n\n- [Development](./development.md)\n  - [macOS](./development/macos.md)\n  - [Linux](./development/linux.md)\n  - [Windows](./development/windows.md)\n  - [FreeBSD](./development/freebsd.md)\n  - [Using Debuggers](./development/debuggers.md)\n  - [Glossary](./development/glossary.md)\n- [Debugging Crashes](./development/debugging-crashes.md)\n\n## Configuration\n\n- [Configuring MonyaCode](./configuring-monyacode.md)\n- [Configuring Languages](./configuring-languages.md)\n  - [Toolchains](./toolchains.md)\n- [Key bindings](./key-bindings.md)\n  - [All Actions](./all-actions.md)\n- [Snippets](./snippets.md)\n- [Themes](./themes.md)\n- [Icon Themes](./icon-themes.md)\n- [Visual Customization](./visual-customization.md)\n- [Vim Mode](./vim.md)\n- [Helix Mode](./helix.md)\n- [SuperTab](./supertab.md)\n\n## Using MonyaCode\n\n- [Multibuffers](./multibuffers.md)\n- [Command Palette](./command-palette.md)\n- [Command-line Interface](./command-line-interface.md)\n- [Outline Panel](./outline-panel.md)\n- [Code Completions](./completions.md)\n- [Git](./git.md)\n- [Debugger](./debugger.md)\n- [Diagnostics](./diagnostics.md)\n- [Tasks](./tasks.md)\n- [Tab Switcher](./tab-switcher.md)\n- [Remote Development](./remote-development.md)\n- [Environment Variables](./environment.md)\n- [REPL](./repl.md)\n\n## Platform Support\n\n- [Windows](./windows.md)\n- [Linux](./linux.md)\n\n## Handling Problems\n\n- [Troubleshooting](./troubleshooting.md)\n- [Uninstall](./uninstall.md)\n\n## Extensions\n\n> **NOTE:** The Zed extension system relies on a closed-source server component,\n> which is stripped from MonyaCode. Instead, all extensions have to be built from\n> source. Currently, there is no extension registry so the extensions have to be\n> installed either via the suggestion popups or an URL and Wasm extensions need\n> rustup installed in order to compile.\n\n- [Overview](./extensions.md)\n- [Installing Extensions](./extensions/installing-extensions.md)\n- [Developing Extensions](./extensions/developing-extensions.md)\n- [Extension Capabilities](./extensions/capabilities.md)\n- [Language Extensions](./extensions/languages.md)\n- [Debugger Extensions](./extensions/debugger-extensions.md)\n- [Theme Extensions](./extensions/themes.md)\n- [Icon Theme Extensions](./extensions/icon-themes.md)\n\n## Integrations and related tools\n\nThere are some related projects mostly around making MonyaCode available in various\npackage managers and Linux distributions. This list is not complete, if you know\nof any packaging effort, MonyaCode-specific extensions or anything like that, feel\nfree to submit a PR at <https://github.com/monyacode/monyacode>.\n\n- **Homebrew (Mac):** <https://formulae.brew.sh/cask/monyacode>\n- **Arch Linux:** <https://archlinux.org/packages/extra/x86_64/monyacode>\n- **Arch Linux (AUR):** <https://aur.archlinux.org/packages/monyacode-git>\n- **Alpine Linux:**\n  <https://pkgs.alpinelinux.org/package/edge/testing/x86_64/monyacode>\n- **Gentoo Linux:** <https://github.com/monyacode/monyacode-gentoo>\n- **Raycast (Mac):** <https://www.raycast.com/justyt65/monyacode>\n- **Chimera Linux (WIP):** <https://github.com/chimera-linux/cports/pull/5506>\n\n## Legal note on accepting contributions\n\nIf you have previously installed Zed and agreed to their license agreement, you\nmay be legally prevented from contributing to MonyaCode despite the open source\nlicense of the code. I am not a lawyer and I suspect that the license that they\nuse would not hold up at least in European court, but I don't know. For that\nexact reason, I never agreed to their license. This is the main reason this fork\neven exists.\n\nIf you do want to contribute patches, you will have to accept full\nresponsibility for ensuring and warranting that you are legally allowed to do\nso.\n\n## You are the community\n\nMonyaCode is proudly open source, in spirit, not just in words. That said, we have\nstrong opinions about what we want to include in the editor. For example, the\nmain reason for this fork from Zed is to remove certain \"features\" that we\ndisagree with, morally. However, you are of course free to make it your own in\nany way you see fit.\n\nThere is no official discord or reddit community, but there is an XMPP chat for\nMonyaCode at [monyacode@rooms.slidge.im][xmpp-link]. Any XMPP client should be able to\nconnect, and there is a [basic web UI][xmpp-webui] available. There are chat\nlogs available in an [online archive][xmpp-archive] as well.\n\n[xmpp-link]: xmpp:monyacode@rooms.slidge.im?join\n[xmpp-webui]: https://slidge.im/monyacode/#/guest?join=monyacode@rooms.slidge.im\n[xmpp-archive]: https://rooms.slidge.im:5281/muc_log/monyacode/\n\n## Strict No AI/LLM Policy\n\nNo more AI. I used to have a milder version of this statement here before, which\nI wrote early on when I wasn't really aware of \"vibe-coding\" as such and was\nmostly annoyed purely at the chatbox / autocomplete version of AI. That was bad\nenough, but I really am not a fan of what that has become (in March 2026 when I\nam writing this). I have copied this policy from the [Zig language project Code\nof Conduct][zig-coc]:\n\n> No LLMs for issues.\n>\n> No LLMs for pull requests.\n>\n> No LLMs for comments on the bug tracker, including translation. English is\n> encouraged, but not required. You are welcome to post in your native language\n> and rely on others to have their own translation tools of choice to interpret\n> your words.\n\nThe Zed code base contains a lot of AI-generated code. It doesn't need a single\nline more.\n\n[zig-coc]: https://ziglang.org/code-of-conduct/\n"},{"path":"key-bindings.md","title":"Key bindings","group":"Guides","content":"# Key bindings\n\n## Predefined Keymaps\n\nIf you're used to a specific editor's defaults, you can change your\n`base_keymap` through the settings window ({#kb monyacode::OpenSettings}) or directly\nthrough your `settings.jsonc` file ({#kb monyacode::OpenSettingsFile}).\n\nThe following keymap styles are currently supported:\n\n- Emacs (default)\n- VS Code\n- Atom\n- JetBrains\n- Sublime Text\n- TextMate\n- Minimal (for vim/helix)\n- None (disables _all_ key bindings)\n\nThis setting can also be changed via the command palette through the\n`monyacode: toggle base keymap selector` action.\n\nYou can also enable `vim_mode` or `helix_mode`, which add modal bindings. For\nmore information, see the documentation for [Vim mode](./vim.md) and\n[Helix mode](./helix.md). You may also want to use the Minimal base keymap if\nyou want a pure vim/helix experience without any key bindings from another\napplication.\n\n## Keymap Editor\n\nYou can access the keymap editor through the {#kb monyacode::OpenKeymap} action or by\nrunning {#action monyacode::OpenKeymap} action from the command palette. You can\neasily add or change a keybind for an action with the `Change Keybinding` or\n`Add Keybinding` button on the command pallets left bottom corner.\n\nIn there, you can see all of the existing actions in MonyaCode as well as the\nassociated keybindings set to them by default.\n\nYou can also customize them right from there, either by clicking on the pencil\nicon that appears when you hover over a particular action, by double-clicking on\nthe action row, or by pressing the `enter` key.\n\nAnything that you end up doing on the keymap editor also gets reflected on the\n`keymap.jsonc` file.\n\n## User Keymaps\n\nThe keymap file is stored in the following locations for each platform:\n\n- macOS/Linux: `~/.config/monyacode/keymap.jsonc`\n- Windows: `~\\AppData\\Roaming\\MonyaCode/keymap.jsonc`\n\nYou can open the keymap with the {#action monyacode::OpenKeymapFile} action from the\ncommand palette.\n\nThis file contains a JSON array of objects with `\"bindings\"`. If no `\"context\"`\nis set, the bindings are always active. If it is set, the binding is only active\nwhen the [context matches](#contexts).\n\nWithin each binding section, a [key sequence](#keybinding-syntax) is mapped to\n[an action](#actions). If conflicts are detected, they are resolved as\n[described below](#precedence).\n\nIf you are using a non-QWERTY, Latin-character keyboard, you may want to set\n`use_key_equivalents` to `true`. See\n[Non-QWERTY keyboards](#non-qwerty-keyboards) for more information.\n\nFor example:\n\n```json\n[\n  {\n    \"bindings\": {\n      \"ctrl-right\": \"editor::SelectLargerSyntaxNode\",\n      \"ctrl-left\": \"editor::SelectSmallerSyntaxNode\",\n    },\n  },\n  {\n    \"context\": \"ProjectPanel && not_editing\",\n    \"bindings\": {\n      \"o\": \"project_panel::Open\",\n    },\n  },\n]\n```\n\nIf you want to debug problems with custom keymaps, you can use\n`dev: Open Key Context View` from the command palette.\n\n### Keybinding Syntax\n\nMonyaCode has the ability to match against not just a single keypress, but a sequence\nof keys typed in order. Each key in the `\"bindings\"` map is a sequence of\nkeypresses separated with a space.\n\nEach keypress is a sequence of modifiers followed by a key. The modifiers are:\n\n- `ctrl-` The control key\n- `cmd-`, `win-` or `super-` for the platform modifier (Command on macOS,\n  Windows key on Windows, and the Super key on Linux).\n- `alt-` for alt (option on macOS)\n- `shift-` The shift key\n- `fn-` The function key\n- `secondary-` Equivalent to `cmd` when MonyaCode is running on macOS and `ctrl` when\n  on Windows and Linux\n\nThe keys can be any single Unicode codepoint that your keyboard generates (for\nexample `a`, `0`, `£` or `ç`), or any named key (`tab`, `f1`, `shift`, or\n`cmd`). If you are using a non-Latin layout (e.g. Cyrillic), you can bind either\nto the Cyrillic character or the Latin character that key generates with `cmd`\npressed.\n\nA few examples:\n\n```json\n \"bindings\": {\n   \"cmd-k cmd-s\": \"monyacode::OpenKeymap\", // matches ⌘-k then ⌘-s\n   \"space e\": \"editor::Complete\", // type space then e\n   \"ç\": \"editor::Complete\", // matches ⌥-c\n   \"shift shift\": \"file_finder::Toggle\", // matches pressing and releasing shift twice\n }\n```\n\nThe `shift-` modifier can only be used in combination with a letter to indicate\nthe uppercase version. For example, `shift-g` matches typing `G`. Although on\nmany keyboards shift is used to type punctuation characters like `(`, the\nkeypress is not considered to be modified, and so `shift-(` does not match.\n\nThe `alt-` modifier can be used on many layouts to generate a different key. For\nexample, on a macOS US keyboard, the combination `alt-c` types `ç`. You can\nmatch against either in your keymap file, though by convention, MonyaCode spells this\ncombination as `alt-c`.\n\nIt is possible to match against typing a modifier key on its own. For example,\n`shift shift` can be used to implement JetBrains' 'Search Everywhere' shortcut.\nIn this case, the binding happens on key release instead of on keypress.\n\n### Contexts\n\nIf a binding group has a `\"context\"` key, it will be matched against the\ncurrently active contexts in MonyaCode.\n\nMonyaCode's contexts make up a tree, with the root being `Workspace`. Workspaces\ncontain Panes and Panels, and Panes contain Editors, etc. The easiest way to see\nwhat contexts are active at a given moment is the key context view, which you\ncan get to with the `dev: open key context view` command in the command palette.\n\nFor example:\n\n```\n# in an editor, it might look like this:\nWorkspace os=macos keyboard_layout=com.apple.keylayout.QWERTY\n  Pane\n    Editor mode=full extension=md vim_mode=insert\n\n# in the project panel\nWorkspace os=macos\n  Dock\n    ProjectPanel not_editing\n```\n\nContext expressions can contain the following syntax:\n\n- `X && Y`, `X || Y` to and/or two conditions\n- `!X` to check that a condition is false\n- `(X)` for grouping\n- `X > Y` to match if an ancestor in the tree matches X and this layer matches\n  Y.\n\nFor example:\n\n- `\"context\": \"Editor\"` - matches any editor (including inline inputs)\n- `\"context\": \"Editor && mode == full\"` - matches the main editors used for\n  editing code\n- `\"context\": \"!Editor && !Terminal\"` - matches anywhere except where an Editor\n  or Terminal is focused\n- `\"context\": \"os == macos > Editor\"` - matches any editor on macOS.\n\nIt's worth noting that attributes are only available on the node they are\ndefined on. This means that if you want to (for example) only enable a\nkeybinding when the debugger is stopped in vim normal mode, you need to do\n`debugger_stopped > vim_mode == normal`.\n\n> Note: Before MonyaCode v0.197.x, the `!` operator only looked at one node at a\n> time, and `>` meant \"parent\" not \"ancestor\". This meant that `!Editor` would\n> match the context `Workspace > Pane > Editor`, because (confusingly) the Pane\n> matches `!Editor`, and that `os == macos > Editor` did not match the context\n> `Workspace > Pane > Editor` because of the intermediate `Pane` node.\n\nIf you're using Vim mode, we have information on how\n[vim modes influence the context](./vim.md#contexts). Helix mode is built on top\nof Vim mode and uses the same contexts.\n\n### Actions\n\nAlmost all of MonyaCode's functionality is exposed as actions.\n\nMost actions do not require any arguments, and so you can bind them as strings:\n`\"ctrl-a\": \"language_selector::Toggle\"`. Some require a single argument and must\nbe bound as an array: `\"cmd-1\": [\"workspace::ActivatePane\", 0]`. Some actions\nrequire multiple arguments and are bound as an array of a string and an object:\n`\"ctrl-a\": [\"pane::DeploySearch\", { \"replace_enabled\": true }]`.\n\n### Precedence\n\nWhen multiple keybindings have the same keystroke and are active at the same\ntime, precedence is resolved in two ways:\n\n- Bindings that match on lower nodes in the context tree win. This means that if\n  you have a binding with a context of `Editor`, it will take precedence over a\n  binding with a context of `Workspace`. Bindings with no context match at the\n  lowest level in the tree.\n- If there are multiple bindings that match at the same level in the tree, then\n  the binding defined later takes precedence. As user keybindings are loaded\n  after system keybindings, this allows user bindings to take precedence over\n  built-in keybindings.\n\nThe other kind of conflict that arises is when you have two bindings, one of\nwhich is a prefix of the other. For example, if you have\n`\"ctrl-w\":\"editor::DeleteToNextWordEnd\"` and\n`\"ctrl-w left\":\"editor::DeleteToEndOfLine\"`.\n\nWhen this happens, and both bindings are active in the current context, MonyaCode\nwill wait for 1 second after you type `ctrl-w` to see if you're about to type\n`left`. If you don't type anything, or if you type a different key, then\n`DeleteToNextWordEnd` will be triggered. If you do, then `DeleteToEndOfLine`\nwill be triggered.\n\n### Non-QWERTY keyboards\n\nMonyaCode's support for non-QWERTY keyboards is still a work in progress.\n\nIf your keyboard can type the full ASCII range (DVORAK, COLEMAK, etc.), then\nshortcuts should work as you expect.\n\nOtherwise, read on...\n\n#### macOS\n\nOn Cyrillic, Hebrew, Armenian, and other keyboards that are mostly non-ASCII,\nmacOS automatically maps keys to the ASCII range when `cmd` is held. MonyaCode takes\nthis a step further, and it can always match key-presses against either the\nASCII layout or the real layout, regardless of modifiers and the\n`use_key_equivalents` setting. For example, in Thai, pressing `ctrl-ๆ` will\nmatch bindings associated with `ctrl-q` or `ctrl-ๆ`.\n\nOn keyboards that support extended Latin alphabets (French AZERTY, German\nQWERTZ, etc.), it is often not possible to type the entire ASCII range without\n`option`. This introduces an ambiguity: `option-2` produces `@`. To ensure that\nall the built-in keyboard shortcuts can still be typed on these keyboards, we\nmove key bindings around. For example, shortcuts bound to `@` on QWERTY are\nmoved to `\"` on a Spanish layout. This mapping is based on the macOS system\ndefaults and can be seen by running `dev: open key context view` from the\ncommand palette.\n\nIf you are defining shortcuts in your personal keymap, you can opt into the key\nequivalent mapping by setting `use_key_equivalents` to `true` in your keymap:\n\n```json\n[\n  {\n    \"use_key_equivalents\": true,\n    \"bindings\": {\n      \"ctrl->\": \"editor::Indent\", // parsed as ctrl-: when a German QWERTZ keyboard is active\n    },\n  },\n]\n```\n\n### Linux\n\nSince v0.196.0, on Linux, if the key that you type doesn't produce an ASCII\ncharacter, then we use the QWERTY-layout equivalent key for keyboard shortcuts.\nThis means that many shortcuts can be typed on many layouts.\n\nWe do not yet move shortcuts around to ensure that all the built-in shortcuts\ncan be typed on every layout, so if there are some ASCII characters that cannot\nbe typed, and your keyboard layout has different ASCII characters on the same\nkeys as would be needed to type them, you may need to add custom key bindings to\nmake this work. We do intend to fix this at some point, and help is very much\nappreciated!\n\n## Tips and tricks\n\n### Disabling a binding\n\nIf you'd like a given binding to do nothing in a given context, you can use\n`null` as the action. This is useful if you hit the key binding by accident and\nwant to disable it, or if you want to type the character that would be typed by\nthe sequence, or if you want to disable multikey bindings starting with that\nkey.\n\n```json\n[\n  {\n    \"context\": \"Workspace\",\n    \"bindings\": {\n      \"cmd-r\": null, // cmd-r will do nothing when the Workspace context is active\n    },\n  },\n]\n```\n\nA `null` binding follows the same precedence rules as normal actions, so it\ndisables all bindings that would match further up in the tree too. If you'd like\na binding that matches further up in the tree to take precedence over a lower\nbinding, you need to rebind it to the action you want in the context you want.\n\nThis is useful for preventing MonyaCode from falling back to a default key binding\nwhen the action you specified is conditional and propagates. For example,\n`buffer_search::DeployReplace` only triggers when the search bar is not in view.\nIf the search bar is in view, it would propagate and trigger the default action\nset for that key binding, such as opening the right dock. To prevent this from\nhappening:\n\n```json\n[\n  {\n    \"context\": \"Workspace\",\n    \"bindings\": {\n      \"cmd-r\": null, // cmd-r will do nothing when the search bar is in view\n    },\n  },\n  {\n    \"context\": \"Workspace\",\n    \"bindings\": {\n      \"cmd-r\": \"buffer_search::DeployReplace\", // cmd-r will deploy replace when the search bar is not in view\n    },\n  },\n]\n```\n\n### Remapping keys\n\nA common request is to be able to map from a single keystroke to a sequence. You\ncan do this with the `workspace::SendKeystrokes` action.\n\n```json\n[\n  {\n    \"bindings\": {\n      // Move down four times\n      \"alt-down\": [\"workspace::SendKeystrokes\", \"down down down down\"],\n      // Expand the selection (editor::SelectLargerSyntaxNode);\n      // copy to the clipboard; and then undo the selection expansion.\n      \"cmd-alt-c\": [\n        \"workspace::SendKeystrokes\",\n        \"ctrl-shift-right ctrl-shift-right ctrl-shift-right cmd-c ctrl-shift-left ctrl-shift-left ctrl-shift-left\",\n      ],\n    },\n  },\n  {\n    \"context\": \"Editor && vim_mode == insert\",\n    \"bindings\": {\n      \"j k\": [\"workspace::SendKeystrokes\", \"escape\"],\n    },\n  },\n]\n```\n\nThere are some limitations to this, notably:\n\n- Any asynchronous operation will not happen until after all your key bindings\n  have been dispatched. For example, this means that while you can use a binding\n  to open a file (as in the `cmd-alt-r` example), you cannot send further\n  keystrokes and hope to have them interpreted by the new view.\n- Other examples of asynchronous things are: opening the command palette,\n  communicating with a language server, changing the language of a buffer,\n  anything that hits the network.\n- There is a limit of 100 simulated keys at a time.\n\nThe argument to `SendKeystrokes` is a space-separated list of keystrokes (using\nthe same syntax as above). Due to the way that keystrokes are parsed, any\nsegment that is not recognized as a keypress will be sent verbatim to the\ncurrently focused input field.\n\nIf the argument to `SendKeystrokes` contains the binding used to trigger it, it\nwill use the next-highest-precedence definition of that binding. This allows you\nto extend the default behavior of a key binding.\n\n### Forward keys to terminal\n\nIf you're on Linux or Windows, you might find yourself wanting to forward key\ncombinations to the built-in terminal instead of them being handled by MonyaCode.\n\nFor example, `ctrl-n` creates a new tab in MonyaCode on Linux. If you want to send\n`ctrl-n` to the built-in terminal when it's focused, add the following to your\nkeymap:\n\n```json\n{\n  \"context\": \"Terminal\",\n  \"bindings\": {\n    \"ctrl-n\": [\"terminal::SendKeystroke\", \"ctrl-n\"],\n  },\n}\n```\n\n### Task Key bindings\n\nYou can also bind keys to launch MonyaCode Tasks defined in your `tasks.jsonc`. See\nthe [tasks documentation](./tasks.md#custom-keybindings-for-tasks) for more.\n"},{"path":"language-servers.md","title":"Language Servers","group":"Guides","content":"# Language Servers\n\nMonyaCode relies on _Language Servers_ to provide semantic information about\ndifferent programming languages. Each language has its own set of language\nservers. A language server is a separate program which MonyaCode communicates with\nusing the _Language Server Protocol_. Many code editors speak the LSP these days\nand so different editors can provide advanced language features like\nautocompletion without having to implement them independently.\n\nIf MonyaCode knows about a particular language, either natively or via an extension,\nit can use any language servers it finds already installed on the system.\n\nMonyaCode can also download and install language servers. The Zed editor will do this\nwithout asking, but MonyaCode tries not to do anything like downloading binaries from\nthe internet without explicit permission to do so.\n\nOnce MonyaCode has failed to launch a language server, an error will be reported and\nthe bot icon in the lower left of the editor window will have a red indicator.\nClick that icon and then choose `Configure Servers`. Alternatively use the\nCommand Palette to run the {#action lsp::OpenLanguageServerConfig} command.\n\nIn this view, you can see all the loaded language servers and choose whether to\ninstall any missing language servers yourself or let the editor try to install\nthem for you.\n"},{"path":"languages/ansible.md","title":"Ansible","group":"Languages","content":"# Ansible\n\nSupport for Ansible in MonyaCode is provided via a community-maintained\n[Ansible extension](https://github.com/kartikvashistha/zed-ansible).\n\n- Tree-sitter:\n  [zed-industries/tree-sitter-yaml](https://github.com/zed-industries/tree-sitter-yaml)\n- Language Server:\n  [ansible/vscode-ansible](https://github.com/ansible/vscode-ansible/tree/main/packages/ansible-language-server)\n\n## Setup\n\n### File detection\n\nTo avoid mishandling non-Ansible YAML files, the Ansible Language is not\nassociated with any file extensions by default. To change this behavior you can\nadd a `\"file_types\"` section to MonyaCode settings inside your project\n(`.monyacode/settings.jsonc`) or your MonyaCode user settings\n(`~/.config/monyacode/settings.jsonc`) to match your folder/naming conventions. For\nexample:\n\n```json\n\"file_types\": {\n    \"Ansible\": [\n      \"**.ansible.yml\",\n      \"**.ansible.yaml\",\n      \"**/defaults/*.yml\",\n      \"**/defaults/*.yaml\",\n      \"**/meta/*.yml\",\n      \"**/meta/*.yaml\",\n      \"**/tasks/*.yml\",\n      \"**/tasks/*.yaml\",\n      \"**/handlers/*.yml\",\n      \"**/handlers/*.yaml\",\n      \"**/group_vars/*.yml\",\n      \"**/group_vars/*.yaml\",\n      \"**/host_vars/*.yml\",\n      \"**/host_vars/*.yaml\",\n      \"**/playbooks/*.yml\",\n      \"**/playbooks/*.yaml\",\n      \"**playbook*.yml\",\n      \"**playbook*.yaml\"\n    ]\n  }\n```\n\nFeel free to modify this list as per your needs.\n\n#### Inventory\n\nIf your inventory file is in the YAML format, you can either:\n\n- Append the `ansible-lint` inventory json schema to it via the following\n  comment at the top of your inventory file:\n\n```yml\n# yaml-language-server: $schema=https://raw.githubusercontent.com/ansible/ansible-lint/main/src/ansiblelint/schemas/inventory.json\n```\n\n- Or configure the yaml language server settings to set this schema for all your\n  inventory files, that match your inventory pattern, under your MonyaCode settings\n  ([ref](./yaml.md)):\n\n```json\n\"lsp\": {\n    \"yaml-language-server\": {\n      \"settings\": {\n        \"yaml\": {\n          \"schemas\": {\n            \"https://raw.githubusercontent.com/ansible/ansible-lint/main/src/ansiblelint/schemas/inventory.json\": [\n              \"./inventory/*.yaml\",\n              \"hosts.yml\",\n            ]\n          }\n        }\n      }\n    }\n},\n```\n\n### LSP Configuration\n\nBy default, the following default config is passed to the Ansible language\nserver. It conveniently mirrors the defaults set by\n[nvim-lspconfig](https://github.com/neovim/nvim-lspconfig/blob/03bc581e05e81d33808b42b2d7e76d70adb3b595/lua/lspconfig/configs/ansiblels.lua)\nfor the Ansible language server:\n\n```json\n{\n  \"ansible\": {\n    \"ansible\": {\n      \"path\": \"ansible\",\n    },\n    \"executionEnvironment\": {\n      \"enabled\": false,\n    },\n    \"python\": {\n      \"interpreterPath\": \"python3\",\n    },\n    \"validation\": {\n      \"enabled\": true,\n      \"lint\": {\n        \"enabled\": true,\n        \"path\": \"ansible-lint\",\n      },\n    },\n  },\n}\n```\n\n> [!NOTE] In order for linting to work, ensure that `ansible-lint` is installed\n> and discoverable on your PATH\n\nWhen desired, any of the above default settings can be overridden under the\n`\"lsp\"` section of your MonyaCode settings file. For example:\n\n```json\n\"lsp\": {\n  // Note, the MonyaCode Ansible extension prefixes all settings with `ansible`\n  // so instead of using `ansible.ansible.path` use `ansible.path`.\n  \"ansible-language-server\": {\n    \"settings\": {\n      \"ansible\": {\n        \"path\": \"ansible\"\n      },\n      \"executionEnvironment\": {\n        \"enabled\": false\n      },\n      \"python\": {\n        \"interpreterPath\": \"python3\"\n      },\n      \"validation\": {\n        \"enabled\": false, // disable validation\n        \"lint\": {\n          \"enabled\": false, // disable ansible-lint\n          \"path\": \"ansible-lint\"\n        }\n      }\n    }\n  }\n}\n```\n\nA full list of options/settings, that can be passed to the server, can be found\nat the project's page\n[here](https://github.com/ansible/vscode-ansible/blob/5a89836d66d470fb9d20e7ea8aa2af96f12f61fb/docs/als/settings.md).\nFeel free to modify option values as needed.\n"},{"path":"languages/asciidoc.md","title":"AsciiDoc","group":"Languages","content":"# AsciiDoc\n\nAsciiDoc language support in MonyaCode is provided by the community-maintained\n[AsciiDoc extension](https://github.com/andreicek/zed-asciidoc). Report issues\nto:\n[https://github.com/andreicek/zed-asciidoc/issues](https://github.com/andreicek/zed-asciidoc/issues)\n\n- Tree-sitter:\n  [cathaysia/tree-sitter-asciidoc](https://github.com/cathaysia/tree-sitter-asciidoc)\n"},{"path":"languages/astro.md","title":"Astro","group":"Languages","content":"# Astro\n\nAstro support is available through the\n[Astro extension](https://github.com/zed-extensions/astro).\n\n- Tree-sitter:\n  [virchau13/tree-sitter-astro](https://github.com/virchau13/tree-sitter-astro)\n- Language Server:\n  [withastro/language-tools](https://github.com/withastro/language-tools)\n\n<!--\nTBD: Documentation Astro usage / configuration\n-->\n"},{"path":"languages/bash.md","title":"Bash","group":"Languages","content":"# Bash\n\nBash language support is provided natively by MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-bash](https://github.com/tree-sitter/tree-sitter-bash)\n- Language Server:\n  [bash-lsp/bash-language-server](https://github.com/bash-lsp/bash-language-server)\n\n## Configuration\n\n### `shellcheck`\n\n`bash-language-server` supports `shellcheck` and will make use of it\nautomatically, if installed. This must be done manually by the user.\n\n```sh\nbrew install shellcheck             # macOS (HomeBrew)\napt install shellcheck              # Ubuntu / Debian\npacman -S shellcheck                # Arch Linux\ndnf install shellcheck              # Fedora\nyum install shellcheck              # CentOS/RHEL\nzypper install shellcheck           # openSUSE\nchoco install shellcheck            # Windows (Chocolatey)\n```\n\nAnd verify it is available from your path:\n\n```sh\nwhich shellcheck\nshellcheck --version\n```\n\nIf you wish to customize the warnings/errors reported you just need to create a\n`.shellcheckrc` file. You can do this in the root of your project or in your\nhome directory (`~/.shellcheckrc`). See:\n[shellcheck documentation](https://github.com/koalaman/shellcheck/wiki/Ignore#ignoring-one-or-more-types-of-errors-forever)\nfor more.\n\n### `smfmt`\n\nSimilarly, `bash-language-server` will make automatic use of `shfmt` for\nformatting, if it is available in `$PATH`. Install it with:\n\n```sh\nbrew install shfmt          # MacOS (HomeBrew)\napt install shfmt           # Ubuntu / Debian\npacman -S shfmt             # Arch Linux\nchoco install shfmt         # Windows (chocolatey)\n```\n\nTo control automatic formatting you can use `format_on_save` in your MonyaCode\nsettings or manually invoke `editor: format document` from the command palette.\n\n### See also:\n\n- [MonyaCode Docs: Language Support: Shell Scripts](./sh.md)\n"},{"path":"languages/biome.md","title":"Biome","group":"Languages","content":"# Biome\n\n[Biome](https://biomejs.dev/) support in MonyaCode is provided by the\ncommunity-maintained [Biome extension](https://github.com/biomejs/biome-zed).\nReport issues to:\n[https://github.com/biomejs/biome-zed/issues](https://github.com/biomejs/biome-zed/issues)\n\n- Language Server: [biomejs/biome](https://github.com/biomejs/biome)\n\n## Biome Language Support\n\nThe Biome extension includes support for the following languages:\n\n- JavaScript\n- TypeScript\n- JSX\n- TSX\n- JSON\n- JSONC\n- Vue.js\n- Astro\n- Svelte\n- CSS\n\n## Configuration\n\nBy default, the `biome.json` file is required to be in the root of the\nworkspace.\n\n```json\n{\n  \"$schema\": \"https://biomejs.dev/schemas/1.8.3/schema.json\",\n}\n```\n\nFor a full list of `biome.json` options see\n[Biome Configuration](https://biomejs.dev/reference/configuration/)\ndocumentation.\n\nSee the [Biome MonyaCode Extension README](https://github.com/biomejs/biome-zed) for\na complete list of features and configuration options.\n"},{"path":"languages/c.md","title":"C","group":"Languages","content":"# C\n\nC support is available natively in MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-c](https://github.com/tree-sitter/tree-sitter-c)\n- Language Server: [clangd/clangd](https://github.com/clangd/clangd)\n- Debug Adapter: [CodeLLDB](https://github.com/vadimcn) (primary),\n  [GDB](https://sourceware.org/gdb/) (secondary, not available on Apple silicon)\n\n## Clangd: Force detect as C\n\nClangd out of the box assumes mixed C++/C projects. If you have a C-only project\nyou may wish to instruct clangd to treat all files as C using the `-xc` flag. To\ndo this, create a `.clangd` file in the root of your project with the following:\n\n```yaml\n# yaml-language-server: $schema=https://json.schemastore.org/clangd.json\nCompileFlags:\n  Add: [-xc]\n```\n\nBy default clang and gcc will recognize `*.C` and `*.H` (uppercase extensions)\nas C++ and not C and so MonyaCode too follows this convention. If you are working\nwith a C-only project (perhaps one with legacy uppercase pathing like\n`FILENAME.C`) you can override this behavior by adding this to your settings:\n\n```json\n{\n  \"file_types\": {\n    \"C\": [\"C\", \"H\"],\n  },\n}\n```\n\n## Formatting\n\nBy default the editor will use the `clangd` language server for formatting C\ncode like the `clang-format` CLI tool. To configure this you can add a\n`.clang-format` file. For example:\n\n```yaml\n# yaml-language-server: $schema=https://json.schemastore.org/clang-format-21.x.json\n---\nBasedOnStyle: GNU\nIndentWidth: 2\n---\n```\n\nSee\n[Clang-Format Style Options](https://clang.llvm.org/docs/ClangFormatStyleOptions.html)\nfor a complete list of options.\n\nYou can trigger formatting via {#kb editor::Format} or the `editor: format`\naction from the command palette or by adding `format_on_save` to your MonyaCode\nsettings:\n\n```json\n  \"languages\": {\n    \"C\": {\n      \"format_on_save\": \"on\",\n      \"tab_size\": 2\n    }\n  }\n```\n\n## Compile Commands\n\nFor some projects Clangd requires a `compile_commands.json` file to properly\nanalyze your project. This file contains the compilation database that tells\nclangd how your project should be built.\n\n### CMake Compile Commands\n\nWith CMake, you can generate `compile_commands.json` automatically by adding the\nfollowing line to your `CMakeLists.txt`:\n\n```cmake\nset(CMAKE_EXPORT_COMPILE_COMMANDS ON)\n```\n\nAfter building your project, CMake will generate the `compile_commands.json`\nfile in the build directory and clangd will automatically pick it up.\n\n## Debugging\n\nYou can use CodeLLDB or GDB to debug native binaries. (Make sure that your build\nprocess passes `-g` to the C compiler, so that debug information is included in\nthe resulting binary.) See below for examples of debug configurations that you\ncan add to `.monyacode/debug.jsonc`.\n\n- [CodeLLDB configuration documentation](https://github.com/vadimcn/codelldb/blob/master/MANUAL.md#starting-a-new-debug-session)\n- [GDB configuration documentation](https://sourceware.org/gdb/current/onlinedocs/gdb.html/Debugger-Adapter-Protocol.html)\n\n### Build and Debug Binary\n\n```json [debug]\n[\n  {\n    \"label\": \"Debug native binary\",\n    \"build\": {\n      \"command\": \"make\",\n      \"args\": [\"-j8\"],\n      \"cwd\": \"$MONYACODE_WORKTREE_ROOT\"\n    },\n    \"program\": \"$MONYACODE_WORKTREE_ROOT/build/prog\",\n    \"request\": \"launch\",\n    \"adapter\": \"CodeLLDB\"\n  }\n]\n```\n"},{"path":"languages/clojure.md","title":"Clojure","group":"Languages","content":"# Clojure\n\nClojure support is available through the\n[Clojure extension](https://github.com/zed-extensions/clojure).\n\n- Tree-sitter:\n  [prcastro/tree-sitter-clojure](https://github.com/prcastro/tree-sitter-clojure)\n- Language Server:\n  [clojure-lsp/clojure-lsp](https://github.com/clojure-lsp/clojure-lsp)\n\n<!--\nTBD: Add some Clojure Docs\n-->\n"},{"path":"languages/cpp.md","title":"C++","group":"Languages","content":"# C++\n\nC++ support is available natively in MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-cpp](https://github.com/tree-sitter/tree-sitter-cpp)\n- Language Server: [clangd/clangd](https://github.com/clangd/clangd)\n\n## Binary\n\nYou can configure which `clangd` binary MonyaCode should use.\n\nBy default, MonyaCode will try to find a `clangd` in your `$PATH` and try to use\nthat. If that binary successfully executes, it's used. Otherwise, MonyaCode will fall\nback to installing its own `clangd` version and use that.\n\nIf you want to install a pre-release `clangd` version instead you can instruct\nMonyaCode to do so by setting `pre_release` to `true` in your `settings.jsonc`:\n\n```json\n{\n  \"lsp\": {\n    \"clangd\": {\n      \"fetch\": {\n        \"pre_release\": true,\n      },\n    },\n  },\n}\n```\n\nIf you want to disable MonyaCode looking for a `clangd` binary, you can set\n`ignore_system_version` to `true` in your `settings.jsonc`:\n\n```json\n{\n  \"lsp\": {\n    \"clangd\": {\n      \"binary\": {\n        \"ignore_system_version\": true,\n      },\n    },\n  },\n}\n```\n\nIf you want to use a binary in a custom location, you can specify a `path` and\noptional `arguments`:\n\n```json\n{\n  \"lsp\": {\n    \"clangd\": {\n      \"binary\": {\n        \"path\": \"/path/to/clangd\",\n        \"arguments\": [],\n      },\n    },\n  },\n}\n```\n\nThis `\"path\"` has to be an absolute path.\n\n## Arguments\n\nYou can pass any number of arguments to clangd. To see a full set of available\noptions, run `clangd --help` from the command line. For example with\n`--function-arg-placeholders=0` completions contain only parentheses for\nfunction calls, while the default (`--function-arg-placeholders=1`) completions\nalso contain placeholders for method parameters.\n\n```json\n{\n  \"lsp\": {\n    \"clangd\": {\n      \"binary\": {\n        \"path\": \"/path/to/clangd\",\n        \"arguments\": [\"--function-arg-placeholders=0\"],\n      },\n    },\n  },\n}\n```\n\n## Formatting\n\nBy default MonyaCode will use the `clangd` language server for formatting C++ code.\nThe Clangd is the same as the `clang-format` CLI tool. To configure this you can\nadd a `.clang-format` file. For example:\n\n```yaml\n# yaml-language-server: $schema=https://json.schemastore.org/clang-format-21.x.json\n---\nBasedOnStyle: LLVM\nIndentWidth: 4\n---\nLanguage: Cpp\n# Force pointers to the type for C++.\nDerivePointerAlignment: false\nPointerAlignment: Left\n---\n```\n\nSee\n[Clang-Format Style Options](https://clang.llvm.org/docs/ClangFormatStyleOptions.html)\nfor a complete list of options.\n\nYou can trigger formatting via {#kb editor::Format} or the `editor: format`\naction from the command palette or by adding `format_on_save` to your MonyaCode\nsettings:\n\n```json\n  \"languages\": {\n    \"C++\": {\n      \"format_on_save\": \"on\",\n      \"tab_size\": 2\n    }\n  }\n```\n\n## More server configuration\n\nIn the root of your project, it is generally common to create a `.clangd` file\nto set extra configuration.\n\n```yaml\n# yaml-language-server: $schema=https://json.schemastore.org/clangd.json\nCompileFlags:\n  Add:\n    - \"--include-directory=/path/to/include\"\nDiagnostics:\n  MissingIncludes: Strict\n  UnusedIncludes: Strict\n```\n\nFor more advanced usage of clangd configuration file, take a look into their\n[official page](https://clangd.llvm.org/config.html).\n\n## Compile Commands\n\nFor some projects Clangd requires a `compile_commands.json` file to properly\nanalyze your project. This file contains the compilation database that tells\nclangd how your project should be built.\n\n### CMake Compile Commands\n\nWith CMake, you can generate `compile_commands.json` automatically by adding the\nfollowing line to your `CMakeLists.txt`:\n\n```cmake\nset(CMAKE_EXPORT_COMPILE_COMMANDS ON)\n```\n\nAfter building your project, CMake will generate the `compile_commands.json`\nfile in the build directory and clangd will automatically pick it up.\n\n## Debugging\n\nYou can use CodeLLDB or GDB to debug native binaries. (Make sure that your build\nprocess passes `-g` to the C++ compiler, so that debug information is included\nin the resulting binary.) See below for examples of debug configurations that\nyou can add to `.monyacode/debug.json`.\n\n- [CodeLLDB configuration documentation](https://github.com/vadimcn/codelldb/blob/master/MANUAL.md#starting-a-new-debug-session)\n- [GDB configuration documentation](https://sourceware.org/gdb/current/onlinedocs/gdb.html/Debugger-Adapter-Protocol.html)\n  - GDB needs to be at least v14.1\n\n### Build and Debug Binary\n\n```json [debug]\n[\n  {\n    \"label\": \"Debug native binary\",\n    \"build\": {\n      \"command\": \"make\",\n      \"args\": [\"-j8\"],\n      \"cwd\": \"$MONYACODE_WORKTREE_ROOT\"\n    },\n    \"program\": \"$MONYACODE_WORKTREE_ROOT/build/prog\",\n    \"request\": \"launch\",\n    \"adapter\": \"CodeLLDB\"\n  }\n]\n```\n\n## Protocol Extensions\n\nZed currently implements the following `clangd`\n[extensions](https://clangd.llvm.org/extensions):\n\n### Inactive Regions\n\nAutomatically dims inactive sections of code due to preprocessor directives,\nsuch as `#if`, `#ifdef`, or `#ifndef` blocks that evaluate to false.\n\n### Switch Between Source and Header Files\n\nAllows switching between corresponding C++ source files (e.g., `.cpp`) and\nheader files (e.g., `.h`). by running the command {#action\neditor::SwitchSourceHeader} from the command palette or by setting a keybinding\nfor the `editor::SwitchSourceHeader` action.\n\n```json\n{\n  \"context\": \"Editor\",\n  \"bindings\": {\n    \"alt-enter\": \"editor::SwitchSourceHeader\",\n  },\n}\n```\n"},{"path":"languages/csharp.md","title":"C","group":"Languages","content":"# C#\n\nNote language name is \"CSharp\" for settings not \"C#'\n\nC# support is available through the\n[C# extension](https://github.com/zed-extensions/csharp).\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-c-sharp](https://github.com/tree-sitter/tree-sitter-c-sharp)\n- Language Server:\n  [OmniSharp/omnisharp-roslyn](https://github.com/OmniSharp/omnisharp-roslyn)\n\n## Configuration\n\nThe `OmniSharp` binary can be configured in a MonyaCode settings file with:\n\n```json\n{\n  \"lsp\": {\n    \"omnisharp\": {\n      \"binary\": {\n        \"path\": \"/path/to/OmniSharp\",\n        \"arguments\": [\"optional\", \"additional\", \"args\", \"-lsp\"],\n      },\n    },\n  },\n}\n```\n"},{"path":"languages/css.md","title":"CSS","group":"Languages","content":"# CSS\n\nMonyaCode has built-in support for CSS.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-css](https://github.com/tree-sitter/tree-sitter-css)\n- Language Servers:\n  - [microsoft/vscode-css-languageservice](https://github.com/microsoft/vscode-css-languageservice)\n  - [tailwindcss-language-server](https://github.com/tailwindlabs/tailwindcss-intellisense)\n\n## Tailwind CSS\n\nMonyaCode also supports [Tailwind CSS](./tailwindcss.md) out-of-the-box for languages\nand frameworks like JavaScript, Astro, Svelte, and more.\n\n<!-- TBD: Document CS -->\n\n## Recommended Reading\n\n- [HTML](./html.md)\n- [TypeScript](./typescript.md)\n- [JavaScript](./javascript.md)\n"},{"path":"languages/dart.md","title":"Dart","group":"Languages","content":"# Dart\n\nDart support is available through the\n[Dart extension](https://github.com/zed-extensions/dart).\n\n- Tree-sitter:\n  [UserNobody14/tree-sitter-dart](https://github.com/UserNobody14/tree-sitter-dart)\n- Language Server: [dart language-server](https://github.com/dart-lang/sdk)\n\n## Pre-requisites\n\nYou will need to install the Dart SDK.\n\nYou can install dart from [dart.dev/get-dart](https://dart.dev/get-dart) or via\nthe\n[Flutter Version Management CLI (fvm)](https://fvm.app/documentation/getting-started/installation)\n\n## Configuration\n\nThe dart extension requires no configuration if you have `dart` in your path:\n\n```sh\nwhich dart\ndart --version\n```\n\nIf you would like to use a specific dart binary or use dart via FVM you can\nspecify the `dart` binary in your MonyaCode settings.jsonc file:\n\n```json\n{\n  \"lsp\": {\n    \"dart\": {\n      \"binary\": {\n        \"path\": \"/opt/homebrew/bin/fvm\",\n        \"arguments\": [\"dart\", \"language-server\", \"--protocol=lsp\"],\n      },\n    },\n  },\n}\n```\n\n### Formatting\n\nDart by-default uses a very conservative maximum line length (80). If you would\nlike the dart LSP to permit a longer line length when auto-formatting, add the\nfollowing to your MonyaCode settings.jsonc:\n\n```json\n{\n  \"lsp\": {\n    \"dart\": {\n      \"settings\": {\n        \"lineLength\": 140,\n      },\n    },\n  },\n}\n```\n\nPlease see the Dart documentation for more information on\n[dart language-server capabilities](https://github.com/dart-lang/sdk/blob/main/pkg/analysis_server/tool/lsp_spec/README.md).\n"},{"path":"languages/deno.md","title":"Deno","group":"Languages","content":"# Deno\n\nDeno support is available through the\n[Deno extension](https://github.com/zed-extensions/deno).\n\n- Language server:\n  [Deno Language Server](https://docs.deno.com/runtime/manual/advanced/language_server/overview/)\n\n## Deno Configuration\n\nTo use the Deno Language Server with TypeScript and TSX files, you will likely\nwish to disable the default language servers and enable deno by adding the\nfollowing to your `settings.jsonc`:\n\n```json\n{\n  \"lsp\": {\n    \"deno\": {\n      \"settings\": {\n        \"deno\": {\n          \"enable\": true,\n        },\n      },\n    },\n  },\n  \"languages\": {\n    \"JavaScript\": {\n      \"language_servers\": [\n        \"deno\",\n        \"!typescript-language-server\",\n        \"!vtsls\",\n        \"!eslint\",\n      ],\n      \"formatter\": \"language_server\",\n    },\n    \"TypeScript\": {\n      \"language_servers\": [\n        \"deno\",\n        \"!typescript-language-server\",\n        \"!vtsls\",\n        \"!eslint\",\n      ],\n      \"formatter\": \"language_server\",\n    },\n    \"TSX\": {\n      \"language_servers\": [\n        \"deno\",\n        \"!typescript-language-server\",\n        \"!vtsls\",\n        \"!eslint\",\n      ],\n      \"formatter\": \"language_server\",\n    },\n  },\n}\n```\n\nSee [Configuring supported languages](../configuring-languages.md) in the MonyaCode\ndocumentation for more information.\n\n## Configuration completion\n\nTo get completions for `deno.json` or `package.json` you can add the following\nto your `settings.jsonc`: (More info here ./json.md)\n\n```json\n\"lsp\": {\n    \"json-language-server\": {\n      \"settings\": {\n        \"json\": {\n          \"schemas\": [\n            {\n              \"fileMatch\": [\n                \"deno.json\",\n                \"deno.jsonc\"\n              ],\n              \"url\": \"https://raw.githubusercontent.com/denoland/deno/refs/heads/main/cli/schemas/config-file.v1.json\"\n            },\n            {\n              \"fileMatch\": [\n                \"package.json\"\n              ],\n              \"url\": \"https://www.schemastore.org/package\"\n            }\n          ]\n        }\n      }\n    }\n  }\n```\n\n## DAP support\n\nTo debug deno programs, add this to `.monyacode/debug.jsonc`\n\n```json [debug]\n[\n  {\n    \"adapter\": \"JavaScript\",\n    \"label\": \"Deno\",\n    \"request\": \"launch\",\n    \"type\": \"pwa-node\",\n    \"cwd\": \"$MONYACODE_WORKTREE_ROOT\",\n    \"program\": \"$MONYACODE_FILE\",\n    \"runtimeExecutable\": \"deno\",\n    \"runtimeArgs\": [\"run\", \"--allow-all\", \"--inspect-wait\"],\n    \"attachSimplePort\": 9229\n  }\n]\n```\n\n## Runnable support\n\nTo run deno tasks like tests from the ui, add this to `.monyacode/tasks.jsonc`\n\n```json [tasks]\n[\n  {\n    \"label\": \"deno test\",\n    \"command\": \"deno test -A --filter '/^$MONYACODE_CUSTOM_DENO_TEST_NAME$/' '$MONYACODE_FILE'\",\n    \"tags\": [\"js-test\"]\n  }\n]\n```\n\n## See also:\n\n- [TypeScript](./typescript.md)\n- [JavaScript](./javascript.md)\n"},{"path":"languages/diff.md","title":"Diff","group":"Languages","content":"# Diff\n\nDiff support is available natively in MonyaCode.\n\n- Tree-sitter:\n  [zed-industries/the-mikedavis/tree-sitter-diff](https://github.com/the-mikedavis/tree-sitter-diff)\n\n## Configuration\n\nMonyaCode will not attempt to format diff files and has\n[`remove_trailing_whitespace_on_save`](../configuring-monyacode.md#remove-trailing-whitespace-on-save)\nand\n[`ensure-final-newline-on-save`](../configuring-monyacode.md#ensure-final-newline-on-save)\nset to false.\n\nMonyaCode will automatically recognize files with `patch` and `diff` extensions as\nDiff files. To recognize other extensions, add them to `file_types` in your MonyaCode\n`settings.jsonc`:\n\n```json\n  \"file_types\": {\n    \"Diff\": [\"dif\"]\n  },\n```\n"},{"path":"languages/docker.md","title":"Docker","group":"Languages","content":"# Docker\n\nSupport for `Dockerfile` and `docker-compose.yaml` in MonyaCode is provided by\ncommunity-maintained extensions.\n\n## Docker Compose\n\nDocker `compose.yaml` language support in MonyaCode is provided by the\n[Docker Compose extension](https://github.com/eth0net/zed-docker-compose).\n\n- Language Server:\n  [microsoft/compose-language-service](https://github.com/microsoft/compose-language-service)\n\n## Dockerfile\n\n`Dockerfile` language support in MonyaCode is provided by the\n[Dockerfile extension](https://github.com/d1y/dockerfile.zed).\n\n- Tree-sitter:\n  [camdencheek/tree-sitter-dockerfile](https://github.com/camdencheek/tree-sitter-dockerfile)\n- Language Server:\n  [rcjsuen/dockerfile-language-server](https://github.com/rcjsuen/dockerfile-language-server)\n"},{"path":"languages/elixir.md","title":"Elixir","group":"Languages","content":"# Elixir\n\nElixir support is available through the\n[Elixir extension](https://github.com/zed-extensions/elixir).\n\n- Tree-sitter:\n  - [elixir-lang/tree-sitter-elixir](https://github.com/elixir-lang/tree-sitter-elixir)\n  - [phoenixframework/tree-sitter-heex](https://github.com/phoenixframework/tree-sitter-heex)\n- Language servers:\n  - [elixir-lang/expert](https://github.com/elixir-lang/expert)\n  - [elixir-lsp/elixir-ls](https://github.com/elixir-lsp/elixir-ls)\n  - [elixir-tools/next-ls](https://github.com/elixir-tools/next-ls)\n  - [lexical-lsp/lexical](https://github.com/lexical-lsp/lexical)\n\n## Choosing a language server\n\nThe Elixir extension offers language server support for `expert`, `elixir-ls`,\n`next-ls`, and `lexical`.\n\n`elixir-ls` is enabled by default.\n\n### Expert\n\nTo switch to `expert`, add the following to your `settings.jsonc`:\n\n```json\n  \"languages\": {\n    \"Elixir\": {\n      \"language_servers\": [\"expert\", \"!elixir-ls\", \"!next-ls\", \"!lexical\", \"...\"]\n    },\n    \"HEEX\": {\n      \"language_servers\": [\"expert\", \"!elixir-ls\", \"!next-ls\", \"!lexical\", \"...\"]\n    }\n  }\n```\n\n### Next LS\n\nTo switch to `next-ls`, add the following to your `settings.jsonc`:\n\n```json\n  \"languages\": {\n    \"Elixir\": {\n      \"language_servers\": [\"next-ls\", \"!expert\", \"!elixir-ls\", \"!lexical\", \"...\"]\n    },\n    \"HEEX\": {\n      \"language_servers\": [\"next-ls\", \"!expert\", \"!elixir-ls\", \"!lexical\", \"...\"]\n    }\n  }\n```\n\n### Lexical\n\nTo switch to `lexical`, add the following to your `settings.jsonc`:\n\n```json\n  \"languages\": {\n    \"Elixir\": {\n      \"language_servers\": [\"lexical\", \"!expert\", \"!elixir-ls\", \"!next-ls\", \"...\"]\n    },\n    \"HEEX\": {\n      \"language_servers\": [\"lexical\", \"!expert\", \"!elixir-ls\", \"!next-ls\", \"...\"]\n    }\n  }\n```\n\n## Setting up `elixir-ls`\n\n1. Install `elixir`:\n\n```sh\nbrew install elixir\n```\n\n2. Install `elixir-ls`:\n\n```sh\nbrew install elixir-ls\n```\n\n3. Restart MonyaCode\n\n> If `elixir-ls` is not running in an elixir project, check the error log via\n> the command palette action `monyacode: open log`. If you find an error message\n> mentioning:\n> `invalid LSP message header \"Shall I install Hex? (if running non-interactively, use \\\"mix local.hex --force\\\") [Yn]`,\n> you might need to install [`Hex`](https://hex.pm). You run `elixir-ls` from\n> the command line and accept the prompt to install `Hex`.\n\n### Formatting with Mix\n\nIf you prefer to format your code with [Mix](https://hexdocs.pm/mix/Mix.html),\nuse the following snippet in your `settings.jsonc` file to configure it as an\nexternal formatter. Formatting will occur on file save.\n\n```json\n{\n  \"languages\": {\n    \"Elixir\": {\n      \"format_on_save\": \"on\",\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"mix\",\n          \"arguments\": [\"format\", \"--stdin-filename\", \"{buffer_path}\", \"-\"],\n        },\n      },\n    },\n  },\n}\n```\n\n### Additional workspace configuration options\n\nYou can pass additional elixir-ls workspace configuration options via lsp\nsettings in `settings.jsonc`.\n\nThe following example disables dialyzer:\n\n```json\n  \"lsp\": {\n    \"elixir-ls\": {\n      \"settings\": {\n        \"dialyzerEnabled\": false\n      }\n    }\n  }\n```\n\nSee\n[ElixirLS configuration settings](https://github.com/elixir-lsp/elixir-ls#elixirls-configuration-settings)\nfor more options.\n\n### HEEx\n\nMonyaCode also supports HEEx templates. HEEx is a mix of\n[EEx](https://hexdocs.pm/eex/1.12.3/EEx.html) (Embedded Elixir) and HTML, and is\nused in Phoenix LiveView applications.\n\n- Tree-sitter:\n  [phoenixframework/tree-sitter-heex](https://github.com/phoenixframework/tree-sitter-heex)\n"},{"path":"languages/elm.md","title":"Elm","group":"Languages","content":"# Elm\n\nElm support is available through the\n[Elm extension](https://github.com/zed-extensions/elm).\n\n- Tree-sitter:\n  [elm-tooling/tree-sitter-elm](https://github.com/elm-tooling/tree-sitter-elm)\n- Language Server:\n  [elm-tooling/elm-language-server](https://github.com/elm-tooling/elm-language-server)\n\n## Setup\n\nMonyaCode support for Elm requires installation of `elm`, `elm-format`, and\n`elm-review`.\n\n1. [Install Elm](https://guide.elm-lang.org/install/elm.html) (or run\n   `brew install elm` on macOS).\n2. Install `elm-review` to support code linting:\n   ```sh\n   npm install elm-review --save-dev\n   ```\n3. Install `elm-format` to support automatic formatting\n   ```sh\n   npm install -g elm-format\n   ```\n\n## Configuring `elm-language-server`\n\nElm language server can be configured in your `settings.jsonc`, e.g.:\n\n```json\n{\n  \"lsp\": {\n    \"elm-language-server\": {\n      \"initialization_options\": {\n        \"disableElmLSDiagnostics\": true,\n        \"onlyUpdateDiagnosticsOnSave\": false,\n        \"elmReviewDiagnostics\": \"warning\",\n      },\n    },\n  },\n}\n```\n\n`elm-format`, `elm-review` and `elm` need to be installed and made available in\nthe environment or configured in the settings. See the\n[full list of server settings here](https://github.com/elm-tooling/elm-language-server?tab=readme-ov-file#server-settings).\n"},{"path":"languages/emmet.md","title":"Emmet","group":"Languages","content":"# Emmet\n\nEmmet support is available through the\n[Emmet extension](https://github.com/zed-extensions/emmet).\n\n[Emmet](https://emmet.io/) is a web-developer’s toolkit that can greatly improve\nyour HTML & CSS workflow.\n\n- Language Server:\n  [olrtg/emmet-language-server](https://github.com/olrtg/emmet-language-server)\n\n<!--\nTBD: Document Emmet usage in monyacode with: HTML, PHP, ERB, Javascript, TSX, CSS\n-->\n"},{"path":"languages/erlang.md","title":"Erlang","group":"Languages","content":"# Erlang\n\nErlang support is built into the editor.\n\n- Tree-sitter:\n  [WhatsApp/tree-sitter-erlang](https://github.com/WhatsApp/tree-sitter-erlang)\n- Language Servers:\n  - [erlang-ls/erlang_ls](https://github.com/erlang-ls/erlang_ls)\n  - [WhatsApp/erlang-language-platform](https://github.com/WhatsApp/erlang-language-platform)\n\n## Choosing a language server\n\nThe Erlang extension offers language server support for `erlang_ls` and\n`erlang-language-platform`.\n\n`erlang_ls` is enabled by default.\n\nTo switch to `erlang-language-platform`, add the following to your\n`settings.jsonc`:\n\n```json\n{\n  \"languages\": {\n    \"Erlang\": {\n      \"language_servers\": [\"elp\", \"!erlang-ls\", \"...\"],\n    },\n  },\n}\n```\n\n## See also:\n\n- [Elixir](./elixir.md)\n- [Gleam](./gleam.md)\n"},{"path":"languages/fish.md","title":"Fish","group":"Languages","content":"# Fish\n\nFish language support in MonyaCode is provided by the community-maintained\n[Fish extension](https://github.com/hasit/zed-fish). Report issues to:\n[https://github.com/hasit/zed-fish/issues](https://github.com/hasit/zed-fish/issues)\n\n- Tree-sitter:\n  [ram02z/tree-sitter-fish](https://github.com/ram02z/tree-sitter-fish)\n\n### Formatting\n\nMonyaCode supports auto-formatting fish code using external tools like\n[`fish_indent`](https://fishshell.com/docs/current/cmds/fish_indent.html), which\nis included with fish.\n\n1. Ensure `fish_indent` is available in your path and check the version:\n\n```sh\nwhich fish_indent\nfish_indent --version\n```\n\n2. Configure MonyaCode to automatically format fish code with `fish_indent`:\n\n```json\n  \"languages\": {\n    \"Fish\": {\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"fish_indent\"\n        }\n      }\n    }\n  },\n```\n"},{"path":"languages/gdscript.md","title":"GDScript","group":"Languages","content":"# GDScript\n\nGodot [GDScript](https://gdscript.com/) language support in MonyaCode is provided by\nthe community-maintained\n[GDScript extension](https://github.com/GDQuest/zed-gdscript). Report issues to:\n[https://github.com/GDQuest/zed-gdscript/issues](https://github.com/GDQuest/zed-gdscript/issues)\n\n- Tree-sitter:\n  [PrestonKnopp/tree-sitter-gdscript](https://github.com/PrestonKnopp/tree-sitter-gdscript)\n  and\n  [PrestonKnopp/tree-sitter-godot-resource](https://github.com/PrestonKnopp/tree-sitter-godot-resource)\n- Language Server:\n  [gdscript-language-server](https://github.com/godotengine/godot)\n\n## Pre-requisites\n\nYou will need:\n\n- [Godot](https://godotengine.org/download/).\n- netcat (`nc` or `ncat`) on your system PATH.\n\n## Setup\n\n1. Inside your Godot editor, open Editor Settings, look for\n   `Text Editor -> External` and set the following options:\n   - Exec Path: `/path/to/zed`\n   - Exec Flags: `{project} {file}:{line}:{col}`\n   - Use External Editor: \"✅ On\"\n2. Open any \\*.gd file through Godot and MonyaCode will launch.\n\n## Usage\n\nWhen Godot is running, the GDScript extension will connect to the language\nserver provided by the Godot runtime and will provide `jump to definition`,\nhover states when you hold Ctrl/cmd and other language server features.\n"},{"path":"languages/gleam.md","title":"Gleam","group":"Languages","content":"# Gleam\n\nGleam support is built into the editor.\n\nGleam is a friendly language for building type-safe systems that scale!\n\nTo learn more:\n\n- [Website](https://gleam.run)\n- [Documentation](https://gleam.run/documentation/)\n- [`gleam_stdlib` reference](https://hexdocs.pm/gleam_stdlib/).\n- [gleam-lang/tree-sitter-gleam](https://github.com/gleam-lang/tree-sitter-gleam)\n\nThe Gleam language server is built into the gleam compiler and has a lot of\nhelpful features, including go-to definition, automatic imports, and\n[more](https://gleam.run/language-server/).\n\nSee also:\n\n- [Elixir](./elixir.md)\n- [Erlang](./erlang.md)\n"},{"path":"languages/glsl.md","title":"GLSL","group":"Languages","content":"# GLSL\n\nGLSL (OpenGL Shading Language) support is available through the\n[GLSL Extension](https://codeberg.org/GramEditor/glsl-extension/)\n\n- Tree-sitter:\n  [theHamsta/tree-sitter-glsl](https://github.com/theHamsta/tree-sitter-glsl)\n- Language Server:\n  [nolanderc/glsl_analyzer](https://github.com/nolanderc/glsl_analyzer)\n"},{"path":"languages/go.md","title":"Go","group":"Languages","content":"# Go\n\nGo support is available natively in MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-go](https://github.com/tree-sitter/tree-sitter-go)\n- Language Server:\n  [golang/tools/tree/master/gopls](https://github.com/golang/tools/tree/master/gopls)\n- Debug Adapter: [delve](https://github.com/go-delve/delve)\n\n## Setup\n\nWe recommend installing gopls via go's package manager and not via Homebrew or\nyour Linux distribution's package manager.\n\n1. Make sure you have uninstalled any version of gopls you have installed via\n   your package manager:\n\n```sh\n# MacOS homebrew\nbrew remove gopls\n# Ubuntu\nsudo apt-get remove gopls\nsudo snap remove gopls\n# Arch\nsudo pacman -R gopls\n```\n\n2. Install/Update `gopls` to the latest version using the go module tool:\n\n```sh\ngo install golang.org/x/tools/gopls@latest\n```\n\n3. Ensure that `gopls` is in your path:\n\n```sh\nwhich gopls\ngopls version\n```\n\nIf `gopls` is not found you will likely need to add\n`export PATH=\"$PATH:$HOME/go/bin\"` to your `.zshrc` / `.bash_profile`\n\n## Inlay Hints\n\nMonyaCode sets the following initialization options for inlay hints:\n\n```json\n\"hints\": {\n    \"assignVariableTypes\": true,\n    \"compositeLiteralFields\": true,\n    \"compositeLiteralTypes\": true,\n    \"constantValues\": true,\n    \"functionTypeParameters\": true,\n    \"parameterNames\": true,\n    \"rangeVariableTypes\": true\n}\n```\n\nto make the language server send back inlay hints when MonyaCode has them enabled in\nthe settings.\n\nUse\n\n```json\n\"lsp\": {\n    \"gopls\": {\n        \"initialization_options\": {\n            \"hints\": {\n                // ....\n            }\n        }\n    }\n}\n```\n\nto override these settings.\n\nSee\n[gopls inlayHints documentation](https://github.com/golang/tools/blob/master/gopls/doc/inlayHints.md)\nfor more information.\n\n## Debugging\n\nMonyaCode supports zero-configuration debugging of Go tests and entry points\n(`func main`) using Delve. Run {#action debugger::Start} ({#kb debugger::Start})\nto see a contextual list of these preconfigured debug tasks.\n\nFor more control, you can add debug configurations to `.monyacode/debug.jsonc`. See\nbelow for examples.\n\n- [Delve configuration documentation](https://github.com/go-delve/delve/blob/master/Documentation/api/dap/README.md#launch-and-attach-configurations)\n\n### Debug Go Packages\n\nTo debug a specific package, you can do so by setting the Delve mode to \"debug\".\nIn this case \"program\" should be set to the package name.\n\n```json [debug]\n[\n  {\n    \"label\": \"Go (Delve)\",\n    \"adapter\": \"Delve\",\n    \"program\": \"$MONYACODE_FILE\",\n    \"request\": \"launch\",\n    \"mode\": \"debug\"\n  },\n  {\n    \"label\": \"Run server\",\n    \"adapter\": \"Delve\",\n    \"request\": \"launch\",\n    \"mode\": \"debug\",\n    // For Delve, the program can be a package name\n    \"program\": \"./cmd/server\"\n    // \"args\": [],\n    // \"buildFlags\": [],\n  }\n]\n```\n\n### Debug Go Tests\n\nTo debug the tests for a package, set the Delve mode to \"test\". The \"program\" is\nstill the package name, and you can use the \"buildFlags\" to do things like set\ntags, and the \"args\" to set args on the test binary. (See `go help testflags`\nfor more information on doing that).\n\n```json [debug]\n[\n  {\n    \"label\": \"Run integration tests\",\n    \"adapter\": \"Delve\",\n    \"request\": \"launch\",\n    \"mode\": \"test\",\n    \"program\": \".\",\n    \"buildFlags\": [\"-tags\", \"integration\"]\n    // To filter down to just the test your cursor is in:\n    // \"args\": [\"-test.run\", \"$MONYACODE_SYMBOL\"]\n  }\n]\n```\n\n### Build and debug separately\n\nIf you need to build your application with a specific command, you can use the\n\"exec\" mode of Delve. In this case \"program\" should point to an executable, and\nthe \"build\" command should build that.\n\n```json [debug]\n[\n  {\n    \"label\": \"Debug Prebuilt Unit Tests\",\n    \"adapter\": \"Delve\",\n    \"request\": \"launch\",\n    \"mode\": \"exec\",\n    \"program\": \"${MONYACODE_WORKTREE_ROOT}/__debug_unit\",\n    \"args\": [\"-test.v\", \"-test.run=${MONYACODE_SYMBOL}\"],\n    \"build\": {\n      \"command\": \"go\",\n      \"args\": [\n        \"test\",\n        \"-c\",\n        \"-tags\",\n        \"unit\",\n        \"-gcflags\\\"all=-N -l\\\"\",\n        \"-o\",\n        \"__debug_unit\",\n        \"./pkg/...\"\n      ]\n    }\n  }\n]\n```\n\n### Attaching to an existing instance of Delve\n\nYou might find yourself needing to connect to an existing instance of Delve\nthat's not necessarily running on your machine; in such case, you can use\n`tcp_arguments` to instrument MonyaCode's connection to Delve.\n\n```json [debug]\n[\n  {\n    \"adapter\": \"Delve\",\n    \"label\": \"Connect to a running Delve instance\",\n    \"program\": \"/Users/monyacode/Projects/language_repositories/golang/hello/hello\",\n    \"cwd\": \"/Users/monyacode/Projects/language_repositories/golang/hello\",\n    \"args\": [],\n    \"env\": {},\n    \"request\": \"launch\",\n    \"mode\": \"exec\",\n    \"stopOnEntry\": false,\n    \"tcp_connection\": { \"host\": \"127.0.0.1\", \"port\": 53412 }\n  }\n]\n```\n\nIn such case MonyaCode won't spawn a new instance of Delve, as it opts to use an\nexisting one. The consequence of this is that _there will be no terminal_ in\nMonyaCode; you have to interact with the Delve instance directly, as it handles\nstdin/stdout of the debuggee.\n\n## Go Mod\n\n- Tree-sitter:\n  [camdencheek/tree-sitter-go-mod](https://github.com/camdencheek/tree-sitter-go-mod)\n- Language Server: N/A\n\n## Go Sum\n\n- Tree-sitter:\n  [amaanq/tree-sitter-go-sum](https://github.com/amaanq/tree-sitter-go-sum)\n- Language Server: N/A\n\n## Go Work\n\n- Tree-sitter: [tree-sitter-go-work](https://github.com/d1y/tree-sitter-go-work)\n- Language Server: N/A\n"},{"path":"languages/groovy.md","title":"Groovy","group":"Languages","content":"# Groovy\n\nGroovy language support in MonyaCode is provided by the community-maintained\n[Groovy extension](https://github.com/valentinegb/zed-groovy). Report issues to:\n[https://github.com/valentinegb/zed-groovy/issues](https://github.com/valentinegb/zed-groovy/issues)\n\n- Tree-sitter:\n  [murtaza64/tree-sitter-groovy](https://github.com/murtaza64/tree-sitter-groovy)\n- Language Server:\n  [GroovyLanguageServer/groovy-language-server](https://github.com/GroovyLanguageServer/groovy-language-server)\n"},{"path":"languages/haskell.md","title":"Haskell","group":"Languages","content":"# Haskell\n\nHaskell support is available through the\n[Haskell extension](https://github.com/zed-extensions/haskell).\n\n- Tree-sitter:\n  [tree-sitter-haskell](https://github.com/tree-sitter/tree-sitter-haskell)\n- Language Server:\n  [haskell-language-server](https://github.com/haskell/haskell-language-server)\n\n## Installing HLS\n\nRecommended method to\n[install haskell-language-server](https://haskell-language-server.readthedocs.io/en/latest/installation.html)\n(HLS) is via [ghcup](https://www.haskell.org/ghcup/install/)\n(`curl --proto '=https' --tlsv1.2 -sSf https://get-ghcup.haskell.org | sh `):\n\n```sh\nghcup install hls\nwhich haskell-language-server-wrapper\n```\n\n## Configuring HLS\n\nIf you need to configure haskell-language-server (hls) you can add configuration\noptions to your MonyaCode `settings.jsonc`:\n\n```json\n{\n  \"lsp\": {\n    \"hls\": {\n      \"initialization_options\": {\n        \"haskell\": {\n          \"formattingProvider\": \"fourmolu\",\n        },\n      },\n    },\n  },\n}\n```\n\nSee the official\n[configuring haskell-language-server](https://haskell-language-server.readthedocs.io/en/latest/configuration.html)\ndocs for more options.\n\nIf you would like to use a specific hls binary, or perhaps use\n[static-ls](https://github.com/josephsumabat/static-ls) as a drop-in replacement\ninstead, you can specify the binary path and arguments:\n\n```json\n{\n  \"lsp\": {\n    \"hls\": {\n      \"binary\": {\n        \"path\": \"static-ls\",\n        \"arguments\": [\"--experimentalFeatures\"],\n      },\n    },\n  },\n}\n```\n"},{"path":"languages/helm.md","title":"Helm","group":"Languages","content":"# Helm\n\nSupport for Helm in MonyaCode is provided by the community-maintained\n[Helm extension](https://github.com/cabrinha/helm.zed).\n\n- Tree-sitter:\n  [tree-sitter-go-template](https://github.com/ngalaiko/tree-sitter-go-template/tree/master)\n- Language Server: [mrjosh/helm-ls](https://github.com/mrjosh/helm-ls)\n\n## Setup\n\nEnable Helm language for Helm files by editing your `.monyacode/settings.jsonc` and\nadding:\n\n```json\n  \"file_types\": {\n    \"Helm\": [\n      \"**/templates/**/*.tpl\",\n      \"**/templates/**/*.yaml\",\n      \"**/templates/**/*.yml\",\n      \"**/helmfile.d/**/*.yaml\",\n      \"**/helmfile.d/**/*.yml\",\n      \"**/values*.yaml\"\n    ]\n  }\n```\n\nThis will also mark values.yaml files as the type helm, since helm-ls supports\nthis.\n"},{"path":"languages/html.md","title":"HTML","group":"Languages","content":"# HTML\n\nHTML support is built into the editor, both syntax highlighting and language\nserver support.\n\n- [Tree sitter grammar](https://github.com/tree-sitter/tree-sitter-html)\n\nThere is language server support available from two different servers:\n\n- [SuperHTML](https://github.com/kristoff-it/superhtml)\n- [microsoft/vscode-html-languageservice](https://github.com/microsoft/vscode-html-languageservice)\n\nBy default, the Microsoft language server is disabled and SuperHTML enabled. To\nswitch language server, add the following to your `settings.jsonc`:\n\n```json\n{\n  \"languages\": {\n    \"HTML\": {\n      \"language_servers\": [\"vscode-html-language-server\", \"!superhtml\", \"...\"],\n    },\n  },\n}\n```\n\n## Formatting\n\nBy default MonyaCode uses [Prettier](https://prettier.io/) for formatting HTML.\n\nYou can disable `format_on_save` by adding the following to your MonyaCode\n`settings.jsonc`:\n\n```json\n  \"languages\": {\n    \"HTML\": {\n      \"format_on_save\": \"off\",\n    }\n  }\n```\n\nYou can still trigger formatting manually with {#kb editor::Format} or by\nopening the Command Palette ({#kb command_palette::Toggle}) and selecting\n\"Format Document\".\n\n### LSP Formatting\n\nTo use the language server auto-formatting instead of Prettier, add the\nfollowing to your MonyaCode settings:\n\n```json\n  \"languages\": {\n    \"HTML\": {\n      \"formatter\": \"language_server\",\n    }\n  }\n```\n\nYou can customize various\n[formatting options](https://code.visualstudio.com/docs/languages/html#_formatting)\nfor `vscode-html-language-server` via your MonyaCode `settings.json`:\n\n```json\n  \"lsp\": {\n    \"vscode-html-language-server\": {\n      \"settings\": {\n        \"html\": {\n          \"format\": {\n            // Indent under <html> and <head> (default: false)\n            \"indentInnerHtml\": true,\n            // Disable formatting inside <svg> or <script>\n            \"contentUnformatted\": \"svg,script\",\n            // Add an extra newline before <div> and <p>\n            \"extraLiners\": \"div,p\"\n          }\n        }\n      }\n    }\n  }\n```\n\n## See also\n\n- [CSS](./css.md)\n- [JavaScript](./javascript.md)\n- [TypeScript](./typescript.md)\n"},{"path":"languages/java.md","title":"Java","group":"Languages","content":"# Java\n\nJava language support in MonyaCode is provided by:\n\n- MonyaCode Java: [zed-extensions/java](https://github.com/zed-extensions/java)\n- Tree-sitter:\n  [tree-sitter/tree-sitter-java](https://github.com/tree-sitter/tree-sitter-java)\n- Language Server:\n  [eclipse-jdtls/eclipse.jdt.ls](https://github.com/eclipse-jdtls/eclipse.jdt.ls)\n\n## Install OpenJDK\n\nYou will need to install a Java runtime (OpenJDK).\n\n- macOS: `brew install openjdk`\n- Ubuntu:\n  `sudo add-apt-repository ppa:openjdk-23 && sudo apt-get install openjdk-23`\n- Windows: `choco install openjdk`\n- Arch Linux: `sudo pacman -S jre-openjdk-headless`\n\nOr manually download and install [OpenJDK 23](https://jdk.java.net/23/).\n\n## Extension Install\n\nYou can install by opening {#action monyacode::Extensions}({#kb monyacode::Extensions})\nand searching for `java`.\n\n## Quick start and configuration\n\nFor the majority of users, Java support should work out of the box.\n\n- It is generally recommended to open projects with the MonyaCode-project root at the\n  Java project root folder (where you would commonly have your `pom.xml` or\n  `build.gradle` file).\n\n- By default the extension will download and run the latest official version of\n  JDTLS for you, but this requires Java version 21 to be available on your\n  system via either the `$JAVA_HOME` environment variable or as a `java(.exe)`\n  executable on your `$PATH`. If your project requires a lower Java version in\n  the environment, you can specify a different JDK to use for running JDTLS via\n  the `java_home` configuration option.\n\n- You can provide a **custom launch script for JDTLS**, by adding an executable\n  named `jdtls` (or `jdtls.bat` on Windows) to your `$PATH` environment\n  variable. If this is present, the extension will skip downloading and\n  launching a managed instance and use the one from the environment.\n\n- To support [Lombok](https://projectlombok.org/), the lombok-jar must be\n  downloaded and registered as a Java-Agent when launching JDTLS. By default the\n  extension automatically takes care of that, but in case you don't want that\n  you can set the `lombok_support` configuration-option to `false`.\n\nHere is a common `settings.jsonc` including the above mentioned configurations:\n\n```json\n{\n  \"lsp\": {\n    \"jdtls\": {\n      \"settings\": {\n        \"java_home\": \"/path/to/your/JDK21+\",\n        \"lombok_support\": true,\n      },\n    },\n  },\n}\n```\n\n## Debugging\n\nDebug support is enabled via our\n[Fork of Java Debug](https://github.com/zed-industries/java-debug), which the\nextension will automatically download and start for you. Please refer to the\n[Debugger Documentation](../debugger.md#getting-started) for general information\nabout how debugging works in MonyaCode.\n\nTo get started with Java, click the `edit debug.jsonc` button in the Debug menu,\nand replace the contents of the file with the following:\n\n```json\n[\n  {\n    \"adapter\": \"Java\",\n    \"request\": \"launch\",\n    \"label\": \"Launch Debugger\",\n    // if your project has multiple entry points, specify the one to use:\n    // \"mainClass\": \"com.myorganization.myproject.MyMainClass\",\n    //\n    // this effectively sets a breakpoint at your program entry:\n    \"stopOnEntry\": true,\n    // the working directory for the debug process\n    \"cwd\": \"$MONYACODE_WORKTREE_ROOT\",\n  },\n]\n```\n\nYou should then be able to start a new Debug Session with the \"Launch Debugger\"\nscenario from the debug menu.\n\n## Launch Scripts (aka Tasks) in Windows\n\nThis extension provides tasks for running your application and tests from within\nMonyaCode via little play buttons next to tests/entry points. However, due to current\nlimitations of MonyaCode's extension interface, we can not provide scripts that will\nwork across Maven and Gradle on both Windows and Unix-compatible systems, so out\nof the box the launch scripts only work on Mac and Linux.\n\nThere is a fairly straightforward fix that you can apply to make it work on\nWindows by supplying your own task scripts. Please see\n[this Issue](https://github.com/zed-extensions/java/issues/94) for information\non how to do that and read the\n[Tasks section in MonyaCodes documentation](../tasks.md) for more information.\n\n## Advanced Configuration/JDTLS initialization Options\n\nJDTLS provides many configuration options that can be passed via the\n`initialize` LSP-request. The extension will pass the JSON-object from\n`lsp.jdtls.settings.initialization_options` in your settings on to JDTLS. Please\nrefer to the\n[JDTLS Configuration Wiki Page](https://github.com/eclipse-jdtls/eclipse.jdt.ls/wiki/Running-the-JAVA-LS-server-from-the-command-line#initialize-request)\nfor the available options and values. Below is an example `settings.jsonc` that\nwould pass on the example configuration from the above wiki page to JDTLS:\n\n```json\n{\n  \"lsp\": {\n    \"jdtls\": {\n      \"settings\": {\n        // this will be sent to JDTLS as initializationOptions:\n        \"initialization_options\": {\n          \"bundles\": [],\n          // use this if your monyacode project root folder is not the same as the java project root:\n          \"workspaceFolders\": [\"file:///home/snjeza/Project\"],\n          \"settings\": {\n            \"java\": {\n              \"home\": \"/usr/local/jdk-9.0.1\",\n              \"errors\": {\n                \"incompleteClasspath\": {\n                  \"severity\": \"warning\",\n                },\n              },\n              \"configuration\": {\n                \"updateBuildConfiguration\": \"interactive\",\n                \"maven\": {\n                  \"userSettings\": null,\n                },\n              },\n              \"import\": {\n                \"gradle\": {\n                  \"enabled\": true,\n                },\n                \"maven\": {\n                  \"enabled\": true,\n                },\n                \"exclusions\": [\n                  \"**/node_modules/**\",\n                  \"**/.metadata/**\",\n                  \"**/archetype-resources/**\",\n                  \"**/META-INF/maven/**\",\n                  \"/**/test/**\",\n                ],\n              },\n              \"referencesCodeLens\": {\n                \"enabled\": false,\n              },\n              \"signatureHelp\": {\n                \"enabled\": false,\n              },\n              \"implementationCodeLens\": \"all\",\n              \"format\": {\n                \"enabled\": true,\n              },\n              \"saveActions\": {\n                \"organizeImports\": false,\n              },\n              \"contentProvider\": {\n                \"preferred\": null,\n              },\n              \"autobuild\": {\n                \"enabled\": false,\n              },\n              \"completion\": {\n                \"favoriteStaticMembers\": [\n                  \"org.junit.Assert.*\",\n                  \"org.junit.Assume.*\",\n                  \"org.junit.jupiter.api.Assertions.*\",\n                  \"org.junit.jupiter.api.Assumptions.*\",\n                  \"org.junit.jupiter.api.DynamicContainer.*\",\n                  \"org.junit.jupiter.api.DynamicTest.*\",\n                ],\n                \"importOrder\": [\"java\", \"javax\", \"com\", \"org\"],\n              },\n            },\n          },\n        },\n      },\n    },\n  },\n}\n```\n\n## See also\n\n[MonyaCode Java Repo](https://github.com/zed-extensions/java)\n[Eclipse JDTLS Repo](https://github.com/eclipse-jdtls/eclipse.jdt.ls)\n"},{"path":"languages/javascript.md","title":"JavaScript","group":"Languages","content":"# JavaScript\n\nJavaScript support is available natively in MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-javascript](https://github.com/tree-sitter/tree-sitter-javascript)\n- Language Server: [yioneko/vtsls](https://github.com/yioneko/vtsls)\n- Alternate Language Server:\n  [typescript-language-server/typescript-language-server](https://github.com/typescript-language-server/typescript-language-server)\n- Debug Adapter: [vscode-js-debug](https://github.com/microsoft/vscode-js-debug)\n\n## Code formatting\n\nFormatting on save is enabled by default for JavaScript, using TypeScript's\nbuilt-in code formatting. But many JavaScript projects use other command-line\ncode-formatting tools, such as [Prettier](https://prettier.io/). You can use one\nof these tools by specifying an _external_ code formatter for JavaScript in your\nsettings. See [the configuration docs](../configuring-monyacode.md) for more\ninformation.\n\nFor example, if you have Prettier installed and on your `PATH`, you can use it\nto format JavaScript files by adding the following to your `settings.jsonc`:\n\n```json\n{\n  \"languages\": {\n    \"JavaScript\": {\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"prettier\",\n          \"arguments\": [\"--stdin-filepath\", \"{buffer_path}\"],\n        },\n      },\n    },\n  },\n}\n```\n\n## JSX\n\nMonyaCode supports JSX syntax highlighting out of the box.\n\nIn JSX strings, the [`tailwindcss-language-server`](./tailwindcss.md) is used to\nprovide autocompletion for Tailwind CSS classes.\n\n## JSDoc\n\nMonyaCode supports JSDoc syntax in JavaScript and TypeScript comments that match the\nJSDoc syntax. MonyaCode uses\n[tree-sitter/tree-sitter-jsdoc](https://github.com/tree-sitter/tree-sitter-jsdoc)\nfor parsing and highlighting JSDoc.\n\n## ESLint\n\nYou can configure MonyaCode to format code using `eslint --fix` by running the ESLint\ncode action when formatting:\n\n```json\n{\n  \"languages\": {\n    \"JavaScript\": {\n      \"code_actions_on_format\": {\n        \"source.fixAll.eslint\": true,\n      },\n    },\n  },\n}\n```\n\nYou can also only execute a single ESLint rule when using `fixAll`:\n\n```json\n{\n  \"languages\": {\n    \"JavaScript\": {\n      \"code_actions_on_format\": {\n        \"source.fixAll.eslint\": true,\n      },\n    },\n  },\n  \"lsp\": {\n    \"eslint\": {\n      \"settings\": {\n        \"codeActionOnSave\": {\n          \"rules\": [\"import/order\"],\n        },\n      },\n    },\n  },\n}\n```\n\n> **Note:** the other formatter you have configured will still run, after\n> ESLint. So if your language server or Prettier configuration don't format\n> according to ESLint's rules, then they will overwrite what ESLint fixed and\n> you end up with errors.\n\nIf you **only** want to run ESLint on save, you can configure code actions as\nthe formatter:\n\n```json\n{\n  \"languages\": {\n    \"JavaScript\": {\n      \"formatter\": [],\n      \"code_actions_on_format\": {\n        \"source.fixAll.eslint\": true,\n      },\n    },\n  },\n}\n```\n\n### Configure ESLint's `nodePath`:\n\nYou can configure ESLint's `nodePath` setting:\n\n```json\n{\n  \"lsp\": {\n    \"eslint\": {\n      \"settings\": {\n        \"nodePath\": \".yarn/sdks\",\n      },\n    },\n  },\n}\n```\n\n### Configure ESLint's `problems`:\n\nYou can configure ESLint's `problems` setting.\n\nFor example, here's how to set `problems.shortenToSingleLine`:\n\n```json\n{\n  \"lsp\": {\n    \"eslint\": {\n      \"settings\": {\n        \"problems\": {\n          \"shortenToSingleLine\": true,\n        },\n      },\n    },\n  },\n}\n```\n\n### Configure ESLint's `rulesCustomizations`:\n\nYou can configure ESLint's `rulesCustomizations` setting:\n\n```json\n{\n  \"lsp\": {\n    \"eslint\": {\n      \"settings\": {\n        \"rulesCustomizations\": [\n          // set all eslint errors/warnings to show as warnings\n          { \"rule\": \"*\", \"severity\": \"warn\" },\n        ],\n      },\n    },\n  },\n}\n```\n\n### Configure ESLint's `workingDirectory`:\n\nYou can configure ESLint's `workingDirectory` setting:\n\n```json\n{\n  \"lsp\": {\n    \"eslint\": {\n      \"settings\": {\n        \"workingDirectory\": {\n          \"mode\": \"auto\",\n        },\n      },\n    },\n  },\n}\n```\n\n## Debugging\n\nMonyaCode supports debugging JavaScript code out of the box with `vscode-js-debug`.\nThe following can be debugged without writing additional configuration:\n\n- Tasks from `package.json`\n- Tests written using several popular frameworks (Jest, Mocha, Vitest, Jasmine,\n  Bun, Node)\n\nRun {#action debugger::Start} ({#kb debugger::Start}) to see a contextual list\nof these predefined debug tasks.\n\n> **Note:** Bun test is automatically detected when `@types/bun` is present in\n> `package.json`.\n>\n> **Note:** Node test is automatically detected when `@types/node` is present in\n> `package.json` (requires Node.js 20+).\n\nAs for all languages, configurations from `.vscode/launch.json` are also\navailable for debugging in MonyaCode.\n\nIf your use-case isn't covered by any of these, you can take full control by\nadding debug configurations to `.monyacode/debug.jsonc`. See below for example\nconfigurations.\n\n### Configuring JavaScript debug tasks\n\nJavaScript debugging is more complicated than other languages because there are\ntwo different environments: Node.js and the browser. `vscode-js-debug` exposes a\n`type` field, that you can use to specify the environment, either `node` or\n`chrome`.\n\n- [vscode-js-debug configuration documentation](https://github.com/microsoft/vscode-js-debug/blob/main/OPTIONS.md)\n\n### Debug the current file with Node\n\n```json [debug]\n[\n  {\n    \"adapter\": \"JavaScript\",\n    \"label\": \"Debug JS file\",\n    \"type\": \"node\",\n    \"request\": \"launch\",\n    \"program\": \"$MONYACODE_FILE\",\n    \"skipFiles\": [\"<node_internals>/**\"]\n  }\n]\n```\n\n### Launch a web app in Chrome\n\n```json [debug]\n[\n  {\n    \"adapter\": \"JavaScript\",\n    \"label\": \"Debug app in Chrome\",\n    \"type\": \"chrome\",\n    \"request\": \"launch\",\n    \"file\": \"$MONYACODE_WORKTREE_ROOT/index.html\",\n    \"webRoot\": \"$MONYACODE_WORKTREE_ROOT\",\n    \"console\": \"integratedTerminal\",\n    \"skipFiles\": [\"<node_internals>/**\"]\n  }\n]\n```\n\n## See also\n\n- [Yarn documentation](./yarn.md) for a walkthrough of configuring your project\n  to use Yarn.\n- [TypeScript documentation](./typescript.md)\n"},{"path":"languages/json.md","title":"JSON","group":"Languages","content":"# JSON\n\nJSON is natively supported in the editor.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-json](https://github.com/tree-sitter/tree-sitter-json)\n- Language Server:\n  [zed-industries/json-language-server](https://github.com/zed-industries/json-language-server)\n\n## JSONC\n\nMonyaCode also supports a super-set of JSON called JSONC, which allows single line\ncomments (`//`) in JSON files. While editing these files you can use `cmd-/`\n(macOS) or `ctrl-/` (Linux) to toggle comments on the current line or selection.\n\n## JSONC Prettier Formatting\n\nIf you use files with the `*.jsonc` extension when using `Format Document` or\nhave `format_on_save` enabled, MonyaCode invokes Prettier as the formatter. Prettier\nhas an [outstanding issue](https://github.com/prettier/prettier/issues/15956)\nwhere it will add trailing commas to files with a `jsonc` extension. JSONC files\nwhich have a `.json` extension are unaffected.\n\nTo workaround this behavior you can add the following to your `.prettierrc`\nconfiguration file:\n\n```json\n{\n  \"overrides\": [\n    {\n      \"files\": [\"*.jsonc\"],\n      \"options\": {\n        \"parser\": \"json\",\n        \"trailingComma\": \"none\",\n      },\n    },\n  ],\n}\n```\n\n## JSON Language Server\n\nMonyaCode automatically out of the box supports JSON Schema validation of\n`package.json` and `tsconfig.json` files, but `json-language-server` can use\nJSON Schema definitions in project files, from the\n[JSON Schema Store](https://www.schemastore.org) or other publicly available\nURLs for JSON validation.\n\n### Inline Schema Specification\n\nTo specify a schema inline with your JSON files, add a `$schema` top level key\nlinking to your json schema file.\n\nFor example to for a `.luarc.json` for use with\n[lua-language-server](https://github.com/LuaLS/lua-language-server/):\n\n```json\n{\n  \"$schema\": \"https://raw.githubusercontent.com/sumneko/vscode-lua/master/setting/schema.json\",\n  \"runtime.version\": \"Lua 5.4\",\n}\n```\n\n### Schema Specification via Settings\n\nYou can alternatively associate JSON Schemas with file paths by via MonyaCode LSP\nsettings.\n\nTo\n\n```json\n\"lsp\": {\n  \"json-language-server\": {\n    \"settings\": {\n      \"json\": {\n        \"schemas\": [\n          {\n            \"fileMatch\": [\"*/*.luarc.json\"],\n            \"url\": \"https://raw.githubusercontent.com/sumneko/vscode-lua/master/setting/schema.json\"\n          }\n        ]\n      }\n    }\n  }\n}\n```\n\nYou can also pass any of the\n[supported settings](https://github.com/Microsoft/vscode/blob/main/extensions/json-language-features/server/README.md#settings)\nto json-language-server by specifying them in your MonyaCode `settings.jsonc`:\n\n<!--\nTBD: Add formatter (prettier) settings (autoformat, tab_size, etc)\n-->\n"},{"path":"languages/jsonnet.md","title":"Jsonnet","group":"Languages","content":"# Jsonnet\n\nJsonnet language support in MonyaCode is provided by the community-maintained\n[Jsonnet extension](https://github.com/narqo/zed-jsonnet).\n\n- Tree-sitter:\n  [sourcegraph/tree-sitter-jsonnet](https://github.com/sourcegraph/tree-sitter-jsonnet)\n- Language Server:\n  [grafana/jsonnet-language-server](https://github.com/grafana/jsonnet-language-server)\n\n## Configuration\n\nWorkspace configuration options can be passed to the language server via the\n`lsp` settings of the `settings.jsonc`.\n\nThe following example enables support for resolving [tanka](https://tanka.dev)\nimport paths in `jsonnet-language-server`:\n\n```json\n{\n  \"lsp\": {\n    \"jsonnet-language-server\": {\n      \"settings\": {\n        \"resolve_paths_with_tanka\": true,\n      },\n    },\n  },\n}\n```\n"},{"path":"languages/julia.md","title":"Julia","group":"Languages","content":"# Julia\n\nJulia language support in MonyaCode is provided by the community-maintained\n[Julia extension](https://github.com/JuliaEditorSupport/zed-julia). Report\nissues to:\n[https://github.com/JuliaEditorSupport/zed-julia/issues](https://github.com/JuliaEditorSupport/zed-julia/issues)\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-julia](https://github.com/tree-sitter/tree-sitter-julia)\n- Language Server:\n  [julia-vscode/LanguageServer.jl](https://github.com/julia-vscode/LanguageServer.jl)\n\n<!--\nTBD: Document Julia Installation\nTBD: Julia REPL Setup instructions\n-->\n"},{"path":"languages/kotlin.md","title":"Kotlin","group":"Languages","content":"# Kotlin\n\nKotlin language support in MonyaCode is provided by the community-maintained\n[Kotlin extension](https://github.com/zed-extensions/kotlin). Report issues to:\n[https://github.com/zed-extensions/kotlin/issues](https://github.com/zed-extensions/kotlin/issues)\n\n- Tree-sitter:\n  [fwcd/tree-sitter-kotlin](https://github.com/fwcd/tree-sitter-kotlin)\n- Language Server:\n  [fwcd/kotlin-language-server](https://github.com/fwcd/kotlin-language-server)\n\n## Configuration\n\nWorkspace configuration options can be passed to the language server via lsp\nsettings in `settings.jsonc`.\n\nThe full list of lsp `settings` can be found\n[here](https://github.com/fwcd/kotlin-language-server/blob/main/server/src/main/kotlin/org/javacs/kt/Configuration.kt)\nunder `class Configuration` and initialization_options under\n`class InitializationOptions`.\n\n### JVM Target\n\nThe following example changes the JVM target from `default` (which is 1.8) to\n`17`:\n\n```json\n{\n  \"lsp\": {\n    \"kotlin-language-server\": {\n      \"settings\": {\n        \"compiler\": {\n          \"jvm\": {\n            \"target\": \"17\",\n          },\n        },\n      },\n    },\n  },\n}\n```\n\n### JAVA_HOME\n\nTo use a specific java installation, just specify the `JAVA_HOME` environment\nvariable with:\n\n```json\n{\n  \"lsp\": {\n    \"kotlin-language-server\": {\n      \"binary\": {\n        \"env\": {\n          \"JAVA_HOME\": \"/Users/whatever/Applications/Work/Android Studio.app/Contents/jbr/Contents/Home\",\n        },\n      },\n    },\n  },\n}\n```\n"},{"path":"languages/lua.md","title":"Lua","group":"Languages","content":"# Lua\n\nLua support is built into the editor.\n\n- Tree-sitter:\n  [tree-sitter-grammars/tree-sitter-lua](https://github.com/tree-sitter-grammars/tree-sitter-lua)\n- Language server:\n  [LuaLS/lua-language-server](https://github.com/LuaLS/lua-language-server)\n\n## luarc.json\n\nTo configure LuaLS you can create a `.luarc.json` file in the root of your\nworkspace.\n\n```json\n{\n  \"$schema\": \"https://raw.githubusercontent.com/LuaLS/vscode-lua/master/setting/schema.json\",\n  \"runtime.version\": \"Lua 5.4\",\n  \"format.enable\": true,\n  \"workspace.library\": [\"../somedir/library\"],\n}\n```\n\nSee [LuaLS Settings Documentation](https://luals.github.io/wiki/settings/) for\nall available configuration options, or when editing this file in MonyaCode available\nsettings options will autocomplete, (e.g `runtime.version` will show\n`\"Lua 5.1\"`, `\"Lua 5.2\"`, `\"Lua 5.3\"`, `\"Lua 5.4\"` and `\"LuaJIT\"` as allowed\nvalues). Note when importing settings options from VS Code, remove the `Lua.`\nprefix. (e.g. `runtime.version` instead of `Lua.runtime.version`).\n\n### LuaCATS Definitions\n\nLuaLS can provide enhanced LSP autocompletion suggestions and type validation\nwith the help of LuaCATS (Lua Comment and Type System) definitions. These\ndefinitions are available for many common Lua libraries, and local paths\ncontaining them can be specified via `workspace.library` in `luarc.json`. You\ncan do this via relative paths if you checkout your definitions into the same\npartent directory of your project (`../playdate-luacats`, `../love2d`, etc).\nAlternatively you can create submodule(s) inside your project for each LuaCATS\ndefinition repo.\n\n### LÖVE (Love2D) {#love2d}\n\nTo use [LÖVE (Love2D)](https://love2d.org/) in MonyaCode, checkout\n[LuaCATS/love2d](https://github.com/LuaCATS/love2d) into a folder called\n`love2d-luacats` into the parent folder of your project:\n\n```sh\ncd .. && git clone https://github.com/LuaCATS/love2d love2d-luacats\n```\n\nThen in your `.luarc.json`:\n\n```\n{\n  \"$schema\": \"https://raw.githubusercontent.com/LuaLS/vscode-lua/master/setting/schema.json\",\n  \"runtime.version\": \"Lua 5.4\",\n  \"workspace.library\": [\"../love2d-luacats\"],\n  \"runtime.special\": {\n    \"love.filesystem.load\": \"loadfile\"\n  }\n}\n```\n\n### PlaydateSDK\n\nTo use [Playdate Lua SDK](https://play.date/dev/) in MonyaCode, checkout\n[playdate-luacats](https://github.com/notpeter/playdate-luacats) into the parent\nfolder of your project:\n\n```sh\ncd .. && git clone https://github.com/notpeter/playdate-luacats\n```\n\nThen in your `.luarc.json`:\n\n```json\n{\n  \"$schema\": \"https://raw.githubusercontent.com/LuaLS/vscode-lua/master/setting/schema.json\",\n  \"runtime.version\": \"Lua 5.4\",\n  \"runtime.nonstandardSymbol\": [\n    \"+=\",\n    \"-=\",\n    \"*=\",\n    \"/=\",\n    \"//=\",\n    \"%=\",\n    \"<<=\",\n    \">>=\",\n    \"&=\",\n    \"|=\",\n    \"^=\",\n  ],\n  \"diagnostics.severity\": { \"duplicate-set-field\": \"Hint\" },\n  \"diagnostics.globals\": [\"import\"],\n  \"workspace.library\": [\"../playdate-luacats\"],\n  \"format.defaultConfig\": {\n    \"indent_style\": \"space\",\n    \"indent_size\": \"4\",\n  },\n  \"format.enable\": true,\n  \"runtime.builtin\": { \"io\": \"disable\", \"os\": \"disable\", \"package\": \"disable\" },\n}\n```\n\n### Inlay Hints\n\nTo enable [Inlay Hints](../configuring-languages.md#inlay-hints) for LuaLS in\nMonyaCode\n\n1. Add the following to your MonyaCode settings.jsonc:\n\n```json\n  \"languages\": {\n    \"Lua\": {\n      \"inlay_hints\": {\n        \"enabled\": true,\n        \"show_type_hints\": true,\n        \"show_parameter_hints\": true,\n        \"show_other_hints\": true\n      }\n    }\n  }\n```\n\n2. Add `\"hint.enable\": true` to your `.luarc.json`.\n\n## Formatting\n\n### LuaLS Formatting\n\nTo enable auto-formatting with your LuaLS (provided by\n[CppCXY/EmmyLuaCodeStyle](https://github.com/CppCXY/EmmyLuaCodeStyle)) make sure\nyou have `\"format.enable\": true,` in your .luarc.json:\n\n```json\n{\n  \"$schema\": \"https://raw.githubusercontent.com/sumneko/vscode-lua/master/setting/schema.json\",\n  \"format.enable\": true,\n}\n```\n\nThen add the following to your MonyaCode `settings.jsonc`:\n\n```json\n{\n  \"languages\": {\n    \"Lua\": {\n      \"format_on_save\": \"on\",\n      \"formatter\": \"language_server\",\n    },\n  },\n}\n```\n\nYou can customize various EmmyLuaCodeStyle style options via `.editorconfig`,\nsee\n[lua.template.editorconfig](https://github.com/CppCXY/EmmyLuaCodeStyle/blob/master/lua.template.editorconfig)\nfor all available options.\n\n### StyLua Formatting\n\nAlternatively to use [StyLua](https://github.com/JohnnyMorganz/StyLua) for\nauto-formatting:\n\n1. Install [StyLua](https://github.com/JohnnyMorganz/StyLua):\n   `brew install stylua` or\n   `cargo install stylua --features lua52,lua53,lua54,luau,luajit` (feel free to\n   remove any Lua versions you don't need).\n2. Add the following to your `settings.jsonc`:\n\n```json\n{\n  \"languages\": {\n    \"Lua\": {\n      \"format_on_save\": \"on\",\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"stylua\",\n          \"arguments\": [\n            \"--syntax=Lua54\",\n            \"--respect-ignores\",\n            \"--stdin-filepath\",\n            \"{buffer_path}\",\n            \"-\",\n          ],\n        },\n      },\n    },\n  },\n}\n```\n\nYou can specify various options to StyLua either on the command line above (like\n`--syntax=Lua54`) or in a `stylua.toml` in your workspace:\n\n```toml\nsyntax = \"Lua54\"\ncolumn_width = 100\nline_endings = \"Unix\"\nindent_type = \"Spaces\"\nindent_width = 4\nquote_style = \"AutoPreferDouble\"\ncall_parentheses = \"Always\"\ncollapse_simple_statement = \"All\"\n\n[sort_requires]\nenabled = true\n```\n\nFor a complete list of available options, see:\n[StyLua Options](https://github.com/JohnnyMorganz/StyLua?tab=readme-ov-file#options).\n"},{"path":"languages/luau.md","title":"Luau","group":"Languages","content":"# Luau\n\n[Luau](https://luau.org/) is a fast, small, safe, gradually typed, embeddable\nscripting language derived from Lua. Luau was developed by Roblox and is\navailable under the MIT license.\n\nLuau language support in MonyaCode is provided by the community-maintained\n[Luau extension](https://github.com/4teapo/zed-luau). Report issues to:\n[https://github.com/4teapo/zed-luau/issues](https://github.com/4teapo/zed-luau/issues)\n\n- Tree-sitter:\n  [4teapo/tree-sitter-luau](https://github.com/4teapo/tree-sitter-luau)\n- Language Server:\n  [JohnnyMorganz/luau-lsp](https://github.com/JohnnyMorganz/luau-lsp)\n\n## Configuration\n\nConfiguration instructions are available in the\n[Luau MonyaCode Extension README](https://github.com/4teapo/zed-luau).\n\n## Formatting\n\nTo support automatically formatting your code, you can use\n[JohnnyMorganz/StyLua](https://github.com/JohnnyMorganz/StyLua), a Lua code\nformatter.\n\nInstall with:\n\n```sh\n# macOS via Homebrew\nbrew install stylua\n# Or via Cargo\ncargo install stylua --features lua52,lua53,lua54,luau\n```\n\nThen add the following to your MonyaCode `settings.json`:\n\n```json\n  \"languages\": {\n    \"Luau\": {\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"stylua\",\n          \"arguments\": [\"-\"]\n        }\n      }\n    }\n  }\n```\n"},{"path":"languages/makefile.md","title":"Makefile","group":"Languages","content":"# Makefile\n\nMakefile language support in MonyaCode is provided by the community-maintained\n[Make extension](https://github.com/caius/zed-make). Report issues to:\n[https://github.com/caius/zed-make/issues](https://github.com/caius/zed-make/issues).\n\n- Tree-sitter:\n  [caius/tree-sitter-make](https://github.com/caius/tree-sitter-make)\n"},{"path":"languages/markdown.md","title":"Markdown","group":"Languages","content":"# Markdown\n\nMarkdown support is available natively in MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter-markdown](https://github.com/tree-sitter-grammars/tree-sitter-markdown)\n- Language Server: N/A\n\n## Syntax Highlighting Code Blocks\n\nMonyaCode supports language-specific syntax highlighting of markdown code blocks by\nleveraging [tree-sitter language grammars](../extensions/languages.md#grammar).\nAll [MonyaCode supported languages](../languages.md), including those provided by\nofficial or community extensions, are available for use in markdown code blocks.\nAll you need to do is provide a language name after the opening <kbd>```</kbd>\ncode fence like so:\n\n````python\n```python\nimport functools as ft\n\n@ft.lru_cache(maxsize=500)\ndef fib(n):\n    return n if n < 2 else fib(n - 1) + fib(n - 2)\n```\n````\n\n## Configuration\n\n### Format\n\nMonyaCode supports using Prettier to automatically re-format Markdown documents. You\ncan trigger this manually via the {#action editor::Format} action or via the\n{#kb editor::Format} keyboard shortcut. Alternately, you can automatically\nformat by enabling [`format_on_save`](../configuring-monyacode.md#format-on-save) in\nyour settings.json:\n\n```json\n  \"languages\": {\n    \"Markdown\": {\n      \"format_on_save\": \"on\"\n    }\n  },\n```\n\n### Trailing Whitespace\n\nBy default MonyaCode will remove trailing whitespace on save. If you rely on\ninvisible trailing whitespace being converted to `<br />` in Markdown files you\ncan disable this behavior with:\n\n```json\n  \"languages\": {\n    \"Markdown\": {\n      \"remove_trailing_whitespace_on_save\": false\n    }\n  },\n```\n"},{"path":"languages/nim.md","title":"Nim","group":"Languages","content":"# Nim\n\nNim language support in MonyaCode is provided by the community-maintained\n[Nim extension](https://github.com/foxoman/zed-nim). Report issues to:\n[https://github.com/foxoman/zed-nim/issues](https://github.com/foxoman/zed-nim/issues)\n\n- Tree-sitter:\n  [alaviss/tree-sitter-nim](https://github.com/alaviss/tree-sitter-nim)\n- Language Server: [nim-lang/langserver](https://github.com/nim-lang/langserver)\n\n## Formatting\n\nTo use [arnetheduck/nph](https://github.com/arnetheduck/nph) as a formatter,\nfollow the\n[nph installation instructions](https://github.com/arnetheduck/nph?tab=readme-ov-file#installation)\nand add this to your MonyaCode `settings.json`:\n\n```json\n  \"languages\": {\n    \"Nim\": {\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"nph\",\n          \"arguments\": [\"-\"]\n        }\n      }\n    }\n  }\n```\n"},{"path":"languages/nix.md","title":"Nix","group":"Languages","content":"# Nix\n\nNix support is built into the editor.\n\n## Choosing a language server\n\nThere is support for two different language servers: `nil` and `nixd`.\n\nTo only enable `nil`:\n\n```json\n  \"languages\": {\n    \"Nix\": {\n      \"language_servers\": [\"nil\", \"!nixd\", \"...\"]\n    },\n  }\n```\n\nTo only enable `nixd`:\n\n```json\n  \"languages\": {\n    \"Nix\": {\n      \"language_servers\": [\"!nil\", \"nixd\", \"...\"]\n    },\n  }\n```\n\n## Configuration\n\nLanguage server options can be set in the `settings.jsonc` file. See the\n[Configuring Languages](../configuring-languages.md) documentation for more\ninformation.\n\n### nixd\n\nFor more details, see the\n[nixd documentation](https://github.com/nix-community/nixd/blob/main/nixd/docs/configuration.md).\n\n```json\n{\n  \"lsp\": {\n    \"nixd\": {\n      \"settings\": {\n        \"diagnostic\": {\n          \"suppress\": [ \"sema-extra-with\" ]\n        }\n      }\n    }\n  }\n}\n```\n\n### nil\n\nFor more details, see the\n[nil documentation](https://github.com/oxalica/nil/blob/main/docs/configuration.md).\n\n```json\n{\n  \"lsp\": {\n    \"nil\": {\n      \"settings\": {\n         \"diagnostics\": {\n          \"ignored\": [ \"unused_binding\" ]\n        }\n      }\n    }\n  }\n}\n```\n"},{"path":"languages/ocaml.md","title":"OCaml","group":"Languages","content":"# OCaml\n\nOCaml support is available through the\n[OCaml extension](https://github.com/zed-extensions/ocaml).\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-ocaml](https://github.com/tree-sitter/tree-sitter-ocaml)\n- Language Server: [ocaml/ocaml-lsp](https://github.com/ocaml/ocaml-lsp)\n\n## Setup Instructions\n\nIf you have the development environment already setup, skip to\n[Launching MonyaCode](#launching-monyacode)\n\n### Using Opam\n\nOpam is the official package manager for OCaml and is highly recommended for\ngetting started with OCaml. To get started using Opam, please follow the\ninstructions provided [here](https://ocaml.org/install).\n\nProceed once opam is installed and you have setup a switch with your development\nenvironment as per the instructions.\n\n### Launching MonyaCode\n\nVerify that the `ocamllsp` command line tool for LSP is installed:\n\n```sh\nocamllsp --help\n```\n\nIf the command is missing or fails to display the help text, revisit the\ninstallation instructions for `ocamllsp` and ensure it's properly installed.\n\nNote: Given how the OCaml package manager works, MonyaCode needs to run from the\nterminal, so make sure to install the MonyaCode cli.\n\nFrom a terminal, navigate to an OCaml project and run\n\n```sh\nmonyacode .\n```\n\nMonyaCode should now be running with OCaml support.\n"},{"path":"languages/odin.md","title":"Odin","group":"Languages","content":"# Odin\n\nOdin support is built into the editor.\n\nOdin is a general-purpose programming language with distinct typing built for\nhigh performance, modern systems and data-oriented programming.\n\nOdin is the C alternative for the Joy of Programming.\n\n- [Website](https://odin-lang.org)\n- [Documentation](https://odin-lang.org/docs/)\n- [Tree Sitter](https://github.com/tree-sitter-grammars/tree-sitter-odin)\n- [Language Server (ols)](https://github.com/DanielGavin/ols)\n"},{"path":"languages/opentofu.md","title":"OpenTofu","group":"Languages","content":"# OpenTofu\n\nOpenTofu support is built into the editor. It is the properly open-source\nalternative to Terraform to provision your infrastructure as code.\n\n- [Website](https://opentofu.org/)\n- [Documentation](https://opentofu.org/docs/)\n- [Tree Sitter](https://github.com/MichaHoffmann/tree-sitter-hcl)\n- [Language Server (tofu-ls)](https://github.com/opentofu/tofu-ls)\n"},{"path":"languages/php.md","title":"PHP","group":"Languages","content":"# PHP\n\nPHP support is available through the\n[PHP extension](https://github.com/zed-extensions/php).\n\n- Tree-sitter: https://github.com/tree-sitter/tree-sitter-php\n- Language Servers:\n  - [phpactor](https://github.com/phpactor/phpactor)\n  - [intelephense](https://github.com/bmewburn/vscode-intelephense/)\n\n## Choosing a language server\n\nThe PHP extension offers both `phpactor` and `intelephense` language server\nsupport.\n\n`phpactor` is enabled by default.\n\n### Phpactor\n\nThe MonyaCode PHP Extension can install `phpactor` automatically but requires `php`\nto be installed and available in your path:\n\n```sh\n# brew install php            # macOS\n# sudo apt-get install php    # Debian/Ubuntu\n# yum install php             # CentOS/RHEL\n# pacman -S php               # Arch Linux\nwhich php\n```\n\n### Intelephense\n\n[Intelephense](https://intelephense.com/) is a\n[proprietary](https://github.com/bmewburn/vscode-intelephense/blob/master/LICENSE.txt#L29)\nlanguage server for PHP operating under a freemium model. Certain features\nrequire purchase of a [premium license](https://intelephense.com/).\n\nTo switch to `intelephense`, add the following to your `settings.json`:\n\n```json\n{\n  \"languages\": {\n    \"PHP\": {\n      \"language_servers\": [\"intelephense\", \"!phpactor\", \"!phptools\", \"...\"],\n    },\n  },\n}\n```\n\nTo use the premium features, you can place your\n[licence.txt file](https://intelephense.com/faq.html) at\n`~/intelephense/licence.txt` inside your home directory. Alternatively, you can\npass the licence key or a path to a file containing the licence key as an\ninitialization option for the `intelephense` language server. To do this, add\nthe following to your `settings.jsonc`:\n\n```json\n{\n  \"lsp\": {\n    \"intelephense\": {\n      \"initialization_options\": {\n        \"licenceKey\": \"/path/to/licence.txt\",\n      },\n    },\n  },\n}\n```\n\n## PHPDoc\n\nMonyaCode supports syntax highlighting for PHPDoc comments.\n\n- Tree-sitter:\n  [claytonrcarter/tree-sitter-phpdoc](https://github.com/claytonrcarter/tree-sitter-phpdoc)\n\n## Setting up Xdebug\n\nMonyaCode’s PHP extension provides a debug adapter for PHP and Xdebug. The adapter\nname is `Xdebug`. Here a couple ways you can use it:\n\n```json\n[\n  {\n    \"label\": \"PHP: Listen to Xdebug\",\n    \"adapter\": \"Xdebug\",\n    \"request\": \"launch\",\n    \"port\": 9003\n  },\n  {\n    \"label\": \"PHP: Debug this test\",\n    \"adapter\": \"Xdebug\",\n    \"request\": \"launch\",\n    \"program\": \"vendor/bin/phpunit\",\n    \"args\": [\"--filter\", \"$MONYACODE_SYMBOL\"]\n  }\n]\n```\n\nIn case you run into issues:\n\n- ensure that you have Xdebug installed for the version of PHP you’re running\n- ensure that Xdebug is configured to run in `debug` mode\n- ensure that Xdebug is actually starting a debugging session\n- check that the host and port matches between Xdebug and MonyaCode\n- look at the diagnostics log by using the `xdebug_info()` function in the page\n  you’re trying to debug\n"},{"path":"languages/powershell.md","title":"PowerShell","group":"Languages","content":"# PowerShell\n\nPowerShell language support in MonyaCode is provided by the community-maintained\n[MonyaCode PowerShell extension](https://github.com/wingyplus/zed-powershell). Please\nreport issues to:\n[github.com/wingyplus/zed-powershell/issues](https://github.com/wingyplus/zed-powershell/issues)\n\n- Tree-sitter:\n  [airbus-cert/tree-sitter-powershell](https://github.com/airbus-cert/tree-sitter-powershell)\n- Language Server:\n  [PowerShell/PowerShellEditorServices](https://github.com/PowerShell/PowerShellEditorServices)\n\n## Setup\n\n### Install PowerShell 7+ {#powershell-install}\n\n- macOS: `brew install powershell/tap/powershell`\n- Alpine:\n  [Installing PowerShell on Alpine Linux](https://learn.microsoft.com/en-us/powershell/scripting/install/install-alpine)\n- Debian:\n  [Install PowerShell on Debian Linux](https://learn.microsoft.com/en-us/powershell/scripting/install/install-debian)\n- RedHat:\n  [Install PowerShell on RHEL](https://learn.microsoft.com/en-us/powershell/scripting/install/install-rhel)\n- Ubuntu:\n  [Install PowerShell on RHEL](https://learn.microsoft.com/en-us/powershell/scripting/install/install-ubuntu)\n- Windows:\n  [Install PowerShell on Windows](https://learn.microsoft.com/en-us/powershell/scripting/install/installing-powershell-on-windows)\n\nThe MonyaCode PowerShell extension will default to the `pwsh` executable found in\nyour path.\n\n### Install PowerShell Editor Services (Optional) {#powershell-editor-services}\n\nThe MonyaCode PowerShell extensions will attempt to download\n[PowerShell Editor Services](https://github.com/PowerShell/PowerShellEditorServices)\nautomatically.\n\nIf want to use a specific binary, you can specify in your that in your MonyaCode\nsettings.json:\n\n```json\n  \"lsp\": {\n    \"powershell-es\": {\n      \"binary\": {\n        \"path\": \"/path/to/PowerShellEditorServices\"\n      }\n    }\n  }\n```\n"},{"path":"languages/prisma.md","title":"Prisma","group":"Languages","content":"# Prisma\n\nPrisma support is available through the\n[Prisma extension](https://github.com/zed-extensions/prisma).\n\n- Tree-sitter:\n  [victorhqc/tree-sitter-prisma](https://github.com/victorhqc/tree-sitter-prisma)\n- Language-Server:\n  [prisma/language-tools](https://github.com/prisma/language-tools)\n\n<!--\nTBD: Prisma usage and configuration documentation\n-->\n"},{"path":"languages/proto.md","title":"Proto","group":"Languages","content":"# Proto\n\nProto/proto3 (Protocol Buffers definition language) support is available through\nthe [Proto extension](https://codeberg.org/GramEditor/proto-extension).\n\n- Tree-sitter:\n  [coder3101/tree-sitter-proto](https://github.com/coder3101/tree-sitter-proto)\n- Language Servers:\n  [protobuf-language-server](https://github.com/lasorda/protobuf-language-server)\n\n## Setup\n\nThere are two available language server implementations for Protobuf: Pick one\nand follow the installation instructions below.\n\n### Install protobuf-language-server\n\nInstall protobuf-language-server and make sure it's in your PATH:\n\n```\ngo install github.com/lasorda/protobuf-language-server@latest\nwhich protobuf-language-server\n```\n\n### Install ProtoLS\n\nInstall protols and make sure it's in your PATH:\n\n```\ncargo install protols\nwhich protols\n```\n\n## Configuration\n\n```json\n\"lsp\": {\n  \"protobuf-language-server\": {\n    \"binary\": {\n      \"path\": \"protols\"\n    }\n  }\n}\n```\n\n## Formatting\n\nProtoLS supports formatting if you have `clang-format` installed.\n\n```sh\n# MacOS:\nbrew install clang-format\n# Ubuntu\nsudo apt-get install clang-format\n# Fedora\nsudo dnf install clang-tools-extra\n```\n\nTo customize your formatting preferences, create a `.clang-format` file, e.g.:\n\n```clang-format\nIndentWidth: 4\nColumnLimit: 120\n```\n\nOr you can have monyacode directly invoke `clang-format` by specifying it as a\nformatter in your settings:\n\n```json\n  \"languages\": {\n    \"Proto\": {\n      \"format_on_save\": \"on\",\n      \"tab_size\": 4,\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"clang-format\",\n          \"arguments\": [\"-style={IndentWidth: 4, ColumnLimit: 0}\"]\n        }\n      }\n    },\n  }\n```\n"},{"path":"languages/purescript.md","title":"PureScript","group":"Languages","content":"# PureScript\n\nPureScript support is available through the\n[PureScript extension](https://github.com/zed-extensions/purescript).\n\n- Tree-sitter:\n  [postsolar/tree-sitter-purescript](https://github.com/postsolar/tree-sitter-purescript)\n- Language-Server:\n  [nwolverson/purescript-language-server](https://github.com/nwolverson/purescript-language-server)\n"},{"path":"languages/python.md","title":"How to Set Up Python in MonyaCode","group":"Languages","content":"# How to Set Up Python in MonyaCode\n\nPython support is available natively in MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter-python](https://github.com/zed-industries/tree-sitter-python)\n- Language Servers:\n  - [DetachHead/basedpyright](https://github.com/DetachHead/basedpyright)\n  - [astral-sh/ruff](https://github.com/astral-sh/ruff)\n  - [astral-sh/ty](https://github.com/astral-sh/ty)\n  - [microsoft/pyright](https://github.com/microsoft/pyright)\n  - [python-lsp/python-lsp-server](https://github.com/python-lsp/python-lsp-server)\n    (PyLSP)\n- Debug Adapter: [debugpy](https://github.com/microsoft/debugpy)\n\n## Install Python\n\nYou'll need both MonyaCode and Python installed before you can begin.\n\n### Step 1: Install Python\n\nMonyaCode does not bundle a Python runtime, so you’ll need to install one yourself.\nChoose one of the following options:\n\n- uv (recommended)\n\n```bash\ncurl -LsSf https://astral.sh/uv/install.sh | sh\n```\n\nTo learn more, visit\n[Astral’s installation guide](https://docs.astral.sh/uv/getting-started/installation/).\n\n- Homebrew:\n\n```bash\nbrew install python\n```\n\n- Python.org installer: Download the latest version from\n  [python.org/downloads](https://python.org/downloads).\n\n### Step 2: Verify Python Installation\n\nConfirm Python is installed and available in your shell:\n\n```bash\npython3 --version\n```\n\nYou should see an output like `Python 3.x.x`.\n\n## Open Your First Python Project in MonyaCode\n\nOnce MonyaCode and Python are installed, open a folder containing Python code to\nstart working.\n\n### Step 1: Launch MonyaCode with a Python Project\n\nOpen MonyaCode. From the menu bar, choose File > Open Folder, or launch from the\nterminal:\n\n```bash\nmonyacode path/to/your/project\n```\n\nMonyaCode will recognize `.py` files automatically using its native\ntree-sitter-python parser, with no plugins or manual setup required.\n\n### Step 2: Use the Integrated Terminal (Optional)\n\nMonyaCode includes an integrated terminal, accessible from the bottom panel. If MonyaCode\ndetects that your project is using a\n[virtual environment](#virtual-environments), it will be activated automatically\nin newly-created terminals. You can configure this behavior with the\n[`detect_venv`](../configuring-monyacode.md#terminal-detect_venv) setting.\n\n## Configure Python Language Servers in MonyaCode\n\nMonyaCode provides several Python language servers out of the box. By default,\n[basedpyright](https://github.com/DetachHead/basedpyright) is the primary\nlanguage server, and [Ruff](https://github.com/astral-sh/ruff) is used for\nformatting and linting.\n\nOther built-in language servers are:\n\n- [Ty](https://docs.astral.sh/ty/), an up-and-coming language server from\n  Astral, built for speed.\n- [Pyright](https://github.com/microsoft/pyright), the basis for basedpyright.\n- [PyLSP](https://github.com/python-lsp/python-lsp-server), a plugin-based\n  language server that integrates with tools like `pycodestyle`, `autopep8`, and\n  `yapf`.\n\nThese are disabled by default, but can be enabled in your settings. For example:\n\n```json\n{\n  \"languages\": {\n    \"Python\": {\n      \"language_servers\": [\n        // Disable basedpyright and enable Ty, and otherwise\n        // use the default configuration.\n        \"ty\",\n        \"!basedpyright\",\n        \"...\",\n      ],\n    },\n  },\n}\n```\n\nSee:\n[Working with Language Servers](../configuring-languages.md#working-with-language-servers)\nfor more information about how to enable and disable language servers.\n\n### Basedpyright\n\n[basedpyright](https://docs.basedpyright.com/latest/) is the primary Python\nlanguage server in MonyaCode beginning with MonyaCode v0.204.0. It provides core language\nserver functionality like navigation (go to definition/find all references) and\ntype checking. Compared to Pyright, it adds support for additional language\nserver features (like inlay hints) and checking rules.\n\nNote that while basedpyright in isolation defaults to the `recommended`\n[type-checking mode](https://docs.basedpyright.com/latest/benefits-over-pyright/better-defaults/#typecheckingmode),\nMonyaCode configures it to use the less-strict `standard` mode by default, which\nmatches the behavior of Pyright. You can set the type-checking mode for your\nproject using the `typeCheckingMode` setting in `pyrightconfig.json` or\n`pyproject.toml`, which will override MonyaCode's default. Read on more for more\ndetails about how to configure basedpyright.\n\n#### Basedpyright Configuration\n\nbasedpyright reads configuration options from two different kinds of sources:\n\n- Language server settings (\"workspace configuration\"), which must be configured\n  per-editor (using `settings.json` in MonyaCode's case) but apply to all projects\n  opened in that editor\n- Configuration files (`pyrightconfig.json`, `pyproject.toml`), which are\n  editor-independent but specific to the project where they are placed\n\nAs a rule of thumb, options that are only relevant when using basedpyright from\nan editor must be set in language server settings, and options that are relevant\neven if you're running it\n[as a command-line tool](https://docs.basedpyright.com/latest/configuration/command-line/)\nmust be set in configuration files. Settings related to inlay hints are examples\nof the first category, and the\n[diagnostic category](https://docs.basedpyright.com/latest/configuration/config-files/#diagnostic-categories)\nsettings are examples of the second category.\n\nExamples of both kinds of configuration are provided below. Refer to the\nbasedpyright documentation on\n[language server settings](https://docs.basedpyright.com/latest/configuration/language-server-settings/)\nand\n[configuration files](https://docs.basedpyright.com/latest/configuration/config-files/)\nfor comprehensive lists of available options.\n\n##### Language server settings\n\nLanguage server settings for basedpyright in MonyaCode can be set in the `lsp`\nsection of your `settings.json`.\n\nFor example, in order to:\n\n- diagnose all files in the workspace instead of the only open files default\n- disable inlay hints on function arguments\n\nYou can use the following configuration:\n\n```json\n{\n  \"lsp\": {\n    \"basedpyright\": {\n      \"settings\": {\n        \"basedpyright.analysis\": {\n          \"diagnosticMode\": \"workspace\",\n          \"inlayHints\": {\n            \"callArgumentNames\": false,\n          },\n        },\n      },\n    },\n  },\n}\n```\n\n##### Configuration files\n\nbasedpyright reads project-specific configuration from the `pyrightconfig.json`\nconfiguration file and from the `[tool.basedpyright]` and `[tool.pyright]`\nsections of `pyproject.toml` manifests. `pyrightconfig.json` overrides\n`pyproject.toml` if configuration is present in both places.\n\nHere's an example `pyrightconfig.json` file that configures basedpyright to use\nthe `strict` type-checking mode and not to issue diagnostics for any files in\n`__pycache__` directories:\n\n```json\n{\n  \"typeCheckingMode\": \"strict\",\n  \"ignore\": [\"**/__pycache__\"],\n}\n```\n\n### PyLSP\n\n[python-lsp-server](https://github.com/python-lsp/python-lsp-server/), more\ncommonly known as PyLSP, by default integrates with a number of external tools\n(autopep8, mccabe, pycodestyle, yapf) while others are optional and must be\nexplicitly enabled and configured (flake8, pylint).\n\nSee\n[Python Language Server Configuration](https://github.com/python-lsp/python-lsp-server/blob/develop/CONFIGURATION.md)\nfor more.\n\n## Virtual Environments\n\n[Virtual environments](https://docs.python.org/3/library/venv.html) are a useful\ntool for fixing a Python version and set of dependencies for a specific project,\nin a way that's isolated from other projects on the same machine. MonyaCode has\nbuilt-in support for discovering, configuring, and activating virtual\nenvironments, based on the language-agnostic concept of a\n[toolchain](../toolchains.md).\n\nNote that if you have a global Python installation, it is also counted as a\ntoolchain for MonyaCode's purposes.\n\n### Create a Virtual Environment\n\nIf your project doesn't have a virtual environment set up already, you can\ncreate one as follows:\n\n```bash\npython3 -m venv .venv\n```\n\nAlternatively, if you're using `uv`, running `uv sync` will create a virtual\nenvironment the first time you run it.\n\n### How MonyaCode Uses Python Toolchains\n\nMonyaCode uses the selected Python toolchain for your project in the following ways:\n\n- Built-in language servers will be automatically configured with the path to\n  the toolchain's Python interpreter and, if applicable, virtual environment.\n  This is important so that they can resolve dependencies. (Note that language\n  servers provided by extensions can't be automatically configured like this\n  currently.)\n- Python tasks (such as pytest tests) will be run using the toolchain's Python\n  interpreter.\n- If the toolchain is a virtual environment, the environment's activation script\n  will be run automatically when you launch a new shell in MonyaCode's integrated\n  terminal, giving you convenient access to the selected Python interpreter and\n  dependency set.\n- If a built-in language server is installed in the active virtual environment,\n  that binary will be used instead of MonyaCode's private automatically-installed\n  binary. This also applies to debugpy.\n\n### Selecting a Toolchain\n\nFor most projects, MonyaCode will automatically select the right Python toolchain. In\ncomplex projects with multiple virtual environments, it might be necessary to\noverride this selection. You can use the\n[toolchain selector](../toolchains.md#selecting-toolchains) to pick a toolchain\nfrom the list discovered by MonyaCode, or\n[specify the path to a toolchain manually](../toolchains.md#adding-toolchains-manually)\nif it's not on the list.\n\n## Code Formatting & Linting\n\nMonyaCode provides the [Ruff](https://docs.astral.sh/ruff/) formatter and linter for\nPython code. (Specifically, MonyaCode runs Ruff as an LSP server using the\n`ruff server` subcommand.) Both formatting and linting are enabled by default,\nincluding format-on-save.\n\n### Configuring formatting\n\nYou can disable format-on-save for Python files in your `settings.json`:\n\n```json\n{\n  \"languages\": {\n    \"Python\": {\n      \"format_on_save\": \"off\",\n    },\n  },\n}\n```\n\nAlternatively, you can use the `black` command-line tool for Python formatting,\nwhile keeping Ruff enabled for linting:\n\n```json\n{\n  \"languages\": {\n    \"Python\": {\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"black\",\n          \"arguments\": [\"--stdin-filename\", \"{buffer_path}\", \"-\"],\n        },\n      },\n      // Or use `\"formatter\": null` to disable formatting entirely.\n    },\n  },\n}\n```\n\n### Configuring Ruff\n\nLike basedpyright, Ruff reads options from both MonyaCode's language server settings\nand configuration files (`ruff.toml`) when used in MonyaCode. Unlike basedpyright,\n_all_ options can be configured in either of these locations, so the choice of\nwhere to put your Ruff configuration comes down to whether you want it to be\nshared between projects but specific to MonyaCode (in which case you should use\nlanguage server settings), or specific to one project but common to all Ruff\ninvocations (in which case you should use `ruff.toml`).\n\nHere's an example of using language server settings in MonyaCode's `settings.json` to\ndisable all Ruff lints in MonyaCode (while still using Ruff as a formatter):\n\n```json\n{\n  \"lsp\": {\n    \"ruff\": {\n      \"initialization_options\": {\n        \"settings\": {\n          \"exclude\": [\"*\"],\n        },\n      },\n    },\n  },\n}\n```\n\nAnd here's an example `ruff.toml` with linting and formatting options, adapted\nfrom the Ruff documentation:\n\n```toml\n[lint]\n# Avoid enforcing line-length violations (`E501`)\nignore = [\"E501\"]\n\n[format]\n# Use single quotes when formatting.\nquote-style = \"single\"\n```\n\nFor more details, refer to the Ruff documentation about\n[configuration files](https://docs.astral.sh/ruff/configuration/) and\n[language server settings](https://docs.astral.sh/ruff/editors/settings/), and\nthe [list of options](https://docs.astral.sh/ruff/settings/).\n\n### Embedded Language Highlighting\n\nMonyaCode supports syntax highlighting for code embedded in Python strings by adding\na comment with the language name.\n\n```python\n# sql\nquery = \"SELECT * FROM users\"\n\n#sql\nquery = \"\"\"\n    SELECT *\n    FROM users\n\"\"\"\n\nresult = func( #sql\n    \"SELECT * FROM users\"\n)\n```\n\n## Debugging\n\nMonyaCode supports Python debugging through the `debugpy` adapter. You can start with\nno configuration or define custom launch profiles in `.monyacode/debug.json`.\n\n### Start Debugging with No Setup\n\nMonyaCode can automatically detect debuggable Python entry points. Press F4 (or run\ndebugger: start from the Command Palette) to see available options for your\ncurrent project. This works for:\n\n- Python scripts\n- Modules\n- pytest tests\n\nMonyaCode uses `debugpy` under the hood, but no manual adapter configuration is\nrequired.\n\n### Define Custom Debug Configurations\n\nFor reusable setups, create a `.monyacode/debug.jsonc` file in your project root.\nThis gives you more control over how MonyaCode runs and debugs your code.\n\n- [debugpy configuration documentation](https://github.com/microsoft/debugpy/wiki/Debug-configuration-settings#launchattach-settings)\n\n#### Debug Active File\n\n```json [debug]\n[\n  {\n    \"label\": \"Python Active File\",\n    \"adapter\": \"Debugpy\",\n    \"program\": \"$MONYACODE_FILE\",\n    \"request\": \"launch\"\n  }\n]\n```\n\nThis runs the file currently open in the editor.\n\n#### Debug a Flask App\n\nFor projects using Flask, you can define a full launch configuration:\n\n```\n.venv/\napp/\n  init.py\n  main.py\n  routes.py\ntemplates/\n  index.html\nstatic/\n  style.css\nrequirements.txt\n```\n\n…the following configuration can be used:\n\n```json [debug]\n[\n  {\n    \"label\": \"Python: Flask\",\n    \"adapter\": \"Debugpy\",\n    \"request\": \"launch\",\n    \"module\": \"app\",\n    \"cwd\": \"$MONYACODE_WORKTREE_ROOT\",\n    \"env\": {\n      \"FLASK_APP\": \"app\",\n      \"FLASK_DEBUG\": \"1\"\n    },\n    \"args\": [\n      \"run\",\n      \"--reload\", // Enables Flask reloader that watches for file changes\n      \"--debugger\" // Enables Flask debugger\n    ],\n    \"autoReload\": {\n      \"enable\": true\n    },\n    \"jinja\": true,\n    \"justMyCode\": true\n  }\n]\n```\n\nThese can be combined to tailor the experience for web servers, test runners, or\ncustom scripts.\n\n## Troubleshoot and Maintain a Productive Python Setup\n\nMonyaCode is designed to minimize configuration overhead, but occasional issues can\nstill arise—especially around environments, language servers, or tooling. Here's\nhow to keep your Python setup working smoothly.\n\n### Resolve Language Server Startup Issues\n\nIf a language server isn't responding or features like diagnostics or\nautocomplete aren't available:\n\n- Check your MonyaCode log (using the {#action monyacode::OpenLog} action) for errors\n  related to the language server you're trying to use. This is where you're\n  likely to find useful information if the language server failed to start up at\n  all.\n- Use the language server logs view to understand the lifecycle of the affected\n  language server. You can access this view using the {#action\n  dev::OpenLanguageServerLogs} action, or by clicking the lightning bolt icon in\n  the status bar and selecting your language server. The most useful pieces of\n  data in this view are:\n  - \"Server Logs\", which shows any errors printed by the language server\n  - \"Server Info\", which shows details about how the language server was started\n- Verify your `settings.json` or `pyrightconfig.json` is syntactically correct.\n- Restart MonyaCode to reinitialize language server connections, or try restarting\n  the language server using the {#action editor::RestartLanguageServer}\n\nIf the language server is failing to resolve imports, and you're using a virtual\nenvironment, make sure that the right environment is chosen in the selector. You\ncan use \"Server Info\" view to confirm which virtual environment MonyaCode is sending\nto the language server. Look for the `* Configuration` section at the end.\n"},{"path":"languages/r.md","title":"R","group":"Languages","content":"# R\n\nR support is available via multiple R MonyaCode extensions:\n\n- [ocsmit/zed-r](https://github.com/ocsmit/zed-r)\n\n  - Tree-sitter: [r-lib/tree-sitter-r](https://github.com/r-lib/tree-sitter-r)\n  - Language-Server:\n    [REditorSupport/languageserver](https://github.com/REditorSupport/languageserver)\n\n- [posit-dev/air](https://github.com/posit-dev/air/tree/main/editors/zed)\n  - Formatter: [posit-dev/air](https://posit-dev.github.io/air/)\n\n## Installation\n\n1. [Download and Install R](https://cloud.r-project.org/).\n2. Install the R packages `languageserver` and `lintr`:\n\n```R\ninstall.packages(\"languageserver\")\ninstall.packages(\"lintr\")\n```\n\n3. Install the [R](https://github.com/ocsmit/zed-r) extension through MonyaCode's\n   extensions manager for basic R language support (syntax highlighting,\n   tree-sitter support) and for\n   [REditorSupport/languageserver](https://github.com/REditorSupport/languageserver)\n   support.\n\n4. Install the [Air](https://posit-dev.github.io/air/) extension through MonyaCode's\n   extensions manager for R code formatting via Air.\n\n## Linting\n\n`REditorSupport/languageserver` bundles support for\n[r-lib/lintr](https://github.com/r-lib/lintr) as a linter. This can be\nconfigured via the use of a `.lintr` inside your project (or in your home\ndirectory for global defaults).\n\n```r\nlinters: linters_with_defaults(\n    line_length_linter(120),\n    commented_code_linter = NULL\n  )\nexclusions: list(\n    \"inst/doc/creating_linters.R\" = 1,\n    \"inst/example/bad.R\",\n    \"tests/testthat/exclusions-test\"\n  )\n```\n\nOr exclude it from linting anything,\n\n```r\nexclusions: list(\".\")\n```\n\nSee [Using lintr](https://lintr.r-lib.org/articles/lintr.html) for a complete\nlist of options,\n\n## Formatting\n\n### Air\n\n[Air](https://posit-dev.github.io/air/) provides code formatting for R,\nincluding support for format-on-save. The\n[Air documentation for MonyaCode](https://posit-dev.github.io/air/editor-zed.html)\ncontains the most up-to-date advice for running Air in MonyaCode.\n\nEnsure that you have installed both the\n[ocsmit/zed-r](https://github.com/ocsmit/zed-r) extension (for general R\nlanguage awareness in MonyaCode) and the [Air](https://posit-dev.github.io/air/)\nextension.\n\nEnable Air in your `settings.json`:\n\n```json\n{\n  \"languages\": {\n    \"R\": {\n      \"language_servers\": [\"air\"],\n    },\n  },\n}\n```\n\nIf you use the `\"r_language_server\"` from `REditorSupport/languageserver`, but\nwould still like to use Air for formatting, use the following configuration:\n\n```json\n{\n  \"languages\": {\n    \"R\": {\n      \"language_servers\": [\"air\", \"r_language_server\"],\n      \"use_on_type_format\": false,\n    },\n  },\n}\n```\n\nNote that `\"air\"` must come first in this list, otherwise\n[r-lib/styler](https://github.com/r-lib/styler) will be invoked via\n`\"r_language_server\"`.\n\n`\"r_language_server\"` provides on-type-formatting that differs from Air's\nformatting rules. To avoid this entirely and let Air be fully in charge of\nformatting your R files, also set `\"use_on_type_format\": false` as shown above.\n\n#### Configuring Air\n\nAir is minimally configurable via an `air.toml` file placed in the root\ndirectory of your project:\n\n```toml\n[format]\nline-width = 80\nindent-width = 2\n```\n\nFor more details, refer to the Air documentation about\n[configuration](https://posit-dev.github.io/air/configuration.html).\n\n### Styler\n\n`REditorSupport/languageserver` bundles support for\n[r-lib/styler](https://github.com/r-lib/styler) as a formatter. See\n[Customizing Styler](https://cran.r-project.org/web/packages/styler/vignettes/customizing_styler.html)\nfor more information on how to customize its behavior.\n\n<!--\nTBD: Get this working\n\n### REditorSupport/languageserver Configuration\n\nYou can configure the [R languageserver settings](https://github.com/REditorSupport/languageserver#settings) via MonyaCode Project Settings `.monyacode/settings.json` or MonyaCode User Settings `~/.config/monyacode/settings.json`:\n\nFor example to disable Lintr linting and suppress code snippet suggestions (both enabled by default):\n\n```json\n{\n  \"lsp\": {\n    \"r_language_server\": {\n      \"settings\": {\n        \"r\": {\n          \"lsp\": {\n            \"diagnostics\": false,\n            \"snippet_support\": false\n          }\n        }\n      }\n    }\n  }\n}\n```\n\n-->\n\n<!--\nTBD: R REPL Docs\n\n## REPL\n\n### Ark Installation\n\nTo use the MonyaCode REPL with R you need to install [Ark](https://github.com/posit-dev/ark), an R Kernel for Jupyter applications.\nYou can down the latest version from the [Ark GitHub Releases](https://github.com/posit-dev/ark/releases) and then extract the `ark` binary to a directory in your `PATH`.\n\nFor example to install the latest non-debug build:\n\n```sh\n# macOS\ncd /tmp\ncurl -L -o ark-latest-darwin.zip \\\n    $(curl -s \"https://api.github.com/repos/posit-dev/ark/releases/latest\" | \\\n    jq -r '.assets[] | select(.name | contains(\"darwin-universal\") and (contains(\"debug\") | not)) | .browser_download_url')\nunzip ark-latest-darwin.zip ark\nsudo mv /tmp/ark /usr/local/bin/\n```\n\n```sh\n# Linux X86_64\ncd /tmp\ncurl -L -o ark-latest-linux.zip \\\n    $(curl -s \"https://api.github.com/repos/posit-dev/ark/releases/latest\" \\\n        | jq -r '.assets[] | select(.name | contains(\"linux-x64\") and (contains(\"debug\") | not)) | .browser_download_url'\n    )\nunzip ark-latest-linux.zip ark\nsudo mv /tmp/ark /usr/local/bin/\n```\n\n-->\n"},{"path":"languages/racket.md","title":"Racket","group":"Languages","content":"# Racket\n\nRacket support is available through the\n[Racket extension](https://github.com/zed-extensions/racket).\n\n- Tree-sitter:\n  [zed-industries/tree-sitter-racket](https://github.com/zed-industries/tree-sitter-racket)\n\nThe\n[racket-language-server](https://docs.racket-lang.org/racket-language-server/index.html)\nis not yet supported in MonyaCode, please see\n[Issue #15789](https://github.com/zed-industries/zed/issues/15789) for more\ninformation.\n"},{"path":"languages/rego.md","title":"Rego","group":"Languages","content":"# Rego\n\nRego language support in MonyaCode is provided by the community-maintained\n[Rego extension](https://github.com/StyraInc/zed-rego).\n\n- Tree-sitter:\n  [FallenAngel97/tree-sitter-rego](https://github.com/FallenAngel97/tree-sitter-rego)\n- Language Server: [StyraInc/regal](https://github.com/StyraInc/regal)\n\n## Installation\n\nThe extension is largely based on the\n[Regal](https://docs.styra.com/regal/language-server) language server which\nshould be installed to make use of the extension. Read the\n[getting started](https://docs.styra.com/regal#getting-started) instructions for\nmore information.\n\n## Configuration\n\nThe extension's behavior is configured in the `.regal/config.yaml` file. The\nfollowing is an example configuration which disables the `todo-comment` rule,\ncustomizes the `line-length` rule, and ignores test files for the `opa-fmt`\nrule:\n\n```yaml\nrules:\n  style:\n    todo-comment:\n      # don't report on todo comments\n      level: ignore\n    line-length:\n      # custom rule configuration\n      max-line-length: 100\n      # warn on too long lines, but don't fail\n      level: warning\n    opa-fmt:\n      # not needed as error is the default, but\n      # being explicit won't hurt\n      level: error\n      # files can be ignored for any individual rule\n      # in this example, test files are ignored\n      ignore:\n        files:\n          - \"*_test.rego\"\n```\n\nRead Regal's\n[configuration documentation](https://docs.styra.com/regal#configuration) for\nmore information.\n"},{"path":"languages/roc.md","title":"Roc","group":"Languages","content":"# Roc\n\n[Roc](https://www.roc-lang.org/) is a fast, friendly, functional language.\n\nRoc language support in MonyaCode is provided by the community-maintained\n[Roc extension](https://github.com/h2000/zed-roc). Report issues to:\n[https://github.com/h2000/zed-roc/issues](https://github.com/h2000/zed-roc/issues)\n\n- Tree-sitter:\n  [faldor20/tree-sitter-roc](https://github.com/faldor20/tree-sitter-roc)\n- Language Server:\n  [roc-lang/roc/tree/main/crates/language_server](https://github.com/roc-lang/roc/tree/main/crates/language_server)\n\n## Setup\n\n1. Follow instructions to [Install Roc](https://www.roc-lang.org/install) from\n   the Roc-Lang website.\n2. Ensure `roc` and `roc_language_server` are in your PATH.\n"},{"path":"languages/roto.md","title":"Roto","group":"Languages","content":"# Roto\n\nRoto support is built into the editor.\n\nA statically-typed, compiled embedded scripting language for Rust.\n\n- [Source repository](https://codeberg.org/NLnetLabs/roto)\n- [Documentation](https://roto.docs.nlnetlabs.nl/en/stable/)\n- [tree-sitter-roto](https://github.com/NLnetLabs/tree-sitter-roto)\n"},{"path":"languages/rst.md","title":"ReStructuredText (rst)","group":"Languages","content":"# ReStructuredText (rst)\n\nReStructuredText language support in MonyaCode is provided by the\ncommunity-maintained [reST extension](https://github.com/elmarco/zed-rst).\nReport issues to:\n[https://github.com/elmarco/zed-rst/issues](https://github.com/elmarco/zed-rst/issues)\n\n- Tree-sitter:\n  [stsewd/tree-sitter-rst.git](https://github.com/stsewd/tree-sitter-rst.git)\n- Language Server: [swyddfa/esbonio](https://github.com/swyddfa/esbonio)\n"},{"path":"languages/ruby.md","title":"Ruby","group":"Languages","content":"# Ruby\n\nRuby support is available through the\n[Ruby extension](https://github.com/zed-extensions/ruby).\n\n- Tree-sitters:\n  - [tree-sitter-ruby](https://github.com/tree-sitter/tree-sitter-ruby)\n  - [tree-sitter-embedded-template](https://github.com/tree-sitter/tree-sitter-embedded-template)\n- Language Servers:\n  - [ruby-lsp](https://github.com/Shopify/ruby-lsp)\n  - [solargraph](https://github.com/castwide/solargraph)\n  - [rubocop](https://github.com/rubocop/rubocop)\n  - [Herb](https://herb-tools.dev)\n- Debug Adapter: [`rdbg`](https://github.com/ruby/debug)\n\nThe Ruby extension also provides support for ERB files.\n\n## Language Servers\n\nThere are multiple language servers available for Ruby. MonyaCode supports the two\nfollowing:\n\n- [solargraph](https://github.com/castwide/solargraph)\n- [ruby-lsp](https://github.com/Shopify/ruby-lsp)\n\nThey both have an overlapping feature set of autocomplete, diagnostics, code\nactions, etc. and it's up to you to decide which one you want to use. Note that\nyou can't use both at the same time.\n\nIn addition to these two language servers, MonyaCode also supports:\n\n- [rubocop](https://github.com/rubocop/rubocop) which is a static code analyzer\n  and linter for Ruby. Under the hood, it's also used by MonyaCode as a language\n  server, but its functionality is complimentary to that of solargraph and\n  ruby-lsp.\n- [sorbet](https://sorbet.org/) which is a static type checker for Ruby with a\n  custom gradual type system.\n- [steep](https://github.com/soutaro/steep) which is a static type checker for\n  Ruby that leverages Ruby Signature (RBS).\n- [Herb](https://herb-tools.dev) which is a language server for ERB files.\n\nWhen configuring a language server, it helps to open the LSP Logs window using\nthe 'dev: Open Language Server Logs' command. You can then choose the\ncorresponding language instance to see any logged information.\n\n## Configuring a language server\n\nThe [Ruby extension](https://github.com/zed-extensions/ruby) offers both\n`solargraph` and `ruby-lsp` language server support.\n\n### Language Server Activation\n\nFor all supported Ruby language servers (`solargraph`, `ruby-lsp`, `rubocop`,\n`sorbet`, and `steep`), the Ruby extension follows this activation sequence:\n\n1. If the language server is found in your project's `Gemfile`, it will be used\n   through `bundle exec`.\n2. If not found in the `Gemfile`, the Ruby extension will look for the\n   executable in your system `PATH`.\n3. If the language server is not found in either location, the Ruby extension\n   will automatically install it as a global gem (note: this will not install to\n   your current Ruby gemset).\n\nYou can skip step 1 and force using the system executable by setting\n`use_bundler` to `false` in your settings:\n\n```json\n{\n  \"lsp\": {\n    \"<SERVER_NAME>\": {\n      \"settings\": {\n        \"use_bundler\": false,\n      },\n    },\n  },\n}\n```\n\n### Using `solargraph`\n\n`solargraph` is enabled by default in the Ruby extension.\n\n### Using `ruby-lsp`\n\nTo switch to `ruby-lsp`, add the following to your `settings.json`:\n\n```json\n{\n  \"languages\": {\n    \"Ruby\": {\n      \"language_servers\": [\"ruby-lsp\", \"!solargraph\", \"!rubocop\", \"...\"],\n    },\n    // Enable herb and ruby-lsp for *.html.erb files\n    \"HTML+ERB\": {\n      \"language_servers\": [\"herb\", \"ruby-lsp\", \"...\"],\n    },\n    // Enable ruby-lsp for *.js.erb files\n    \"JS+ERB\": {\n      \"language_servers\": [\"ruby-lsp\", \"...\"],\n    },\n    // Enable ruby-lsp for *.yaml.erb files\n    \"YAML+ERB\": {\n      \"language_servers\": [\"ruby-lsp\", \"...\"],\n    },\n  },\n}\n```\n\nThat disables `solargraph` and `rubocop` and enables `ruby-lsp`.\n\n### Using `rubocop`\n\nThe Ruby extension also provides support for `rubocop` language server for\noffense detection and autocorrection.\n\nTo enable it, add the following to your `settings.json`:\n\n```json\n{\n  \"languages\": {\n    \"Ruby\": {\n      \"language_servers\": [\"ruby-lsp\", \"rubocop\", \"!solargraph\", \"...\"],\n    },\n  },\n}\n```\n\nOr, conversely, you can disable `ruby-lsp` and enable `solargraph` and `rubocop`\nby adding the following to your `settings.json`:\n\n```json\n{\n  \"languages\": {\n    \"Ruby\": {\n      \"language_servers\": [\"solargraph\", \"rubocop\", \"!ruby-lsp\", \"...\"],\n    },\n  },\n}\n```\n\n## Setting up `solargraph`\n\nSolargraph has formatting and diagnostics disabled by default. We can tell MonyaCode\nto enable them by adding the following to your `settings.json`:\n\n```json\n{\n  \"lsp\": {\n    \"solargraph\": {\n      \"initialization_options\": {\n        \"diagnostics\": true,\n        \"formatting\": true,\n      },\n    },\n  },\n}\n```\n\n### Configuration\n\nSolargraph reads its configuration from a file called `.solargraph.yml` in the\nroot of your project. For more information about this file, see the\n[Solargraph configuration documentation](https://solargraph.org/guides/configuration).\n\n## Setting up `ruby-lsp`\n\nYou can pass Ruby LSP configuration to `initialization_options`, e.g.\n\n```json\n{\n  \"languages\": {\n    \"Ruby\": {\n      \"language_servers\": [\"ruby-lsp\", \"!solargraph\", \"...\"],\n    },\n  },\n  \"lsp\": {\n    \"ruby-lsp\": {\n      \"initialization_options\": {\n        \"enabledFeatures\": {\n          // \"someFeature\": false\n        },\n      },\n    },\n  },\n}\n```\n\nLSP `settings` and `initialization_options` can also be project-specific. For\nexample to use [standardrb/standard](https://github.com/standardrb/standard) as\na formatter and linter for a particular project, add this to a\n`.monyacode/settings.jsonc` inside your project repo:\n\n```json\n{\n  \"lsp\": {\n    \"ruby-lsp\": {\n      \"initialization_options\": {\n        \"formatter\": \"standard\",\n        \"linters\": [\"standard\"],\n      },\n    },\n  },\n}\n```\n\n## Setting up `rubocop` LSP\n\nRubocop has unsafe autocorrection disabled by default. We can tell MonyaCode to\nenable it by adding the following to your `settings.json`:\n\n```json\n{\n  \"languages\": {\n    \"Ruby\": {\n      // Use ruby-lsp as the primary language server and rubocop as the secondary.\n      \"language_servers\": [\"ruby-lsp\", \"rubocop\", \"!solargraph\", \"...\"],\n    },\n  },\n  \"lsp\": {\n    \"rubocop\": {\n      \"initialization_options\": {\n        \"safeAutocorrect\": false,\n      },\n    },\n    \"ruby-lsp\": {\n      \"initialization_options\": {\n        \"enabledFeatures\": {\n          \"diagnostics\": false,\n        },\n      },\n    },\n  },\n}\n```\n\n## Setting up Sorbet\n\n[Sorbet](https://sorbet.org/) is a popular static type checker for Ruby that\nincludes a language server.\n\nTo enable Sorbet, add `\\\"sorbet\\\"` to the `language_servers` list for Ruby in\nyour `settings.json`. You may want to disable other language servers if Sorbet\nis intended to be your primary LSP, or if you plan to use it alongside another\nLSP for specific features like type checking.\n\n```json\n{\n  \"languages\": {\n    \"Ruby\": {\n      \"language_servers\": [\n        \"ruby-lsp\",\n        \"sorbet\",\n        \"!rubocop\",\n        \"!solargraph\",\n        \"...\",\n      ],\n    },\n  },\n}\n```\n\nFor all aspects of installing Sorbet, setting it up in your project, and\nconfiguring its behavior, please refer to the\n[official Sorbet documentation](https://sorbet.org/docs/overview).\n\n## Setting up Steep\n\n[Steep](https://github.com/soutaro/steep) is a static type checker for Ruby that\nuses RBS files to define types.\n\nTo enable Steep, add `\\\"steep\\\"` to the `language_servers` list for Ruby in your\n`settings.json`. You may need to adjust the order or disable other LSPs\ndepending on your desired setup.\n\n```json\n{\n  \"languages\": {\n    \"Ruby\": {\n      \"language_servers\": [\n        \"ruby-lsp\",\n        \"steep\",\n        \"!solargraph\",\n        \"!rubocop\",\n        \"...\",\n      ],\n    },\n  },\n}\n```\n\n## Setting up Herb\n\n`Herb` is enabled by default for the `HTML+ERB` language.\n\n## Using the Tailwind CSS Language Server with Ruby\n\nIt's possible to use the\n[Tailwind CSS Language Server](https://github.com/tailwindlabs/tailwindcss-intellisense/tree/HEAD/packages/tailwindcss-language-server#readme)\nin Ruby and ERB files.\n\nIn order to do that, you need to configure the language server so that it knows\nabout where to look for CSS classes in Ruby/ERB files by adding the following to\nyour `settings.json`:\n\n```json\n{\n  \"languages\": {\n    \"Ruby\": {\n      \"language_servers\": [\"tailwindcss-language-server\", \"...\"],\n    },\n  },\n  \"lsp\": {\n    \"tailwindcss-language-server\": {\n      \"settings\": {\n        \"experimental\": {\n          \"classRegex\": [\"\\\\bclass:\\\\s*['\\\"]([^'\\\"]*)['\\\"]\"],\n        },\n      },\n    },\n  },\n}\n```\n\nWith these settings you will get completions for Tailwind CSS classes in HTML\nattributes inside ERB files and inside Ruby/ERB strings that are coming after a\n`class:` key. Examples:\n\n```rb\n# Ruby file:\ndef method\n  div(class: \"pl-2 <completion here>\") do\n    p(class: \"mt-2 <completion here>\") { \"Hello World\" }\n  end\nend\n\n# ERB file:\n<%= link_to \"Hello\", \"/hello\", class: \"pl-2 <completion here>\" %>\n<a href=\"/hello\" class=\"pl-2 <completion here>\">Hello</a>\n```\n\n## Running tests\n\nTo run tests in your Ruby project, you can set up custom tasks in your local\n`.monyacode/tasks.jsonc` configuration file. These tasks can be defined to work with\ndifferent test frameworks like Minitest, RSpec, quickdraw, and tldr. Below are\nsome examples of how to set up these tasks to run your tests from within your\neditor.\n\n### Minitest with Rails\n\n```json [tasks]\n[\n  {\n    \"label\": \"test $MONYACODE_RELATIVE_FILE -n /$MONYACODE_CUSTOM_RUBY_TEST_NAME/\",\n    \"command\": \"bin/rails\",\n    \"args\": [\n      \"test\",\n      \"$MONYACODE_RELATIVE_FILE\",\n      \"-n\",\n      \"\\\"$MONYACODE_CUSTOM_RUBY_TEST_NAME\\\"\"\n    ],\n    \"cwd\": \"$MONYACODE_WORKTREE_ROOT\",\n    \"tags\": [\"ruby-test\"]\n  }\n]\n```\n\n### Minitest\n\nPlain minitest does not support running tests by line number, only by name, so\nwe need to use `$MONYACODE_CUSTOM_RUBY_TEST_NAME` instead:\n\n```json [tasks]\n[\n  {\n    \"label\": \"-Itest $MONYACODE_RELATIVE_FILE -n /$MONYACODE_CUSTOM_RUBY_TEST_NAME/\",\n    \"command\": \"bundle\",\n    \"args\": [\n      \"exec\",\n      \"ruby\",\n      \"-Itest\",\n      \"$MONYACODE_RELATIVE_FILE\",\n      \"-n\",\n      \"\\\"$MONYACODE_CUSTOM_RUBY_TEST_NAME\\\"\"\n    ],\n    \"cwd\": \"$MONYACODE_WORKTREE_ROOT\",\n    \"tags\": [\"ruby-test\"]\n  }\n]\n```\n\n### RSpec\n\n```json [tasks]\n[\n  {\n    \"label\": \"test $MONYACODE_RELATIVE_FILE:$MONYACODE_ROW\",\n    \"command\": \"bundle\",\n    \"args\": [\"exec\", \"rspec\", \"\\\"$MONYACODE_RELATIVE_FILE:$MONYACODE_ROW\\\"\"],\n    \"cwd\": \"$MONYACODE_WORKTREE_ROOT\",\n    \"tags\": [\"ruby-test\"]\n  }\n]\n```\n\nSimilar task syntax can be used for other test frameworks such as `quickdraw` or\n`tldr`.\n\n## Debugging\n\nThe Ruby extension provides a debug adapter for debugging Ruby code. MonyaCode's name\nfor the adapter (in the UI and `debug.json`) is `rdbg`, and under the hood, it\nuses the [`debug`](https://github.com/ruby/debug) gem. The extension uses the\n[same activation logic](#language-server-activation) as the language servers.\n\n### Examples\n\n#### Debug a Ruby script\n\n```json [debug]\n[\n  {\n    \"label\": \"Debug current file\",\n    \"adapter\": \"rdbg\",\n    \"request\": \"launch\",\n    \"script\": \"$MONYACODE_FILE\",\n    \"cwd\": \"$MONYACODE_WORKTREE_ROOT\"\n  }\n]\n```\n\n#### Debug Rails server\n\n```json [debug]\n[\n  {\n    \"label\": \"Debug Rails server\",\n    \"adapter\": \"rdbg\",\n    \"request\": \"launch\",\n    \"command\": \"./bin/rails\",\n    \"args\": [\"server\"],\n    \"cwd\": \"$MONYACODE_WORKTREE_ROOT\",\n    \"env\": {\n      \"RUBY_DEBUG_OPEN\": \"true\"\n    }\n  }\n]\n```\n\n## Formatters\n\n### `erb-formatter`\n\nTo format ERB templates, you can use the `erb-formatter` formatter. This\nformatter uses the [`erb-formatter`](https://rubygems.org/gems/erb-formatter)\ngem to format ERB templates.\n\n```json\n{\n  \"HTML+ERB\": {\n    \"formatter\": {\n      \"external\": {\n        \"command\": \"erb-formatter\",\n        \"arguments\": [\"--stdin-filename\", \"{buffer_path}\"],\n      },\n    },\n  },\n}\n```\n"},{"path":"languages/rust.md","title":"Rust","group":"Languages","content":"# Rust\n\nRust support is available natively in MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-rust](https://github.com/tree-sitter/tree-sitter-rust)\n- Language Server:\n  [rust-lang/rust-analyzer](https://github.com/rust-lang/rust-analyzer)\n- Debug Adapter: [CodeLLDB](https://github.com/vadimcn/codelldb) (primary),\n  [GDB](https://sourceware.org/gdb/) (secondary, not available on Apple silicon)\n\n<!--\nTBD: Polish Rust Docs. MonyaCode is a good rust editor, good Rust docs make it look like we care about Rust (we do!)\nTBD: Users may not know what inlayHints, don't start there.\nTBD: Provide explicit examples not just `....`\n-->\n\n## Inlay Hints\n\nThe following configuration can be used to change the inlay hint settings for\n`rust-analyzer` in Rust:\n\n```json\n{\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"initialization_options\": {\n        \"inlayHints\": {\n          \"maxLength\": null,\n          \"lifetimeElisionHints\": {\n            \"enable\": \"skip_trivial\",\n            \"useParameterNames\": true,\n          },\n          \"closureReturnTypeHints\": {\n            \"enable\": \"always\",\n          },\n        },\n      },\n    },\n  },\n}\n```\n\nSee\n[Inlay Hints](https://rust-analyzer.github.io/book/features.html#inlay-hints) in\nthe Rust Analyzer Manual for more information.\n\n## Target directory\n\nThe `rust-analyzer` target directory can be set in `initialization_options`:\n\n```json\n{\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"initialization_options\": {\n        \"rust\": {\n          \"analyzerTargetDir\": true,\n        },\n      },\n    },\n  },\n}\n```\n\nA `true` setting will set the target directory to `target/rust-analyzer`. You\ncan set a custom directory with a string like `\"target/analyzer\"` instead of\n`true`.\n\n## Binary\n\nYou can configure which `rust-analyzer` binary MonyaCode should use.\n\nBy default, MonyaCode will try to find a `rust-analyzer` in your `$PATH` and try to\nuse that. If that binary successfully executes `rust-analyzer --help`, it's\nused. Otherwise, MonyaCode will fall back to installing its own stable\n`rust-analyzer` version and use that.\n\nIf you want to install pre-release `rust-analyzer` version instead you can\ninstruct MonyaCode to do so by setting `pre_release` to `true` in your\n`settings.json`:\n\n```json\n{\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"fetch\": {\n        \"pre_release\": true,\n      },\n    },\n  },\n}\n```\n\nIf you want to disable MonyaCode looking for a `rust-analyzer` binary, you can set\n`ignore_system_version` to `true` in your `settings.json`:\n\n```json\n{\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"binary\": {\n        \"ignore_system_version\": true,\n      },\n    },\n  },\n}\n```\n\nIf you want to use a binary in a custom location, you can specify a `path` and\noptional `arguments`:\n\n```json\n{\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"binary\": {\n        \"path\": \"/Users/example/bin/rust-analyzer\",\n        \"arguments\": [],\n      },\n    },\n  },\n}\n```\n\nThis `\"path\"` has to be an absolute path.\n\n## Alternate Targets\n\nIf you want rust-analyzer to provide diagnostics for a target other than your\ncurrent platform (e.g. for windows when running on macOS) you can use the\nfollowing MonyaCode lsp settings:\n\n```json\n{\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"initialization_options\": {\n        \"cargo\": {\n          \"target\": \"x86_64-pc-windows-msvc\",\n        },\n      },\n    },\n  },\n}\n```\n\nIf you are using `rustup`, you can find a list of available target triples\n(`aarch64-apple-darwin`, `x86_64-unknown-linux-gnu`, etc) by running:\n\n```sh\nrustup target list --installed\n```\n\n## LSP tasks\n\nMonyaCode provides tasks using tree-sitter, but rust-analyzer has an LSP extension\nmethod for querying file-related tasks via LSP. This is enabled by default and\ncan be configured as\n\n```json\n\"lsp\": {\n  \"rust-analyzer\": {\n    \"enable_lsp_tasks\": true,\n  }\n}\n```\n\n## Manual Cargo Diagnostics fetch\n\nBy default, rust-analyzer has `checkOnSave: true` enabled, which causes every\nbuffer save to trigger a `cargo check --workspace --all-targets` command. If\ndisabled with `checkOnSave: false` (see the example of the server configuration\njson above), it's still possible to fetch the diagnostics manually, with the\n`editor: run/clear/cancel flycheck` commands in Rust files to refresh cargo\ndiagnostics; the project diagnostics editor will also refresh cargo diagnostics\nwith `editor: run flycheck` command when the setting is enabled.\n\n## More server configuration\n\n<!--\nTBD: Is it possible to specify RUSTFLAGS? https://github.com/zed-industries/zed/issues/14334\n-->\n\nRust-analyzer [manual](https://rust-analyzer.github.io/book/) describes various\nfeatures and configuration options for rust-analyzer language server.\nRust-analyzer in MonyaCode runs with the default parameters.\n\n### Large projects and performance\n\nOne of the main caveats that might cause extensive resource usage on large\nprojects, is the combination of the following features:\n\n```\nrust-analyzer.checkOnSave (default: true)\n    Run the check command for diagnostics on save.\n```\n\n```\nrust-analyzer.check.workspace (default: true)\n    Whether --workspace should be passed to cargo check. If false, -p <package> will be passed instead.\n```\n\n```\nrust-analyzer.cargo.allTargets (default: true)\n    Pass --all-targets to cargo invocation\n```\n\nWhich would mean that every time MonyaCode saves, a\n`cargo check --workspace --all-targets` command is run, checking the entire\nproject (workspace), lib, doc, test, bin, bench and\n[other targets](https://doc.rust-lang.org/cargo/reference/cargo-targets.html).\n\nWhile that works fine on small projects, it does not scale well.\n\nThe alternatives would be to use [tasks](../tasks.md), as MonyaCode already provides\na `cargo check --workspace --all-targets` task and the ability to cmd/ctrl-click\non the terminal output to navigate to the error, and limit or turn off the check\non save feature entirely.\n\nCheck on save feature is responsible for returning part of the diagnostics based\non cargo check output, so turning it off will limit rust-analyzer with its own\n[diagnostics](https://rust-analyzer.github.io/book/diagnostics.html).\n\nConsider more `rust-analyzer.cargo.` and `rust-analyzer.check.` and\n`rust-analyzer.diagnostics.` settings from the manual for more fine-grained\nconfiguration. Here's a snippet for MonyaCode settings.json (the language server will\nrestart automatically after the `lsp.rust-analyzer` section is edited and\nsaved):\n\n```json\n{\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"initialization_options\": {\n        // get more cargo-less diagnostics from rust-analyzer,\n        // which might include false-positives (those can be turned off by their names)\n        \"diagnostics\": {\n          \"experimental\": {\n            \"enable\": true,\n          },\n        },\n        // To disable the checking entirely\n        // (ignores all cargo and check settings below)\n        \"checkOnSave\": false,\n        // To check the `lib` target only.\n        \"cargo\": {\n          \"allTargets\": false,\n        },\n        // Use `-p` instead of `--workspace` for cargo check\n        \"check\": {\n          \"workspace\": false,\n        },\n      },\n    },\n  },\n}\n```\n\n### Multi-project workspaces\n\nIf you want rust-analyzer to analyze multiple Rust projects in the same folder\nthat are not listed in `[members]` in the Cargo workspace, you can list them in\n`linkedProjects` in the local project settings:\n\n```json\n{\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"initialization_options\": {\n        \"linkedProjects\": [\"./path/to/a/Cargo.toml\", \"./path/to/b/Cargo.toml\"],\n      },\n    },\n  },\n}\n```\n\n### Snippets\n\nThere's a way to get custom completion items from rust-analyzer, that will\ntransform the code according to the snippet body:\n\n```json\n{\n  \"lsp\": {\n    \"rust-analyzer\": {\n      \"initialization_options\": {\n        \"completion\": {\n          \"snippets\": {\n            \"custom\": {\n              \"Arc::new\": {\n                \"postfix\": \"arc\",\n                \"body\": [\"Arc::new(${receiver})\"],\n                \"requires\": \"std::sync::Arc\",\n                \"scope\": \"expr\",\n              },\n              \"Some\": {\n                \"postfix\": \"some\",\n                \"body\": [\"Some(${receiver})\"],\n                \"scope\": \"expr\",\n              },\n              \"Ok\": {\n                \"postfix\": \"ok\",\n                \"body\": [\"Ok(${receiver})\"],\n                \"scope\": \"expr\",\n              },\n              \"Rc::new\": {\n                \"postfix\": \"rc\",\n                \"body\": [\"Rc::new(${receiver})\"],\n                \"requires\": \"std::rc::Rc\",\n                \"scope\": \"expr\",\n              },\n              \"Box::pin\": {\n                \"postfix\": \"boxpin\",\n                \"body\": [\"Box::pin(${receiver})\"],\n                \"requires\": \"std::boxed::Box\",\n                \"scope\": \"expr\",\n              },\n              \"vec!\": {\n                \"postfix\": \"vec\",\n                \"body\": [\"vec![${receiver}]\"],\n                \"description\": \"vec![]\",\n                \"scope\": \"expr\",\n              },\n            },\n          },\n        },\n      },\n    },\n  },\n}\n```\n\n## Debugging\n\nMonyaCode supports debugging Rust binaries and tests out of the box with `CodeLLDB`\nand `GDB`. Run {#action debugger::Start} ({#kb debugger::Start}) to launch one\nof these preconfigured debug tasks.\n\nFor more control, you can add debug configurations to `.monyacode/debug.jsonc`. See\nthe examples below.\n\n- [CodeLLDB configuration documentation](https://github.com/vadimcn/codelldb/blob/master/MANUAL.md#starting-a-new-debug-session)\n- [GDB configuration documentation](https://sourceware.org/gdb/current/onlinedocs/gdb.html/Debugger-Adapter-Protocol.html)\n\n### Build binary then debug\n\n```json [debug]\n[\n  {\n    \"label\": \"Build & Debug native binary\",\n    \"build\": {\n      \"command\": \"cargo\",\n      \"args\": [\"build\"]\n    },\n    \"program\": \"$MONYACODE_WORKTREE_ROOT/target/debug/binary\",\n    // sourceLanguages is required for CodeLLDB (not GDB) when using Rust\n    \"sourceLanguages\": [\"rust\"],\n    \"request\": \"launch\",\n    \"adapter\": \"CodeLLDB\"\n  }\n]\n```\n\n### Automatically locate a debug target based on build command\n\nWhen you use `cargo build` or `cargo test` as the build command, MonyaCode can infer\nthe path to the output binary.\n\n```json [debug]\n[\n  {\n    \"label\": \"Build & Debug native binary\",\n    \"adapter\": \"CodeLLDB\",\n    \"build\": {\n      \"command\": \"cargo\",\n      \"args\": [\"build\"]\n    },\n    // sourceLanguages is required for CodeLLDB (not GDB) when using Rust\n    \"sourceLanguages\": [\"rust\"]\n  }\n]\n```\n"},{"path":"languages/scala.md","title":"Scala","group":"Languages","content":"# Scala\n\nScala language support in MonyaCode is provided by the community-maintained\n[Scala extension](https://github.com/scalameta/metals-zed). Report issues to:\n[https://github.com/scalameta/metals-zed/issues](https://github.com/scalameta/metals-zed/issues)\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-scala](https://github.com/tree-sitter/tree-sitter-scala)\n- Language Server: [scalameta/metals](https://github.com/scalameta/metals)\n\n## Setup\n\n- Install Scala with `cs setup` (Coursier): https://www.scala-lang.org/download/\n  - `brew install coursier/formulas/coursier && cs setup`\n- REPL (Almond) Setup Instructions https://almond.sh/docs/quick-start-install\n  - `brew install --cask temurin` (Eclipse foundation official OpenJDK binaries)\n  - `brew install coursier/formulas/coursier && cs setup`\n  - `coursier launch --use-bootstrap almond -- --install`\n\n## Configuration\n\nBehavior of the Metals language server can be controlled with:\n\n- `.scalafix.conf` file - See\n  [Scalafix Configuration](https://scalacenter.github.io/scalafix/docs/users/configuration.html)\n- `.scalafmt.conf` file - See\n  [Scalafmt Configuration](https://scalameta.org/scalafmt/docs/configuration.html)\n\nYou can place these files in the root of your project or specifying their\nlocation in the Metals configuration. See\n[Metals User Configuration](https://scalameta.org/metals/docs/editors/user-configuration)\nfor more.\n\n<!--\nTBD: Provide LSP configuration example for metals in MonyaCode settings.json. metals.{javaHome,excludedPackages,customProjectRoot} etc.\n-->\n"},{"path":"languages/scheme.md","title":"Scheme","group":"Languages","content":"# Scheme\n\nScheme is supported in the editor.\n\n- Tree-sitter:\n  [6cdh/tree-sitter-scheme](https://github.com/6cdh/tree-sitter-scheme)\n"},{"path":"languages/sh.md","title":"Shell Scripts","group":"Languages","content":"# Shell Scripts\n\nShell Scripts (bash, zsh, dash, sh) are supported natively by MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-bash](https://github.com/tree-sitter/tree-sitter-bash)\n\n## Settings\n\nYou can configure various settings for Shell Scripts in your MonyaCode User Settings\n(`~/.config/monyacode/settings.json`) or MonyaCode Project Settings\n(`.monyacode/settings.json`):\n\n```json\n  \"languages\": {\n    \"Shell Script\": {\n      \"tab_size\": 2,\n      \"hard_tabs\": false\n    }\n  }\n```\n\n### Formatting\n\nMonyaCode supports auto-formatting Shell Scripts using external tools like\n[`shfmt`](https://github.com/mvdan/sh).\n\n1. Install `shfmt`:\n\n```sh\nbrew install shfmt            # macos (homebrew)\nsudo apt-get install shfmt    # debian/ubuntu\ndnf install shfmt             # fedora\nyum install shfmt             # redhat\npacman -Sy shfmt              # archlinux\nchoco install shfmt           # windows (chocolatey)\n```\n\n2. Ensure `shfmt` is available in your path and check the version:\n\n```sh\nwhich shfmt\nshfmt --version\n```\n\n3. Configure MonyaCode to automatically format Shell Scripts with `shfmt` on save:\n\n```json\n  \"languages\": {\n    \"Shell Script\": {\n      \"format_on_save\": \"on\",\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"shfmt\",\n          // Change `--indent 2` to match your preferred tab_size\n          \"arguments\": [\"--filename\", \"{buffer_path}\", \"--indent\", \"2\"]\n        }\n      }\n    }\n  }\n```\n\n## See also:\n\n- [Language Support for Bash](./bash.md)\n- [Language Support for Fish](./fish.md)\n"},{"path":"languages/sql.md","title":"SQL","group":"Languages","content":"# SQL\n\nSyntax highlighting and formatting support for SQL is built into the editor.\n\n- [Tree sitter grammar](https://github.com/DerekStride/tree-sitter-sql)\n\n### Formatting\n\nSQL auto-formatting can be added to the editor using external tools like\n[`sql-formatter`](https://github.com/sql-formatter-org/sql-formatter).\n\n1. Install `sql-formatter`:\n\n```sh\nnpm install -g sql-formatter\n```\n\n2. Ensure `sql-formatter` is available in your path and check the version:\n\n```sh\nwhich sql-formatter\nsql-formatter --version\n```\n\n3. Configure MonyaCode to automatically format SQL with `sql-formatter`:\n\n```json\n  \"languages\": {\n    \"SQL\": {\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"sql-formatter\",\n          \"arguments\": [\"--language\", \"mysql\"]\n        }\n      }\n    }\n  },\n```\n\nSubstitute your preferred [SQL Dialect] for `mysql` above (`duckdb`, `hive`,\n`mariadb`, `postgresql`, `redshift`, `snowflake`, `sqlite`, `spark`, etc).\n\nYou can add this to MonyaCode project settings (`.monyacode/settings.json`) or via your\nMonyaCode user settings (`~/.config/monyacode/settings.json`).\n\n### Advanced Formatting\n\nSql-formatter also allows more precise control by providing\n[sql-formatter configuration options](https://github.com/sql-formatter-org/sql-formatter#configuration-options).\nTo provide these, create a `.sql-formatter.json` file in your project:\n\n```json\n{\n  \"language\": \"postgresql\",\n  \"tabWidth\": 2,\n  \"keywordCase\": \"upper\",\n  \"linesBetweenQueries\": 2,\n}\n```\n\nWhen using a `.sql-formatter.json` file you can use a more simplified set of\nMonyaCode settings since the language need not be specified inline:\n\n```json\n  \"languages\": {\n    \"SQL\": {\n      \"formatter\": {\n        \"external\": {\n          \"command\": \"sql-formatter\"\n        }\n      }\n    }\n  },\n```\n"},{"path":"languages/svelte.md","title":"Svelte","group":"Languages","content":"# Svelte\n\nSvelte support is available through the\n[Svelte extension](https://github.com/zed-extensions/svelte).\n\n- Tree-sitter:\n  [tree-sitter-grammars/tree-sitter-svelte](https://github.com/tree-sitter-grammars/tree-sitter-svelte)\n- Language Server:\n  [sveltejs/language-tools](https://github.com/sveltejs/language-tools)\n\n## Extra theme styling configuration\n\nYou can modify how certain styles, such as directives and modifiers, appear in\nattributes:\n\n```json\n\"syntax\": {\n  // Styling for directives (e.g., `class:foo` or `on:click`) (the `on` or `class` part of the attribute).\n  \"attribute.function\": {\n    \"color\": \"#ff0000\"\n  },\n  // Styling for modifiers at the end of attributes, e.g. `on:<click|preventDefault|stopPropagation>`\n  \"attribute.special\": {\n    \"color\": \"#00ff00\"\n  }\n}\n```\n\n## Inlay Hints\n\nWhen inlay hints is enabled in MonyaCode, to make the language server send them back,\nMonyaCode sets the following initialization options:\n\n```json\n\"inlayHints\": {\n  \"parameterNames\": {\n    \"enabled\": \"all\",\n    \"suppressWhenArgumentMatchesName\": false\n  },\n  \"parameterTypes\": {\n    \"enabled\": true\n  },\n  \"variableTypes\": {\n    \"enabled\": true,\n    \"suppressWhenTypeMatchesName\": false\n  },\n  \"propertyDeclarationTypes\": {\n    \"enabled\": true\n  },\n  \"functionLikeReturnTypes\": {\n    \"enabled\": true\n  },\n  \"enumMemberValues\": {\n    \"enabled\": true\n  }\n}\n```\n\nTo override these settings, use the following:\n\n```json\n\"lsp\": {\n  \"svelte-language-server\": {\n    \"initialization_options\": {\n      \"configuration\": {\n        \"typescript\": {\n          // ......\n        },\n        \"javascript\": {\n          // ......\n        }\n      }\n    }\n  }\n}\n```\n\nSee\n[the TypeScript language server `package.json`](https://github.com/microsoft/vscode/blob/main/extensions/typescript-language-features/package.json)\nfor more information.\n"},{"path":"languages/swift.md","title":"Swift","group":"Languages","content":"# Swift\n\nSwift language support in MonyaCode is provided by the community-maintained\n[Swift extension](https://github.com/zed-extensions/swift). Report issues to:\n[https://github.com/zed-extensions/swift/issues](https://github.com/zed-extensions/swift/issues)\n\n- Tree-sitter:\n  [alex-pinkus/tree-sitter-swift](https://github.com/alex-pinkus/tree-sitter-swift)\n- Language Server:\n  [swiftlang/sourcekit-lsp](https://github.com/swiftlang/sourcekit-lsp)\n- Debug Adapter:\n  [`lldb-dap`](https://github.com/swiftlang/llvm-project/blob/next/lldb/tools/lldb-dap/README.md)\n\n## Language Server Configuration\n\nYou can modify the behavior of SourceKit LSP by creating a\n`.sourcekit-lsp/config.json` under your home directory or in your project root.\nSee\n[SourceKit-LSP configuration file](https://github.com/swiftlang/sourcekit-lsp/blob/main/Documentation/Configuration%20File.md)\nfor complete documentation.\n\n## Debugging\n\nThe Swift extension provides a debug adapter for debugging Swift code. MonyaCode's\nname for the adapter (in the UI and `debug.jsonc`) is `Swift`, and under the\nhood it uses\n[`lldb-dap`](https://github.com/swiftlang/llvm-project/blob/next/lldb/tools/lldb-dap/README.md),\nas provided by the Swift toolchain. The extension tries to find an `lldb-dap`\nbinary using `swiftly`, using `xcrun`, and by searching `$PATH`, in that order\nof preference. The extension doesn't attempt to download `lldb-dap` if it's not\nfound.\n\n- [lldb-dap configuration documentation](https://github.com/llvm/llvm-project/blob/main/lldb/tools/lldb-dap/README.md#configuration-settings-reference)\n\n### Examples\n\n#### Build and debug a Swift binary\n\n```json [debug]\n[\n  {\n    \"label\": \"Debug Swift\",\n    \"build\": {\n      \"command\": \"swift\",\n      \"args\": [\"build\"]\n    },\n    \"program\": \"$MONYACODE_WORKTREE_ROOT/swift-app/.build/arm64-apple-macosx/debug/swift-app\",\n    \"request\": \"launch\",\n    \"adapter\": \"Swift\"\n  }\n]\n```\n"},{"path":"languages/tailwindcss.md","title":"Tailwind CSS","group":"Languages","content":"# Tailwind CSS\n\nMonyaCode has built-in support for Tailwind CSS autocomplete, linting, and hover\npreviews.\n\n- Language Server:\n  [tailwindlabs/tailwindcss-intellisense](https://github.com/tailwindlabs/tailwindcss-intellisense)\n\n## Configuration\n\nTo configure the Tailwind CSS language server, refer\n[to the extension settings](https://github.com/tailwindlabs/tailwindcss-intellisense?tab=readme-ov-file#extension-settings)\nand add them to the `lsp` section of your `settings.json`:\n\n```json\n{\n  \"lsp\": {\n    \"tailwindcss-language-server\": {\n      \"settings\": {\n        \"classFunctions\": [\"cva\", \"cx\"],\n        \"experimental\": {\n          \"classRegex\": [\"[cls|className]\\\\s\\\\:\\\\=\\\\s\\\"([^\\\"]*)\"],\n        },\n      },\n    },\n  },\n}\n```\n\nLanguages which can be used with Tailwind CSS in MonyaCode:\n\n- [Astro](./astro.md)\n- [CSS](./css.md)\n- [ERB](./ruby.md)\n- [Gleam](./gleam.md)\n- [HEEx](./elixir.md#heex)\n- [HTML](./html.md)\n- [TypeScript](./typescript.md)\n- [JavaScript](./javascript.md)\n- [PHP](./php.md)\n- [Svelte](./svelte.md)\n- [Vue](./vue.md)\n\n### Prettier Plugin\n\nMonyaCode supports Prettier out of the box, which means that if you have the\n[Tailwind CSS Prettier plugin](https://github.com/tailwindlabs/prettier-plugin-tailwindcss)\ninstalled, adding it to your Prettier configuration will make it work\nautomatically:\n\n```json\n// .prettierrc\n{\n  \"plugins\": [\"prettier-plugin-tailwindcss\"],\n}\n```\n"},{"path":"languages/terraform.md","title":"Terraform","group":"Languages","content":"# Terraform\n\nTerraform support is available through the\n[Terraform extension](https://github.com/zed-extensions/terraform).\n\n- Tree-sitter:\n  [MichaHoffmann/tree-sitter-hcl](https://github.com/MichaHoffmann/tree-sitter-hcl)\n- Language Server:\n  [hashicorp/terraform-ls](https://github.com/hashicorp/terraform-ls)\n\n## Configuration\n\n<!--\nTBD: Add example using `rootModulePaths` to match upstream example https://github.com/hashicorp/terraform-ls/blob/main/docs/SETTINGS.md#vs-code\n-->\n\nThe Terraform language server can be configured in your `settings.jsonc`, e.g.:\n\n```json\n{\n  \"lsp\": {\n    \"terraform-ls\": {\n      \"initialization_options\": {\n        \"experimentalFeatures\": {\n          \"prefillRequiredFields\": true,\n        },\n      },\n    },\n  },\n}\n```\n\nSee the\n[full list of server settings here](https://github.com/hashicorp/terraform-ls/blob/main/docs/SETTINGS.md).\n"},{"path":"languages/toml.md","title":"TOML","group":"Languages","content":"# TOML\n\nTOML support is built into the editor.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-toml](https://github.com/tree-sitter/tree-sitter-toml)\n\n- Crate: [adot-tree-sitter-toml](https://crates.io/crates/adot-tree-sitter-toml)\n\nThere is LSP support via [Taplo](https://taplo.tamasfe.dev), but it is disabled\nby default. To enable the LSP, add this to your `settings.jsonc`:\n\n```json\n{\n  \"languages\": {\n    \"TOML\": {\n      \"language_servers\": [\"taplo\", \"...\"],\n    },\n  },\n}\n```\n"},{"path":"languages/typescript.md","title":"TypeScript","group":"Languages","content":"# TypeScript\n\nTypeScript and TSX support are available natively in MonyaCode.\n\n- Tree-sitter:\n  [tree-sitter/tree-sitter-typescript](https://github.com/tree-sitter/tree-sitter-typescript)\n- Language Server: [yioneko/vtsls](https://github.com/yioneko/vtsls)\n- Alternate Language Server:\n  [typescript-language-server/typescript-language-server](https://github.com/typescript-language-server/typescript-language-server)\n- Debug Adapter: [vscode-js-debug](https://github.com/microsoft/vscode-js-debug)\n\n<!--\nTBD: Document the difference between Language servers\n-->\n\n## Language servers\n\nBy default MonyaCode uses [vtsls](https://github.com/yioneko/vtsls) for TypeScript,\nTSX, and JavaScript files. You can configure the use of\n[typescript-language-server](https://github.com/typescript-language-server/typescript-language-server)\nper language in your settings file:\n\n```json\n{\n  \"languages\": {\n    \"TypeScript\": {\n      \"language_servers\": [\"typescript-language-server\", \"!vtsls\", \"...\"],\n    },\n    \"TSX\": {\n      \"language_servers\": [\"typescript-language-server\", \"!vtsls\", \"...\"],\n    },\n    \"JavaScript\": {\n      \"language_servers\": [\"typescript-language-server\", \"!vtsls\", \"...\"],\n    },\n  },\n}\n```\n\nPrettier will also be used for TypeScript files by default. To disable this:\n\n```json\n{\n  \"languages\": {\n    \"TypeScript\": {\n      \"prettier\": { \"allowed\": false },\n    },\n    //...\n  },\n}\n```\n\n## Large projects\n\n`vtsls` may run out of memory on very large projects. We default the limit to\n8092 (8 GiB) vs. the default of 3072 but this may not be sufficient for you:\n\n```json\n{\n  \"lsp\": {\n    \"vtsls\": {\n      \"settings\": {\n        // For TypeScript:\n        \"typescript\": { \"tsserver\": { \"maxTsServerMemory\": 16184 } },\n        // For JavaScript:\n        \"javascript\": { \"tsserver\": { \"maxTsServerMemory\": 16184 } },\n      },\n    },\n  },\n}\n```\n\n## Inlay Hints\n\nMonyaCode sets the following initialization options to make the language server send\nback inlay hints (that is, when MonyaCode has inlay hints enabled in the settings).\n\nYou can override these settings in your MonyaCode `settings.jsonc` when using\n`typescript-language-server`:\n\n```json\n{\n  \"lsp\": {\n    \"typescript-language-server\": {\n      \"initialization_options\": {\n        \"preferences\": {\n          \"includeInlayParameterNameHints\": \"all\",\n          \"includeInlayParameterNameHintsWhenArgumentMatchesName\": true,\n          \"includeInlayFunctionParameterTypeHints\": true,\n          \"includeInlayVariableTypeHints\": true,\n          \"includeInlayVariableTypeHintsWhenTypeMatchesName\": true,\n          \"includeInlayPropertyDeclarationTypeHints\": true,\n          \"includeInlayFunctionLikeReturnTypeHints\": true,\n          \"includeInlayEnumMemberValueHints\": true,\n        },\n      },\n    },\n  },\n}\n```\n\nSee\n[typescript-language-server inlayhints documentation](https://github.com/typescript-language-server/typescript-language-server?tab=readme-ov-file#inlay-hints-textdocumentinlayhint)\nfor more information.\n\nWhen using `vtsls`:\n\n```json\n{\n  \"lsp\": {\n    \"vtsls\": {\n      \"settings\": {\n        // For JavaScript:\n        \"javascript\": {\n          \"inlayHints\": {\n            \"parameterNames\": {\n              \"enabled\": \"all\",\n              \"suppressWhenArgumentMatchesName\": false,\n            },\n            \"parameterTypes\": {\n              \"enabled\": true,\n            },\n            \"variableTypes\": {\n              \"enabled\": true,\n              \"suppressWhenTypeMatchesName\": true,\n            },\n            \"propertyDeclarationTypes\": {\n              \"enabled\": true,\n            },\n            \"functionLikeReturnTypes\": {\n              \"enabled\": true,\n            },\n            \"enumMemberValues\": {\n              \"enabled\": true,\n            },\n          },\n        },\n        // For TypeScript:\n        \"typescript\": {\n          \"inlayHints\": {\n            \"parameterNames\": {\n              \"enabled\": \"all\",\n              \"suppressWhenArgumentMatchesName\": false,\n            },\n            \"parameterTypes\": {\n              \"enabled\": true,\n            },\n            \"variableTypes\": {\n              \"enabled\": true,\n              \"suppressWhenTypeMatchesName\": true,\n            },\n            \"propertyDeclarationTypes\": {\n              \"enabled\": true,\n            },\n            \"functionLikeReturnTypes\": {\n              \"enabled\": true,\n            },\n            \"enumMemberValues\": {\n              \"enabled\": true,\n            },\n          },\n        },\n      },\n    },\n  },\n}\n```\n\n## Debugging\n\nMonyaCode supports debugging TypeScript code out of the box with `vscode-js-debug`.\nThe following can be debugged without writing additional configuration:\n\n- Tasks from `package.json`\n- Tests written using several popular frameworks (Jest, Mocha, Vitest, Jasmine,\n  Bun, Node)\n\nRun {#action debugger::Start} ({#kb debugger::Start}) to see a contextual list\nof these predefined debug tasks.\n\n> **Note:** Bun test is automatically detected when `@types/bun` is present in\n> `package.json`.\n>\n> **Note:** Node test is automatically detected when `@types/node` is present in\n> `package.json` (requires Node.js 20+).\n\nAs for all languages, configurations from `.vscode/launch.json` are also\navailable for debugging in MonyaCode.\n\nIf your use-case isn't covered by any of these, you can take full control by\nadding debug configurations to `.monyacode/debug.jsonc`. See below for example\nconfigurations.\n\n### Configuring JavaScript debug tasks\n\nJavaScript debugging is more complicated than other languages because there are\ntwo different environments: Node.js and the browser. `vscode-js-debug` exposes a\n`type` field, that you can use to specify the environment, either `node` or\n`chrome`.\n\n- [vscode-js-debug configuration documentation](https://github.com/microsoft/vscode-js-debug/blob/main/OPTIONS.md)\n\n### Attach debugger to a server running in web browser (`npx serve`)\n\nGiven an externally-ran web server (e.g., with `npx serve` or `npx live-server`)\none can attach to it and open it with a browser.\n\n```json [debug]\n[\n  {\n    \"label\": \"Launch Chrome (TypeScript)\",\n    \"adapter\": \"JavaScript\",\n    \"type\": \"chrome\",\n    \"request\": \"launch\",\n    \"url\": \"http://localhost:5500\",\n    \"program\": \"$MONYACODE_FILE\",\n    \"webRoot\": \"${MONYACODE_WORKTREE_ROOT}\",\n    \"build\": {\n      \"command\": \"npx\",\n      \"args\": [\"tsc\"]\n    },\n    \"skipFiles\": [\"<node_internals>/**\"]\n  }\n]\n```\n\n## See also\n\n- [MonyaCode Yarn documentation](./yarn.md) for a walkthrough of configuring your\n  project to use Yarn.\n- [MonyaCode Deno documentation](./deno.md)\n"},{"path":"languages/uiua.md","title":"Uiua","group":"Languages","content":"# Uiua\n\n[Uiua](https://www.uiua.org/) is a general purpose, stack-based, array-oriented\nprogramming language with a focus on simplicity, beauty, and tacit code.\n\nUiua support is available through the\n[Uiua extension](https://github.com/zed-extensions/uiua).\n\n- Tree-sitter:\n  [shnarazk/tree-sitter-uiua](https://github.com/shnarazk/tree-sitter-uiua)\n- Language Server: [uiua-lang/uiua](https://github.com/uiua-lang/uiua/)\n"},{"path":"languages/vue.md","title":"Vue","group":"Languages","content":"# Vue\n\nVue support is available through the\n[Vue extension](https://github.com/zed-extensions/vue).\n\n- Tree-sitter:\n  [tree-sitter-grammars/tree-sitter-vue](https://github.com/tree-sitter-grammars/tree-sitter-vue)\n- Language Server:\n  [vuejs/language-tools/](https://github.com/vuejs/language-tools/)\n"},{"path":"languages/xml.md","title":"XML","group":"Languages","content":"# XML\n\nXML syntax highlighting is supported natively.\n\n- Tree-sitter:\n  [tree-sitter-grammars/tree-sitter-xml](https://github.com/tree-sitter-grammars/tree-sitter-xml)\n\n## Configuration\n\nIf you have additional file extensions that are not being automatically\nrecognized as XML just add them to\n[file_types](../configuring-monyacode.md#file-types) in your MonyaCode settings:\n\n```json\n  \"file_types\": {\n    \"XML\": [\"rdf\", \"gpx\", \"kml\"]\n  }\n```\n"},{"path":"languages/yaml.md","title":"YAML","group":"Languages","content":"# YAML\n\nYAML support is available natively in MonyaCode.\n\n- Tree-sitter:\n  [zed-industries/tree-sitter-yaml](https://github.com/zed-industries/tree-sitter-yaml)\n- Language Server:\n  [redhat-developer/yaml-language-server](https://github.com/redhat-developer/yaml-language-server)\n\n## Configuration\n\nYou can configure various\n[yaml-language-server settings](https://github.com/redhat-developer/yaml-language-server?tab=readme-ov-file#language-server-settings)\nby adding them to your MonyaCode `settings.jsonc` in a `yaml-language-server` block\nunder the `lsp` key. For example:\n\n```json\n  \"lsp\": {\n    \"yaml-language-server\": {\n      \"settings\": {\n        \"yaml\": {\n          \"keyOrdering\": true,\n          \"format\": {\n            \"singleQuote\": true\n          },\n          \"schemas\": {\n              \"https://getcomposer.org/schema.json\": [\"/*\"],\n              \"../relative/path/schema.json\": [\"/config*.yaml\"]\n          }\n        }\n      }\n    }\n  }\n```\n\nNote, settings keys must be nested, so `yaml.keyOrdering` becomes\n`{\"yaml\": { \"keyOrdering\": true }}`.\n\n## Formatting\n\nBy default, MonyaCode uses Prettier for formatting YAML files.\n\n### Prettier Formatting\n\nYou can customize the formatting behavior of Prettier. For example to use\nsingle-quotes in yaml files add the following to your `.prettierrc`\nconfiguration file:\n\n```json\n{\n  \"overrides\": [\n    {\n      \"files\": [\"*.yaml\", \"*.yml\"],\n      \"options\": {\n        \"singleQuote\": false,\n      },\n    },\n  ],\n}\n```\n\n### yaml-language-server Formatting\n\nTo use `yaml-language-server` instead of Prettier for YAML formatting, add the\nfollowing to your MonyaCode `settings.json`:\n\n```json\n  \"languages\": {\n    \"YAML\": {\n      \"formatter\": \"language_server\"\n    }\n  }\n```\n\n## Schemas\n\nBy default yaml-language-server will attempt to determine the correct schema for\na given yaml file and retrieve the appropriate JSON Schema from\n[Json Schema Store](https://schemastore.org/).\n\nYou can override any auto-detected schema via the `schemas` settings key\n(demonstrated above) or by providing an\n[inlined schema](https://github.com/redhat-developer/yaml-language-server#using-inlined-schema)\nreference via a modeline comment at the top of your yaml file:\n\n```yaml\n# yaml-language-server: $schema=https://www.schemastore.org/github-action.json\nname: Issue Assignment\non:\n  issues:\n    types: [opened]\n```\n\nYou can disable the automatic detection and retrieval of schemas from the JSON\nSchema if desired:\n\n```json\n  \"lsp\": {\n    \"yaml-language-server\": {\n      \"settings\": {\n        \"yaml\": {\n          \"schemaStore\": {\n            \"enable\": false\n          }\n        }\n      }\n    }\n  }\n```\n\n## Custom Tags\n\nYaml-language-server supports\n[custom tags](https://github.com/redhat-developer/yaml-language-server#adding-custom-tags)\nwhich can be used to inject custom application functionality at runtime into\nyour yaml files.\n\nFor example Amazon CloudFormation YAML uses a number of custom tags, to support\nthese you can add the following to your settings.json:\n\n```json\n  \"lsp\": {\n    \"yaml-language-server\": {\n      \"settings\": {\n        \"yaml\": {\n          \"customTags\": [\n            \"!And scalar\",\n            \"!And mapping\",\n            \"!And sequence\",\n            \"!If scalar\",\n            \"!If mapping\",\n            \"!If sequence\",\n            \"!Not scalar\",\n            \"!Not mapping\",\n            \"!Not sequence\",\n            \"!Equals scalar\",\n            \"!Equals mapping\",\n            \"!Equals sequence\",\n            \"!Or scalar\",\n            \"!Or mapping\",\n            \"!Or sequence\",\n            \"!FindInMap scalar\",\n            \"!FindInMap mapping\",\n            \"!FindInMap sequence\",\n            \"!Base64 scalar\",\n            \"!Base64 mapping\",\n            \"!Base64 sequence\",\n            \"!Cidr scalar\",\n            \"!Cidr mapping\",\n            \"!Cidr sequence\",\n            \"!Ref scalar\",\n            \"!Ref mapping\",\n            \"!Ref sequence\",\n            \"!Sub scalar\",\n            \"!Sub mapping\",\n            \"!Sub sequence\",\n            \"!GetAtt scalar\",\n            \"!GetAtt mapping\",\n            \"!GetAtt sequence\",\n            \"!GetAZs scalar\",\n            \"!GetAZs mapping\",\n            \"!GetAZs sequence\",\n            \"!ImportValue scalar\",\n            \"!ImportValue mapping\",\n            \"!ImportValue sequence\",\n            \"!Select scalar\",\n            \"!Select mapping\",\n            \"!Select sequence\",\n            \"!Split scalar\",\n            \"!Split mapping\",\n            \"!Split sequence\",\n            \"!Join scalar\",\n            \"!Join mapping\",\n            \"!Join sequence\",\n            \"!Condition scalar\",\n            \"!Condition mapping\",\n            \"!Condition sequence\"\n          ]\n        }\n      }\n    }\n  }\n```\n"},{"path":"languages/yara.md","title":"Yara","group":"Languages","content":"# Yara\n\n`Yara` language support in MonyaCode is provided by the\n[Yara language extension](https://github.com/egibs/yara.zed). Please report\nissues to\n[https://github.com/egibs/yara.monyacode/issues](https://github.com/egibs/yara.monyacode/issues).\n\n- Tree-sitter:\n  [egibs/tree-sitter-yara](https://github.com/egibs/tree-sitter-yara)\n- Language Server: [avast/yls](https://github.com/avast/yls)\n"},{"path":"languages/yarn.md","title":"Yarn","group":"Languages","content":"# Yarn\n\n[Yarn](https://yarnpkg.com/) is a versatile package manager that improves\ndependency management and workflow efficiency for JavaScript and other\nlanguages. It ensures a deterministic dependency tree, offers offline support,\nand enhances security for reliable builds.\n\n## Setup\n\n1. Run `yarn dlx @yarnpkg/sdks base` to generate a `.yarn/sdks` directory.\n2. Set your language server (e.g. VTSLS) to use TypeScript SDK from\n   `.yarn/sdks/typescript/lib` directory in\n   [LSP initialization options](../configuring-monyacode.md#lsp). The actual setting\n   for that depends on language server; for example, for VTSLS you should set\n   [`typescript.tsdk`](https://github.com/yioneko/vtsls/blob/6adfb5d3889ad4b82c5e238446b27ae3ee1e3767/packages/service/configuration.schema.json#L5).\n3. Voilla! Language server functionalities such as Go to Definition, Code\n   Completions and On Hover documentation should work.\n"},{"path":"languages/zig.md","title":"Zig","group":"Languages","content":"# Zig\n\nZig is a general-purpose programming language and toolchain for maintaining\nrobust, optimal and reusable software.\n\nZig support is built into the editor.\n\n- [Website](https://ziglang.org)\n- [Language Reference](https://ziglang.org/documentation/master/)\n- [Standard Library](https://ziglang.org/documentation/master/std/)\n- [tree-sitter-zig](https://github.com/tree-sitter-grammars/tree-sitter-zig)\n- [Language Server (zls)](https://github.com/zigtools/zls)\n"},{"path":"languages.md","title":"Language Support in MonyaCode","group":"Guides","content":"# Language Support in MonyaCode\n\nMonyaCode supports hundreds of programming languages and text formats. Some work\nout-of-the box and others rely on 3rd party extensions.\n\n> The ones included out-of-the-box, natively built into MonyaCode, are marked with\n> \\*.\n\n## Languages with Documentation\n\n- [Ansible](./languages/ansible.md)\n- [AsciiDoc](./languages/asciidoc.md)\n- [Astro](./languages/astro.md)\n- [Bash](./languages/bash.md)\n- [Biome](./languages/biome.md)\n- [C](./languages/c.md) \\*\n- [C++](./languages/cpp.md) \\*\n- [C#](./languages/csharp.md)\n- [Clojure](./languages/clojure.md)\n- [CSS](./languages/css.md) \\*\n- [Dart](./languages/dart.md)\n- [Deno](./languages/deno.md)\n- [Diff](./languages/diff.md) \\*\n- [Docker](./languages/docker.md)\n- [Elixir](./languages/elixir.md)\n- [Elm](./languages/elm.md)\n- [Emmet](./languages/emmet.md)\n- [Erlang](./languages/erlang.md) \\*\n- [Fish](./languages/fish.md)\n- [GDScript](./languages/gdscript.md)\n- [Gleam](./languages/gleam.md) \\*\n- [GLSL](./languages/glsl.md)\n- [Go](./languages/go.md) \\*\n- [Groovy](./languages/groovy.md)\n- [Haskell](./languages/haskell.md)\n- [Helm](./languages/helm.md)\n- [HTML](./languages/html.md) \\*\n- [Java](./languages/java.md)\n- [JavaScript](./languages/javascript.md) \\*\n- [Julia](./languages/julia.md)\n- [JSON](./languages/json.md) \\*\n- [Jsonnet](./languages/jsonnet.md)\n- [Kotlin](./languages/kotlin.md)\n- [Lua](./languages/lua.md) \\*\n- [Luau](./languages/luau.md)\n- [Makefile](./languages/makefile.md)\n- [Markdown](./languages/markdown.md) \\*\n- [Nim](./languages/nim.md)\n- [Nix](./languages/nix.md) \\*\n- [OCaml](./languages/ocaml.md)\n- [Odin](./languages/odin.md) \\*\n- [OpenTofu](./languages/opentofu.md) \\*\n- [PHP](./languages/php.md)\n- [Prisma](./languages/prisma.md)\n- [Proto](./languages/proto.md)\n- [PureScript](./languages/purescript.md)\n- [Python](./languages/python.md) \\*\n- [R](./languages/r.md)\n- [Rego](./languages/rego.md)\n- [ReStructuredText](./languages/rst.md)\n- [Racket](./languages/racket.md)\n- [Roc](./languages/roc.md)\n- [Roto](./languages/roto.md) \\*\n- [Ruby](./languages/ruby.md)\n- [Rust](./languages/rust.md) \\*\n- [Scala](./languages/scala.md)\n- [Scheme](./languages/scheme.md) \\*\n- [Shell Script](./languages/sh.md)\n- [SQL](./languages/sql.md) \\*\n- [Svelte](./languages/svelte.md)\n- [Swift](./languages/swift.md)\n- [Tailwind CSS](./languages/tailwindcss.md) \\*\n- [Terraform](./languages/terraform.md)\n- [TOML](./languages/toml.md) \\*\n- [TypeScript](./languages/typescript.md) \\*\n- [Uiua](./languages/uiua.md)\n- [Vue](./languages/vue.md)\n- [XML](./languages/xml.md) \\*\n- [YAML](./languages/yaml.md) \\*\n- [Yara](./languages/yarn.md)\n- [Yarn](./languages/yarn.md)\n- [Zig](./languages/zig.md) \\*\n\n## Additional Language Extensions\n\n- [Ada](https://github.com/wisn/zed-ada-language)\n- [Aiken](https://github.com/aiken-lang/zed-aiken)\n- [Amber](https://github.com/amber-lang/zed-amber-extension)\n- [Assembly](https://github.com/DevBlocky/zed-asm)\n- [AWK](https://github.com/dangh/zed-awk)\n- [Beancount](https://github.com/zed-extensions/beancount)\n- [Bend](https://github.com/mrpedrobraga/zed-bend)\n- [Blade](https://github.com/bajrangCoder/zed-laravel-blade)\n- [Blueprint](https://github.com/tfuxu/zed-blueprint)\n- [BQN](https://github.com/DavidZwitser/zed-bqn)\n- [Brainfuck](https://github.com/JosephTLyons/zed-brainfuck)\n- [Cadence](https://github.com/janezpodhostnik/cadence.zed)\n- [Cairo](https://github.com/trbutler4/zed-cairo)\n- [Cap'n Proto](https://github.com/cmackenzie1/zed-capnp)\n- [Cedar](https://github.com/chrnorm/zed-cedar)\n- [CFEngine policy language](https://github.com/olehermanse/zed-cfengine)\n- [CSV](https://github.com/huacnlee/zed-csv)\n- [Cucumber/Gherkin](https://github.com/thlcodes/zed-extension-cucumber)\n- [CUE](https://github.com/jkasky/zed-cue)\n- [Curry](https://github.com/fwcd/zed-curry)\n- [D](https://github.com/staysail/zed-d)\n- [Database Markup Language (DBML)](https://github.com/shuklaayush/zed-dbml)\n- [Earthfile](https://github.com/glehmann/earthfile.zed)\n- [EJS template](https://github.com/dangh/zed-ejs)\n- [Elisp](https://github.com/JosephTLyons/zed-elisp)\n- [Ember](https://github.com/jylamont/zed-ember)\n- [Env](https://github.com/zarifpour/zed-env)\n- [Exograph](https://github.com/exograph/zed-extension)\n- [Fortran](https://github.com/Xavier-Maruff/zed-fortran)\n- [F#](https://github.com/nathanjcollins/zed-fsharp)\n- [Git Firefly](https://github.com/d1y/git_firefly)\n- [GraphQL](https://github.com/11bit/zed-extension-graphql)\n- [Groq](https://github.com/juice49/zed-groq)\n- [INI](https://github.com/bajrangCoder/zed-ini)\n- [Java](https://github.com/zed-extensions/java)\n- [Justfiles](https://github.com/jackTabsCode/zed-just)\n- [LaTeX](https://github.com/rzukic/zed-latex)\n- [Ledger](https://github.com/mrkstwrt/zed-ledger)\n- [Less](https://github.com/jimliang/zed-less)\n- [LilyPond](https://github.com/nwhetsell/lilypond-zed-extension)\n- [Liquid](https://github.com/TheBeyondGroup/zed-shopify-liquid)\n- [Log](https://github.com/evrensen467/zed-log)\n- [Lox](https://github.com/arian81/zed-lox)\n- [Markdown Oxide](https://github.com/Feel-ix-343/markdown-oxide-zed)\n- [Marksman](https://github.com/vitallium/zed-marksman)\n- [Matlab](https://github.com/rzukic/zed-matlab)\n- [Meson](https://github.com/jobpaardekooper/zed-meson)\n- [Navi](https://github.com/navi-language/zed-navi)\n- [NeoCMake](https://github.com/k0tran/zed_neocmake)\n- [Nginx](https://github.com/d1y/nginx-zed)\n- [Nim](https://github.com/foxoman/zed-nim)\n- [Nix](https://github.com/zed-extensions/nix)\n- [Noir](https://github.com/shuklaayush/zed-noir)\n- [Nu](https://github.com/zed-extensions/nu)\n- [Pact](https://github.com/kadena-community/pact-zed)\n- [Pest](https://github.com/pest-parser/zed-pest)\n- [PICA200 assembly](https://github.com/Squareheron942/zed-pica200)\n- [Pkl](https://github.com/Moshyfawn/pkl-zed)\n- [PlaydateSDK](https://github.com/notpeter/playdate-zed-extension)\n- [QML](https://github.com/lkroll/zed-qml)\n- [Rainbow CSV](https://github.com/weartist/zed-rainbow-csv)\n- [Rego](https://github.com/StyraInc/zed-rego)\n- [Rescript](https://github.com/humaans/rescript-zed)\n- [Roclang](https://github.com/h2000/zed-roc)\n- [Ron](https://github.com/onbjerg/zed-ron)\n- [Metals](https://github.com/scalameta/metals-zed)\n- [SCSS](https://github.com/bajrangCoder/zed-scss)\n- [Slim](https://github.com/calmyournerves/zed-slim)\n- [Slint](https://gitlab.com/flukejones/zed-slint)\n- [Smithy](https://github.com/joshrutkowski/zed-smithy)\n- [Solidity](https://github.com/zarifpour/zed-solidity)\n- [Strace](https://github.com/sigmaSd/zed-strace)\n- [Swift](https://github.com/zed-extensions/swift)\n- [Templ](https://github.com/makifdb/zed-templ)\n- [Tmux](https://github.com/dangh/zed-tmux)\n- [Twig](https://github.com/YussufSassi/zed-twig)\n- [Typst](https://github.com/WeetHet/typst.zed)\n- [Unison](https://github.com/zetashift/unison-zed)\n- [UnoCSS](https://github.com/bajrangCoder/zed-unocss)\n- [Vlang](https://github.com/lv37/zed-v)\n- [Vala](https://github.com/FyraLabs/zed-vala)\n- [Vale](https://github.com/koozz/zed-vale)\n- [Verilog](https://github.com/someone13574/zed-verilog-extension)\n- [VHS](https://github.com/eth0net/zed-vhs)\n- [Wgsl](https://github.com/luan/zed-wgsl)\n- [WIT](https://github.com/valentinegb/zed-wit)\n"},{"path":"linux.md","title":"MonyaCode on Linux","group":"Guides","content":"# MonyaCode on Linux\n\n## Distro Packages\n\nThe preferred way to install is via adding a MonyaCode repository file and installing\nfrom it. This provides the user with automatic updates once new packages are\nreleased. See instructions below. Alternatively, MonyaCode provides prebuilt `.deb`\nand `.rpm` packages as release assets which can be downloaded at\n[MonyaCode releases](https://github.com/monyacode/monyacode/releases).\n\n### Debian/Ubuntu\n\n```sh\n# Add the repository key\nsudo curl https://codeberg.org/api/packages/MonyaCode/debian/repository.key -o /etc/apt/keyrings/forgejo-MonyaCode.asc\n# Add the repository file\necho \"deb [arch=amd64 signed-by=/etc/apt/keyrings/forgejo-MonyaCode.asc] https://codeberg.org/api/packages/MonyaCode/debian monyacode release\" | sudo tee -a /etc/apt/sources.list.d/monyacode.list\n# Update package cache and install\nsudo apt update && sudo apt install monyacode\n```\n\nRequires Debian 12 (Bookworm)/Ubuntu 24.04 (noble) or later.\n\n### Fedora/RHEL/Rocky/Alma\n\n```sh\n# Add repository file\nsudo dnf config-manager addrepo --from-repofile=\"https://codeberg.org/api/packages/MonyaCode/rpm.repo\"\n# Install\nsudo dnf install monyacode\n```\n\n**Note**: At the time of this writing RPM package signing does not work. In\norder to be able to install MonyaCode you need to set `gpgcheck=0` inside\n`/etc/yum.repos.d/rpm.repo`.\n\nRequires Fedora 42/RHEL 10.1/Rocky 10.1/Alma 10.1 or later.\n\n### OpenSUSE\n\n```sh\nsudo zypper addrepo https://codeberg.org/api/packages/MonyaCode/rpm.repo\nsudo zypper install monyacode\n```\n\n**Note**: At the time of this writing RPM package signing does not work. In\norder to be able to install MonyaCode you need to set `gpgcheck=0` inside\n`/etc/yum.repos.d/rpm.repo`.\n\nRequires Leap 16 (or Tumbleweed/Slowroll) or later.\n\n### Arch\n\nArch Linux publishes official packages for MonyaCode in their\n[extra](https://archlinux.org/packages/extra/x86_64/monyacode/) repository.\n\n```sh\nsudo pacman -S monyacode\n```\n\nAlternatively, to build and install a development snapshot from the latest Git\nHEAD, build the VCS package from the aur:\n[`monyacode-git`](https://aur.archlinux.org/packages/monyacode-git). If you install\npackages from the AUR, it is your responsibility to verify their integrity\nyourself.\n\n### Alpine\n\nAlpine Linux publishes official packages for MonyaCode in their\n[testing](https://pkgs.alpinelinux.org/package/edge/testing/x86_64/monyacode)\nrepository. Follow the\n[instructions](https://wiki.alpinelinux.org/wiki/Repositories#Using_testing_repository)\nto enable the testing repository, then run\n\n```sh\ndoas apk add monyacode@testing\n```\n\n### Gentoo GNU/Linux\n\nGentoo provided by MonyaCode's overlay.\n\n```sh\n# replace doas if you using sudo\ndoas eselect repository add monyacode git https://github.com/monyacode/monyacode-gentoo.git\ndoas emerge --sync monyacode\ndoas emerge -av app-editors/monyacode\n```\n\n**Note**: For more information about Gentoo package and installation ways go to\n[MonyaCode's overlay repo](https://github.com/monyacode/monyacode-gentoo).\n\n## Flatpak\n\nMonyaCode provides a prebuilt flatpak as a release asset. It can be downloaded from\nat [MonyaCode releases](https://github.com/monyacode/monyacode/releases) and installed\nby running:\n\n```sh\nflatpak install /path/to/app.liten.MonyaCode-x86_64-${version}.flatpak\n```\n\n## From Tarball\n\nIf there is a tarball available for your architecture at the\n[MonyaCode Codeberg](https://github.com/monyacode/monyacode/releases) repository, you\ncan follow these instructions:\n\n1. Download the\n   [install.sh](https://github.com/monyacode/monyacode/raw/branch/main/script/install.sh)\n   script.\n2. Run the script.\n\n```sh\n./install.sh\n```\n\nThis will download latest release of MonyaCode and install MonyaCode to `$HOME/.local`. To\ninstall system-wide, use the `--prefix PREFIX` argument:\n\n```sh\n./install.sh --prefix /usr/local ./monyacode-linux-x86_64-1.1.0.tar.gz\n```\n\n## From Source\n\nMonyaCode is open source, and you can install from source. See\n[developer notes](./development/linux.md) for instructions.\n\n## Troubleshooting\n\n### Graphics issues\n\n#### MonyaCode fails to open windows\n\nMonyaCode requires a GPU to run effectively. Under the hood, it uses\n[Vulkan](https://www.vulkan.org/) to communicate with the GPU. If you are seeing\nproblems with performance or MonyaCode fails to load, it is possible that Vulkan is\nthe culprit.\n\nIf you see a notification saying\n`MonyaCode failed to open a window: NoSupportedDeviceFound` this means that Vulkan\ncannot find a compatible GPU. Try running\n[vkcube](https://github.com/krh/vkcube) (usually available as part of the\n`vulkaninfo` or `vulkan-tools` package on various distributions) to troubleshoot\nwhere the issue is coming from like so:\n\n```text\nvkcube\n```\n\n> **_Note_**: Try running in both X11 and wayland modes by running\n> `vkcube -m [x11|wayland]`. Some versions of `vkcube` use `vkcube` to run in\n> X11 and `vkcube-wayland` to run in wayland.\n\nThis should output a line describing your current graphics setup and show a\nrotating cube. If this does not work, you should be able to fix it by installing\nVulkan compatible GPU drivers, however in some cases there is no Vulkan support\nyet.\n\nYou can find out which graphics card MonyaCode is using by looking in the MonyaCode log\n(`~/.local/share/monyacode/logs/MonyaCode.log`) for `Using GPU: ...`.\n\nIf you see errors like `ERROR_INITIALIZATION_FAILED` or `GPU Crashed` or\n`ERROR_SURFACE_LOST_KHR` then you may be able to work around this by installing\ndifferent drivers for your GPU, or by selecting a different GPU to run on. (See\n[#14225](https://github.com/zed-industries/zed/issues/14225))\n\nOn some systems the file `/etc/prime-discrete` can be used to enforce the use of\na discrete GPU using [PRIME](https://wiki.archlinux.org/title/PRIME). Depending\non the details of your setup, you may need to change the contents of this file\nto \"on\" (to force discrete graphics) or \"off\" (to force integrated graphics).\n\nOn others, you may be able to the environment variable `DRI_PRIME=1` when\nrunning MonyaCode to force the use of the discrete GPU.\n\nIf you're using an AMD GPU and MonyaCode crashes when selecting long lines, try\nsetting the `MONYACODE_PATH_SAMPLE_COUNT=0` environment variable. (See\n[#26143](https://github.com/zed-industries/zed/issues/26143))\n\nIf you're using an AMD GPU, you might get a 'Broken Pipe' error. Try using the\nRADV or Mesa drivers. (See\n[#13880](https://github.com/zed-industries/zed/issues/13880))\n\nIf you are using `amdvlk`, the default open-source AMD graphics driver, you may\nfind that MonyaCode consistently fails to launch. This is a known issue for some\nusers, for example on Omarchy (see issue\n[#28851](https://github.com/zed-industries/zed/issues/28851)). To fix this, you\nwill need to use a different driver. We recommend removing the `amdvlk` and\n`lib32-amdvlk` packages and installing `vulkan-radeon` instead (see issue\n[#14141](https://github.com/zed-industries/zed/issues/14141)).\n\nFor more information, the\n[Arch guide to Vulkan](https://wiki.archlinux.org/title/Vulkan) has some good\nsteps that translate well to most distributions.\n\n#### Forcing MonyaCode to use a specific GPU\n\nThere are a few different ways to force MonyaCode to use a specific GPU:\n\n##### Option A\n\nYou can use the `MONYACODE_DEVICE_ID={device_id}` environment variable to specify the\ndevice ID of the GPU you wish to have MonyaCode use.\n\nYou can obtain the device ID of your GPU by running `lspci -nn | grep VGA` which\nwill output each GPU on one line like:\n\n```sh\n08:00.0 VGA compatible controller [0300]: NVIDIA Corporation GA104 [GeForce RTX 3070] [10de:2484] (rev a1)\n```\n\nwhere the device ID here is `2484`. This value is in hexadecimal, so to force\nMonyaCode to use this specific GPU you would set the environment variable like so:\n\n```sh\nMONYACODE_DEVICE_ID=0x2484 monyacode\n```\n\nMake sure to export the variable if you choose to define it globally in a\n`.bashrc` or similar.\n\n##### Option B\n\nIf you are using Mesa, you can run\n`MESA_VK_DEVICE_SELECT=list monyacode --foreground` to get a list of available GPUs\nand then export `MESA_VK_DEVICE_SELECT=xxxx:yyyy` to choose a specific device.\nFurthermore, you can fallback to xwayland with an additional export of\n`WAYLAND_DISPLAY=\"\"`.\n\n##### Option C\n\nUsing [vkdevicechooser](https://github.com/jiriks74/vkdevicechooser).\n\n#### Generating debug reports\n\nPassing the `--system-specs` flag to MonyaCode like\n\n```sh\nmonyacode --system-specs\n```\n\nwill print the system specs to the terminal.\n\nThe editor log is usually located at `~/.local/share/monyacode/logs/MonyaCode.log`.\n\nTo generate a clean log file for debugging graphics issues, run:\n\n```sh\ntruncate -s 0 ~/.local/share/monyacode/logs/MonyaCode.log # Clear the log file\nMONYACODE_LOG=wgpu=info monyacode .\ncat ~/.local/share/monyacode/logs/MonyaCode.log\n# copy the output\n```\n\nOr, if you have the MonyaCode cli setup, you can do\n\n```sh\nMONYACODE_LOG=wgpu=info /path/to/monyacode/cli --foreground .\n# copy the output\n```\n\n### Forcing X11 scale factor\n\nOn X11 systems, MonyaCode automatically detects the appropriate scale factor for\nhigh-DPI displays. The scale factor is determined using the following priority\norder:\n\n1. `GPUI_X11_SCALE_FACTOR` environment variable (if set)\n2. `Xft.dpi` from X resources database (xrdb)\n3. Automatic detection via RandR based on monitor resolution and physical size\n\nIf you want to customize the scale factor beyond what MonyaCode detects\nautomatically, you have several options:\n\n#### Check your current scale factor\n\nYou can verify if you have `Xft.dpi` set:\n\n```sh\nxrdb -query | grep Xft.dpi\n```\n\nIf this command returns no output, MonyaCode is using RandR (X11's monitor management\nextension) to automatically calculate the scale factor based on your monitor's\nreported resolution and physical dimensions.\n\n#### Option 1: Set Xft.dpi (X Resources Database)\n\n`Xft.dpi` is a standard X11 setting that many applications use for consistent\nfont and UI scaling. Setting this ensures MonyaCode scales the same way as other X11\napplications that respect this setting.\n\nEdit or create the `~/.Xresources` file:\n\n```sh\nvim ~/.Xresources\n```\n\nAdd this line with your desired DPI:\n\n```sh\nXft.dpi: 96\n```\n\nCommon DPI values:\n\n- `96` for standard 1x scaling\n- `144` for 1.5x scaling\n- `192` for 2x scaling\n- `288` for 3x scaling\n\nLoad the configuration:\n\n```sh\nxrdb -merge ~/.Xresources\n```\n\nRestart MonyaCode for the changes to take effect.\n\n#### Option 2: Use the GPUI_X11_SCALE_FACTOR environment variable\n\nThis MonyaCode-specific environment variable directly sets the scale factor,\nbypassing all automatic detection.\n\n```sh\nGPUI_X11_SCALE_FACTOR=1.5 monyacode\n```\n\nYou can use decimal values (e.g., `1.25`, `1.5`, `2.0`) or set\n`GPUI_X11_SCALE_FACTOR=randr` to force RandR-based detection even when `Xft.dpi`\nis set.\n\nTo make this permanent, add it to your shell profile or desktop entry.\n\n#### Option 3: Adjust system-wide RandR DPI\n\nThis changes the reported DPI for your entire X11 session, affecting how RandR\ncalculates scaling for all applications that use it.\n\nAdd this to your `.xprofile` or `.xinitrc`:\n\n```sh\nxrandr --dpi 192\n```\n\nReplace `192` with your desired DPI value. This affects the system globally and\nwill be used by MonyaCode's automatic RandR detection when `Xft.dpi` is not set.\n\n### Font rendering parameters\n\nOn Linux, the `MONYACODE_FONTS_GAMMA` and `MONYACODE_FONTS_GRAYSCALE_ENHANCED_CONTRAST`\nenvironment variables are read for the values to use for font rendering.\n\n`MONYACODE_FONTS_GAMMA` corresponds to\n[getgamma](https://learn.microsoft.com/en-us/windows/win32/api/dwrite/nf-dwrite-idwriterenderingparams-getgamma)\nvalues. Allowed range [1.0, 2.2], other values are clipped. Default: 1.8\n\n`MONYACODE_FONTS_GRAYSCALE_ENHANCED_CONTRAST` corresponds to\n[getgrayscaleenhancedcontrast](https://learn.microsoft.com/en-us/windows/win32/api/dwrite_1/nf-dwrite_1-idwriterenderingparams1-getgrayscaleenhancedcontrast)\nvalues. Allowed range: [0.0, ..), other values are clipped. Default: 1.0\n"},{"path":"migrate/vs-code.md","title":"How to Migrate from VS Code to MonyaCode","group":"Migrate","content":"# How to Migrate from VS Code to MonyaCode\n\nThis guide is for developers who spent serious time in VS Code and want to try\nMonyaCode without starting from scratch.\n\nIf you're here, you are probably a VS Code user frustrated by the slow,\nElectron-based implementation and the increasing reliance on flaky AI tooling\nthat is creeping into editors like VS Code and Zed, created by US-based\ncorporations that want to trap you in a subscription-based model of code\nediting.\n\nMonyaCode is a fully open source editor without any AI, telemetry or subscriptions,\nwith support for many of the same languages that VS Code supports.\n\n## Install MonyaCode\n\nMonyaCode is available for macOS, Windows, and Linux.\n\nTo install the editor, you will need to build it from source. Fortunately,\nbuilding a Rust project is really easy. All you need is https://rustup.rs, which\nwill help you install the compiler and tooling that compiling MonyaCode requires.\n\nAfter installation, you can launch MonyaCode in the same way that you usually run\napplications on your platform.\n\n## Import Settings from VS Code\n\nDuring setup, you have the option to import key settings from VS Code. MonyaCode\nimports the following settings:\n\n### Settings Imported from VS Code\n\nThe following VS Code settings are automatically imported when you use **Import\nSettings from VS Code**:\n\n**Editor**\n\n| VS Code Setting                             | MonyaCode Setting                                   |\n| ------------------------------------------- | ---------------------------------------------- |\n| `editor.fontFamily`                         | `buffer_font_family`                           |\n| `editor.fontSize`                           | `buffer_font_size`                             |\n| `editor.fontWeight`                         | `buffer_font_weight`                           |\n| `editor.tabSize`                            | `tab_size`                                     |\n| `editor.insertSpaces`                       | `hard_tabs` (inverted)                         |\n| `editor.wordWrap`                           | `soft_wrap`                                    |\n| `editor.wordWrapColumn`                     | `preferred_line_length`                        |\n| `editor.cursorStyle`                        | `cursor_shape`                                 |\n| `editor.cursorBlinking`                     | `cursor_blink`                                 |\n| `editor.renderLineHighlight`                | `current_line_highlight`                       |\n| `editor.lineNumbers`                        | `gutter.line_numbers`, `relative_line_numbers` |\n| `editor.showFoldingControls`                | `gutter.folds`                                 |\n| `editor.minimap.enabled`                    | `minimap.show`                                 |\n| `editor.minimap.autohide`                   | `minimap.show`                                 |\n| `editor.minimap.showSlider`                 | `minimap.thumb`                                |\n| `editor.minimap.maxColumn`                  | `minimap.max_width_columns`                    |\n| `editor.stickyScroll.enabled`               | `sticky_scroll.enabled`                        |\n| `editor.scrollbar.horizontal`               | `scrollbar.axes.horizontal`                    |\n| `editor.scrollbar.vertical`                 | `scrollbar.axes.vertical`                      |\n| `editor.mouseWheelScrollSensitivity`        | `scroll_sensitivity`                           |\n| `editor.fastScrollSensitivity`              | `fast_scroll_sensitivity`                      |\n| `editor.cursorSurroundingLines`             | `vertical_scroll_margin`                       |\n| `editor.hover.enabled`                      | `hover_popover_enabled`                        |\n| `editor.hover.delay`                        | `hover_popover_delay`                          |\n| `editor.parameterHints.enabled`             | `auto_signature_help`                          |\n| `editor.multiCursorModifier`                | `multi_cursor_modifier`                        |\n| `editor.selectionHighlight`                 | `selection_highlight`                          |\n| `editor.roundedSelection`                   | `rounded_selection`                            |\n| `editor.find.seedSearchStringFromSelection` | `seed_search_query_from_cursor`                |\n| `editor.rulers`                             | `wrap_guides`                                  |\n| `editor.renderWhitespace`                   | `show_whitespaces`                             |\n| `editor.guides.indentation`                 | `indent_guides.enabled`                        |\n| `editor.linkedEditing`                      | `linked_edits`                                 |\n| `editor.autoSurround`                       | `use_auto_surround`                            |\n| `editor.formatOnSave`                       | `format_on_save`                               |\n| `editor.formatOnPaste`                      | `auto_indent_on_paste`                         |\n| `editor.formatOnType`                       | `use_on_type_format`                           |\n| `editor.trimAutoWhitespace`                 | `remove_trailing_whitespace_on_save`           |\n| `editor.suggestOnTriggerCharacters`         | `show_completions_on_input`                    |\n| `editor.suggest.showWords`                  | `completions.words`                            |\n| `editor.inlineSuggest.enabled`              | `show_edit_predictions`                        |\n\n**Files & Workspace**\n\n| VS Code Setting             | MonyaCode Setting                   |\n| --------------------------- | ------------------------------ |\n| `files.autoSave`            | `autosave`                     |\n| `files.autoSaveDelay`       | `autosave.milliseconds`        |\n| `files.insertFinalNewline`  | `ensure_final_newline_on_save` |\n| `files.associations`        | `file_types`                   |\n| `files.watcherExclude`      | `file_scan_exclusions`         |\n| `files.watcherInclude`      | `file_scan_inclusions`         |\n| `files.simpleDialog.enable` | `use_system_path_prompts`      |\n| `search.smartCase`          | `use_smartcase_search`         |\n| `search.useIgnoreFiles`     | `search.include_ignored`       |\n\n**Terminal**\n\n| VS Code Setting                       | MonyaCode Setting                        |\n| ------------------------------------- | ----------------------------------- |\n| `terminal.integrated.fontFamily`      | `terminal.font_family`              |\n| `terminal.integrated.fontSize`        | `terminal.font_size`                |\n| `terminal.integrated.lineHeight`      | `terminal.line_height`              |\n| `terminal.integrated.cursorStyle`     | `terminal.cursor_shape`             |\n| `terminal.integrated.cursorBlinking`  | `terminal.blinking`                 |\n| `terminal.integrated.copyOnSelection` | `terminal.copy_on_select`           |\n| `terminal.integrated.scrollback`      | `terminal.max_scroll_history_lines` |\n| `terminal.integrated.macOptionIsMeta` | `terminal.option_as_meta`           |\n| `terminal.integrated.{platform}Exec`  | `terminal.shell`                    |\n| `terminal.integrated.env.{platform}`  | `terminal.env`                      |\n\n**Tabs & Panels**\n\n| VS Code Setting                                    | MonyaCode Setting                                       |\n| -------------------------------------------------- | -------------------------------------------------- |\n| `workbench.editor.showTabs`                        | `tab_bar.show`                                     |\n| `workbench.editor.showIcons`                       | `tabs.file_icons`                                  |\n| `workbench.editor.tabActionLocation`               | `tabs.close_position`                              |\n| `workbench.editor.tabActionCloseVisibility`        | `tabs.show_close_button`                           |\n| `workbench.editor.focusRecentEditorAfterClose`     | `tabs.activate_on_close`                           |\n| `workbench.editor.enablePreview`                   | `preview_tabs.enabled`                             |\n| `workbench.editor.enablePreviewFromQuickOpen`      | `preview_tabs.enable_preview_from_file_finder`     |\n| `workbench.editor.enablePreviewFromCodeNavigation` | `preview_tabs.enable_preview_from_code_navigation` |\n| `workbench.editor.editorActionsLocation`           | `tab_bar.show_tab_bar_buttons`                     |\n| `workbench.editor.limit.enabled` / `value`         | `max_tabs`                                         |\n| `workbench.editor.restoreViewState`                | `restore_on_file_reopen`                           |\n| `workbench.statusBar.visible`                      | `status_bar.show`                                  |\n\n**Project Panel (File Explorer)**\n\n| VS Code Setting                | MonyaCode Setting                        |\n| ------------------------------ | ----------------------------------- |\n| `explorer.compactFolders`      | `project_panel.auto_fold_dirs`      |\n| `explorer.autoReveal`          | `project_panel.auto_reveal_entries` |\n| `explorer.excludeGitIgnore`    | `project_panel.hide_gitignore`      |\n| `problems.decorations.enabled` | `project_panel.show_diagnostics`    |\n| `explorer.decorations.badges`  | `project_panel.git_status`          |\n\n**Git**\n\n| VS Code Setting                      | MonyaCode Setting                                   |\n| ------------------------------------ | ---------------------------------------------- |\n| `git.enabled`                        | `git_panel.button`                             |\n| `git.defaultBranchName`              | `git_panel.fallback_branch_name`               |\n| `git.decorations.enabled`            | `git.inline_blame`, `project_panel.git_status` |\n| `git.blame.editorDecoration.enabled` | `git.inline_blame.enabled`                     |\n\n**Window & Behavior**\n\n| VS Code Setting                                  | MonyaCode Setting                             |\n| ------------------------------------------------ | ---------------------------------------- |\n| `window.confirmBeforeClose`                      | `confirm_quit`                           |\n| `window.nativeTabs`                              | `use_system_window_tabs`                 |\n| `window.closeWhenEmpty`                          | `when_closing_with_no_tabs`              |\n| `accessibility.dimUnfocused.enabled` / `opacity` | `active_pane_modifiers.inactive_opacity` |\n\n**Other**\n\n| VS Code Setting      | MonyaCode Setting                                             |\n| -------------------- | -------------------------------------------------------- |\n| `http.proxy`         | `proxy`                                                  |\n| `npm.packageManager` | `node.npm_path`                                          |\n| `outline.icons`      | `outline_panel.file_icons`, `outline_panel.folder_icons` |\n\nMonyaCode doesn’t import extensions or keybindings, but this is the fastest way to\nget a familiar feel while trying something new. If you skip that step during\nsetup, you can still import settings manually later via the command palette:\n\n`Cmd+Shift+P → MonyaCode: Import VS Code Settings`\n\n## Set Up Editor Preferences\n\nYou can also configure settings manually in the Settings Editor.\n\nTo edit your settings:\n\n1. `Cmd+,` to open the Settings Editor.\n2. Run `monyacode: open settings` in the Command Palette.\n\nHere’s how common VS Code settings translate:\n\n| VS Code             | MonyaCode               | Notes                          |\n| ------------------- | ------------------ | ------------------------------ |\n| editor.fontFamily   | buffer_font_family | MonyaCode uses Fira Code by default |\n| editor.fontSize     | buffer_font_size   | Set in pixels                  |\n| editor.tabSize      | tab_size           | Can override per language      |\n| editor.insertSpaces | insert_spaces      | Boolean                        |\n| editor.formatOnSave | format_on_save     | Works with formatter enabled   |\n| editor.wordWrap     | soft_wrap          | Supports optional wrap column  |\n\nMonyaCode also supports per-project settings. You can find these in the Settings\nEditor as well.\n\n## Open or Create a Project\n\nAfter setup, press `Cmd+O` (`Ctrl+O` on Linux) to open a folder. This becomes\nyour workspace in MonyaCode. There's no support for multi-root workspaces or\n`.code-workspace` files like in VS Code. MonyaCode keeps it simple: one folder, one\nworkspace.\n\nTo start a new project, create a directory using your terminal or file manager,\nthen open it in MonyaCode. The editor will treat that folder as the root of your\nproject.\n\nYou can also launch MonyaCode from the terminal inside any folder with: `monyacode .`\n\nOnce inside a project, use `Cmd+P` to jump between files quickly. `Cmd+Shift+P`\n(`Ctrl+Shift+P` on Linux) opens the command palette for running actions / tasks,\ntoggling settings, or running other actions.\n\nOpen buffers appear as tabs across the top. The sidebar shows your file tree and\nGit status. Collapse it with `Cmd+B` for a distraction-free view.\n\n## Differences in Keybindings\n\nIf you chose the VS Code keymap during onboarding, you're likely good to go, and\nmost of your shortcuts should already feel familiar. Here’s a quick reference\nguide for how our keybindings compare to what you’re used to coming from VS\nCode.\n\n### Common Shared Keybindings (MonyaCode <> VS Code)\n\n| Action                      | Shortcut               |\n| --------------------------- | ---------------------- |\n| Find files                  | `Cmd + P`              |\n| Run a command               | `Cmd + Shift + P`      |\n| Search text (project-wide)  | `Cmd + Shift + F`      |\n| Find symbols (project-wide) | `Cmd + T`              |\n| Find symbols (file-wide)    | `Cmd + Shift + O`      |\n| Toggle left dock            | `Cmd + B`              |\n| Toggle bottom dock          | `Cmd + J`              |\n| Open terminal               | `Ctrl + ~`             |\n| Open file tree explorer     | `Cmd + Shift + E`      |\n| Close current buffer        | `Cmd + W`              |\n| Close whole project         | `Cmd + Shift + W`      |\n| Refactor: rename symbol     | `F2`                   |\n| Change theme                | `Cmd + K, Cmd + T`     |\n| Wrap text                   | `Opt + Z`              |\n| Navigate open tabs          | `Cmd + Opt + Arrow`    |\n| Syntactic fold / unfold     | `Cmd + Opt + {` or `}` |\n\n### Different Keybindings (MonyaCode <> VS Code)\n\n| Action              | VS Code               | MonyaCode                   |\n| ------------------- | --------------------- | ---------------------- |\n| Open recent project | `Ctrl + R`            | `Cmd + Opt + O`        |\n| Move lines up/down  | `Opt + Up/Down`       | `Cmd + Ctrl + Up/Down` |\n| Split panes         | `Cmd + \\`             | `Cmd + K, Arrow Keys`  |\n| Expand Selection    | `Shift + Alt + Right` | `Opt + Up`             |\n\n### Unique to MonyaCode\n\n| Action              | Shortcut                     | Notes                                            |\n| ------------------- | ---------------------------- | ------------------------------------------------ |\n| Toggle right dock   | `Cmd + R` or `Cmd + Alt + B` |                                                  |\n| Syntactic selection | `Opt + Up/Down`              | Selects code by structure (e.g., inside braces). |\n\n### How to Customize Keybindings\n\nTo edit your keybindings:\n\n- Open the command palette (`Cmd+Shift+P`)\n- Run `MonyaCode: Open Keymap Editor`\n\nThis opens a list of all available bindings. You can override individual\nshortcuts, remove conflicts, or build a layout that works better for your setup.\n\nMonyaCode also supports chords (multi-key sequences) like `Cmd+K Cmd+C`, like VS Code\ndoes.\n\n## Differences in User Interfaces\n\n### No Workspace\n\nVS Code uses a dedicated Workspace concept, with multi-root folders,\n`.code-workspace` files, and a clear distinction between “a window” and “a\nworkspace.” MonyaCode simplifies this model.\n\nIn MonyaCode:\n\n- There is no workspace file format. Opening a folder is your project context.\n\n- MonyaCode does not support multi-root workspaces. You can only open one folder at a\n  time in a window.\n\n- Most project-level behavior is scoped to the folder you open. Search, Git\n  integration, tasks, and environment detection all treat the opened directory\n  as the project root.\n\n- Per-project settings are optional. You can add a `.monyacode/settings.json` file\n  inside a project to override global settings, but MonyaCode does not use\n  `.code-workspace` files and won’t import them.\n\n- You can start from a single file or an empty window. MonyaCode doesn’t require you\n  to open a folder to begin editing.\n\nThe result is a simpler model: Open a folder → work inside that folder → no\nadditional workspace layer.\n\n### Navigating in a Project\n\nIn VS Code, the standard entry point is opening a folder. From there, the\nleft-hand sidebar is central to your navigation. MonyaCode takes a different\napproach:\n\n- You can still open folders, but you don’t need to. Opening a single file or\n  even starting with an empty workspace is valid.\n- The Command Palette (`Cmd+Shift+P`) and File Finder (`Cmd+P`) are your primary\n  navigation tools. The File Finder searches across the entire workspace\n  instantly; files, symbols and commands.\n- Instead of a persistent sidebar, MonyaCode encourages you to:\n  - Fuzzy-find files by name (`Cmd+P`)\n  - Jump directly to symbols (`Cmd+Shift+O`)\n  - Use split panes and tabs for context, rather than keeping a large file tree\n    open (though you can do this with the Project Panel if you prefer).\n\nThe UI is intentionally minimal. Panels slide in only when needed, then get out\nof your way. The focus is on flowing between code instead of managing panes.\n\n### Extensions vs. Marketplace\n\nMonyaCode does not offer as many extensions as VS Code. The available extensions are\nfocused on language support, themes, syntax highlighting, and other core editing\nenhancements.\n\nHowever there are several features that typically require extensions in VS Code\nwhich we built directly into MonyaCode:\n\n- Vim modal editing mode (as well as a Helix mode)\n- Built-in terminal panel\n- Project-wide fuzzy search\n- Task runner with JSON config\n- Inline diagnostics and code actions via LSP\n\nYou won’t find one-to-one replacements for every VS Code extension, especially\nif you rely on tools for DevOps, containers, or test runners. MonyaCode's extension\necosystem is still growing, and the catalog is smaller by design.\n\n### Advanced Config and Productivity Tweaks\n\nMonyaCode exposes advanced settings for power users who want to fine-tune their\nenvironment.\n\nHere are a few useful tweaks:\n\n**Format on Save:**\n\n```json\n\"format_on_save\": \"on\"\n```\n\n**Enable direnv support:**\n\n```json\n\"load_direnv\": \"shell_hook\"\n```\n\n**Custom Tasks**: Define build or run commands in your `tasks.jsonc` (accessed\nvia command palette: `monyacode: open tasks`):\n\n```json\n[\n  {\n    \"label\": \"build\",\n    \"command\": \"cargo build\"\n  }\n]\n```\n\n**Bring over custom snippets** Copy your VS Code snippet JSON directly into\nMonyaCode's snippets folder (`monyacode: configure snippets`).\n"},{"path":"migrate/zed.md","title":"Migrating from Zed to MonyaCode","group":"Migrate","content":"# Migrating from Zed to MonyaCode\n\nMonyaCode is a fork of the Zed code editor made roughly around the version `0.217`.\nFrom there, a lot of \"features\" have been stripped out:\n\n- All telemetry was removed\n- All AI-related functionality was removed\n- Collaboration and its proprietary server component were removed\n- Automatic extension installation was removed\n- Auto-updates were removed\n- There is no license agreement to accept beyond the GPL open source license\n\nHowever, some functionality has also been added or changed that is not available\nin Zed.\n\n- Integrated documentation viewer\n- Various bugfixes\n- Rewritten documentation\n- Modified defaults\n- More themes included\n\n## Configuration\n\nThe configuration should be mostly compatible: Try copying your `keymap.json`\nand `settings.json` files from `~/.config/zed/` to `~/.config/monyacode/` and\nrenaming them `keymap.jsonc` and `settings.jsonc`. It might work.\n\n## Extension support\n\nSince Zed extensions in the Zed editor are installed as binary blobs from the\nproprietary servers owned by Zed Industries, this installation method has been\nremoved from MonyaCode. Instead, extensions can be installed using the extension\nrepository URL directly. Language extensions that need to be compiled to Wasm\nrequire a [rustup](https://rustup.rs) toolchain to do the compilation.\n\nIf the extension doesn't work, check the MonyaCode log file using\n`cmd-shift-p Open Log` (also available in the Command Palette).\n\nCurrently, this functionality is very crude, and as Zed continues to develop it\nis likely that extensions that compile to Wasm will stop being compatible with\nMonyaCode. Therefore, the plan is to eventually replace the extension system from Zed\nwith something else, but also to integrate language support for the most popular\nlanguages directly into MonyaCode so that language extensions aren't needed in most\ncases.\n\nNote: This is a work in progress. The goal is not to keep up with Zed and to\nstay a stripped-down version of the same editor. I am not interested in _Zed_.\nThe goal is simply to have a good-enough baseline editor which is simple to\ninstall and use, and can replace VS Code as the editor to recommend to\nbeginners.\n"},{"path":"mission.md","title":"The Mission","group":"Guides","content":"# The Mission\n\n> “I come with empty hands and the desire to unbuild walls.”\n\n― Ursula K. Le Guin, The Dispossessed\n\nThe MonyaCode text editor is a fork of the Zed text editor. This document is an\nattempt to explain why the fork exists.\n\n## Performance, but at what cost?\n\nProgramming is more than just productivity. We need tools that are fit for\npurpose, that aren't beholden to investors or share holders. There are aspects\nof the Zed code editor that I think are wrong in the moral and ethical sense,\nand there are aspects that I think are simply bad choices from a technical\nperspective.\n\nAt first, the promise of Zed seemed alluring. I moved from Emacs to Neovim\nbecause I started coding in Rust, and using the Rust LSP in Emacs was a very\nfrustrating experience at the time. Whenever the LSP did any work the editor\nwould freeze, drop keystrokes or otherwise behave badly.\n\nNeovim was better in this aspect, but as soon as I worked on any larger rust\nproject I would have the same kind of experience. Not good. At the same time,\nmore and more languages assume that the editor can use a language server and\ndon't even try to support editors without it.\n\nWhen Zed appeared, promising an actually fast and smooth editing experience able\nto handle large Rust projects without getting bogged down, and also came with\ngood support for Emacs- and Vim-style editing, it seemed like exactly the thing\nthat I ought to try.\n\nHowever, I never even managed to get the editor installed. When I tried to\ndownload and install the binaries from the Zed website or Github page I would\nget presented with the Zed End User Terms. To install, I would have to accept\nthem.\n\nSo, I read the terms!\n\n> Editor's note, 2026-03-03\n>\n> Credit where credit is due: Within 24 hours of me publishing this fork, Zed\n> updated their Terms of Use and removed the section that seemed to conflict\n> with the terms of the open source licenses.\n\nThe quotes below are excerpts from the\n[Zed End User Terms](https://zed.dev/terms).\n\n> PLEASE READ THESE TERMS AND CONDITIONS CAREFULLY BEFORE USING THE SERVICE OR\n> SOFTWARE OFFERED BY ZED INDUSTRIES, INC. (\"ZED\", OR \"WE\"). BY ACCESSING OR\n> USING THE SOLUTION (AS DEFINED BELOW) IN ANY MANNER, YOU (\"YOU\" OR \"CUSTOMER\")\n> AGREE TO BE BOUND BY THESE TERMS (THE \"AGREEMENT\") TO THE EXCLUSION OF ALL\n> OTHER TERMS.\n\nMy (non-lawyer) interpretation of this section is that if I agree to these\nterms, I am waiving my right to use any software by Zed Industries under any\nother license. Does this mean that I could no longer use any of the code from\nZed under an open source license, if the rights afforded by that license\ncontradicts these terms? That is my interpretation.\n\nLet's read on, to section 2 \"TERMS APPLICABLE TO THE EDITOR\", paragraph 2.2,\n\"License Limitations\":\n\n> You agree that You shall not: (a) exceed the scope of the licenses granted in\n> Section 2.1; (b) make copies of the Editor; (c) distribute, sublicense,\n> assign, delegate, rent, lease, sell, time-share or otherwise transfer the\n> benefits of, use under, or rights to, the license granted in Section 2.1; (d)\n> reverse engineer, decompile, disassemble or otherwise attempt to learn the\n> source code, structure or algorithms underlying the Editor, except to the\n> extent required to be permitted under applicable law; (e) modify, translate or\n> create derivative works of the Editor; or (f) remove any copyright, trademark,\n> patent or other proprietary notice that appears on the Editor or copies\n> thereof.\n\n...\n\n> You shall not [..] make copies of the Editor\n\n...\n\n> You shall not [..] reverse engineer, decompile, disassemble or otherwise\n> attempt to learn the source code, structure or algorithms underlying the\n> Editor\n\n...\n\n> You shall not [..] modify, translate or create derivative works of the Editor\n\n...etc.\n\nI cannot try to learn the source code, structure or algorithms underlying the\nEditor? I cannot create derivative works?\n\nI couldn't accept it. I didn't want to agree to any of it. So I gave up.\n\n---\n\nZed kept popping up. People I had respected left their jobs to go work for Zed,\nand not only that - to work on AI integration in Zed.\n\nI don't want AI in my editor. I don't want AI in _anything_. It's not just\nbecause I think AI is bad for productivity, performance and quality (I do), but\nbecause I think the companies behind the technology and the _cost_ of the\ntechnology make it not only unsustainable but unethical. Even if AI tools _did_\nwork to make their users more productive (which I doubt), I still don't want to\nuse them. At all.\n\nI also object to making myself and my work depend on paying a subscription fee\nto anyone. I don't want an outage at Anthropic to affect my ability to do my\nwork. I think it is a grave mistake to build anything on such shaky foundations\nas the sustainability and profit margins of the AI industry.\n\nI think these are bad features to put in a code editor. Why should a code editor\nhave a license agreement that flies in the face of open source? Why should my\ncode editor integrate with technology that I find distasteful?\n\nThinking is a feature. Generative LLM technology is not.\n\nAnother selling point of Zed is \"multiplayer editing\" and built-in\n\"collaboration\", meaning integrated audio/video chat with a proprietary server\ncomponent, subscriptions and end user license agreements.\n\nI don't want it. In fact, I think these are bad features to put in a code\neditor. In my team, we use different operating systems, different editors and\nwork from different time zones. We keep meetings to a minimum, and when we do\npair programming or mob programming, one person is typing while the others\nthink.\n\nMultiple people editing the same files at once while video chatting at the same\ntime sounds like a nightmare.\n\nI don't want to sign in, or sign up, or join the community. I want my tools to\nbe hammers, screwdrivers and saws. I don't need my hammer to come with a\nsubscription fee.\n\nIn Zed, the editor will automatically update itself, downloading binaries from\nthe Zed website vetted only by Zed. Its extensions are binary blobs hosted by\nZed, automatically updated and downloaded without user control. Those extensions\nwill, in turn, download and install anything they feel like.\n\nAs a band aid for this, newer versions of Zed have copied the workspace trust\nmechanism from VS Code. I think this is, once again, a bad solution to a problem\nthat should not exist.\n\nI don't want my software to update itself automatically, or to download binaries\nwithout me explicitly asking it to.\n\nThe editor can offer to download and install something from source, but it\nshould tell you exactly what it wants to download and from where, and it should\nask first. In Zed, everything invasive and exploitative is opt out: AI,\nTelemetry, auto updates - auto-installation isn't even offered as something to\nopt out of. Zed does not respect you.\n\nI believe that the tools I use should belong to me. My tools are not there for\ncorporations to bind me with.\n\n## Curiosity and spite\n\nAt first, I tried to build some other efforts I found online to make Zed work\nwithout the AI features just so I could check it out, but didn't manage to get\nthem to work. At some point, the curiosity turned into spite. I became\ndetermined to not only get the editor to run without all of the misfeatures, but\nto make it a full-blown fork of the project. Independent of corporate control,\nin the spirit of Vim and the late Bram Moolenaar who could have added\nsubscription fees and abusive license agreements had he so wanted, but instead\ngave his work as a gift to the world and asked only for donations to a\n[good cause close to his heart](https://en.wikipedia.org/wiki/ICCF_Holland) in\nreturn.\n\nThis is the result. Feel free to build it and see if it works for you. There is\nno license agreement or subscription beyond the open source license of the code\n(GPLv3). It is yours now, to do with as you please.\n\nTo be honest, I am happy in Neovim. For most projects I still prefer it over\nthis editor. Don't expect this project to keep pace with Zed or to be a\nZed-killer, unless other people take it and keep building from here. This\nproject is mainly therapeutic. I had to get it out of my system.\n\n## Features\n\n- Open: No telemetry, AI or privacy-invading third party integrations.\n- Performance: Focuses on helping you with your programming tasks instead of\n  spending CPU and GPU cycles on communicating with AI services, sending your\n  keystrokes to a company server in the US or convincing you to sign up for a\n  subscription.\n- Integrated: MonyaCode includes the documentation directly in the editor. Long term,\n  the goal is to include language support for most languages without requiring\n  the use of extensions.\n\n## Other options\n\nIf you don't want to use an editor that has gone down the AI route but do want a\ncapable, general-purpose, open source text editor, what other options are there?\n\nHere are some other editors that I have used and would consider reasonable\nalternatives to Zed. What they all have in common is that they are first and\nforemost _text editors_, and do not push or endorse LLM services. Some of them\nare also strict about the use of LLMs to develop the editors themselves, but not\nall of them are.\n\n> _Note:_ MonyaCode is also not AI free. I have not and do not think it is feasible\n> to remove all generated code from the code base. Not all of it is clearly\n> marked as such. We also merge patches from upstream sometimes, mainly to fix\n> bugs. This stance may change in the future.\n\nAs far as I know, these editors are completely free of AI-generated code.\n\n- [Vim Classic](https://vim-classic.org)\n- [Emacs](https://www.gnu.org/software/emacs/)\n- [fred](https://fred-dev.tech) - Has committed to be and remain AI-free.\n\nThese editors may not be AI free, but I am still OK with recommending them.\n\n- [Neovim](https://neovim.io)\n- [Vim](https://www.vim.org)\n- [Flow Control](https://flow-control.dev) - A TUI-based and very promising\n  editor with planned collaboration features.\n- [Helix](https://helix-editor.com)\n- [Kakoune](https://kakoune.org)\n\n> _Note:_ Also see:\n> [ethical-foss/open-slopware](https://codeberg.org/ethical-foss/open-slopware#text-and-code-editors).\n"},{"path":"multibuffers.md","title":"Multibuffers","group":"Guides","content":"# Multibuffers\n\nUsing multibuffers, it's possible to edit multiple files simultaneously from a\nsingle view.\n\nMultibuffers work well together with multiple cursors.\n\n## Editing in a multibuffer\n\nEditing a multibuffer is the same as editing a normal file. Changes will be\nreflected in the open copies of the files in the rest of the editor.\n\nSave all files with {#action editor::Save} (bound to `cmd-s` on macOS, `ctrl-s`\non Windows/Linux, or `:w` in Vim mode).\n\nWhen in a multibuffer, it is often useful to use multiple cursors to edit every\nfile simultaneously. To edit multiple instances at once, select them with the\nmouse (`option-click` on macOS, `alt-click` on Window/Linux) or the keyboard.\n`cmd-d` on macOS, `ctrl-d` on Windows/Linux, or `gl` in Vim mode will select the\nnext match of the word under the cursor.\n\nTo edit all matches you can select them by running the {#action\neditor::SelectAllMatches} command (`cmd-shift-l` on macOS, `ctrl-shift-l` on\nWindows/Linux, or `g a` in Vim mode).\n\n## Navigating to the Source File\n\nClick on any of the divider lines between excerpts or place the cursor in an\nexcerpt, and run the {#action editor::OpenExcerpts} command. Note: If multiple\ncursors are being used, the command will open the source file positioned under\neach cursor within the multibuffer.\n\nDouble-clicking excerpts with the mouse to open the source file is disabled by\ndefault, and can be enabled using the setting:\n`\"double_click_in_multibuffer\": \"open\"`.\n\n## Project search\n\nRun {#action pane::ToggleSearch} (`cmd-shift-f` on macOS, `ctrl-shift-f` on\nWindows/Linux, or `g/` in Vim mode) to search across the whole project. After\nthe search has completed, the results will be shown in a new multibuffer. There\nwill be one excerpt for each matching line across the whole project.\n\n## Diagnostics\n\nIf an appropriate language server is installed, the diagnostics pane can display\nall errors across the project. Open it by clicking on the icon in the status\nbar, or run the {#action diagnostics::Deploy} command` ('cmd-shift-m` on macOS,\n`ctrl-shift-m` on Windows/Linux, or `:clist` in Vim mode).\n\n## Find References\n\nWith a language server installed, find all references to the symbol under the\ncursor using the {#action editor::FindReferences} command (`cmd-click` on macOS,\n`ctrl-click` on Windows/Linux, or `g A` in Vim mode.\n\nDepending on the language server, commands like {#action editor::GoToDefinition}\nand {#action editor::GoToTypeDefinition} will also open a multibuffer if there\nare multiple possible definitions.\n"},{"path":"outline-panel.md","title":"Outline Panel","group":"Guides","content":"# Outline Panel\n\nIn addition to the modal outline (`cmd-shift-o`), MonyaCode has the _outline panel_.\nThe outline panel can be deployed via `cmd-shift-b` ({#action\noutline_panel::ToggleFocus} via the command palette), or by clicking the\n`Outline Panel` button in the status bar.\n\nWhen viewing a \"singleton\" buffer (i.e., a single file on a tab), the outline\npanel works similarly to that of the outline modal－it displays the outline of\nthe current buffer's symbols, as reported by\n[tree-sitter](https://tree-sitter.github.io/tree-sitter/). Clicking on an entry\njumps to the associated section in the file. The outline view will also\nautomatically scroll to the section associated with the current cursor position\nwithin the file.\n\n## Project Search Results\n\nGet an overview of search results across the project.\n\n## Project Diagnostics\n\nView a summary of all errors and warnings reported by the language server.\n\n## Find All References\n\nQuickly navigate through all references when using the {#action\neditor::FindAllReferences} action.\n\nThe outline view provides a way to quickly navigate to specific parts of the\ncode to help maintain context when working with large result sets in\nmulti-buffers.\n"},{"path":"remote-development.md","title":"Remote Development","group":"Guides","content":"# Remote Development\n\nMonyaCode supports remote editing over SSH, where you run the editor on your main\nmachine but open and edit projects on remote machines.\n\nTo make this work, MonyaCode uses a remote process called `remote-server`, which\nneeds to be installed on the machine that the editor should connect to. The\neditor will SSH to the remote machine and start the remote server which then\ntakes care of opening, reading and writing files as well as running language\nservers.\n\nThe remote server communicates with the editor over SSH and GRPC.\n\n## Overview\n\nRemote development requires two computers, your local machine that runs the UI\nand the remote server which runs a small server. The two communicate over SSH,\nso you will need to be able to SSH from your local machine into the remote\nserver to use this feature.\n\nOn your local machine, MonyaCode runs its UI, uses\nTree-sitter to parse and syntax-highlight code, and store unsaved changes and\nrecent projects. The source code, language servers, tasks, and the terminal all\nrun on the remote server.\n\n## Setup\n\n1. Install the editor on your main machine.\n\n2. Build and copy the remote editor process to your development machine.\n\n   Either get a pre-built copy of the remote server from the\n   [Codeberg releases](https://github.com/monyacode/monyacode/releases) page, or\n   build it from source.\n\n   If you download the pre-built release, make sure to get a version that\n   matches your remote server. If you are running the editor on a Mac but want\n   to connect to a Linux machine, you will need the Linux version of the\n   remote-server.\n\n   Also make sure to get the same version of the remote server as your editor.\n\n   Decompress and copy the server to\n   `~/.monyacode_server/monyacode-remote-server-{RELEASE_CHANNEL}-{VERSION}`.\n\n   For example:\n\n   ```sh\n   mkdir -p ~/.monyacode_server\n   curl -L -o - \\\n     https://github.com/monyacode/monyacode/releases/download/1.1.0/monyacode-remote-server-linux-x86_64-1.1.0.gz \\\n     | gunzip > ~/.monyacode_server/monyacode-remote-server-stable-1.1.0\n   chmod +x ~/.monyacode_server/monyacode-remote-server-stable-1.1.0\n   ```\n\n3. Use {#kb projects::OpenRemote} to open the \"Remote Projects\" dialog.\n\n4. Click \"Connect New Server\" and enter the command you use to SSH into the\n   server. See [Supported SSH options](#supported-ssh-options) for options you\n   can pass.\n\n5. Your local machine will attempt to connect to the remote server using the\n   `ssh` binary on your path. Assuming the connection is successful and a\n   compatible server binary is found, the editor will start up and start\n   communicating with the remote server.\n\n6. Once the remote server is running, you will be prompted to choose a path to\n   open on the remote server.\n\n   > **Note:** The remote server does not currently handle opening very large\n   > directories (for example, `/` or `~` that may have >100,000 files) very\n   > well.\n\n   On a Mac, the editor may fail to connect the first time you try, and you will\n   be presented with a permissions dialog to allow MonyaCode to connect to the\n   network. You'll need to accept this and then try again.\n\n### Connect from the command line\n\nTo open the editor and connect to a remote project directly from the terminal,\nuse a command like `monyacode ssh://[<user>@]<host>[:<port>]/<path-to-project>`.\n\nIt is currently not possible to pass any other arguments to SSH from the\ncommandline. A workaround here is to set other options you need in your SSH\nconfig file. See the SSH documentation (`man ssh_config`) for more details.\n\nTo hotlink into an SSH project, use a link of the format:\n`monyacode://ssh/[<user>@]<host>[:<port>]/<path>`.\n\n## Supported platforms\n\nThe remote machine must be able to run the remote server process, and so you\nneed to compile the remote server process for the target platform (cross\ncompilation).\n\nThe following platforms should work:\n\n- macOS Catalina or later (Intel or Apple Silicon)\n- Linux (x86_64 or arm64)\n- Windows is not yet supported as a remote server, but Windows can be used as a\n  local machine to connect to remote servers. Note that Windows support in is\n  currently not well tested in general.\n\n## Configuration\n\nThe list of remote servers is stored in your settings file {#kb\nmonyacode::OpenSettings}. You can edit this list using the Remote Projects dialog\n{#kb projects::OpenRemote}, which provides some robustness - for example it\nchecks that the connection can be established before writing it to the settings\nfile.\n\n```json\n{\n  \"ssh_connections\": [\n    {\n      \"host\": \"192.168.1.10\",\n      \"projects\": [{ \"paths\": [\"~/code/monyacode/monyacode\"] }],\n    },\n  ],\n}\n```\n\nMonyaCode shells out to the `ssh` on your path, and so it will inherit any\nconfiguration you have in `~/.ssh/config` for the given host. That said, if you\nneed to override anything you can configure the following additional options on\neach connection:\n\n```json\n{\n  \"ssh_connections\": [\n    {\n      \"host\": \"192.168.1.10\",\n      \"projects\": [{ \"paths\": [\"~/code/monyacode/monyacode\"] }],\n      // any argument to pass to the ssh master process\n      \"args\": [\"-i\", \"~/.ssh/work_id_file\"],\n      \"port\": 22, // defaults to 22\n      // defaults to your username on your local machine\n      \"username\": \"me\",\n    },\n  ],\n}\n```\n\nYou can also set a nickname for the remote server:\n\n```json\n{\n  \"ssh_connections\": [\n    {\n      \"host\": \"192.168.1.10\",\n      \"projects\": [{ \"paths\": [\"~/code/monyacode/monyacode\"] }],\n      // Shown in the UI to help distinguish multiple hosts.\n      \"nickname\": \"lil-linux\",\n    },\n  ],\n}\n```\n\nIf you use the command line to open a connection to a host by doing\n`monyacode ssh://192.168.1.10/~/.vimrc`, then extra options are read from your\nsettings file by finding the first connection that matches the\nhost/username/port of the URL on the command line.\n\nAdditionally it's worth noting that while you can pass a password on the command\nline `monyacode ssh://user:password@host/~`, we do not support writing a password to\nyour settings file. If you're connecting repeatedly to the same host, you should\nconfigure key-based authentication.\n\n## Remote Development on Windows (SSH)\n\nMonyaCode on Windows supports SSH remoting and will prompt for credentials when\nneeded.\n\nIf you encounter authentication issues, confirm that your SSH key agent is\nrunning (e.g., ssh-agent or your Git client's agent) and that ssh.exe is on\nPATH.\n\n### Troubleshooting SSH on Windows\n\nWhen prompted for credentials, use the graphical askpass dialog. If it doesn't\nappear, check for credential manager conflicts and that GUI prompts aren't\nblocked by your terminal.\n\n## WSL Support\n\nMonyaCode supports opening folders inside of WSL natively on Windows.\n\n### Opening a local folder in WSL\n\nTo open a local folder inside a WSL container, use the `projects: open in wsl`\naction and select the folder you want to open. You will be presented with a list\nof available WSL distributions to open the folder in.\n\n### Opening a folder already in WSL\n\nTo open a folder that's already located inside of a WSL container, use the\n`projects: open wsl` action and select the WSL distribution. The distribution\nwill be added to the `Remote Projects` window where you will be able to open the\nfolder.\n\n## Port forwarding\n\nIf you'd like to be able to connect to ports on your remote server from your\nlocal machine, you can configure port forwarding in your settings file. This is\nparticularly useful for developing websites so you can load the site in your\nbrowser while working.\n\n```json\n{\n  \"ssh_connections\": [\n    {\n      \"host\": \"192.168.1.10\",\n      \"port_forwards\": [{ \"local_port\": 8080, \"remote_port\": 80 }],\n    },\n  ],\n}\n```\n\nThis will cause requests from your local machine to `localhost:8080` to be\nforwarded to the remote machine's port 80. Under the hood this uses the `-L`\nargument to ssh.\n\nBy default these ports are bound to localhost, so other computers in the same\nnetwork as your development machine cannot access them. You can set the\nlocal_host to bind to a different interface, for example, 0.0.0.0 will bind to\nall local interfaces.\n\n```json\n{\n  \"ssh_connections\": [\n    {\n      \"host\": \"192.168.1.10\",\n      \"port_forwards\": [\n        {\n          \"local_port\": 8080,\n          \"remote_port\": 80,\n          \"local_host\": \"0.0.0.0\",\n        },\n      ],\n    },\n  ],\n}\n```\n\nThese ports also default to the `localhost` interface on the remote host. If you\nneed to change this, you can also set the remote host:\n\n```json\n{\n  \"ssh_connections\": [\n    {\n      \"host\": \"192.168.1.10\",\n      \"port_forwards\": [\n        {\n          \"local_port\": 8080,\n          \"remote_port\": 80,\n          \"remote_host\": \"docker-host\",\n        },\n      ],\n    },\n  ],\n}\n```\n\n## MonyaCode settings\n\nWhen opening a remote project there are three relevant settings locations:\n\n- The local MonyaCode settings (in `~/.monyacode/settings.json` on macOS or\n  `~/.config/monyacode/settings.json` on Linux) on your local machine.\n- The server MonyaCode settings (in the same place) on the remote server.\n- The project settings (in `.monyacode/settings.json` or `.editorconfig` of your\n  project)\n\nBoth the local MonyaCode and the server MonyaCode read the project settings, but they are\nnot aware of the other's main `settings.json`.\n\nWhich settings file you should use depends on the kind of setting you want to\nmake:\n\n- Project settings should be used for things that affect the project:\n  indentation settings, which formatter / language server to use, etc.\n- Server settings should be used for things that affect the server: paths to\n  language servers, proxy settings, etc.\n- Local settings should be used for things that affect the UI: font size, etc.\n\nIn addition any extensions you have installed locally will be propagated to the\nremote server. This means that language servers, etc. will run correctly.\n\n## Proxy Configuration\n\nThe remote server will not use your local machine's proxy configuration because\nthey may be under different network policies. If your remote server requires a\nproxy to access the internet, you must configure it on the remote server itself.\n\nIn most cases, your remote server will already have proxy environment variables\nconfigured. MonyaCode will automatically use them when downloading language servers,\ncommunicating with LLM models, etc.\n\nIf needed, you can set these environment variables in the server's shell\nconfiguration (e.g., `~/.bashrc`):\n\n```bash\nexport http_proxy=\"http://proxy.example.com:8080\"\nexport https_proxy=\"http://proxy.example.com:8080\"\nexport no_proxy=\"localhost,127.0.0.1\"\n```\n\nAlternatively, you can configure the proxy in the remote machine's\n`~/.config/monyacode/settings.json` (Linux) or `~/.monyacode/settings.json` (macOS):\n\n```json\n{\n  \"proxy\": \"http://proxy.example.com:8080\"\n}\n```\n\nSee the [proxy documentation](./configuring-monyacode.md#network-proxy) for supported\nproxy types and additional configuration options.\n\n## Initializing the remote server\n\nOnce you provide the SSH options, MonyaCode shells out to `ssh` on your local machine\nto create a ControlMaster connection with the options you provide.\n\nAny prompts that SSH needs will be shown in the UI, so you can verify host keys,\ntype key passwords, etc.\n\nOnce the master connection is established, MonyaCode will check to see if the remote\nserver binary is present in `~/.monyacode_server` on the remote, and that its version\nmatches the current version of MonyaCode that you're using.\n\nTo build the remote server binary, run `cargo build -p remote_server --release`.\nUpload it to `~/.monyacode_server/monyacode-remote-server-{RELEASE_CHANNEL}-{VERSION}` on\nthe server, for example `~/.monyacode_server/monyacode-remote-server-stable-0.181.6`. The\nversion must exactly match the version of MonyaCode itself you are using.\n\n## Maintaining the SSH connection\n\nOnce the server is initialized. MonyaCode will create new SSH connections (reusing\nthe existing ControlMaster) to run the remote development server.\n\nEach connection tries to run the development server in proxy mode. This mode\nwill start the daemon if it is not running, and reconnect to it if it is. This\nway when your connection drops and is restarted, you can continue to work\nwithout interruption.\n\nIn the case that reconnecting fails, the daemon will not be re-used. That said,\nunsaved changes are by default persisted locally, so that you do not lose work.\nYou can always reconnect to the project at a later date and MonyaCode will restore\nunsaved changes.\n\nIf you are struggling with connection issues, you should be able to see more\ninformation in the MonyaCode log `cmd-shift-p Open Log`.\n\n## Supported SSH Options\n\nUnder the hood, MonyaCode shells out to the `ssh` binary to connect to the remote\nserver. We create one SSH control master per project, and then use that to\nmultiplex SSH connections for the MonyaCode protocol itself, any terminals you open\nand tasks you run. We read settings from your SSH config file, but if you want\nto specify additional options to the SSH control master you can configure MonyaCode\nto set them.\n\nWhen typing in the \"Connect New Server\" dialog, you can use bash-style quoting\nto pass options containing a space. Once you have created a server it will be\nadded to the `\"ssh_connections\": []` array in your settings file. You can edit\nthe settings file directly to make changes to SSH connections.\n\nSupported options:\n\n- `-p` / `-l` - these are equivalent to passing the port and the username in the\n  host string.\n- `-L` / `-R` for port forwarding\n- `-i` - to use a specific key file\n- `-o` - to set custom options\n- `-J` / `-w` - to proxy the SSH connection\n- `-F` for specifying an `ssh_config`\n- And also... `-4`, `-6`, `-A`, `-B`, `-C`, `-D`, `-I`, `-K`, `-P`, `-X`, `-Y`,\n  `-a`, `-b`, `-c`, `-i`, `-k`, `-l`, `-m`, `-o`, `-p`, `-w`, `-x`, `-y`\n\nNote that we deliberately disallow some options (for example `-t` or `-T`) that\nMonyaCode will set for you.\n\n## Known Limitations\n\n- You can't open files from the remote Terminal by typing the `monyacode` command.\n"},{"path":"repl.md","title":"REPL","group":"Guides","content":"# REPL\n\n## Getting started\n\nBring the power of\n[Jupyter kernels](https://docs.jupyter.org/en/latest/projects/kernels.html) to\nyour editor! The built-in REPL for MonyaCode allows you to run code interactively in\nyour editor similarly to a notebook with your own text files.\n\n## Installation\n\nMonyaCode supports running code in multiple languages. To get started, you need to\ninstall a kernel for the language you want to use.\n\n**Currently supported languages:**\n\n- [Python (ipykernel)](#python)\n- [TypeScript (Deno)](#typescript-deno)\n- [R (Ark)](#r-ark)\n- [R (Xeus)](#r-xeus)\n- [Julia](#julia)\n- [Scala (Almond)](#scala)\n\nOnce installed, you can start using the REPL in the respective language files,\nor other places those languages are supported, such as Markdown. If you recently\nadded the kernels, run the `repl: refresh kernelspecs` command to make them\navailable in the editor.\n\n## Using the REPL\n\nTo start the REPL, open a file with the language you want to use and use the\n`repl: run` command (defaults to `ctrl-shift-enter` on macOS) to run a block,\nselection, or line. You can also click on the REPL icon in the toolbar.\n\nThe `repl: run` command will be executed on your selection(s), and the result\nwill be displayed below the selection.\n\nOutputs can be cleared with the `repl: clear outputs` command, or from the REPL\nmenu in the toolbar.\n\n### Cell mode\n\nMonyaCode supports\n[notebooks as scripts](https://jupytext.readthedocs.io/en/latest/formats-scripts.html)\nusing the `# %%` cell separator in Python and `// %%` in TypeScript. This allows\nyou to write code in a single file and run it as if it were a notebook, cell by\ncell.\n\nThe `repl: run` command will run each block of code between the `# %%` markers\nas a separate cell.\n\n```python\n# %% Cell 1\nimport time\nimport numpy as np\n\n# %% Cell 2\nimport matplotlib.pyplot as plt\nimport matplotlib.pyplot as plt\nfrom matplotlib import style\nstyle.use('ggplot')\n```\n\n## Language specific instructions\n\n### Python {#python}\n\n#### Global environment\n\n<div class=\"warning\">\n\nOn macOS, your system Python will _not_ work. Either set up\n[pyenv](https://github.com/pyenv/pyenv?tab=readme-ov-file#installation) or use a\nvirtual environment.\n\n</div>\n\nTo setup your current Python to have an available kernel, run:\n\n```sh\npip install ipykernel\npython -m ipykernel install --user\n```\n\n#### Conda Environment\n\n```sh\nsource activate myenv\nconda install ipykernel\npython -m ipykernel install --user --name myenv --display-name \"Python (myenv)\"\n```\n\n#### Virtualenv with pip\n\n```sh\nsource activate myenv\npip install ipykernel\npython -m ipykernel install --user --name myenv --display-name \"Python (myenv)\"\n```\n\n### R (Ark Kernel) {#r-ark}\n\nInstall [Ark](https://github.com/posit-dev/ark/releases) by downloading the\nrelease for your operating system. For example, for macOS just unpack `ark`\nbinary and put it into `/usr/local/bin`. Then run:\n\n```sh\nark --install\n```\n\n### R (Xeus Kernel) {#r-xeus}\n\n- Install [Xeus-R](https://github.com/jupyter-xeus/xeus-r)\n- Install the R Extension for MonyaCode (search for `R` in MonyaCode Extensions)\n\n<!--\nTBD: Improve R REPL (Ark Kernel) instructions\n-->\n\n### TypeScript: Deno {#typescript-deno}\n\n- [Install Deno](https://docs.deno.com/runtime/manual/getting_started/installation/)\n  and then install the Deno jupyter kernel:\n\n```sh\ndeno jupyter --install\n```\n\n<!--\nTBD: Improve R REPL (Ark Kernel) instructions\n-->\n\n### Julia\n\n- Download and install Julia from the\n  [official website](https://julialang.org/downloads/).\n- Install the Julia Extension for MonyaCode (search for `Julia` in MonyaCode Extensions)\n\n<!--\nTBD: Improve Julia REPL instructions\n-->\n\n### Scala\n\n- [Install Scala](https://www.scala-lang.org/download/) with `cs setup`\n  (Coursier):\n  - `brew install coursier/formulas/coursier && cs setup`\n- REPL (Almond)\n  [setup instructions](https://almond.sh/docs/quick-start-install):\n  - `brew install --cask temurin` (Eclipse foundation official OpenJDK binaries)\n  - `brew install coursier/formulas/coursier && cs setup`\n  - `coursier launch --use-bootstrap almond -- --install`\n\n## Changing which kernel is used per language {#changing-kernels}\n\nMonyaCode automatically detects the available kernels on your system. If you need to\nconfigure a different default kernel for a language, you can assign a kernel for\nany supported language in your `settings.json`.\n\n```json\n{\n  \"jupyter\": {\n    \"kernel_selections\": {\n      \"python\": \"conda-env\",\n      \"typescript\": \"deno\",\n      \"javascript\": \"deno\",\n      \"r\": \"ark\",\n    },\n  },\n}\n```\n\n## Debugging Kernelspecs\n\nAvailable kernels are shown via the `repl: sessions` command. To refresh the\nkernels you can run, use the `repl: refresh kernelspecs` command.\n\nIf you have `jupyter` installed, you can run `jupyter kernelspec list` to see\nthe available kernels.\n\n```sh\n$ jupyter kernelspec list\nAvailable kernels:\n  ark                   /Users/z/Library/Jupyter/kernels/ark\n  conda-base            /Users/z/Library/Jupyter/kernels/conda-base\n  deno                  /Users/z/Library/Jupyter/kernels/deno\n  python-chatlab-dev    /Users/z/Library/Jupyter/kernels/python-chatlab-dev\n  python3               /Users/z/Library/Jupyter/kernels/python3\n  ruby                  /Users/z/Library/Jupyter/kernels/ruby\n  rust                  /Users/z/Library/Jupyter/kernels/rust\n```\n\n> Note: MonyaCode makes best effort usage of `sys.prefix` and `CONDA_PREFIX` to find\n> kernels in Python environments. If you want explicitly control run\n> `python -m ipykernel install --user --name myenv --display-name \"Python (myenv)\"`\n> to install the kernel directly while in the environment.\n"},{"path":"snippets.md","title":"Snippets","group":"Guides","content":"# Snippets\n\nUse the {#action snippets::ConfigureSnippets} action to create a new snippets\nfile or edit an existing snippets file for a specified [scope](#scopes).\n\nThe snippets are located in `~/.config/monyacode/snippets` directory to which you can\nnavigate to with the {#action snippets::OpenFolder} action.\n\n## Example configuration\n\n```json\n{\n  // Each snippet must have a name and body, but the prefix and description are optional.\n  // The prefix is used to trigger the snippet, but when omitted then the name is used.\n  // Use placeholders like $1, $2 or ${1:defaultValue} to define tab stops.\n  // The $0 determines the final cursor position.\n  // Placeholders with the same value are linked.\n  \"Log to console\": {\n    \"prefix\": \"log\",\n    \"body\": [\"console.info(\\\"Hello, ${1:World}!\\\")\", \"$0\"],\n    \"description\": \"Logs to console\",\n  },\n}\n```\n\n## Scopes\n\nThe scope is determined by the language name in lowercase e.g. `python.json` for\nPython, `shell script.json` for Shell Script, but there are some exceptions to\nthis rule:\n\n| Scope      | Filename        |\n| ---------- | --------------- |\n| Global     | snippets.json   |\n| JSX        | javascript.json |\n| Plain Text | plaintext.json  |\n\nTo create JSX snippets you have to use `javascript.json` snippets file, instead\nof `jsx.json`, but this does not apply to TSX and TypeScript which follow the\nabove rule.\n\n## Known Limitations\n\n- Only the first prefix is used when a list of prefixes is passed in.\n- Currently only the `json` snippet file format is supported, even though the\n  `simple-completion-language-server` supports both `json` and `toml` file\n  formats.\n\n## See also\n\nThe `feature_paths` option in `simple-completion-language-server` is disabled by\ndefault.\n\nIf you want to enable it you can add the following to your `settings.json`:\n\n```json\n{\n  \"lsp\": {\n    \"snippet-completion-server\": {\n      \"settings\": {\n        \"feature_paths\": true,\n      },\n    },\n  },\n}\n```\n\nFor more configuration information, see the\n[`simple-completion-language-server` instructions](https://github.com/zed-industries/simple-completion-language-server/tree/main).\n"},{"path":"supertab.md","title":"SuperTab","group":"Guides","content":"# SuperTab\n\nInspired by the Vim\n[SuperTab](https://www.vim.org/scripts/script.php?script_id=1643) plugin, this\nfeature makes the Tab key context-sensitive so that the same key can indent,\nproduce a literal tab character and perform completion, all in one.\n\nThe basic concept is that it's possible to figure out what to do when the user\npresses tab based on the current context. If the cursor is at the end of some\ntext, activating SuperTab will open the completion menu. If the cursor is at the\nbeginning of the line or only preceded by whitespace, it will indent the line.\nIn any other scenario, it will produce a tab character.\n\nTo enable SuperTab, bind the tab key to {#action editor::SuperTab} in your\n`keymap.jsonc`:\n\n```json\n{\n  \"context\": \"Editor && mode == full\",\n  \"use_key_equivalents\": true,\n  \"bindings\": {\n    \"tab\": \"editor::SuperTab\"\n  },\n}\n```\n\n## Fallback mode\n\nBy default, Supertab will fall back to inserting a literal `tab` character when\nnot at the beginning of the line or at a completion point. To have it always\nindent unless completing, you can configure `supertab_fallback` in\n`settings.jsonc` or via the Settings interface:\n\n```json\n{\n  \"supertab_fallback\": \"indent\", // default is \"tab\"\n}\n```\n"},{"path":"tab-switcher.md","title":"Tab Switcher","group":"Guides","content":"# Tab Switcher\n\nThe Tab Switcher provides a quick way to navigate between open tabs in MonyaCode. It\ndisplays a list of your open tabs sorted by recent usage, making it easy to jump\nback to whatever you were just working on.\n\n## Quick Switching\n\nWhen the Tab Switcher is opened using {#kb tab_switcher::Toggle}, instead of\nrunning the {#action tab_switcher::Toggle} from the command palette, it'll stay\nactive as long as the <kbd class=\"keybinding\">ctrl</kbd> key is held down.\n\nWhile holding down <kbd class=\"keybinding\">ctrl</kbd>, each subsequent <kbd\nclass=\"keybinding\">tab</kbd> press cycles to the next item (<kbd\nclass=\"keybinding\">shift</kbd> to cycle backwards) and, when <kbd\nclass=\"keybinding\">ctrl</kbd> is released, the selected item is confirmed and\nthe switcher is closed.\n\n## Opening the Tab Switcher\n\nThe Tab Switcher can also be opened with either {#action tab_switcher::Toggle}\nor {#action tab_switcher::ToggleAll}. Using {#kb tab_switcher::Toggle} will show\nonly the tabs for the current pane, while {#kb tab_switcher::ToggleAll} shows\nall tabs for all panes.\n\nWhile the Tab Switcher is open, you can:\n\n- Press {#kb menu::SelectNext} to move to the next tab in the list\n- Press {#kb menu::SelectPrevious} to move to the previous tab\n- Press <kbd class=\"keybinding\">enter</kbd> to confirm the selected tab and\n  close the switcher\n- Press <kbd class=\"keybinding\">escape</kbd> to close the switcher and return to\n  the original tab from which the switcher was opened\n- Press {#kb tab_switcher::CloseSelectedItem} to close the currently selected\n  tab\n\nAs you navigate through the list, MonyaCode will update the pane's active item to\nmatch the selected tab.\n\n## Action Reference\n\n| Action                                    | Description                                       |\n| ----------------------------------------- | ------------------------------------------------- |\n| {#action tab_switcher::Toggle}            | Open the Tab Switcher for the current pane        |\n| {#action tab_switcher::ToggleAll}         | Open the Tab Switcher showing tabs from all panes |\n| {#action tab_switcher::CloseSelectedItem} | Close the selected tab in the Tab Switcher        |\n"},{"path":"tasks.md","title":"Tasks","group":"Guides","content":"# Tasks\n\nMonyaCode supports ways to spawn (and rerun) commands using its integrated terminal\nto output the results. These commands can read a limited subset of MonyaCode state\n(such as a path to the file currently being edited or selected text).\n\n```json [tasks]\n[\n  {\n    \"label\": \"Example task\",\n    \"command\": \"for i in {1..5}; do echo \\\"Hello $i/5\\\"; sleep 1; done\",\n    //\"args\": [],\n    // Env overrides for the command, will be appended to the terminal's environment from the settings.\n    \"env\": { \"foo\": \"bar\" },\n    // Current working directory to spawn the command into, defaults to current project root.\n    //\"cwd\": \"/path/to/working/directory\",\n    // Whether to use a new terminal tab or reuse the existing one to spawn the process, defaults to `false`.\n    \"use_new_terminal\": false,\n    // Whether to allow multiple instances of the same task to be run, or rather wait for the existing ones to finish, defaults to `false`.\n    \"allow_concurrent_runs\": false,\n    // What to do with the terminal pane and tab, after the command was started:\n    // * `always` — always show the task's pane, and focus the corresponding tab in it (default)\n    // * `no_focus` — always show the task's pane, add the task's tab in it, but don't focus it\n    // * `never` — do not alter focus, but still add/reuse the task's tab in its pane\n    \"reveal\": \"always\",\n    // What to do with the terminal pane and tab, after the command has finished:\n    // * `never` — Do nothing when the command finishes (default)\n    // * `always` — always hide the terminal tab, hide the pane also if it was the last tab in it\n    // * `on_success` — hide the terminal tab on task success only, otherwise behaves similar to `always`\n    \"hide\": \"never\",\n    // Which shell to use when running a task inside the terminal.\n    // May take 3 values:\n    // 1. (default) Use the system's default terminal configuration in /etc/passwd\n    //      \"shell\": \"system\"\n    // 2. A program:\n    //      \"shell\": {\n    //        \"program\": \"sh\"\n    //      }\n    // 3. A program with arguments:\n    //     \"shell\": {\n    //         \"with_arguments\": {\n    //           \"program\": \"/bin/bash\",\n    //           \"args\": [\"--login\"]\n    //         }\n    //     }\n    \"shell\": \"system\",\n    // Whether to show the task line in the output of the spawned task, defaults to `true`.\n    \"show_summary\": true,\n    // Whether to show the command line in the output of the spawned task, defaults to `true`.\n    \"show_command\": true,\n    // Which edited buffers to save before running the task:\n    // * `none` — don't save any buffers (default)\n    // * `all` — save all edited buffers\n    // * `current` — save current buffer only\n    \"save\": \"none\",\n    // Represents the tags for inline runnable indicators, or spawning multiple tasks at once.\n    // \"tags\": []\n  }\n]\n```\n\nThere are two actions that drive the workflow of using tasks: `task: spawn` and\n`task: rerun`. `task: spawn` opens a modal with all available tasks in the\ncurrent file. `task: rerun` reruns the most recently spawned task. You can also\nrerun tasks from the task modal.\n\nBy default, rerunning tasks reuses the same terminal (due to the\n`\"use_new_terminal\": false` default) but waits for the previous task to finish\nbefore starting (due to the `\"allow_concurrent_runs\": false` default).\n\nKeep `\"use_new_terminal\": false` and set `\"allow_concurrent_runs\": true` to\nallow cancelling previous tasks on rerun.\n\n## Task templates\n\nTasks can be defined:\n\n- in the global `tasks.jsonc` file; such tasks are available in all MonyaCode\n  projects you work on. This file is usually located in\n  `~/.config/monyacode/tasks.json`. You can edit them by using the `monyacode: open tasks`\n  action.\n- in the worktree-specific (local) `.monyacode/tasks.jsonc` file; such tasks are\n  available only when working on a project with that worktree included. You can\n  edit worktree-specific tasks by using the `monyacode: open project tasks` action.\n- on the fly with [oneshot tasks](#oneshot-tasks). These tasks are\n  project-specific and do not persist across sessions.\n- by language extension.\n\n## Variables\n\nMonyaCode tasks act just like your shell; that also means that you can reference\nenvironmental variables via sh-esque `$VAR_NAME` syntax. A couple of additional\nenvironmental variables are set for your convenience. These variables allow you\nto pull information from the current editor and use it in your tasks. The\nfollowing variables are available:\n\n- `MONYACODE_COLUMN`: current line column\n- `MONYACODE_ROW`: current line row\n- `MONYACODE_FILE`: absolute path of the currently opened file (e.g.\n  `/Users/my-user/path/to/project/src/main.rs`)\n- `MONYACODE_FILENAME`: filename of the currently opened file (e.g. `main.rs`)\n- `MONYACODE_DIRNAME`: absolute path of the currently opened file with file name\n  stripped (e.g. `/Users/my-user/path/to/project/src`)\n- `MONYACODE_RELATIVE_FILE`: path of the currently opened file, relative to\n  `MONYACODE_WORKTREE_ROOT` (e.g. `src/main.rs`)\n- `MONYACODE_RELATIVE_DIR`: path of the currently opened file's directory, relative\n  to `MONYACODE_WORKTREE_ROOT` (e.g. `src`)\n- `MONYACODE_STEM`: stem (filename without extension) of the currently opened file\n  (e.g. `main`)\n- `MONYACODE_SYMBOL`: currently selected symbol; should match the last symbol shown\n  in a symbol breadcrumb (e.g. `mod tests > fn test_task_contexts`)\n- `MONYACODE_SELECTED_TEXT`: currently selected text\n- `MONYACODE_WORKTREE_ROOT`: absolute path to the root of the current worktree. (e.g.\n  `/Users/my-user/path/to/project`)\n- `MONYACODE_CUSTOM_RUST_PACKAGE`: (Rust-specific) name of the parent package of\n  $MONYACODE_FILE source file.\n\nTo use a variable in a task, prefix it with a dollar sign (`$`):\n\n```json\n{\n  \"label\": \"echo current file's path\",\n  \"command\": \"echo $MONYACODE_FILE\",\n}\n```\n\nYou can also use verbose syntax that allows specifying a default if a given\nvariable is not available: `${MONYACODE_FILE:default_value}`\n\nThese environmental variables can also be used in tasks' `cwd`, `args`, and\n`label` fields.\n\n### Variable Quoting\n\nWhen working with paths containing spaces or other special characters, please\nensure variables are properly escaped.\n\nFor example, instead of this (which will fail if the path has a space):\n\n```json\n{\n  \"label\": \"stat current file\",\n  \"command\": \"stat $MONYACODE_FILE\",\n}\n```\n\nProvide the following:\n\n```json\n{\n  \"label\": \"stat current file\",\n  \"command\": \"stat\",\n  \"args\": [\"$MONYACODE_FILE\"],\n}\n```\n\nOr explicitly include escaped quotes like so:\n\n```json\n{\n  \"label\": \"stat current file\",\n  \"command\": \"stat \\\"$MONYACODE_FILE\\\"\",\n}\n```\n\n### Task filtering based on variables\n\nTask definitions with variables which are not present at the moment the task\nlist is determined are filtered out. For example, the following task will appear\nin the spawn modal only if there is a text selection:\n\n```json\n{\n  \"label\": \"selected text\",\n  \"command\": \"echo \\\"$MONYACODE_SELECTED_TEXT\\\"\",\n}\n```\n\nSet default values to such variables to have such tasks always displayed:\n\n```json\n{\n  \"label\": \"selected text with default\",\n  \"command\": \"echo \\\"${MONYACODE_SELECTED_TEXT:no text selected}\\\"\",\n}\n```\n\n## Oneshot tasks\n\nThe same task modal opened via `task: spawn` supports arbitrary bash-like\ncommand execution: type a command inside the modal text field, and use\n`opt-enter` to spawn it.\n\nThe task modal persists these ad-hoc commands for the duration of the session,\n`task: rerun` will also rerun such tasks if they were the last ones spawned.\n\nYou can also adjust the currently selected task in a modal (`tab` is the default\nkey binding). Doing so will put its command into a prompt that can then be\nedited & spawned as a oneshot task.\n\n### Ephemeral tasks\n\nYou can use the `cmd` modifier when spawning a task via a modal; tasks spawned\nthis way will not have their usage count increased (thus, they will not be\nrespawned with `task: rerun` and they won't have a high rank in the task modal).\nThe intended use of ephemeral tasks is to stay in the flow with continuous\n`task: rerun` usage.\n\n### More task rerun control\n\nBy default, tasks capture their variables into a context once, and this\n\"resolved task\" is being rerun always.\n\nThis can be controlled with the `\"reevaluate_context\"` argument to the task:\nsetting it to `true` will force the task to be reevaluated before each run.\n\n```json\n{\n  \"context\": \"Workspace\",\n  \"bindings\": {\n    \"alt-t\": [\"task::Rerun\", { \"reevaluate_context\": true }],\n  },\n}\n```\n\n## Custom keybindings for tasks\n\nYou can define your own keybindings for your tasks via an additional argument to\n`task::Spawn`. If you wanted to bind the aforementioned\n`echo current file's path` task to `alt-g`, you would add the following snippet\nin your [`keymap.json`](./key-bindings.md) file:\n\n```json\n{\n  \"context\": \"Workspace\",\n  \"bindings\": {\n    \"alt-g\": [\"task::Spawn\", { \"task_name\": \"echo current file's path\" }],\n  },\n}\n```\n\nNote that these tasks can also have a 'target' specified to control where the\nspawned task should show up. This could be useful for launching a terminal\napplication that you want to use in the center area:\n\n```json [tasks]\n// In tasks.jsonc\n{\n  \"label\": \"start lazygit\",\n  \"command\": \"lazygit -p $MONYACODE_WORKTREE_ROOT\"\n}\n```\n\n```json\n// In keymap.jsonc\n{\n  \"context\": \"Workspace\",\n  \"bindings\": {\n    \"alt-g\": [\n      \"task::Spawn\",\n      { \"task_name\": \"start lazygit\", \"reveal_target\": \"center\" },\n    ],\n  },\n}\n```\n\n## Binding runnable tags to task templates\n\nMonyaCode supports overriding the default action for inline runnable indicators via\nworkspace-local and global `tasks.jsonc` file with the following precedence\nhierarchy:\n\n1. Workspace `tasks.jsonc`\n2. Global `tasks.jsonc`\n3. Language-provided tag bindings (default).\n\nTo tag a task, add the runnable tag name to the `tags` field on the task\ntemplate:\n\n```json\n{\n  \"label\": \"echo current file's path\",\n  \"command\": \"echo $MONYACODE_FILE\",\n  \"tags\": [\"rust-test\"],\n}\n```\n\nIn doing so, you can change which task is shown in the runnables indicator.\n\n## Keybindings to run tasks bound to runnables\n\nWhen you have a task definition that is bound to the runnable, you can quickly\nrun it using [Code Actions](./configuring-languages?#code-actions.md) that you\ncan trigger either via `editor: Toggle Code Actions` command or by the\n`cmd-.`/`ctrl-.` shortcut. Your task will be the first in the dropdown. The task\nwill run immediately if there are no additional Code Actions for this line.\n"},{"path":"themes.md","title":"Themes","group":"Guides","content":"# Themes\n\nMonyaCode comes with a number of built-in themes, with more themes available as\nextensions.\n\n## Selecting a Theme\n\nSee what themes are installed and preview them via the Theme Selector, which you\ncan open from the command palette with `theme selector: toggle` (bound to {#kb\ntheme_selector::Toggle}).\n\nNavigating through the theme list by moving up and down will change the theme in\nreal time and hitting enter will save it to your settings file.\n\n## Installing more Themes\n\nMonyaCode can use Zed themes so you can install any theme from\n[zed-themes.com](https://zed-themes.com), a gallery with visible previews for\nmany of them.\n\n## Configuring a Theme\n\nYour selected theme is stored in your settings file. You can open your settings\nfile from the command palette with {#action monyacode::OpenSettingsFile} (bound to\n{#kb monyacode::OpenSettingsFile}).\n\nBy default, MonyaCode maintains two themes: one for light mode and one for dark mode.\nYou can set the mode to `\"dark\"` or `\"light\"` to ignore the current system mode.\n\n```json\n{\n  \"theme\": {\n    \"mode\": \"system\",\n    \"light\": \"One Light\",\n    \"dark\": \"One Dark\",\n  },\n}\n```\n\n## Theme Overrides\n\nTo override specific attributes of a theme, use the `theme_overrides` setting.\nThis setting can be used to configure theme-specific overrides.\n\nFor example, add the following to your `settings.json` if you wish to override\nthe background color of the editor and display comments and doc comments as\nitalics:\n\n```json\n{\n  \"theme_overrides\": {\n    \"One Dark\": {\n      \"editor.background\": \"#333\",\n      \"syntax\": {\n        \"comment\": {\n          \"font_style\": \"italic\",\n        },\n        \"comment.doc\": {\n          \"font_style\": \"italic\",\n        },\n      },\n      \"accents\": [\n        \"#ff0000\",\n        \"#ff7f00\",\n        \"#ffff00\",\n        \"#00ff00\",\n        \"#0000ff\",\n        \"#8b00ff\",\n      ],\n    },\n  },\n}\n```\n\nTo see a comprehensive list of list of captures (like `comment` and\n`comment.doc`) see\n[Language Extensions: Syntax highlighting](./extensions/languages.md#syntax-highlighting).\n\nTo see a list of available theme attributes look at the JSON file for your\ntheme.\n\n## Local Themes\n\nStore new themes locally by placing them in the `~/.config/monyacode/themes`\ndirectory (macOS and Linux) or `%USERPROFILE%\\AppData\\Roaming\\MonyaCode\\themes\\`\n(Windows).\n\nFor example, to create a new theme called `my-cool-theme`, create a file called\n`my-cool-theme.json` in that directory. It will be available in the theme\nselector the next time MonyaCode loads.\n\n## Theme Development\n\nSee: [Developing MonyaCode Themes](./extensions/themes.md)\n"},{"path":"toolchains.md","title":"Toolchains","group":"Guides","content":"# Toolchains\n\nThe toolchain selection UI is used to pick from a set of tools when working with\na given language in a current project.\n\nImagine that you are working in a Python project, which has virtual environments\nthat encapsulate a set of dependencies of your project along with a suitable\ninterpreter to run it with. The language server has to know which virtual\nenvironment you are working with, as it uses the virtual environment to resolve\ndependencies and parse the code in the project.\n\nThe toolchain selector UI is used to pick the correct virtual environment for\nyour project.\n\nIt's also possible to select different toolchains for different sub-projects\nwithin a MonyaCode project. The exact definition of a sub-project depends on the\nlanguage used.\n\nIn [remote projects](./remote-development.md), use the toolchain selector to\ncontrol the active toolchain on the remote host.\n\n## Why are toolchains necessary?\n\nLanguage servers need to use the correct toolchain / virtual environment to\nfunction properly. If it cannot resolve dependencies, functionality like \"Go to\ndefinition\" or code completion may be unavailable.\n\nIf the toolchain provides an \"activation script\", the integrated terminal panel\nwill invoke the script automatically so that the shell environment is set up\ncorrectly.\n\nThis also applies to [tasks](./tasks.md). Tasks behave as if they were launched\nfrom a new terminal tab, so any activation script will be called before\nexecuting a task.\n\n## Selecting toolchains\n\nThe active toolchain (if any) is displayed in the status bar, on the right hand\nside of the editor window. Clicking it open the toolchain selector.\nAlternatively, run the command {#action toolchain::Select} from the command\npalette.\n\nThe editor will automatically try to infer a set of toolchains to choose from. A\ndefault will also be selected on a best-effort basis when opening a project for\nthe first time.\n\nToolchain selection applies to the current subproject. This might be the whole\nproject, or in the case of a monorepo with multiple subprojects it may apply to\nthe current subproject only.\n\n## Adding toolchains manually\n\nIf the automatic detection fails to discover all toolchains you can add\nadditional toolchains manually using the \"Add toolchain\" button in the toolchain\nselector UI.\n\nProvide a path to the toolchain and give it a descriptive name.\n"},{"path":"troubleshooting.md","title":"Troubleshooting","group":"Guides","content":"# Troubleshooting\n\nThis guide covers common troubleshooting techniques for MonyaCode. Sometimes you'll\nbe able to identify and resolve issues on your own using this information. Other\ntimes, troubleshooting means gathering the right information—logs, profiles, or\nreproduction steps—to help developers diagnose and fix the problem.\n\n> **Note**: To open the command palette, use `cmd-shift-p` on macOS or\n> `ctrl-shift-p` on Windows / Linux.\n\n## Retrieve MonyaCode and System Information\n\nWhen reporting issues or seeking help, it's useful to know your MonyaCode version and\nsystem specifications. You can retrieve this information using the following\nactions from the command palette:\n\n- {#action monyacode::About}: Find your MonyaCode version number\n- {#action monyacode::CopySystemSpecsIntoClipboard}: Populate your clipboard with\n  MonyaCode version number, operating system version, and hardware specs\n\n## MonyaCode Log\n\nOften, a good first place to look when troubleshooting any issue in MonyaCode is the\nMonyaCode log, which might contain clues about what's going wrong. You can review the\nmost recent 1000 lines of the log by running the {#action monyacode::OpenLog} action\nfrom the command palette. If you want to view the full file, you can reveal it\nin your operating system's native file manager via {#action\nmonyacode::RevealLogInFileManager} from the command palette.\n\nYou'll find the MonyaCode log in the respective location on each operating system:\n\n- macOS: `~/Library/Logs/MonyaCode/MonyaCode.log`\n- Windows: `C:\\Users\\YOU\\AppData\\Local\\MonyaCode\\logs\\MonyaCode.log`\n- Linux: `~/.local/share/monyacode/logs/MonyaCode.log` or `$XDG_DATA_HOME`\n\n> Note: In some cases, it might be useful to monitor the log live, such as when\n> [developing a MonyaCode extension](./extensions/developing-extensions.md). Example:\n> `tail -f ~/Library/Logs/MonyaCode/MonyaCode.log`\n\nThe log may contain enough context to help you debug the issue yourself, or you\nmay find specific errors that are useful when filing an issue.\n\n## Performance Issues (Profiling)\n\nIf you're running into performance issues — such as hitches, hangs, or general\nunresponsiveness — having a performance profile will help zero in on what is\ngetting stuck.\n\n## Minidumps\n\nIf MonyaCode is crashing you can set `MONYACODE_GENERATE_MINIDUMPS=1` when running MonyaCode to\nget the editor to generate a crash dump file that can be analyzed to debug the\nissue. See [debugging-crashes.md](./development/debugging-crashes.md) for more\ndetails.\n\n### macOS\n\nXcode Instruments (which comes bundled with\n[Xcode](https://apps.apple.com/us/app/xcode/id497799835)) is the standard tool\nfor profiling on macOS.\n\n1. With MonyaCode running, open Instruments\n1. Select `Time Profiler` as the profiling template\n1. In the `Time Profiler` configuration, set the target to the running MonyaCode\n   process\n1. Start recording\n1. If the performance issue occurs when performing a specific action in MonyaCode,\n   perform that action now\n1. Stop recording\n1. Save the trace file\n\n<!--### Windows-->\n\n<!--### Linux-->\n\n## Startup and Workspace Issues\n\nMonyaCode creates local SQLite databases to persist data relating to its workspace\nand your projects. These databases store, for instance, the tabs and panes you\nhave open in a project, the scroll position of each open file, the list of all\nprojects you've opened (for the recent projects modal picker), etc. You can find\nand explore these databases in the following locations:\n\n- macOS: `~/Library/Application Support/MonyaCode/db`\n- Linux and FreeBSD: `~/.local/share/monyacode/db` (or within `XDG_DATA_HOME` or\n  `FLATPAK_XDG_DATA_HOME`)\n- Windows: `%LOCALAPPDATA%\\MonyaCode\\db`\n\nThe naming convention of these databases takes on the form of\n`0-<release_channel>`:\n\n- Stable: `0-stable`\n- Dev: `0-dev`\n\nWhile rare, we've seen a few cases where workspace databases became corrupted,\nwhich prevented MonyaCode from starting. If you're experiencing startup issues, you\ncan test whether it's workspace-related by temporarily moving the database from\nits location, then trying to start MonyaCode again.\n\n> **Note**: Moving the workspace database will cause MonyaCode to create a fresh one.\n> Your recent projects, open tabs, etc. will be reset to \"factory\".\n\nIf your issue persists after regenerating the database, please file an issue.\n\n## Language Server Issues\n\nIf you're experiencing language-server related issues, such as stale diagnostics\nor issues jumping to definitions, restarting the language server via {#action\neditor::RestartLanguageServer} from the command palette will often resolve the\nissue.\n\n## Licenses and cargo-about\n\nLicense information for third party dependencies must be correctly provided for\nCI to pass.\n\n[`cargo-about`](https://github.com/EmbarkStudios/cargo-about) is used to\nautomatically comply with open source licenses.\n\n- Is it showing a `no license specified` error for a crate you've created? If\n  so, add `publish = false` under `[package]` in your crate's Cargo.toml.\n- Is the error `failed to satisfy license requirements` for a dependency? If so,\n  first determine what license the project has and whether this system is\n  sufficient to comply with this license's requirements. If you're unsure, ask a\n  lawyer. Once you've verified that this system is acceptable add the license's\n  SPDX identifier to the `accepted` array in `script/licenses/licenses.toml`.\n- Is `cargo-about` unable to find the license for a dependency? If so, add a\n  clarification field at the end of `script/licenses/licenses.toml`, as\n  specified in the\n  [cargo-about book](https://embarkstudios.github.io/cargo-about/cli/generate/config.html#crate-configuration).\n\n## Unable to watch for file system changes\n\nBy default, MonyaCode uses a method native to the current platform to watch for file\nsystem changes (inotify on Linux, FSEvents on Mac). For some file systems, this\nmethod does not work, for example for NFS or SSHfs mounts, or when connecting to\na Windows remote.\n\nIt is possible to use a poll-based file watcher on these file systems instead.\nThe polling watcher is more resource-intensive than the native file watchers,\nbut works in more scenarios.\n\nTo use the poll watcher, set the `file_watcher` option in the Worktree settings.\n\nFor example:\n\n```json\n{\n  \"file_watcher\": {\n    \"mode\": \"poll\",  // \"native\" | \"poll\"\n    \"poll_interval_ms\": 2000  // 500-30000ms\n  }\n}\n```\n"},{"path":"uninstall.md","title":"Uninstall","group":"Guides","content":"# Uninstall\n\nThis guide covers how to uninstall MonyaCode on different operating systems.\n\n## macOS\n\n### Bundle compilation\n\nIf you installed MonyaCode by compiling it using the `script/bundle-mac` command:\n\n1. Quit MonyaCode if it's running\n2. Open Finder and go to your Applications folder\n3. Drag MonyaCode to the Trash (or right-click and select \"Move to Trash\")\n4. Empty the Trash\n\n### Removing User Data (Optional)\n\nTo completely remove all MonyaCode configuration files and data:\n\n1. Open Finder\n2. Press `Cmd + Shift + G` to open \"Go to Folder\"\n3. Delete the following directories if they exist:\n   - `~/Library/Application Support/MonyaCode`\n   - `~/Library/Saved Application State/app.liten.MonyaCode.savedState`\n   - `~/Library/Logs/MonyaCode`\n   - `~/Library/Caches/app.liten.MonyaCode`\n\n## Linux\n\n### Standard Uninstall\n\nIf MonyaCode was installed using the default installation script, run:\n\n```sh\nmonyacode --uninstall\n```\n\nYou'll be prompted whether to keep or delete your preferences. After making a\nchoice, you should see a message that MonyaCode was successfully uninstalled.\n\nIf the `monyacode` command is not found in your PATH, try:\n\n```sh\n$HOME/.local/bin/monyacode --uninstall\n```\n\nor:\n\n```sh\n$HOME/.local/monyacode.app/bin/monyacode --uninstall\n```\n\n### Package Manager\n\nIf you installed MonyaCode using a package manager (such as Flatpak or a\ndistribution-specific package manager), consult that package manager's\ndocumentation for uninstallation instructions.\n\n### Manual Removal\n\nIf the uninstall command fails or MonyaCode was installed to a custom location, you\ncan manually remove:\n\n- Installation directory: `~/.local/monyacode.app` (or your custom installation path)\n- Binary symlink: `~/.local/bin/monyacode`\n- Configuration and data: `~/.config/monyacode`\n\n## Windows\n\n### Standard Installation\n\n1. Quit MonyaCode if it's running\n2. Open Settings (Windows key + I)\n3. Go to \"Apps\" > \"Installed apps\" (or \"Apps & features\" on Windows 10)\n4. Search for \"MonyaCode\"\n5. Click the three dots menu next to MonyaCode and select \"Uninstall\"\n6. Follow the prompts to complete the uninstallation\n\nAlternatively, you can:\n\n1. Open the Start menu\n2. Right-click on MonyaCode\n3. Select \"Uninstall\"\n\n### Removing User Data (Optional)\n\nTo completely remove all MonyaCode configuration files and data:\n\n1. Press `Windows key + R` to open Run\n2. Type `%APPDATA%` and press Enter\n3. Delete the `MonyaCode` folder if it exists\n4. Press `Windows key + R` again, type `%LOCALAPPDATA%` and press Enter\n5. Delete the `MonyaCode` folder if it exists\n\n## Troubleshooting\n\nIf you encounter issues during uninstallation:\n\n- **macOS/Windows**: Ensure MonyaCode is completely quit before attempting to\n  uninstall. Check Activity Manager (macOS) or Task Manager (Windows) for any\n  running MonyaCode processes.\n- **Linux**: If the uninstall script fails, check the error message and consider\n  manual removal of the directories listed above.\n- **All platforms**: If you want to start fresh while keeping MonyaCode installed,\n  you can delete the configuration directories instead of uninstalling the\n  application entirely.\n\nFor additional help, see the [Linux-specific documentation](./linux.md).\n"},{"path":"vim.md","title":"Vim Mode","group":"Guides","content":"# Vim Mode\n\nMonyaCode includes an emulation of Vim mode (modal editing). To learn more about Vim\nand modal editing, see the [Vim documentation](https://www.vim.org/docs.php).\n\nFull compatibility is impossible, but many of the basic features of Vim are\nsupported as well as some features of various vim plugins.\n\n## MonyaCode's vim mode design\n\nThe MonyaCode Vim mode tries to offer a familiar experience to Vim users: it\nreplicates the behavior of motions and commands precisely when it makes sense\nand uses MonyaCode-specific functionality where appropriate.\n\nThis includes support for semantic navigation, multiple cursors, or other\nfeatures usually provided by plugins like surrounding text.\n\nSo, MonyaCode's vim mode does not replicate Vim one-to-one, but meshes Vim's modal\ndesign with MonyaCode's extended feature set. It's also possible to add additional\nkey bindings or override the defaults.\n\n### Core differences\n\nThere are four types of features in vim mode that use MonyaCode's core functionality,\nleading to some differences in behavior:\n\n1. **Motions**: vim mode uses MonyaCode's semantic parsing to tune the behavior of\n   motions per language. For example, in Rust, jumping to matching bracket with\n   `%` works with the pipe character `|`. In JavaScript, `w` considers `$` to be\n   a word character.\n2. **Visual block selections**: vim mode uses MonyaCode's multiple cursor to emulate\n   visual block selections, making block selections a lot more flexible. For\n   example, anything you insert after a block selection updates on every line in\n   real-time, and you can add or remove cursors anytime.\n3. **Macros**: vim mode uses MonyaCode's recording system for vim macros. So, you can\n   capture and replay more complex actions, like autocompletion.\n4. **Search and replace**: vim mode uses MonyaCode's search system, so, the syntax\n   for regular expressions is slightly different compared to Vim. See the\n   [Regex differences](#regex-differences) section for details.\n\nThe foundations of MonyaCode's vim mode should already cover many use cases, but\npatches to add features or additional functionality is always welcome.\n\n## Enabling and disabling vim mode\n\nVim mode can be enabled from the Onboarding screen shown when first installing\nthe editor.\n\nLater, vim mode can be toggled on or off at any time using the command {#action\nworkspace::ToggleVimMode}.\n\n> **Note**: This command toggles the following property in `settings.json`:\n>\n> ```json\n> {\n>   \"vim_mode\": true,\n> }\n> ```\n\n## MonyaCode-specific features\n\nMonyaCode uses Tree-sitter and the language server protocol to understand the\nstructure of files and supports multiple cursors out of the box.\n\nVim mode has several \"core MonyaCode\" key bindings that will help make the most of\nMonyaCode's specific feature set.\n\n### Language server (LSP)\n\nThe following commands use a language server (if available) to help navigate and\nrefactor code semantically.\n\n| Command                                  | Default Shortcut |\n| ---------------------------------------- | ---------------- |\n| Go to definition                         | `g d`            |\n| Go to declaration                        | `g D`            |\n| Go to type definition                    | `g y`            |\n| Go to implementation                     | `g I`            |\n| Rename (change definition)               | `c d`            |\n| Go to All references to the current word | `g A`            |\n| Find symbol in current file              | `g s`            |\n| Find symbol in entire project            | `g S`            |\n| Go to next diagnostic                    | `g ]` or `] d`   |\n| Go to previous diagnostic                | `g [` or `[ d`   |\n| Show inline error (hover)                | `g h`            |\n| Open the code actions menu               | `g .`            |\n\n### Git\n\n| Command                         | Default Shortcut |\n| ------------------------------- | ---------------- |\n| Go to next git change           | `] c`            |\n| Go to previous git change       | `[ c`            |\n| Expand diff hunk                | `d o`            |\n| Toggle staged                   | `d O`            |\n| Stage and next (in diff view)   | `d u`            |\n| Unstage and next (in diff view) | `d U`            |\n| Restore change                  | `d p`            |\n\n### Tree-sitter\n\n[Tree-sitter](https://tree-sitter.github.io/tree-sitter/) is an open source\nproject independent from MonyaCode which MonyaCode uses to understand the structure of\ncode, and which supports many programming languages. MonyaCode provides motions that\nchange the current cursor position, and text objects that can be used as the\ntarget of actions.\n\n| Command                         | Default Shortcut            |\n| ------------------------------- | --------------------------- |\n| Go to next/previous method      | `] m` / `[ m`               |\n| Go to next/previous method end  | `] M` / `[ M`               |\n| Go to next/previous section     | `] ]` / `[ [`               |\n| Go to next/previous section end | `] [` / `[ ]`               |\n| Go to next/previous comment     | `] /`, `] *` / `[ /`, `[ *` |\n| Select a larger syntax node     | `[ x`                       |\n| Select a smaller syntax node    | `] x`                       |\n\n| Text Objects                                               | Default Shortcut |\n| ---------------------------------------------------------- | ---------------- |\n| Around a class, definition, etc.                           | `a c`            |\n| Inside a class, definition, etc.                           | `i c`            |\n| Around a function, method etc.                             | `a f`            |\n| Inside a function, method, etc.                            | `i f`            |\n| A comment                                                  | `g c`            |\n| An argument, or list item, etc.                            | `i a`            |\n| An argument, or list item, etc. (including trailing comma) | `a a`            |\n| Around an HTML-like tag                                    | `a t`            |\n| Inside an HTML-like tag                                    | `i t`            |\n| The current indent level, and one line before and after    | `a I`            |\n| The current indent level, and one line before              | `a i`            |\n| The current indent level                                   | `i i`            |\n\nNote that the definitions for the targets of the `[m` family of motions are the\nsame as the boundaries defined by `af`. The targets of the `[[` are the same as\nthose defined by `ac`, though if there are no classes, then functions are also\nused. Similarly `gc` is used to find `[ /`. `g c`\n\nThe definition of functions, classes and comments is language dependent, and\nsupport can be added to extensions by adding a [`textobjects.scm`]. The\ndefinition of arguments and tags operates at the Tree-sitter level, but looks\nfor certain patterns in the parse tree and is not currently configurable per\nlanguage.\n\n### Multi cursor\n\nVim does not support multiple cursors out of the box, but these commands can be\nused in MonyaCode to manipulate them.\n\n| Command                                                      | Default Shortcut |\n| ------------------------------------------------------------ | ---------------- |\n| Add a cursor selecting the next copy of the current word     | `g l`            |\n| Add a cursor selecting the previous copy of the current word | `g L`            |\n| Skip latest word selection, and add next                     | `g >`            |\n| Skip latest word selection, and add previous                 | `g <`            |\n| Add a visual selection for every copy of the current word    | `g a`            |\n\n### Pane management\n\nThese commands open new panes or jump to specific panes.\n\n| Command                                    | Default Shortcut   |\n| ------------------------------------------ | ------------------ |\n| Open a project-wide search                 | `g /`              |\n| Open the current search excerpt            | `g <space>`        |\n| Open the current search excerpt in a split | `<ctrl-w> <space>` |\n| Go to definition in a split                | `<ctrl-w> g d`     |\n| Go to type definition in a split           | `<ctrl-w> g D`     |\n\n### In insert mode\n\nThe following commands control MonyaCode's completion menu.\n\n| Command                    | Default Shortcut |\n| -------------------------- | ---------------- |\n| Open the completion menu   | `ctrl-x ctrl-o`  |\n| Open the code actions menu | `ctrl-x ctrl-l`  |\n| Hides all suggestions      | `ctrl-x ctrl-z`  |\n\n### Supported plugins\n\nMonyaCode's vim mode includes some features that are usually provided by popular\nplugins in the Vim ecosystem:\n\n- Surround text objects with `ys` (yank surround), change surrounding with `cs`,\n  and delete surrounding with `ds`.\n- Comment and uncomment selections with `gc` in visual mode and `gcc` in normal\n  mode.\n- The project panel supports many shortcuts modeled after the Vim plugin\n  `netrw`: navigation with `hjkl`, open file with `o`, open file in a new tab\n  with `t`, etc.\n- It's possible to add key bindings to your keymap to navigate \"camelCase\"\n  names. See the [optional key bindings](#optional-key-bindings) section to\n  learn how.\n- Use `gR` to do\n  [ReplaceWithRegister](https://github.com/vim-scripts/ReplaceWithRegister).\n- Use `cx` for [vim-exchange](https://github.com/tommcdo/vim-exchange)\n  functionality. Note that it does not have a default binding in visual mode,\n  but you can add one to your keymap (refer to the\n  [optional key bindings](#optional-key-bindings) section).\n- Navigate to indent depths relative to your cursor with the\n  [indent wise](https://github.com/jeetsukumaran/vim-indentwise) plugin `[-`,\n  `]-`, `[+`, `]+`, `[=`, `]=`.\n- Select quoted text with AnyQuotes and bracketed text with AnyBrackets text\n  objects. MonyaCode also provides MiniQuotes and MiniBrackets which offer\n  alternative selection behavior based on the\n  [mini.ai](https://github.com/echasnovski/mini.nvim/blob/main/readmes/mini-ai.md)\n  Neovim plugin. See the\n  [Quote and Bracket text objects](#quote-and-bracket-text-objects) section\n  below for details.\n- It's possible to configure AnyQuotes, AnyBrackets, MiniQuotes, and\n  MiniBrackets text objects for selecting quoted and bracketed text using\n  different selection strategies. See the\n  [Any Bracket Functionality](#any-bracket-functionality) section below for\n  details.\n\n### Any Bracket Functionality\n\nMonyaCode offers two different strategies for selecting text surrounded by any quote,\nor any bracket. These text objects are **not enabled by default** and must be\nconfigured in your keymap to be used.\n\n#### Included Characters\n\nEach text object type works with specific characters:\n\n| Text Object              | Characters                                                                             |\n| ------------------------ | -------------------------------------------------------------------------------------- |\n| AnyQuotes/MiniQuotes     | Single quote (`'`), Double quote (`\"`), Backtick (`` ` ``)                             |\n| AnyBrackets/MiniBrackets | Parentheses (`()`), Square brackets (`[]`), Curly braces (`{}`), Angle brackets (`<>`) |\n\nBoth \"Any\" and \"Mini\" variants work with the same character sets, but differ in\ntheir selection strategy.\n\n#### AnyQuotes and AnyBrackets (Traditional Vim behavior)\n\nThese text objects implement traditional Vim behavior:\n\n- **Selection priority**: Finds the innermost (closest) quotes or brackets first\n- **Fallback mechanism**: If none are found, falls back to the current line\n- **Character-based matching**: Focuses solely on open and close characters\n  without considering syntax\n- **Vanilla Vim similarity**: AnyBrackets matches the behavior of commands like\n  `ci<`, `ci(`, etc., in vanilla Vim, including potential edge cases (like\n  considering `>` in `=>` as a closing delimiter)\n\n#### MiniQuotes and MiniBrackets (mini.ai behavior)\n\nThese text objects implement the behavior of the\n[mini.ai](https://github.com/echasnovski/mini.nvim/blob/main/readmes/mini-ai.md)\nNeovim plugin:\n\n- **Selection priority**: Searches the current line first before expanding\n  outward\n- **Tree-sitter integration**: Uses Tree-sitter queries for more context-aware\n  selections\n- **Syntax-aware matching**: Can distinguish between actual brackets and similar\n  characters in other contexts (like `>` in `=>`)\n\n#### Choosing Between Approaches\n\n- Use **AnyQuotes/AnyBrackets** if you:\n\n  - Prefer traditional Vim behavior\n  - Want consistent character-based selection prioritizing innermost delimiters\n  - Need behavior that closely matches vanilla Vim's text objects\n\n- Use **MiniQuotes/MiniBrackets** if you:\n  - Prefer the mini.ai plugin behavior\n  - Want more context-aware selections using Tree-sitter\n  - Prefer current-line priority when searching\n\n#### Example Configuration\n\nTo use these text objects, you need to add bindings to your keymap. Here's an\nexample configuration that makes them available when using text object operators\n(`i` and `a`) or change-surrounds (`cs`):\n\n```json\n{\n  \"context\": \"vim_operator == a || vim_operator == i || vim_operator == cs\",\n  \"bindings\": {\n    // Traditional Vim behavior\n    \"q\": \"vim::AnyQuotes\",\n    \"b\": \"vim::AnyBrackets\",\n\n    // mini.ai plugin behavior\n    \"Q\": \"vim::MiniQuotes\",\n    \"B\": \"vim::MiniBrackets\",\n  },\n}\n```\n\nWith this configuration, you can use commands like:\n\n- `cib` - Change inside brackets using AnyBrackets behavior\n- `ciB` - Change inside brackets using MiniBrackets behavior\n- `ciq` - Change inside quotes using AnyQuotes behavior\n- `ciQ` - Change inside quotes using MiniQuotes behavior\n\n## Command palette\n\nVim mode allows you to open MonyaCode's command palette with `:`. You can then type\nto access any usual MonyaCode command. Additionally, vim mode adds aliases for\npopular Vim commands to ensure your muscle memory transfers to MonyaCode. For\nexample, you can write `:w` or `:write` to save the file.\n\nBelow, you'll find tables listing the commands you can use in the command\npalette. We put optional characters in square brackets to indicate that you can\nomit them.\n\n> **Note**: MonyaCode does not emulate the full power of Vim's command line yet.\n> Commands currently do not support arguments, and full command history is not\n> available.\n\n### File and window management\n\nThis table shows commands for managing windows, tabs, and panes. As commands\ndon't support arguments currently, you cannot specify a filename when saving or\ncreating a new file.\n\n| Command        | Description                                           |\n| -------------- | ----------------------------------------------------- |\n| `:w[rite][!]`  | Save the current file                                 |\n| `:wq[!]`       | Save the file and close the buffer                    |\n| `:q[uit][!]`   | Close the buffer                                      |\n| `:wa[ll][!]`   | Save all open files                                   |\n| `:wqa[ll][!]`  | Save all open files and close all buffers             |\n| `:qa[ll][!]`   | Close all buffers                                     |\n| `:[e]x[it][!]` | Close the buffer                                      |\n| `:up[date]`    | Save the current file                                 |\n| `:cq`          | Quit completely (close all running instances of MonyaCode) |\n| `:vs[plit]`    | Split the pane vertically                             |\n| `:sp[lit]`     | Split the pane horizontally                           |\n| `:new`         | Create a new file in a horizontal split               |\n| `:vne[w]`      | Create a new file in a vertical split                 |\n| `:tabedit`     | Create a new file in a new tab                        |\n| `:tabnew`      | Create a new file in a new tab                        |\n| `:tabn[ext]`   | Go to the next tab                                    |\n| `:tabp[rev]`   | Go to previous tab                                    |\n| `:tabc[lose]`  | Close the current tab                                 |\n| `:ls`          | Show all buffers                                      |\n\n> **Note:** The `!` character is used to force the command to execute without\n> saving changes or prompting before overwriting a file.\n\n### Ex commands\n\nThese ex commands open MonyaCode's various panels and windows.\n\n| Command                      | Default Shortcut |\n| ---------------------------- | ---------------- |\n| Open the project panel       | `:E[xplore]`     |\n| Open the git panel           | `:G[it]`         |\n| Open the debug panel         | `:D[ebug]`       |\n| Open the notifications panel | `:No[tif]`       |\n| Open the diagnostics window  | `:cl[ist]`       |\n| Open the terminal            | `:te[rm]`        |\n| Open the extensions window   | `:Ext[ensions]`  |\n\n### Navigating diagnostics\n\nThese commands navigate diagnostics.\n\n| Command                  | Description                    |\n| ------------------------ | ------------------------------ |\n| `:cn[ext]` or `:ln[ext]` | Go to the next diagnostic      |\n| `:cp[rev]` or `:lp[rev]` | Go to the previous diagnostics |\n| `:cc` or `:ll`           | Open the errors page           |\n\n### Git\n\nThese commands interact with the version control system git.\n\n| Command         | Description                                             |\n| --------------- | ------------------------------------------------------- |\n| `:dif[fupdate]` | View the diff under the cursor (`d o` in normal mode)   |\n| `:rev[ert]`     | Revert the diff under the cursor (`d p` in normal mode) |\n\n### Jump\n\nThese commands jump to specific positions in the file.\n\n| Command             | Description                         |\n| ------------------- | ----------------------------------- |\n| `:<number>`         | Jump to a line number               |\n| `:$`                | Jump to the end of the file         |\n| `:/foo` and `:?foo` | Jump to next/prev line matching foo |\n\n### Replacement\n\nThis command replaces text. It emulates the substitute command in vim. The\nsubstitute command uses regular expressions, and MonyaCode uses a slightly different\nsyntax than vim. You can learn more about MonyaCode's syntax below,\n[in the regex differences section](#regex-differences). MonyaCode will replace only\nthe first occurrence of the search pattern in the current line. To replace all\nmatches append the `g` flag.\n\n| Command                 | Description                       |\n| ----------------------- | --------------------------------- |\n| `:[range]s/foo/bar/[g]` | Replace instances of foo with bar |\n\n### Editing\n\nThese commands help you edit text.\n\n| Command           | Description                                             |\n| ----------------- | ------------------------------------------------------- |\n| `:j[oin]`         | Join the current line                                   |\n| `:d[elete][l][p]` | Delete the current line                                 |\n| `:s[ort] [i]`     | Sort the current selection (with i, case-insensitively) |\n| `:y[ank]`         | Yank (copy) the current selection or line               |\n\n### Set\n\nThese commands modify editor options locally for the current buffer.\n\n| Command                         | Description                                                                                   |\n| ------------------------------- | --------------------------------------------------------------------------------------------- |\n| `:se[t] [no]wrap`               | Lines longer than the width of the window will wrap and displaying continues on the next line |\n| `:se[t] [no]nu[mber]`           | Print the line number in front of each line                                                   |\n| `:se[t] [no]r[elative]nu[mber]` | Changes the displayed number to be relative to the cursor                                     |\n| `:se[t] [no]i[gnore]c[ase]`     | Controls whether the buffer and project search use case-sensitive matching                    |\n\n### Command mnemonics\n\nAs any MonyaCode command is available, you may find that it's helpful to remember\nmnemonics that run the correct command. For example:\n\n- `:diffs` for \"toggle all hunk diffs\"\n- `:cpp` for \"copy path to file\"\n- `:crp` for \"copy relative path\"\n- `:reveal` for \"reveal in finder\"\n- `:zlog` for \"open monyacode log\"\n- `:clank` for \"cancel language server work\"\n\n## Customizing key bindings\n\nIn this section, we'll learn how to customize the key bindings of MonyaCode's vim\nmode. You'll learn:\n\n- How to select the correct context for your new key bindings.\n- Useful contexts for vim mode key bindings.\n- Common key bindings to customize for extra productivity.\n\n### Selecting the correct context\n\nMonyaCode's key bindings are evaluated only when the `\"context\"` property matches\nyour location in the editor. For example, if you add key bindings to the\n`\"Editor\"` context, they will only work when you're editing a file. If you add\nkey bindings to the `\"Workspace\"` context, they will work everywhere in MonyaCode.\nHere's an example of a key binding that saves when you're editing a file:\n\n```json\n{\n  \"context\": \"Editor\",\n  \"bindings\": {\n    \"ctrl-s\": \"file::Save\",\n  },\n}\n```\n\nContexts are nested, so when you're editing a file, the context is the\n`\"Editor\"` context, which is inside the `\"Pane\"` context, which is inside the\n`\"Workspace\"` context. That's why any key bindings you add to the `\"Workspace\"`\ncontext will work when you're editing a file. Here's an example:\n\n```json\n// This key binding will work when you're editing a file. It comes built into MonyaCode by default as the workspace: save command.\n{\n  \"context\": \"Workspace\",\n  \"bindings\": {\n    \"ctrl-s\": \"workspace::Save\",\n  },\n}\n```\n\nContexts are expressions. They support boolean operators like `&&` (and) and\n`||` (or). For example, you can use the context `\"Editor && vim_mode == normal\"`\nto create key bindings that only work when you're editing a file _and_ you're in\nvim's normal mode.\n\nVim mode adds several contexts to the `\"Editor\"` context:\n\n| Operator             | Description                                                                                                                                                                        |\n| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| VimControl           | Indicates that vim keybindings should work. Currently an alias for `vim_mode == normal \\|\\| vim_mode == visual \\|\\| vim_mode == operator`, but the definition may change over time |\n| vim_mode == normal   | Normal mode                                                                                                                                                                        |\n| vim_mode == visual   | Visual mode                                                                                                                                                                        |\n| vim_mode == insert   | Insert mode                                                                                                                                                                        |\n| vim_mode == replace  | Replace mode                                                                                                                                                                       |\n| vim_mode == waiting  | Waiting for an arbitrary key (e.g., after typing `f` or `t`)                                                                                                                       |\n| vim_mode == operator | Waiting for another binding to trigger (e.g., after typing `c` or `d`)                                                                                                             |\n| vim_operator         | Set to `none` unless `vim_mode == operator`, in which case it is set to the current operator's default keybinding (e.g., after typing `d`, `vim_operator == d`)                    |\n\n> **Note**: Contexts are matched only on one level at a time. So it is possible\n> to use the expression `\"Editor && vim_mode == normal\"`, but\n> `\"Workspace && vim_mode == normal\"` will never match because we set the vim\n> context at the `\"Editor\"` level.\n\n### Useful contexts for vim mode key bindings\n\nHere's a template with useful vim mode contexts to help you customize your vim\nmode key bindings. You can copy it and integrate it into your user keymap.\n\n```json\n[\n  {\n    \"context\": \"VimControl && !menu\",\n    \"bindings\": {\n      // Put key bindings here if you want them to work in normal & visual mode.\n    },\n  },\n  {\n    \"context\": \"vim_mode == normal && !menu\",\n    \"bindings\": {\n      // \"shift-y\": [\"workspace::SendKeystrokes\", \"y $\"] // Use neovim's yank behavior: yank to end of line.\n    },\n  },\n  {\n    \"context\": \"vim_mode == insert\",\n    \"bindings\": {\n      // \"j k\": \"vim::NormalBefore\" // In insert mode, make jk escape to normal mode.\n    },\n  },\n  {\n    \"context\": \"EmptyPane\",\n    \"bindings\": {\n      // Put key bindings here (in addition to the context above) if you want them to\n      // work when no editor exists.\n      // \"space f\": \"file_finder::Toggle\"\n    },\n  },\n]\n```\n\n> **Note**: If you would like to emulate Vim's `map` commands (`nmap`, etc.),\n> you can use the action `workspace::SendKeystrokes` in the correct context.\n\n### Optional key bindings\n\nBy default, you can navigate between the different files open in the editor with\nshortcuts like `ctrl+w` followed by one of `hjkl` to move to the left, down, up,\nor right, respectively.\n\nBut you cannot use the same shortcuts to move between all the editor docks (the\nterminal, project panel, ...). If you want to use the same shortcuts to navigate\nto the docks, you can add the following key bindings to your user keymap.\n\n```json\n{\n  \"context\": \"Dock\",\n  \"bindings\": {\n    \"ctrl-w h\": \"workspace::ActivatePaneLeft\",\n    \"ctrl-w l\": \"workspace::ActivatePaneRight\",\n    \"ctrl-w k\": \"workspace::ActivatePaneUp\",\n    \"ctrl-w j\": \"workspace::ActivatePaneDown\",\n    // ... or other keybindings\n  },\n}\n```\n\nSubword motion, which allows you to navigate and select individual words in\n`camelCase` or `snake_case`, is not enabled by default. To enable it, add these\nbindings to your keymap.\n\n```json\n{\n  \"context\": \"VimControl && !menu && vim_mode != operator\",\n  \"bindings\": {\n    \"w\": \"vim::NextSubwordStart\",\n    \"b\": \"vim::PreviousSubwordStart\",\n    \"e\": \"vim::NextSubwordEnd\",\n    \"g e\": \"vim::PreviousSubwordEnd\",\n  },\n}\n```\n\n> Note: Operations like `dw` remain unaffected. If you would like operations to\n> also use subword motion, remove `vim_mode != operator` from the `context`.\n\nVim mode comes with shortcuts to surround the selection in normal mode (`ys`),\nbut it doesn't have a shortcut to add surrounds in visual mode. By default,\n`shift-s` substitutes the selection (erases the text and enters insert mode). To\nuse `shift-s` to add surrounds in visual mode, you can add the following object\nto your keymap.\n\n```json\n{\n  \"context\": \"vim_mode == visual\",\n  \"bindings\": {\n    \"shift-s\": \"vim::PushAddSurrounds\",\n  },\n}\n```\n\nIn non-modal text editors, cursor navigation typically wraps when moving past\nline ends. MonyaCode, however, handles this behavior exactly like Vim by default: the\ncursor stops at line boundaries. If you prefer your cursor to wrap between\nlines, override these keybindings:\n\n```json\n// In VimScript, this would look like this:\n// set whichwrap+=<,>,[,],h,l\n{\n  \"context\": \"VimControl && !menu\",\n  \"bindings\": {\n    \"left\": \"vim::WrappingLeft\",\n    \"right\": \"vim::WrappingRight\",\n    \"h\": \"vim::WrappingLeft\",\n    \"l\": \"vim::WrappingRight\",\n  },\n}\n```\n\nThe [Sneak motion](https://github.com/justinmk/vim-sneak) feature allows for\nquick navigation to any two-character sequence in your text. You can enable it\nby adding the following keybindings to your keymap. By default, the `s` key is\nmapped to `vim::Substitute`. Adding these bindings will override that behavior,\nso ensure this change aligns with your workflow preferences.\n\n```json\n{\n  \"context\": \"vim_mode == normal || vim_mode == visual\",\n  \"bindings\": {\n    \"s\": \"vim::PushSneak\",\n    \"shift-s\": \"vim::PushSneakBackward\",\n  },\n}\n```\n\nThe [vim-exchange](https://github.com/tommcdo/vim-exchange) feature does not\nhave a default binding for visual mode, as the `shift-x` binding conflicts with\nthe default `shift-x` binding for visual mode (`vim::VisualDeleteLine`). To\nassign the default vim-exchange binding, add the following keybinding to your\nkeymap:\n\n```json\n{\n  \"context\": \"vim_mode == visual\",\n  \"bindings\": {\n    \"shift-x\": \"vim::Exchange\",\n  },\n}\n```\n\n### Restoring common text editing and MonyaCode keybindings\n\nIf you're using vim mode on Linux or Windows, you may find it overrides\nkeybindings you can't live without: `ctrl+v` to paste, `ctrl+f` to search, etc.\nYou can restore them by copying this data into your keymap:\n\n```json\n{\n  \"context\": \"Editor && !menu\",\n  \"bindings\": {\n    \"ctrl-c\": \"editor::Copy\",               // vim default: return to normal mode\n    \"ctrl-x\": \"editor::Cut\",                // vim default: decrement\n    \"ctrl-v\": \"editor::Paste\",              // vim default: visual block mode\n    \"ctrl-y\": \"editor::Undo\",               // vim default: line up\n    \"ctrl-f\": \"buffer_search::Deploy\",      // vim default: page down\n    \"ctrl-o\": \"workspace::Open\",            // vim default: go back\n    \"ctrl-s\": \"workspace::Save\",            // vim default: show signature\n    \"ctrl-a\": \"editor::SelectAll\",          // vim default: increment\n    \"ctrl-b\": \"workspace::ToggleLeftDock\"   // vim default: down\n  }\n},\n```\n\n## Changing vim mode settings\n\nYou can change the following settings to modify vim mode's behavior:\n\n| Property                     | Description                                                                                                                                                                                   | Default Value |\n| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |\n| default_mode                 | The default mode to start in. One of \"normal\", \"insert\", \"replace\", \"visual\", \"visual_line\", \"visual_block\", \"helix_normal\".                                                                  | \"normal\"      |\n| use_system_clipboard         | Determines how system clipboard is used:<br><ul><li>\"always\": use for all operations</li><li>\"never\": only use when explicitly specified</li><li>\"on_yank\": use for yank operations</li></ul> | \"always\"      |\n| use_multiline_find           | deprecated                                                                                                                                                                                    |\n| use_smartcase_find           | If `true`, `f` and `t` motions are case-insensitive when the target letter is lowercase.                                                                                                      | false         |\n| toggle_relative_line_numbers | If `true`, line numbers are relative in normal mode and absolute in insert mode, giving you the best of both options.                                                                         | false         |\n| custom_digraphs              | An object that allows you to add custom digraphs. Read below for an example.                                                                                                                  | {}            |\n| highlight_on_yank_duration   | The duration of the highlight animation(in ms). Set to `0` to disable                                                                                                                         | 200           |\n\nHere's an example of adding a digraph for the zombie emoji. This allows you to\ntype `ctrl-k f z` to insert a zombie emoji. You can add as many digraphs as you\nlike.\n\n```json\n{\n  \"vim\": {\n    \"custom_digraphs\": {\n      \"fz\": \"🧟‍♀️\",\n    },\n  },\n}\n```\n\nHere's an example of these settings changed:\n\n```json\n{\n  \"vim\": {\n    \"default_mode\": \"insert\",\n    \"use_system_clipboard\": \"never\",\n    \"use_smartcase_find\": true,\n    \"relative_line_numbers\": \"enabled\",\n    \"highlight_on_yank_duration\": 50,\n    \"custom_digraphs\": {\n      \"fz\": \"🧟‍♀️\",\n    },\n  },\n}\n```\n\n## Useful core MonyaCode settings for vim mode\n\nHere are a few general MonyaCode settings that can help you fine-tune your Vim\nexperience:\n\n| Property                | Description                                                                                                                                                   | Default Value        |\n| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |\n| cursor_blink            | If `true`, the cursor blinks.                                                                                                                                 | `true`               |\n| relative_line_numbers   | If `\"enabled\"`, line numbers in the left gutter are relative to the cursor. If `\"wrapped\"`, they also display for wrapped lines.                              | `\"disabled\"`         |\n| scrollbar               | Object that controls the scrollbar display. Set to `{ \"show\": \"never\" }` to hide the scroll bar.                                                              | `{ \"show\": \"auto\" }` |\n| scroll_beyond_last_line | If set to `\"one_page\"`, allows scrolling up to one page beyond the last line. Set to `\"off\"` to prevent this behavior.                                        | `\"one_page\"`         |\n| vertical_scroll_margin  | The number of lines to keep above or below the cursor when scrolling. Set to `0` to allow the cursor to go up to the edges of the screen vertically.          | `3`                  |\n| gutter.line_numbers     | Controls the display of line numbers in the gutter. Set the `\"line_numbers\"` property to `false` to hide line numbers.                                        | `true`               |\n| command_aliases         | Object that defines aliases for commands in the command palette. You can use it to define shortcut names for commands you use often. Read below for examples. | `{}`                 |\n\nHere's an example of these settings changed:\n\n```json\n{\n  // Disable cursor blink\n  \"cursor_blink\": false,\n  // Use relative line numbers\n  \"relative_line_numbers\": \"enabled\",\n  // Hide the scroll bar\n  \"scrollbar\": { \"show\": \"never\" },\n  // Prevent the buffer from scrolling beyond the last line\n  \"scroll_beyond_last_line\": \"off\",\n  // Allow the cursor to reach the edges of the screen\n  \"vertical_scroll_margin\": 0,\n  \"gutter\": {\n    // Disable line numbers completely\n    \"line_numbers\": false,\n  },\n  \"command_aliases\": {\n    \"W\": \"w\",\n    \"Wq\": \"wq\",\n    \"Q\": \"q\",\n  },\n}\n```\n\nThe `command_aliases` property is a single object that maps keys or key\nsequences to vim mode commands. The example above defines multiple aliases: `W`\nfor `w`, `Wq` for `wq`, and `Q` for `q`.\n\n## Regex differences\n\nMonyaCode uses a different regular expression engine from Vim. This means that you\nwill have to use a different syntax in some cases. Here are the most common\ndifferences:\n\n- **Capture groups**: Vim uses `\\(` and `\\)` to represent capture groups, in\n  MonyaCode these are `(` and `)`. On the flip side, in Vim, `(` and `)` represent\n  literal parentheses, but in MonyaCode these must be escaped to `\\(` and `\\)`.\n- **Matches**: When replacing, Vim uses the backslash character followed by a\n  number to represent a matched capture group. For example, `\\1`. MonyaCode uses the\n  dollar sign instead. So, when in Vim you use `\\0` to represent the entire\n  match, in MonyaCode the syntax is `$0` instead. Same for numbered capture groups:\n  `\\1` in Vim is `$1` in MonyaCode.\n- **Global option**: By default, in Vim, regex searches only match the first\n  occurrence on a line, and you append `/g` at the end of your query to find all\n  matches. In MonyaCode, regex searches are global by default.\n- **Case sensitivity**: Vim uses `/i` to indicate a case-insensitive search. In\n  MonyaCode you can either write `(?i)` at the start of the pattern or toggle\n  case-sensitivity with the shortcut {#kb search::ToggleCaseSensitive}.\n\n> **Note**: To help with the transition, the command palette will fix\n> parentheses and replace groups for you when you write a Vim-style substitute\n> command, `:%s//`. So, MonyaCode will convert `%s:/\\(a\\)(b)/\\1/` into a search for\n> \"(a)\\(b\\)\" and a replacement of \"$1\".\n\nFor the full syntax supported by MonyaCode's regex engine\n[see the regex crate documentation](https://docs.rs/regex/latest/regex/#syntax).\n\n## Smaller tips for vim users\n\n### Space as leader and default keybindings\n\nThe default key bindings for space interfere with using space as leader, and in\nparticular getting the which key-popup to stay open. To fix this, add this key\nbinding to your user keymap:\n\n```json\n// Make which-key persist longer by disabling\n// the default action vim::WrappingRight\n{\n  \"context\": \"(VimControl && !menu)\",\n  \"bindings\": {\n    \"space\": null\n  }\n}\n```\n\nNote that this overrides a default binding, so this is mainly for those that\nwant to use space as the leader key.\n\n### Keep selection during indent/outdent\n\nThis is a common rebinding used by vim users to retain the block selection after\nindent and outdent using the `<` and `<` keys, allowing you to hit the keys\nmultiple times to indent/outdent to the desired depth:\n\n```json\n// keep selection during indent/outdent\n{\n  \"context\": \"vim_mode == visual\",\n  \"bindings\": {\n    \"<\": [\"workspace::SendKeystrokes\", \"< g v\"],\n    \">\": [\"workspace::SendKeystrokes\", \"> g v\"]\n  }\n}\n```\n"},{"path":"visual-customization.md","title":"Visual Customization","group":"Guides","content":"# Visual Customization\n\nVarious aspects of MonyaCode's visual layout can be configured via either the\nsettings window or the `settings.json` file.\n\nThese commands can be used to open the settings UI or file:\n\n- {#action monyacode::OpenSettings} ({#kb monyacode::OpenSettings})\n- {#action monyacode::OpenSettingsFile} ({#kb monyacode::OpenSettingsFile})\n\nSee [Configuring MonyaCode](./configuring-monyacode.md) for additional information and\nother non-visual settings.\n\n## Themes\n\n[Themes](./themes.md) and [Icon themes](./icon-themes.md) can be installed as\nextensions. Most Zed extension themes and icon themes should be compatible.\n\nCommand: {#action monyacode::Extensions}.\n\nPreview and choose the current theme using the theme selectors: {#action\ntheme_selector::Toggle} ({#kb theme_selector::Toggle}) and {#action\nicon_theme_selector::Toggle}.\n\nThe selectors will modify these settings:\n\n```json\n{\n  \"theme\": \"One Dark\",\n  \"icon_theme\": \"MonyaCode (Default)\",\n}\n```\n\nDistinct themes for light mode/dark mode can be set like this:\n\n```json\n{\n  \"theme\": {\n    \"dark\": \"One Dark\",\n    \"light\": \"One Light\",\n    // Mode to use (dark, light) or \"system\" to follow the OS's light/dark mode (default)\n    \"mode\": \"system\",\n  },\n  \"icon_theme\": {\n    \"dark\": \"MonyaCode (Default)\",\n    \"light\": \"MonyaCode (Default)\",\n    // Mode to use (dark, light) or \"system\" to follow the OS's light/dark mode (default)\n    \"mode\": \"system\",\n  },\n}\n```\n\n## Fonts\n\n```json\n  // UI Font. Use \".SystemUIFont\" to use the default system font (SF Pro on macOS),\n  // or \".MonyaCodeSans\" for the bundled default.\n  \"ui_font_family\": \".SystemUIFont\",\n  \"ui_font_weight\": 400, // Font weight in standard CSS units from 100 to 900.\n  \"ui_font_size\": 16,\n\n  // Buffer Font - Used by editor buffers\n  // use \".MonyaCodeMono\" for the bundled default monospace\n  \"buffer_font_family\": \"Berkeley Mono\", // Font name for editor buffers\n  \"buffer_font_size\": 15,                 // Font size for editor buffers\n  \"buffer_font_weight\": 400,              // Font weight in CSS units [100-900]\n  // Line height \"comfortable\" (1.618), \"standard\" (1.3) or custom: `{ \"custom\": 2 }`\n  \"buffer_line_height\": \"comfortable\",\n\n  // Terminal Font Settings\n  \"terminal\": {\n    \"font_family\": \"\",\n    \"font_size\": 15,\n    // Terminal line height: comfortable (1.618), standard(1.3) or `{ \"custom\": 2 }`\n    \"line_height\": \"standard\",\n  },\n```\n\n### Font ligatures\n\nFont ligatures visually combine certain adjacent characters. For example, `=>`\nis displayed as `→` and `!=` as `≠`.\n\nThis is purely cosmetic and the individual characters remain unchanged.\n\nTo disable this behavior use:\n\n```json\n{\n  \"buffer_font_features\": {\n    \"calt\": false, // Disable ligatures\n  },\n}\n```\n\n### Status Bar\n\n```json\n{\n  // Whether to show full labels in line indicator or short ones\n  //   - `short`: \"2 s, 15 l, 32 c\"\n  //   - `long`: \"2 selections, 15 lines, 32 characters\"\n  \"line_indicator_format\": \"long\",\n\n  // Individual status bar icons can be hidden:\n  // \"project_panel\": {\"button\": false },\n  // \"outline_panel\": {\"button\": false },\n  // \"git_panel\": {\"button\": false },\n  // \"debugger\": {\"button\": false },\n  // \"diagnostics\": {\"button\": false },\n  // \"search\": {\"button\": false },\n}\n```\n\n### Titlebar\n\n```json\n  // Control which items are shown/hidden in the title bar\n  \"title_bar\": {\n    \"show\": true,                   // Show/hide the title bar itself\n    \"show_branch_icon\": false,      // Show/hide branch icon beside branch switcher\n    \"show_branch_name\": true,       // Show/hide branch name\n    \"show_project_items\": true,     // Show/hide project host and name\n    \"show_onboarding_banner\": true, // Show/hide onboarding banners\n    \"show_user_picture\": true,      // Show/hide user avatar\n    \"show_menus\": false             // Show/hide menus\n  },\n```\n\n## Workspace\n\n```json\n{\n  // Force usage of MonyaCode build in path prompts (file and directory pickers)\n  // instead of OS native pickers (false).\n  \"use_system_path_prompts\": true,\n  // Force usage of MonyaCode built in confirmation prompts (\"Do you want to save?\")\n  // instead of OS native prompts (false). On linux this is ignored (always false).\n  \"use_system_prompts\": true,\n\n  // Active pane styling settings.\n  \"active_pane_modifiers\": {\n    // Inset border size of the active pane, in pixels.\n    \"border_size\": 0.0,\n    // Opacity of the inactive panes. 0 means transparent, 1 means opaque.\n    \"inactive_opacity\": 1.0,\n  },\n\n  // Layout mode of the bottom dock: contained, full, left_aligned, right_aligned\n  \"bottom_dock_layout\": \"contained\",\n\n  // Whether to resize all the panels in a dock when resizing the dock.\n  // Can be a combination of \"left\", \"right\" and \"bottom\".\n  \"resize_all_panels_in_dock\": [\"left\"],\n}\n```\n\n<!--\nTBD: Centered layout related settings\n```json\n    \"centered_layout\": {\n    // The relative width of the left padding of the central pane from the\n    // workspace when the centered layout is used.\n    \"left_padding\": 0.2,\n    // The relative width of the right padding of the central pane from the\n    // workspace when the centered layout is used.\n    \"right_padding\": 0.2\n    },\n```\n-->\n\n## Editor\n\n```json\n  // Whether the cursor blinks in the editor.\n  \"cursor_blink\": true,\n\n  // Cursor shape for the default editor: bar, block, underline, hollow\n  \"cursor_shape\": null,\n\n  // Highlight the current line in the editor: none, gutter, line, all\n  \"current_line_highlight\": \"all\",\n\n  // When does the mouse cursor hide: never, on_typing, on_typing_and_movement\n  \"hide_mouse\": \"on_typing_and_movement\",\n\n  // Whether to highlight all occurrences of the selected text in an editor.\n  \"selection_highlight\": true,\n\n  // Visually show tabs and spaces  (none, all, selection, boundary, trailing)\n  \"show_whitespaces\": \"selection\",\n  \"whitespace_map\": { // Which characters to show when `show_whitespaces` enabled\n    \"space\": \"•\",\n    \"tab\": \"⟶\"       // use \"→\", for a shorter arrow\n  },\n\n  \"unnecessary_code_fade\": 0.3, // How much to fade out unused code.\n\n  // Defines window border radius for platforms that use client side decorations.\n  \"client_side_decoration_rounding\": 10.0,\n  // Defines window shadow size for platforms that use client side decorations.\n  \"client_side_decoration_shadow\": 10.0,\n\n  // Hide the values of in variables from visual display in private files\n  \"redact_private_values\": false,\n\n  // Soft-wrap and rulers\n  \"soft_wrap\": \"none\",          // none, editor_width, preferred_line_length, bounded\n  \"preferred_line_length\": 80,  // Column to soft-wrap\n  \"show_wrap_guides\": true,     // Show/hide wrap guides (vertical rulers)\n  \"wrap_guides\": [],            // Where to position wrap_guides (character counts)\n\n  // Gutter Settings\n  \"gutter\": {\n    \"line_numbers\": true,         // Show/hide line numbers in the gutter.\n    \"runnables\": true,            // Show/hide runnables buttons in the gutter.\n    \"breakpoints\": true,          // Show/hide show breakpoints in the gutter.\n    \"folds\": true,                // Show/hide show fold buttons in the gutter.\n    \"min_line_number_digits\": 4   // Reserve space for N digit line numbers\n  },\n  \"relative_line_numbers\": \"enabled\", // Show relative line numbers in gutter\n\n  // Indent guides\n  \"indent_guides\": {\n    \"enabled\": true,\n    \"line_width\": 1,                  // Width of guides in pixels [1-10]\n    \"active_line_width\": 1,           // Width of active guide in pixels [1-10]\n    \"coloring\": \"fixed\",              // disabled, fixed, indent_aware\n    \"background_coloring\": \"disabled\" // disabled, indent_aware\n  },\n\n  // Smooth scrolling\n  \"smooth_scroll\": {\n    \"enabled\": false // Whether to animate scrolling with a smooth easing effect. Disabled by default.\n    \"duration\": 160 // Duration of the scrolling animation (ms)\n  },\n\n  \"sticky_scroll\": {\n    \"enabled\": false // Whether to stick scopes to the top of the editor. Disabled by default.\n  }\n```\n\n### Git Blame {#editor-blame}\n\n```json\n  \"git\": {\n    \"inline_blame\": {\n      \"enabled\": true,             // Show/hide inline blame\n      \"delay_ms\": 0,                  // Show after delay (ms)\n      \"min_column\": 0,             // Minimum column to inline display blame\n      \"padding\": 7,                // Padding between code and inline blame (em)\n      \"show_commit_summary\": false // Show/hide commit summary\n    },\n    \"hunk_style\": \"staged_hollow\"  // staged_hollow, unstaged_hollow\n  }\n```\n\n### Editor Toolbar\n\n```json\n  // Editor toolbar related settings\n  \"toolbar\": {\n    \"breadcrumbs\": true, // Whether to show breadcrumbs.\n    \"quick_actions\": true, // Whether to show quick action buttons.\n    \"selections_menu\": true, // Whether to show the Selections menu\n    \"code_actions\": false // Whether to show code action buttons\n  }\n```\n\n### Editor Scrollbar and Minimap {#editor-scrollbar}\n\n```json\n  // Scrollbar related settings\n  \"scrollbar\": {\n    // When to show the scrollbar in the editor (auto, system, always, never)\n    \"show\": \"auto\",\n    \"cursors\": true,          // Show cursor positions in the scrollbar.\n    \"git_diff\": true,         // Show git diff indicators in the scrollbar.\n    \"search_results\": true,   // Show buffer search results in the scrollbar.\n    \"selected_text\": true,    // Show selected text occurrences in the scrollbar.\n    \"selected_symbol\": true,  // Show selected symbol occurrences in the scrollbar.\n    \"diagnostics\": \"all\",     // Show diagnostics (none, error, warning, information, all)\n    \"axes\": {\n      \"horizontal\": true,     // Show/hide the horizontal scrollbar\n      \"vertical\": true        // Show/hide the vertical scrollbar\n    }\n  },\n\n  // Minimap related settings\n  \"minimap\": {\n    \"show\": \"never\",                // When to show (auto, always, never)\n    \"display_in\": \"active_editor\",  // Where to show (active_editor, all_editor)\n    \"thumb\": \"always\",              // When to show thumb (always, hover)\n    \"thumb_border\": \"left_open\",    // Thumb border (left_open, right_open, full, none)\n    \"max_width_columns\": 80,        // Maximum width of minimap\n    \"current_line_highlight\": null  // Highlight current line (null, line, gutter)\n  },\n\n  // Control Editor scroll beyond the last line: off, one_page, vertical_scroll_margin\n  \"scroll_beyond_last_line\": \"one_page\",\n  // Lines to keep above/below the cursor when scrolling with the keyboard\n  \"vertical_scroll_margin\": 3,\n  // The number of characters to keep on either side when scrolling with the mouse\n  \"horizontal_scroll_margin\": 5,\n  // Scroll sensitivity multiplier\n  \"scroll_sensitivity\": 1.0,\n  // Scroll sensitivity multiplier for fast scrolling (hold alt while scrolling)\n  \"fast_scroll_sensitivity\": 4.0,\n```\n\n### Editor Tabs\n\n```json\n  // Maximum number of tabs per pane. Unset for unlimited.\n  \"max_tabs\": null,\n\n  // Customize the tab bar appearance\n  \"tab_bar\": {\n    \"show\": true,                     // Show/hide the tab bar\n    \"show_nav_history_buttons\": true, // Show/hide history buttons on tab bar\n    \"show_tab_bar_buttons\": true      // Show hide buttons (new, split, zoom)\n  },\n  \"tabs\": {\n    \"git_status\": false,              // Color to show git status\n    \"close_position\": \"right\",        // Close button position (left, right, hidden)\n    \"show_close_button\": \"hover\",     // Close button shown (hover, always, hidden)\n    \"file_icons\": false,              // Icon showing file type\n    // Show diagnostics in file icon (off, errors, all). Requires file_icons=true\n    \"show_diagnostics\": \"off\"\n  }\n```\n\n### Status Bar\n\n```json\n  \"status_bar\": {\n    // Show/hide a button that displays the active buffer's language.\n    // Clicking the button brings up the language selector.\n    // Defaults to true.\n    \"active_language_button\": true,\n    // Show/hide a button that displays the cursor's position.\n    // Clicking the button brings up an input for jumping to a line and column.\n    // Defaults to true.\n    \"cursor_position_button\": true,\n    // Show/hide a button that displays the buffer's line-ending mode.\n    // Clicking the button brings up the line-ending selector.\n    // Defaults to false.\n    \"line_endings_button\": false,\n    // Show/hide a button that displays the buffer's character encoding.\n    // If set to \"non_utf8\", the button is hidden only for UTF-8 without BOM.\n    // Defaults to \"non_utf8\".\n    \"active_encoding_button\": \"non_utf8\"\n  },\n  \"global_lsp_settings\": {\n    // Show/hide the LSP button in the status bar.\n    // Activity from the LSP is still shown.\n    // Button is not shown if \"enable_language_server\" if false.\n    \"button\": true\n  },\n```\n\n### Multibuffer\n\n```json\n{\n  // The default number of lines to expand excerpts in the multibuffer by.\n  \"expand_excerpt_lines\": 5,\n  // The default number of lines of context provided for excerpts in the multibuffer by.\n  \"excerpt_context_lines\": 2,\n}\n```\n\n### Editor Completions, Snippets, Actions, Diagnostics {#editor-lsp}\n\n```json\n  \"snippet_sort_order\": \"inline\",        // Snippets completions: top, inline, bottom, none\n  \"show_completions_on_input\": true,     // Show completions while typing\n  \"show_completion_documentation\": true, // Show documentation in completions\n  \"auto_signature_help\": false,          // Show method signatures inside parentheses\n\n  // Whether to show the signature help after completion or a bracket pair inserted.\n  // If `auto_signature_help` is enabled, this setting will be treated as enabled also.\n  \"show_signature_help_after_edits\": false,\n\n  // Whether to show code action button at start of buffer line.\n  \"inline_code_actions\": true,\n\n  // Which level to use to filter out diagnostics displayed in the editor:\n  \"diagnostics_max_severity\": null,      // off, error, warning, info, hint, null (all)\n\n  // How to render LSP `textDocument/documentColor` colors in the editor.\n  \"lsp_document_colors\": \"inlay\",        // none, inlay, border, background\n  // When to show the scrollbar in the completion menu.\n  \"completion_menu_scrollbar\": \"never\", // auto, system, always, never\n  // What should the Supertab action do when not completing?\n  \"supertab_fallback\": \"tab\", // tab, indent\n  // Sync the kill ring (emacs mode)\n  \"sync_kill_ring\": \"clipboard\", // clipboard, primary or none\n  // Line number scale\n  \"line_number_scale\": \"default\", // default, small, xsmall\n  // Turn on colorization of brackets in editors (configurable per language)\n  \"colorize_brackets\": true,\n```\n\n### Editor Inlay Hints\n\n```json\n{\n  \"inlay_hints\": {\n    \"enabled\": false,\n    // Toggle certain types of hints on and off, all switched on by default.\n    \"show_type_hints\": true,\n    \"show_parameter_hints\": true,\n    \"show_other_hints\": true,\n\n    // Whether to show a background for inlay hints (theme `hint.background`)\n    \"show_background\": false, //\n\n    // Time to wait after editing before requesting hints (0 to disable debounce)\n    \"edit_debounce_ms\": 700,\n    // Time to wait after scrolling before requesting hints (0 to disable debounce)\n    \"scroll_debounce_ms\": 50,\n\n    // A set of modifiers which, when pressed, will toggle the visibility of inlay hints.\n    \"toggle_on_modifiers_press\": {\n      \"control\": false,\n      \"shift\": false,\n      \"alt\": false,\n      \"platform\": false,\n      \"function\": false,\n    },\n  },\n}\n```\n\n## File Finder\n\n```json\n  // File Finder Settings\n  \"file_finder\": {\n    \"file_icons\": true,         // Show/hide file icons\n    \"modal_max_width\": \"small\", // Horizontal size: small, medium, large, xlarge, full\n    \"git_status\": true,         // Show the git status for each entry\n    \"include_ignored\": null     // gitignored files in results: true, false, null\n  },\n```\n\n## Project Panel\n\nThe project panel can be shown/hidden using {#action project_panel::ToggleFocus}\n({#kb project_panel::ToggleFocus}) or {#action pane::RevealInProjectPanel} ({#kb\npane::RevealInProjectPanel}).\n\n```json\n  // Project Panel Settings\n  \"project_panel\": {\n    \"button\": true,                 // Show/hide button in the status bar\n    \"default_width\": 240,           // Default panel width\n    \"dock\": \"left\",                 // Position of the dock (left, right)\n    \"entry_spacing\": \"comfortable\", // Vertical spacing (comfortable, standard)\n    \"file_icons\": true,             // Show/hide file icons\n    \"folder_icons\": true,           // Show/hide folder icons\n    \"git_status\": true,             // Indicate new/updated files\n    \"indent_size\": 20,              // Pixels for each successive indent\n    \"auto_reveal_entries\": true,    // Show file in panel when activating its buffer\n    \"auto_fold_dirs\": true,         // Fold dirs with single subdir\n    \"sticky_scroll\": true,          // Stick parent directories at top of the project panel.\n    \"drag_and_drop\": true,          // Whether drag and drop is enabled\n    \"scrollbar\": {                  // Project panel scrollbar settings\n      \"show\": null                  // Show/hide: (auto, system, always, never)\n    },\n    \"show_diagnostics\": \"all\",      //\n    // Settings related to indent guides in the project panel.\n    \"indent_guides\": {\n      // When to show indent guides in the project panel. (always, never)\n      \"show\": \"always\"\n    },\n    // Sort order for entries (directories_first, mixed, files_first)\n    \"sort_mode\": \"directories_first\",\n    // Whether to hide the root entry when only one folder is open in the window.\n    \"hide_root\": false,\n    // Whether to hide the hidden entries in the project panel.\n    \"hide_hidden\": false\n  }\n```\n\n## Terminal Panel\n\n```json\n  // Terminal Panel Settings\n  \"terminal\": {\n    \"dock\": \"bottom\",                   // Where to dock: left, right, bottom\n    \"button\": true,                     // Show/hide status bar icon\n    \"default_width\": 640,               // Default width (left/right docked)\n    \"default_height\": 320,              // Default height (bottom docked)\n\n    // Set the cursor blinking behavior in the terminal (on, off, terminal_controlled)\n    \"blinking\": \"terminal_controlled\",\n    // Default cursor shape for the terminal cursor (block, bar, underline, hollow)\n    \"cursor_shape\": \"block\",\n\n    // Environment variables to add to terminal's process environment\n    \"env\": {\n      // \"KEY\": \"value\"\n    },\n\n    // Terminal scrollbar\n    \"scrollbar\": {\n      \"show\": null                       // Show/hide: (auto, system, always, never)\n    },\n    // Terminal Font Settings\n    \"font_family\": \"Fira Code\",\n    \"font_size\": 15,\n    \"font_weight\": 400,\n    // Terminal line height: comfortable (1.618), standard(1.3) or `{ \"custom\": 2 }`\n    \"line_height\": \"comfortable\",\n\n    \"max_scroll_history_lines\": 10000,   // Scrollback history (0=disable, max=100000)\n  }\n```\n\nSee [Terminal settings](./configuring-monyacode.md#terminal) for additional\nnon-visual customization options.\n\n### Other Panels\n\n```json\n  // Git Panel\n  \"git_panel\": {\n    \"button\": true,               // Show/hide status bar icon\n    \"dock\": \"left\",               // Where to dock: left, right\n    \"default_width\": 360,         // Default width of the git panel.\n    \"status_style\": \"icon\",       // label_color, icon\n    \"sort_by_path\": false,        // Sort by path (false) or status (true)\n    \"tree_view\": false,           // Tree (true) or flat list (false)\n    \"diff_stats\": true,           // Show added/deleted line counts\n    \"scrollbar\": {\n      \"show\": null                // Show/hide: (auto, system, always, never)\n    }\n  },\n\n  // Debugger Panel\n  \"debugger\": {\n    \"dock\": \"bottom\",             // Where to dock: left, right, bottom\n    \"button\": true                // Show/hide status bar icon\n  },\n\n  // Outline Panel\n  \"outline_panel\": {\n    \"button\": true,               // Show/hide status bar icon\n    \"default_width\": 300,         // Default width of the git panel\n    \"dock\": \"left\",               // Where to dock: left, right\n    \"file_icons\": true,           // Show/hide file_icons\n    \"folder_icons\": true,         // Show file_icons (true), chevrons (false) for dirs\n    \"git_status\": true,           // Show git status\n    \"indent_size\": 20,            // Indentation for nested items (pixels)\n    \"indent_guides\": {\n      \"show\": \"always\"            // Show indent guides (always, never)\n    },\n    \"auto_reveal_entries\": true,  // Show file in panel when activating its buffer\n    \"auto_fold_dirs\": true,       // Fold dirs with single subdir\n    \"scrollbar\": {                // Project panel scrollbar settings\n      \"show\": null                // Show/hide: (auto, system, always, never)\n    }\n  }\n```\n"},{"path":"windows.md","title":"MonyaCode on Windows","group":"Guides","content":"# MonyaCode on Windows\n\n## Installing MonyaCode\n\nCurrently, MonyaCode can only be installed on Windows by building\n[from source](./development/windows.md).\n\n## Uninstall\n\n- Installed via installer: Use `Settings` → `Apps` → `Installed apps`, search\n  for MonyaCode, and click Uninstall.\n- Built from source: Remove the build output directory you created (e.g., your\n  target/install folder).\n\nSettings and extensions are stored in your user profile. When uninstalling, you\ncan choose to keep or remove them.\n\n## Remote Development (SSH)\n\nMonyaCode supports remote development on Windows through both SSH and WSL. You can\nconnect to remote servers via SSH or work with files inside WSL distributions\ndirectly from MonyaCode.\n\nFor detailed instructions on setting up and using remote development features,\nincluding SSH configuration, WSL setup, and troubleshooting, see the\n[Remote Development documentation](./remote-development.md).\n\n## Troubleshooting\n\n### MonyaCode fails to start or shows a blank window\n\n- Check that your hardware and operating system version are compatible with\n  MonyaCode.\n- Update your GPU drivers from your GPU vendor (Intel/AMD/NVIDIA/Qualcomm).\n- Ensure hardware acceleration is enabled in Windows and not blocked by\n  third‑party software.\n- Try launching MonyaCode with no extensions or custom settings to isolate conflicts.\n\n### Terminal issues\n\nIf activation scripts don’t run, update to the latest version and verify your\nshell profile files are not exiting early. For Git operations, confirm Git Bash\nor PowerShell is available and on PATH.\n\n### SSH remoting problems\n\nWhen prompted for credentials, use the graphical askpass dialog. If it doesn’t\nappear, check for credential manager conflicts and that GUI prompts aren’t\nblocked by your terminal.\n\n### Graphics issues\n\n#### MonyaCode fails to open / degraded performance\n\nMonyaCode requires a DirectX 11 compatible GPU to run. If MonyaCode fails to open, your\nGPU may not meet the minimum requirements.\n\nTo check if your GPU supports DirectX 11, run the following command:\n\n```\ndxdiag\n```\n\nThis will open the DirectX Diagnostic Tool, which shows the DirectX version your\nGPU supports under `System` → `System Information` → `DirectX Version`.\n\nIf you're running MonyaCode inside a virtual machine, it will use the emulated\nadapter provided by your VM. While MonyaCode will work in this environment,\nperformance may be degraded.\n"}];
